# July 7 Launch Board

Purpose: provide the first Copperknot-owned launch-control board for the July 7, 2026 launch decision.

## Evidence Snapshot

- Snapshot date: `2026-06-08`
- Branch: `production`
- Commit anchor: `a621b6f54`
- Worktree: dirty with active Video workflow work included in this local proof boundary. Current dirty files focus on Video Lip Sync / Omnihuman route scaffolding, media-library audio reference support, Kie/Seedance shot-mode cleanup, Video panel state/restore/submission plumbing, model catalog/pricing/route inventory, video docs, and Copperknot scratchpad/reporting instructions. Treat this as current local worktree proof only; production readiness still requires commit/deploy and production-safe checks.
- Audit mode: rolling weakness audit plus bounded Video workflow source hardening. This pass corrected local model-catalog and Video UI/state guardrail seams without committing, pushing, deploying, spending credits, or claiming production behavior.
- Current operating mode: rolling weakness audit. While lanes are moving, prefer source hardening, narrow invariant tests, meaningful variant checks, and bounded validation; reserve final production/user-journey proof for stable lanes, launch week, or cheap non-mutating checks that directly guide hardening.
- Current repo sweep: on `2026-06-04`, `npm -C frontend run check:size-budget`, `npm -C frontend run validate:media-rendering-guardrails`, `npm -C frontend run build`, `node scripts/check_secret_exposure.js`, and `git diff --check` passed. `npm -C frontend run validate` also passed after bounded Copperknot validation hygiene repairs, covering lint, type-check, guard scripts, and Vitest at `828` files / `6149` passing tests / `50` skipped. Lint remains at `0` errors / `34` existing warnings, and size budget still warns only on `frontend/features/ai-studio/hooks/useAiStudioState.ts` at `844` lines over the `750` reference-grid target.
- Supabase image transformation invariant: the current source scan found no runtime construction of Supabase transform URLs or signed transform params; remaining hits are guard/enforcement scripts or rejection logic in preview trust/state parsing paths.
- Production non-mutating check: `node scripts/verify_deployment_route_parity.mjs --base-url https://www.shortpulse.ai` passed on `2026-06-04`; resolved deployment `https://shortpulse-l2m439gho-kirk-artmans-projects.vercel.app`, created at `2026-06-04T01:38:05.038Z`, inspected `155` route entries, and verified required internal routes.
- Production fail-closed check: `node scripts/verify_internal_route_runtime.mjs --base-url https://www.shortpulse.ai --skip-auth --route generation_recovery --route user_health_fleet --route media_derivatives` passed on `2026-06-04`; unauthenticated probes returned `401` for generation recovery, user health fleet, and media derivatives. This does not prove authenticated operator success because auth was skipped.
- Production route parity refresh: on `2026-06-08`, `node scripts/verify_deployment_route_parity.mjs --base-url https://www.shortpulse.ai` passed; resolved deployment `https://shortpulse-l1zfdw3ab-kirk-artmans-projects.vercel.app`, created at `2026-06-08T13:21:13.333Z`, inspected `163` route entries, and verified required internal routes.
- Production fail-closed refresh: on `2026-06-08`, `node scripts/verify_internal_route_runtime.mjs --base-url https://www.shortpulse.ai --skip-auth --route generation_recovery --route user_health_fleet --route media_derivatives` passed with unauthenticated `401` for generation recovery, user health fleet, and media derivatives. Treat this as protected-route fail-closed proof, not authenticated operator success proof.
- Public entry/account production refresh: on `2026-06-08`, production checks returned `200` for `/`, `/pricing`, and `/dashboard`; `/api/auth/callback-url?flow=recovery&next=%2Fprofile%3Fsection%3Daccount` returned the canonical `https://www.shortpulse.ai/auth/callback?...` URL; unauthenticated `/api/account/media-compliance` returned `401`.
- Credits/pricing fail-closed refresh: on `2026-06-08`, unauthenticated production probes returned `401` for `/api/pricing/model-policy`, `/api/billing/catalog`, `/api/credits/snapshot`, and `/api/admin/pricing/state`. Current source confirms `/api/pricing/model-policy` is authenticated through `requireApiUser`, so this is protected-route fail-closed proof, not public catalog or paid-flow proof.
- Media API fail-closed refresh: on `2026-06-08`, unauthenticated production probes returned `401` for `GET /api/media/list`, `POST /api/media/sign-batch`, the now-retired `POST /api/media/upload`, and `POST /api/media/stage-reference-image`. Current source confirms the remaining media hot-path APIs require `requireApiUser`. This strengthens auth-boundary confidence, but does not prove authenticated media sign/list/resolve behavior or derivative backlog health.
- Generation submission fail-closed refresh: on `2026-06-08`, unauthenticated production probes returned `401` for `POST /api/ai/studio-agent-standard`, `POST /api/ai/studio-agent-pulse`, `POST /api/openai/image-generate`, `POST /api/openai/image-edit`, and `POST /api/fal/submit`. This strengthens protected-route confidence for Create/Pulse, Expert Edit, and provider runtime entry points, but does not prove authenticated generation success or credit-consuming provider behavior.
- Project/workspace fail-closed refresh: on `2026-06-08`, unauthenticated production probes returned `401` for `GET /api/projects`, `POST /api/projects/create`, `GET /api/projects/:projectId/workspace`, `PUT /api/projects/:projectId/workspace`, and `DELETE /api/projects/:projectId`. This strengthens protected-route confidence for project ownership, but does not prove authenticated save/reopen/restore behavior.
- Secret exposure refresh: on `2026-06-08`, `node scripts/check_secret_exposure.js` passed.
- Environment contract refresh: on `2026-06-08`, `node scripts/check_vercel_env_contract.mjs` passed for development, preview, and production with known undeclared-key warnings. Copperknot did not add those keys to `frontend/.env.example`: the three Reference Grid warnings did not appear in active runtime reads during this scan, and `SHORTPULSE_ADMIN_EMAILS` remains a required Vercel contract key with admin-auth migration caveats.
- Video/Lip Sync local validation refresh: on `2026-06-08`, Copperknot hardened the active dirty Video workflow WIP by removing edit-specific `promptPolicy` metadata from hidden Omnihuman runtime config, keeping restored Kling custom state mapped to visible `Multi` counter/controls, preserving Lip Sync draft snapshot-builder stability through refs, adding Lip Sync Generate guardrails for missing voice audio, and documenting checkpoint scratchpads as non-authoritative paper-trail notes. Local validation passed `npm -C frontend test -- --run` across `10` focused Video/model/session/view-model files with `215` tests, `npm -C frontend run type-check:touched`, `npm -C frontend run docs:check`, `npm -C frontend run model:doctor`, `npm -C frontend run check:generate-cta-contract`, `npm -C frontend run fal:routes:check`, `npm -C frontend run lint` with `0` errors / `34` existing warnings, and `git diff --check`.
- Security boundary refresh: on `2026-06-10`, Copperknot ran a bounded local release-security pass against the current `production` worktree. `node scripts/check_secret_exposure.js` passed. Targeted auth/proxy/protected-route proof passed across `tests/api/protected-api-paths.parity.test.ts`, `tests/api/internal-route-inventory-regression.test.ts`, `tests/api/auth-helper.test.ts`, `tests/api/proxy-internal-utils.test.ts`, and `tests/api/fal-status.auth-context.test.ts` with `31` tests. Targeted internal cron-route proof passed across `tests/api/internal-admin-user-health-fleet-run.test.ts`, `tests/api/internal-billing-contract-renewals-run.test.ts`, `tests/api/internal-generation-recovery-run.test.ts`, and `tests/api/internal-media-derivatives-run.test.ts` with `30` tests. This is local route/security proof only; hosted SQL/RPC security audit, production fail-closed refresh, and authenticated production owner-boundary proof remain unproven. An over-wide `npm -C frontend run validate:media-rendering-guardrails` failed on Media Library size budgets and is classified as media-lane validation spillover, not a confirmed security-boundary defect.
- Sound workflow local route refresh: on `2026-06-10`, Copperknot fixed customer-facing ElevenLabs retry guidance by preserving normalized `audio provider` copy in `sanitizeCustomerFacingProviderText` instead of letting generic provider-brand stripping rewrite it into malformed text. Focused validation passed across `14` Sound/sanitizer files with `91` tests, covering ElevenLabs voices, voice delete, text-to-speech, speech-to-speech, music, sound effects, text-to-voice create, voice clone, voice-source staging, saved voices, media autosave preference, and the provider-text sanitizer. This is local route/helper proof only; live provider credentials, authenticated production output insertion, and any credit-consuming Sound generation proof remain unproven approval gates.
- Media rendering guardrail cleanup: on `2026-06-10`, Copperknot cleared the local Media Library size-budget spillover by removing redundant callback/cleanup indirection and restoring the existing pending folder-drop spinner affordance in the status area. `npm -C frontend run validate:media-rendering-guardrails` passed; focused media panel/signing validation passed across `2` files with `96` tests and `9` skipped. This is source-hardening evidence only, not broader Media Library production readiness.
- Creative Libraries style-preview route hardening: on `2026-06-10`, Copperknot added server-side input-budget enforcement to `/api/ai/generate-style-preview` so oversized direct API requests stop before billing reservation or provider dispatch. Focused validation passed across the route, client helper, and style-details normalization with `3` files / `14` tests, plus touched-file type-check. This is Styles route source hardening only, not full Creative Libraries launch readiness.
- Creative Libraries linked-element prep hardening: on `2026-06-10`, Copperknot aligned Kie Kling linked Character/Element image prep with the existing provider payload max of `4` image refs, so saved library assets with extra refs do not spend remote-prep work on images that the payload will discard. Focused validation passed across Video/Kling payload, submission, panel, and token slices with `4` files / `124` tests, plus touched-file type-check. This is local source hardening at the Creative Libraries-to-Video boundary, not Video or Creative Libraries production readiness.
- Creative Libraries Elements readiness hardening: on `2026-06-10`, Copperknot made Elements local list state, initial-save validation, and persistence share one readiness rule: image Elements require two distinct image refs, video Elements require a motion ref, and duplicate image refs are normalized before reference-set save. Focused validation passed across Elements readiness, persistence, reference-slot, shell reorder, and split-host slices with `5` files / `18` tests, plus touched-file type-check. This is local Elements source hardening only, not full Creative Libraries launch readiness.
- Creative Libraries Styles preference hardening: on `2026-06-10`, Copperknot added one shared Styles Library style-id budget and applied it to style order, details, and deleted-style preference normalization so malformed over-budget IDs cannot bloat or confuse saved catalog state. Focused validation passed across Styles catalog/details/preference/runtime slices with `6` files / `20` tests, plus touched-file type-check. This is local source hardening only, not full Creative Libraries launch readiness.
- Creative Libraries Character/Preset no-fix audit: on `2026-06-10`, Copperknot inspected Character selection, character look tab/persistence helpers, Pulse preference normalization, Prompt Presets preference normalization, and Presets library surfaces. Focused validation passed across Characters logic/persistence with `5` files / `36` tests and Pulse/Prompt Presets with `5` files / `41` tests. No concrete behavior-preserving source defect was found in those seams during this pass.
- Post-deploy governance checks: public root returned `200`; `npm -C frontend run docs:check`, `node scripts/check_secret_exposure.js`, and `npm -C frontend run check:generate-cta-contract` passed. `node scripts/check_vercel_env_contract.mjs` passed with env-example drift warnings for undeclared env keys.
- Post-deploy validation convergence: Copperknot cleared the local validation blocker on `2026-06-04` without UI/UX or intended-behavior changes. Focused failing slices passed after stale test-contract and one coverage-allowlist correction, then `npm -C frontend run validate` passed end-to-end with `828` test files and `6149` passing tests.
- June 10 post-deploy sweep: after the user deployed, Copperknot confirmed `production` branch/allowed-branch alignment, production route parity against `https://www.shortpulse.ai` resolved to deployment `shortpulse-cpv3ka8ix-kirk-artmans-projects.vercel.app` created `2026-06-10T18:23:57.906Z`, inspected `169` route entries, and verified required internal routes. Production-safe checks returned `200` for `/`, `/pricing`, and `/dashboard`; the recovery callback URL returned the canonical production callback; unauthenticated `/api/account/media-compliance`, `/api/fal/upload-url`, `/api/fal/omnihuman-v15-submit`, and `/api/fal/omnihuman-v15-status` returned `401`. Local convergence then cleared Fal route-wrapper drift by preserving `/api/fal/upload-url` as an explicit hand-written route, restored media-rendering guardrail enforcement, and fixed stale Video handler test fixtures around Motion Control prep and mock isolation. `npm -C frontend run fal:routes:check`, `npm -C frontend run validate:media-rendering-guardrails`, `npm -C frontend run type-check:touched`, focused Fal/media/Video tests at `6` files / `108` tests, `git diff --check`, `docs:check`, secret exposure, Generate CTA, and Supabase transform guard checks passed. This is production route/fail-closed proof plus local validation proof; it is not authenticated customer workflow or credit-spending generation proof.
- Video submission mode fanout refresh: on `2026-06-10`, the current task-submission source forwards `klingWorkflowMode` through the `useAiStudioTaskSubmission` -> `routeDispatch` -> `handleVideoModelSubmission` path so Seedance/Kling Multi or Custom state is not silently flattened before provider payload assembly. Focused validation passed `features/ai-studio/hooks/__tests__/useAiStudioTaskSubmission.test.ts` and `features/ai-studio/hooks/taskSubmission/__tests__/videoHandlers.test.ts` at `2` files / `105` tests, plus `npm -C frontend run type-check:touched` and `git diff --check`. This is local source-contract proof only; it does not prove authenticated production Video generation or credit-spending provider success.
- Post-deploy source validation refresh: on `2026-06-10`, Copperknot moved shared style-creator prompt-budget constants out of the UI feature layer into `frontend/lib/model-runtime/styleCreatorLimits.ts`, keeping `/api/ai/generate-style-preview` on a neutral server-safe import path and clearing the media-rendering architecture-boundary guardrail. The same pass corrected a stale controlled-component Music panel test so controlled composer-mode transitions require parent rerender before asserting Custom-only controls. Focused validation passed `6` AI Studio/Music/style files with `55` tests, `npm -C frontend run type-check:touched`, `npm -C frontend run validate:media-rendering-guardrails`, and `git diff --check`. Production route parity passed against `https://www.shortpulse.ai`, resolving deployment `https://shortpulse-pospqlut9-kirk-artmans-projects.vercel.app`, created `2026-06-10T19:41:33.153Z`, inspecting `169` route entries and verifying required internal routes. Cheap production checks returned `200` for `/`, `/pricing`, and `/dashboard`; the recovery callback URL returned canonical production JSON; unauthenticated `/api/account/media-compliance` returned `401`. This is source/guardrail and production-safe route proof only; it does not prove authenticated customer workflows or provider generation.
- June 11 post-deploy route/fail-closed sweep: after the user deployed again, Copperknot confirmed `production` branch/allowed-branch alignment, production route parity against `https://www.shortpulse.ai` resolved to deployment `shortpulse-4ucuyzy1r-kirk-artmans-projects.vercel.app` created `2026-06-11T01:01:03.938Z`, inspected `173` route entries, and verified required internal routes. Production-safe checks returned `200` for `/`, `/pricing`, and `/dashboard`; the recovery callback URL returned the canonical production callback; unauthenticated `/api/account/media-compliance`, `/api/fal/upload-url`, `/api/fal/omnihuman-v15-submit`, `/api/fal/omnihuman-v15-status`, pricing/billing/credits, media list/sign-batch, projects, Standard/Pulse submit, and OpenAI image generate/edit routes returned `401`. `node scripts/verify_internal_route_runtime.mjs --base-url https://www.shortpulse.ai --skip-auth --route generation_recovery --route user_health_fleet --route media_derivatives` passed. This is fresh production route/fail-closed proof only; it does not prove authenticated customer workflows, Expert Edit UI behavior, storage SQL/derivative posture, recovery/settlement lifecycle, or credit-consuming generation.
- Expert Edit active-worktree convergence: on `2026-06-10` and `2026-06-11`, Copperknot validated the dirty Expert Edit flip/crop/flatten/session worktree and repaired collateral contracts: modal stage tests now count only render viewports instead of transform chrome, blank outer-stage drag/drop remains inert while canvas-frame drops ingest references, transform-chrome drops again feed the same primary ingest contract, and stale submission-dispatch assertions now include the explicit standard-edit `referenceInputsLimit` and `modelIdOverride` option shape. Focused validation first passed `7` Expert Edit/Create files with `211` tests and `39` skipped/deferred tests, deeper transform/session/export/ingress/stage validation passed `11` files with `40` tests, then the full Expert Edit local fanout passed `44` files with `374` tests and `39` skipped/deferred tests. `npm -C frontend run type-check:touched`, `npm -C frontend run check:generate-cta-contract`, and `git diff --check` passed. This is local worktree proof only; the current Expert Edit WIP needs commit/deploy and production-safe Expert Edit smoke before relying on the older production-proven evidence for this exact code.
- Storage/delivery source audit: on `2026-06-04`, Copperknot inspected media signing, preview trust, adaptive preview, media preview path resolution, and derivative-worker seams. Current source signs originals/durable variant paths without Supabase transform options; render-image direct preview URLs are rejected before direct preview or Next optimizer use; image variants are generated locally with `sharp` and uploaded as durable storage objects. Targeted tests passed across transform guard, trust policy, adaptive resolver, media preview path, media-library adaptive preview, media preview signing/recovery controllers, AI Studio reference-grid/media preview rejection, preview modal rejection, and derivative processing. `npm -C frontend run type-check`, `npm -C frontend run lint`, and `git diff --check` passed after the new resolver regression test.
- Create/Pulse workflow source hardening: on `2026-06-04`, Copperknot fixed the Pulse route-boundary seam so `/api/ai/studio-agent-pulse` requires non-empty matching preset ids between `clientSessionNamespace` and `context.pulse.presetId` before Pulse runtime execution. Local validation passed `tests/api/studio-agent.runtime.test.ts` at `45/45`, `npm -C frontend run check:generate-cta-contract`, a focused Create/Pulse suite at `14` files / `114` tests, `npm -C frontend run type-check`, and `npm -C frontend run lint` with `0` errors and `34` existing warnings. This is local/repo proof, not production generation proof.
- Public entry/account trust audit: on `2026-06-04`, Copperknot inspected public entry, dashboard, pricing, auth callback, protected-route bootstrap, media-compliance, profile/account, and account API seams. Focused local validation passed `23` files / `124` tests across public pages, auth/callback redirects, protected route bootstrapping, media-compliance client/server behavior, account identity, profile sections/actions, pricing auth bootstrap, protected API parity, and auth redirect helpers. Non-mutating production checks against `https://www.shortpulse.ai` returned `200` for `/`, `/pricing`, and `/dashboard`; `/api/auth/callback-url?flow=recovery&next=%2Fprofile%3Fsection%3Daccount` returned the canonical production callback URL; unauthenticated `/api/account/media-compliance` returned `401`. This is production-safe public/fail-closed proof, not authenticated profile mutation or billing-action proof.
- AI Studio shell/navigation scope audit: on `2026-06-04`, Copperknot inspected Abismia's UI/UX authority, AI Studio product/route/style/UX docs, and shell source seams. The lane spans `AiStudioToolbar`, `AiStudioShellFrame`, `panelVisibility`, `shellResize`, modal-layer activity, shared right-rail/global state, canvas/session/style surfaces, and desktop/mobile fit. Because this is significant visible-runtime/UI work, it is packaged as an Abismia handoff rather than local Copperknot implementation.
- Generation provider contract audit: on `2026-06-04`, Copperknot inspected Bactuo's generation source map, ownership manifest, architecture consolidation plan, provider SOPs, model catalog, provider dispatchers, runtime config, and task-submission surfaces. Local provider contract validation passed `19` provider/model files / `125` tests; `npm -C frontend run fal:routes:check` passed for `14` route families; local task-submission validation passed `9` files / `96` tests. This is local contract proof, not live provider compatibility or credit-consuming generation proof.
- Credits/pricing/billing source audit: on `2026-06-04`, Copperknot inspected ADR 0088, the AI Studio pricing contract, client pricing display, create/edit billed-credit lookup, pricing-grid materialization, server generation billing, pricing observability metadata, and billing tests. Targeted pricing/billing validation passed `15` files / `213` tests, including create/edit billed-credit lookups, pricing grid/materialization, pricing credits/formulas/coverage, generation billing reservations and pricing params, AI Studio task submission/view model pricing metadata, admin generation trace, and admin pricing. This is local contract proof, not live Stripe/catalog/paid-flow proof.
- Recovery/settlement source hardening: lineage resolver, settle-before-visible-recovery/failure invariants, status persisted-result lineage fallback, admin trace projection expansion, direct OpenAI/ElevenLabs pre-visible settlement gates, and admin-health projection lineage evidence advanced on `2026-06-03`; targeted lint passed and `266` targeted recovery/billing/webhook/control-plane/status/admin/direct-provider tests passed.
- Media Library focused pass: canonical folder/global membership, save-to-library/project association, copy-from-url, upload/finalize, media list, and panel runtime tests advanced on `2026-06-03`; one stale upload metadata assertion was updated to the current `image_admission` contract.
- Repo evidence used: `README.md`, `docs/routes.md`, `docs/product/shortpulse_ai_studio.md`, `docs/operator-map.md`, `docs/systems/catalog.md`, current Copperknot queue and reports.

This is the first launch-control board under the July 7 model. It is not a final launch-ready claim. It identifies where evidence is strong enough to rely on today and where the next proof must come from. It also treats the current UI, UX, and intended behavior as the preferred baseline; visible redesign or behavior changes are launch work only when smaller strengthening cannot protect the launch promise.

## Board

| Priority | Launch system                                 | Launch state                           | Evidence level       | Human risk | Operational risk | Technical risk | Next proof                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------: | --------------------------------------------- | -------------------------------------- | -------------------- | ---------- | ---------------- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|        1 | `Recovery, settlement, and output integrity`  | `Blocked - Handed Off`                 | `Locally Tested`     | Critical   | Critical         | Critical       | Bactuo continues from `docs/agents/copperknot/handoffs/2026-06-03-recovery-settlement-output-integrity.md`; prove production-safe lifecycle evidence, then decide whether a full shared terminal convergence coordinator is still needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
|        2 | `Media library and organization`              | `Below Floor - Source Hardened`        | `Locally Tested`     | Critical   | High             | High           | Holomony continues from `docs/agents/copperknot/handoffs/2026-06-03-media-library-organization-launch-readiness.md`; local source now keeps browser-direct `/api/media/prepare-upload` -> `/api/media/finalize-upload` as the only media upload route pair and retires `/api/media/upload`. Prove canonical ingest/save paths, autosave-off behavior, global folder membership, project association, and save-to-library continuity.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
|        3 | `Storage, delivery, and variants`             | `Below Floor - Source Hardened`        | `Production Checked` | High       | High             | High           | Continue from `docs/agents/copperknot/handoffs/2026-06-04-storage-delivery-variants-production-sql-proof.md`; repo/local no-transform, signing, preview trust, adaptive, derivative source proof, list/companion-art/sign-batch stale-object existence gating, and `/api/media/upload` retirement are current, and strict production route parity on `2026-06-21` proves `/api/media/upload` is absent from the deployed route table. Authenticated production media sign/list/resolve behavior plus hosted derivative backlog, variant coverage, storage-scope drift SQL, and object-delivery checks remain unproven.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
|        4 | `Create and Pulse workflow`                   | `Below Floor - Handoff Ready`          | `Locally Tested`     | Critical   | High             | High           | Continue from `docs/agents/copperknot/handoffs/2026-06-04-create-pulse-workflow-production-proof.md`; local Standard/Pulse route-boundary, Generate CTA, panel-contract, runtime hydration, Pulse session, and submission-invariant proof is current, but production-safe authenticated workflow proof and any credit-consuming generation smoke remain unproven approval/proof gates.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
|        5 | `AI Studio shell and navigation`              | `Below Floor - Source Hardened`        | `Locally Tested`     | High       | Medium           | Medium         | Preservation-minded closeout received at `docs/records/artifacts/agent/copperknot/reports/external-lane-closeouts/2026-06-19-ai-studio-shell-navigation-preservation-audit-closeout.md`; source/test proof covers workflow navigation, left-rail ownership, shared right rail, modal layering, and a DetailModal workflow-reload media-kind hint fix at `8` shell/detail files / `144` passing tests plus `docs:check`. Remaining proof is authenticated production visual behavior for the shell/navigation workflow.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
|        6 | `Projects and workspace restore`              | `Launchable With Watch`                | `Production Checked` | High       | Medium           | Medium         | Current bounded validation passed on `2026-06-18` across project listing/previews, workspace state save/read/repair-pending behavior, project workspace API client behavior, autosave/restore controller behavior, project route hardening, and item/workspace route guards at `6` files / `160` tests. Final production authenticated save/open/reopen remains watch proof before calling the lane launch ready.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
|        7 | `Generation runtime and providers`            | `Below Floor - Handoff Ready`          | `Locally Tested`     | High       | High             | High           | Continue from `docs/agents/copperknot/handoffs/2026-06-04-generation-runtime-provider-contract-audit.md`; local provider dispatcher, runtime config, model catalog, route-wrapper, Kie/Fal status/result, trusted URL, task-submission payload contracts, and retired upload-route guard are green, but production-safe provider/env posture and any minimal generation smoke remain unproven approval/proof gates.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
|        8 | `Credits, pricing, billing, and entitlements` | `Launchable With Watch`                | `Production Checked` | Critical   | High             | High           | Read-only production billing readiness passed on `2026-06-18` at `9 pass / 1 warn / 0 fail`; production Vercel env contract, billing route parity, signup callback plan intent, public pricing catalog, production Supabase catalog, hidden free tier, signup trigger, and renewal-worker fail-closed checks pass, while Stripe webhook endpoint event proof remains gated by unavailable local `STRIPE_SECRET_KEY`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
|        9 | `Security and ownership boundaries`           | `Launchable With Watch`                | `Production Checked` | Critical   | High             | Medium         | Hosted production SQL security audit passed `353 / 353` checks with `failing_checks = 0`; secret-exposure, protected-route parity, token-first auth/proxy, and internal cron-route gates pass. Local project service and route coverage now proves owner-scoped project list/read/update/delete queries, preview enrichment scoping, and not-owned item/workspace `404` guards before project exposure or workspace state access. Remaining proof is focused authenticated owner-boundary checks before final launch classification.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
|       10 | `Expert Edit workflow`                        | `Launchable With Watch`                | `Production Checked` | Medium     | Medium           | Medium         | Post-deploy re-baseline on `2026-06-18` passed the deterministic Expert Edit coordinate/parity gate at `7` files / `75` tests and focused Expert Edit submit/API/page validation at `12` files / `80` tests. Production-safe probes returned `200` for `/ai-studio` and `401` for unauthenticated `/api/openai/image-edit`, `/api/ai/expert-edit-system-presets`, and `/api/admin/agent-instructions/edit-system-presets`. Authenticated generation/output proof for the exact deployed code remains the watch boundary.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
|       11 | `Right-rail asset reuse`                      | `Launchable With Watch`                | `Repo Inspected`     | High       | Medium           | Medium         | Reconcile current right-rail/canvas/reference-grid commit state with ADR 0083 and production behavior before final readiness.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
|       12 | `Video workflow`                              | `Blocked - Gutan Handoff`              | `Repo Inspected`     | High       | High             | High           | Continue from `docs/records/artifacts/agent/gutan/reports/2026-06-18-kie-motion-control-provider-admission-handoff.md`; Motion Control can fail with provider `File type not supported` because product-valid media is not guaranteed to be Kie Motion Control-valid before submit. Next proof is Gutan-owned provider-admission implementation plus focused local tests for local uploads, Reference Grid/internal refs, storage-backed sources, deterministic pre-submit failures, and no Supabase image transformations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
|       13 | `Sound workflow`                              | `Below Floor - Production Env Checked` | `Production Checked` | Medium     | Medium           | Medium         | Local ElevenLabs route/helper proof covers voice ownership, staging, autosave preference, voice-source preparation, retry-guidance copy, stale upload-adapter retirement, and browser-direct local audio-reference staging. On `2026-06-18`, `ELEVENLABS_API_KEY` became production-required in the Vercel env contract, production env audit passed, and unauthenticated voices/TTS/music/SFX/speech-to-speech/voice-design/create/clone routes returned `401`. Authenticated output insertion and any approved credit-consuming Sound generation proof remain open.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
|       14 | `Creative libraries`                          | `Below Floor - Local Source Hardened`  | `Locally Tested`     | Medium     | Medium           | Medium         | Local source hardening now covers style preview budgets, Styles preference ID budgets, Styles ordering action-id normalization, Elements readiness, linked Character/Element video prep, Character selection/look persistence, Character Sheet preset metadata durability, Pulse preferences, Prompt Presets, and product-image asset admission route canonicalization. Next proof is stable-lane integrated UI reuse plus authenticated production save/reopen/select/generate behavior across the cluster.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
|       15 | `Public entry and account trust`              | `Blocked - Policy Content Gate`        | `Repo Inspected`     | High       | Medium           | Medium         | Public root/pricing/dashboard, canonical auth callback URL, and unauthenticated media-compliance fail-closed behavior remain previously production checked, and focused local auth/profile/account tests are green. Fresh repo inspection found the shared public footer links to `/terms`, `/privacy`, and `/refund-policy`, but no corresponding pages exist. Continue from `docs/agents/copperknot/handoffs/2026-06-20-public-legal-policy-pages-launch-gate.md`; Copperknot must not invent policy content, hide legal links, or make public promises without explicit approval.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
|       16 | `Admin and launch operations`                 | `Launchable With Watch`                | `Production Checked` | Medium     | High             | Medium         | Strict production route parity passed on `2026-06-21` against deployment `shortpulse-kbam1m8m2-kirk-artmans-projects.vercel.app`, created `2026-06-21T14:01:59.680Z`, with `173` route entries inspected, confirming required internal routes and absence of the current default retired fallback/legacy route list, including `/onboarding`, `/api/upload-video`, `/api/upload-audio`, `/api/media/upload`, and `/api/media/admit-image-asset`. Protected internal generation-recovery, user-health-fleet, and media-derivatives routes also failed closed with unauthenticated `401`, and secret exposure checks passed. Final launch-week admin/operator confidence still needs authenticated operator proof where safe.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
|       17 | `Quality of experience`                       | `Below Floor - Local Source Hardened`  | `Production Checked` | High       | Medium           | Medium         | Production `/` and anonymous `/dashboard` previously exposed unfinished dashboard-offer header copy (`tsting teseting`) from active public offer data, and protected-entry loading states on `/ai-studio`, `/profile`, `/report-issue`, and `/admin` previously server-rendered with empty titles. Local source now filters obvious placeholder/test dashboard offers before public static-props serialization, rejects active admin-saved public offer header copy that is placeholder/test-like, adds stable document titles to existing protected loading shells, retires the public `/onboarding` placeholder route, and guards `/onboarding` in deployment route parity. Strict production route parity on `2026-06-21` proves `/onboarding` is absent from the deployed route table, while the current deployed public HTML payload still contains the old placeholder offer strings until the new source hardening is deployed. Fresh source audit also found the protected Performance pair is still staged: authenticated dashboard points to `/performance-soon` ("Analytics coming soon") while direct `/performance` is a demo/sample-data analytics surface. Resolving that Performance exposure is a product/UX scope decision; do not silently hide, redirect, or delete it without approval. Remaining proof is deploy/content-cleanup confirmation, Performance launch-scope decision, plus continued cross-route blocker/watch/polish classification without redesign. |

## Current Judgment

ShortPulse is not yet July 7 launch-ready. The top launch blockers are recovery/settlement/output integrity, media ingest/save, storage/delivery, Create/Pulse workflow proof, and the newly identified public legal/policy footer gate. Recovery/settlement source hardening is now locally tested across direct settlement, webhook, recovery, billing, control-plane, status-route, admin-trace, admin-health, and direct OpenAI/ElevenLabs settlement-gate slices, but production-safe lifecycle proof is still incomplete. Because Copperknot has already made five focused passes in that lane, it is now marked for Bactuo continuation instead of more Copperknot momentum work. Media Library and organization has one focused Copperknot pass with targeted local tests and one stale upload-metadata assertion fixed, but full launch-readiness proof is broad enough to hand off to Holomony. The post-deploy validation blocker is locally cleared after bounded Copperknot validation hygiene fixes and a green aggregate `npm -C frontend run validate`.

Current data-backed decision: continue launch-readiness work from this refreshed method baseline, but do not reopen the handed-off recovery or Media Library lanes locally. The `Storage, delivery, and variants` lane has stronger repo/local proof for transform-free signing, trusted preview resolution, adaptive preview rejection, and local derivative generation. It stays `Below Floor` because authenticated production media delivery and hosted derivative/storage SQL posture are not yet proven, and the remaining proof is packaged as `docs/agents/copperknot/handoffs/2026-06-04-storage-delivery-variants-production-sql-proof.md` instead of more Copperknot local implementation. The `Create and Pulse workflow` lane has a narrow route-boundary source fix and strong local contract proof, but remains below floor because production-safe authenticated workflow proof and any credit-consuming generation smoke are not yet proven. The `AI Studio shell and navigation` lane now has local source/test proof and a closeout report, but remains below floor because authenticated production visual behavior is not yet proven. The `Generation runtime and providers` lane has local provider-contract proof and is handoff-ready for Bactuo because live provider/env proof and cross-provider lifecycle interpretation remain Bactuo-owned. The `Credits, pricing, billing, and entitlements` lane has local contract proof across pricing-grid, billing, task-submission, and admin observability slices, but live catalog/Stripe/paid-flow proof remains approval-gated. The `Public entry and account trust` lane is now `Blocked - Policy Content Gate` because the public footer links to `/terms`, `/privacy`, and `/refund-policy` without shipped pages, and Copperknot cannot invent policy content or hide legal links without explicit approval.

June 8 update: production-safe fail-closed evidence improved across public entry, pricing/account, media, generation, project/workspace, and internal protected routes. This reduces security/ownership ambiguity but does not prove authenticated customer workflows, paid use, media sign/list/resolve, project restore, recovery/settlement, or credit-consuming generation. Later on June 8, the active dirty worktree shifted to a Video/Lip Sync WIP and received bounded local validation. This improves the Video workflow evidence rung from `Repo Inspected` to `Locally Tested`, but does not move it above `Below Floor` because the work is not yet committed, deployed, production-checked, authenticated, or generation-proven.

June 10 Creative Libraries update: source hardening and bounded local validation now cover the cluster's highest-risk reuse seams: style-preview direct API input budgets, Styles preference ID budgets, Elements ready-state normalization, linked Character/Element video prep, Character selection/look persistence, Pulse preferences, and Prompt Presets. This moves the evidence rung to `Locally Tested`, but the launch state remains `Below Floor` because an integrated stable-lane/customer proof still needs authenticated save/reopen/select/reuse and generation-adjacent behavior across the live cluster.

June 10 post-deploy update: the deployed production route surface and protected-route fail-closed checks are fresh, and the local post-deploy validation blockers found in Fal route inventory, media rendering guardrails, and Video handler tests are converged. This improves Admin/launch-operations confidence and keeps Video/Sound route-surface work locally validated, but it does not change the top launch blockers because authenticated customer workflows, recovery/settlement, media library organization, storage SQL/derivative posture, and credit-consuming provider proof remain unproven or owner-gated.

June 11 post-deploy update: the newest deployment refreshes route parity and unauthenticated fail-closed confidence across public entry, account, Fal, billing, media, projects, generation submit, OpenAI image routes, and protected internal routes. This does not change the top queue order because the missing launch evidence is still authenticated customer task completion, recovery/settlement/output integrity, media organization, storage SQL/derivative posture, and any approved credit-consuming provider proof.

June 17 production-safe freshness update: Copperknot refreshed route parity after the latest deploy against deployment `shortpulse-fug3hxhsc-kirk-artmans-projects.vercel.app`, created `2026-06-17T15:33:16.081Z`, with `175` route entries inspected. Public `/`, `/pricing`, and `/dashboard` returned `200`; the recovery callback URL returned the canonical production callback; unauthenticated account/Fal probes returned `401`; protected internal route runtime checks passed for generation recovery, user health fleet, and media derivatives; `node scripts/check_secret_exposure.js` passed; and hosted production `sql/check_runtime_sql_security_audit.sql` returned `353 / 353` passing checks with `failing_checks = 0`. Focused local project owner-boundary invariants also passed in `frontend/lib/server/__tests__/projectsService.test.ts` and `frontend/tests/api/projects-create.test.ts`, proving owner-scoped project list/read/update/delete service queries, workspace preview and display-record preview enrichment user scoping, plus not-owned item read/update/delete and workspace read/save/reset route requests stop at `404` before project exposure or workspace state access. This refresh keeps the route/fail-closed/security-posture floor current but does not change the top queue order or prove authenticated customer workflows, recovery/settlement/output lifecycle, storage derivative posture, or credit-consuming provider success.

June 17 Video route/provider label refresh: Copperknot kept the Video lane source-hardening pass narrow and corrected stale internal `Kie Seedance 2.0` route/provider labels to the canonical `Kie Seedance 2` and `Kie Seedance 2 Fast` labels at the Fal route inventory, generated wrapper, status/submit route tests, and Kie provider-contract boundary. Focused validation passed `tests/api/kie-status-route-config.test.ts`, `tests/api/fal-submit-proxy.test.ts`, and `lib/server/providerIntegration/__tests__/kieModelContracts.test.ts` at `47` tests, plus `npm -C frontend run fal:routes:check` and `npm -C frontend run type-check:touched`. This is local source-contract proof only; it intentionally does not alter visible UI copy and does not prove authenticated production Video generation.

June 17 Video upload-route canonicalization: Copperknot retired the legacy `/api/upload-video` adapter and moved Motion Control stale-upload cleanup plus lease-aware committed-clip retirement onto the canonical `/api/media/stage-motion-reference-video` route. Local validation passed `tests/api/motion-reference-video-upload-route.test.ts`, `features/ai-studio/utils/__tests__/videoUpload.test.ts`, and `tests/api/protected-api-paths.parity.test.ts`, plus `npm -C frontend run type-check:touched`, `npm -C frontend run docs:check`, `git diff --check`, and `npm -C frontend run build`. The build route table no longer lists `/api/upload-video`. This is local route-retirement proof only; deployed absence still needs a post-deploy forbidden-route check.

June 17 Sound upload-route canonicalization: Copperknot retired the stale `/api/upload-audio` adapter and moved current audio-reference intake to `/api/media/prepare-upload` -> browser direct storage upload -> `/api/media/finalize-upload`, avoiding function-body upload limits without adding a new route. Local validation passed the audio upload, media prepare/finalize upload, and protected API parity slices, plus `npm -C frontend run type-check:touched`, `npm -C frontend run docs:check`, `git diff --check`, and `npm -C frontend run build`. The build route table no longer lists `/api/upload-audio`. Sound remains below floor because production-safe provider/env posture, authenticated output insertion, and any approved credit-consuming generation proof are still missing.

June 17 Product image asset route canonicalization: Copperknot retired the stale multipart `/api/media/admit-image-asset` compatibility route after current clients were already using `/api/media/prepare-product-image-asset-upload` -> browser direct storage upload -> `/api/media/finalize-product-image-asset-upload` for local files and `/api/media/admit-image-asset-from-storage` for owned storage-backed references. Local validation passed `lib/server/__tests__/productImageAssetAdmission.test.ts`, `tests/api/media-product-image-asset-routes.test.ts`, and `tests/api/protected-api-paths.parity.test.ts` at `7` tests, plus `npm -C frontend run type-check:touched`, `npm -C frontend run docs:check`, `git diff --check`, and `npm -C frontend run build`. The build route table no longer lists `/api/media/admit-image-asset`. Creative Libraries remains below floor because integrated authenticated save/reopen/select/reuse and generation-adjacent proof are still missing.

June 17 default forbidden-route parity hardening: Copperknot upgraded `scripts/verify_deployment_route_parity.mjs` so normal deployment parity now enforces the known retired route list by default while `--ignore-default-forbidden-routes` is available only for explicitly labeled older-deployment baseline inspection. `node scripts/verify_deployment_route_parity.mjs --base-url https://www.shortpulse.ai --ignore-default-forbidden-routes` passed against deployment `shortpulse-fug3hxhsc-kirk-artmans-projects.vercel.app`; the strict default command failed on that same deployment because `/api/upload-video`, `/api/upload-audio`, and `/api/media/admit-image-asset` are still exposed until the local route-retirement work is deployed. This is a useful deploy gate, not a new source regression.

June 18 deploy-gate closure: after the user deployed, strict `node scripts/verify_deployment_route_parity.mjs --base-url https://www.shortpulse.ai` passed against deployment `shortpulse-ogzhk3tnx-kirk-artmans-projects.vercel.app`, created `2026-06-18T21:17:16.250Z`, with `173` route entries inspected. The active production route table now excludes `/api/upload-video`, `/api/upload-audio`, and `/api/media/admit-image-asset`, along with the rest of the default retired fallback/legacy list. The protected internal route runtime check passed for generation recovery, user health fleet, and media derivatives, and `node scripts/check_secret_exposure.js` passed. This moves Admin and launch operations back to `Launchable With Watch` / `Production Checked` for route-surface confidence only; authenticated operator behavior remains final launch-week proof.

June 19 deploy freshness refresh: after the user deployed again, strict `node scripts/verify_deployment_route_parity.mjs --base-url https://www.shortpulse.ai` passed against deployment `shortpulse-mzpa74ohs-kirk-artmans-projects.vercel.app`, created `2026-06-19T12:57:49.178Z`, with `173` route entries inspected. The protected internal route runtime check passed for generation recovery, user health fleet, and media derivatives; `node scripts/check_secret_exposure.js` passed; and unauthenticated production media list, sign-batch, resolve-previews, and media-derivatives run probes returned `401`. This refreshes route-surface and fail-closed confidence only; it does not prove authenticated customer workflows, storage derivative posture, recovery/settlement lifecycle, authenticated operator behavior, or credit-consuming generation.

June 18 billing readiness refresh: escalated production billing readiness passed `9 pass / 1 warn / 0 fail` against `https://www.shortpulse.ai`. The pass covered production env contract, billing route parity, signup callback plan intent, public pricing catalog, production Supabase catalog, hidden free tier, signup trigger, and billing renewal worker fail-closed behavior. The single warning remains Stripe webhook endpoint event proof because local `STRIPE_SECRET_KEY` is unavailable; Vercel production env presence is covered by the env contract check.

June 18 Video Motion Control provider-admission intake: Gutan produced `docs/records/artifacts/agent/gutan/reports/2026-06-18-kie-motion-control-provider-admission-handoff.md` after auditing production/user-reported `File type not supported` failures on Kie Kling 3.0 Motion Control. Copperknot accepts this as launch-control evidence that Video should be treated as `Blocked - Gutan Handoff` until Motion Control has one canonical provider-admission path for Kie-valid character images and motion videos across local upload, Reference Grid/internal refs, and storage-backed sources. This is report-intake evidence, not implementation proof.

June 18 Expert Edit post-deploy re-baseline: Copperknot retired the stale local-WIP wording for Expert Edit after the user deployed and current source files were clean. `npm run test:expert-edit:coordinate-parity:gate` passed at `7` files / `75` tests, and focused submit/API/page validation passed at `12` files / `80` tests across prompt references, submit preparation/dispatch, primary ingress, session snapshot restore, OpenAI image edit, preset routes, and Edit panel route boundary. Production-safe probes returned `200` for `/ai-studio` and `401` for unauthenticated `/api/openai/image-edit`, `/api/ai/expert-edit-system-presets`, and `/api/admin/agent-instructions/edit-system-presets`. This supports `Launchable With Watch` / `Production Checked`; it is not authenticated generation/output proof for the exact deployed code.

June 18 Sound provider-env contract hardening: Copperknot found that `ELEVENLABS_API_KEY` was declared in `.env.example` but was not required by the production Vercel env contract, meaning prior env readiness could miss the Sound provider credential. The contract now requires `ELEVENLABS_API_KEY` for production and treats it as sensitive presence-only. Local env-contract tests passed, `node scripts/check_vercel_env_contract.mjs --environment production` passed, and production-safe unauthenticated probes returned `401` for `/api/elevenlabs/voices`, `/api/elevenlabs/text-to-speech`, `/api/elevenlabs/music`, `/api/elevenlabs/sound-effects`, `/api/elevenlabs/speech-to-speech`, `/api/elevenlabs/text-to-voice/design`, `/api/elevenlabs/text-to-voice/create`, and `/api/elevenlabs/voices/clone`. This raises Sound provider/env confidence only; it does not prove authenticated Sound output insertion or provider generation success.

June 20 Quality-of-experience public-header and protected-title hardening: production `/` and anonymous `/dashboard` returned `200` but exposed unfinished dashboard-offer copy (`tsting teseting`) ahead of the guest action links. Production `/ai-studio`, `/profile`, `/report-issue`, and `/admin` also returned protected loading shells with empty document titles. Current source traced the public-header copy to admin-managed `dashboard_offers` rendered as public header cards, not static component copy. Copperknot added a shared public-copy guard so the public header suppresses obvious placeholder/test offer copy while keeping the next valid offer visible, and the admin offer save path rejects active public offer header copy that is placeholder/test-like. Copperknot also added stable document titles to the existing protected-route bootstrap and AI Studio pre-runtime loading shells. Focused validation passed admin-offer/dashboard and protected-entry tests, plus touched-file type-check, scoped ESLint, docs check, and diff whitespace checks. This is local source hardening only; production remains visibly unfinished until the change is deployed or the active production offer row is cleaned up.

June 20 public onboarding-placeholder retirement: Copperknot classified `/onboarding` as a public unfinished route rather than a proof target. Local source now removes `frontend/pages/onboarding.tsx`, keeps the hidden legacy dashboard-section flag on the canonical New Project/Open Projects actions instead of stale onboarding links, removes `/onboarding` from `docs/routes.md`, and adds `/onboarding` to the default forbidden route-parity list. Focused validation passed dashboard actions plus route-parity tests, touched-file type-check, scoped ESLint, docs check, `node --check` for the parity script, and diff whitespace checks. This is local source hardening only; strict production route parity should be rerun after deploy to prove `/onboarding` is absent from the deployed route table.

June 20 public legal/policy footer gate: Copperknot found that `frontend/features/dashboard/components/PublicHomeFooter.tsx` renders `Terms of Service`, `Privacy Policy`, and `Refund Policy` links to `/terms`, `/privacy`, and `/refund-policy`, but the repo has no matching pages. This is a launch trust blocker because public policy content cannot be invented by Copperknot and quietly hiding legal links would be a UX/legal posture decision. Handoff created at `docs/agents/copperknot/handoffs/2026-06-20-public-legal-policy-pages-launch-gate.md`. Public entry/account trust is lowered to `Blocked - Policy Content Gate` / `Repo Inspected` until approved policy content, approved external URLs, or an approved temporary suppression decision exists and is implemented with tests/docs.

June 20 media upload adapter retirement: Copperknot retired the unused multipart/raw `POST /api/media/upload` compatibility route after current source showed active upload lanes use `/api/media/prepare-upload` -> browser direct storage upload -> `/api/media/finalize-upload`. The route file and route-specific tests were removed, `SHORTPULSE_MEDIA_UPLOAD_API_ENABLED` was removed from the Vercel env contract, active docs/skills/handoffs were aligned to the browser-direct upload pair, and `/api/media/upload` was added to default deployment forbidden routes. Focused validation passed canonical media prepare/finalize route tests, protected API parity, deployment route-parity defaults, env-contract tests, scoped ESLint, touched-file type-check, docs check, route-parity script syntax, and diff whitespace checks. This was local source hardening first; deployed absence is now covered by the June 21 route-parity refresh.

June 20 protected Performance staging finding: during the preservation-minded quality sweep, Copperknot found that the authenticated dashboard still points the Performance card to `/performance-soon` with explicit "Analytics coming soon" copy, while direct `/performance` remains a protected demo/sample-data analytics surface. This is not a hidden broken link, but it is an unfinished/staged product surface. Copperknot did not hide, redirect, delete, or retarget it because that would be a product/UX scope decision. Quality of experience remains below floor until Performance is explicitly accepted as out-of-launch scope, retired, hidden, or made launch-ready through an approved preservation-minded change.

June 21 deploy freshness refresh: after the user deployed, strict `node scripts/verify_deployment_route_parity.mjs --base-url https://www.shortpulse.ai` passed against deployment `shortpulse-kbam1m8m2-kirk-artmans-projects.vercel.app`, created `2026-06-21T14:01:59.680Z`, with `173` route entries inspected. The current production route table excludes `/onboarding`, `/api/media/upload`, `/api/upload-video`, `/api/upload-audio`, `/api/media/admit-image-asset`, and the rest of the default retired fallback/legacy list. The protected internal route runtime check passed for generation recovery, user health fleet, and media derivatives, and `node scripts/check_secret_exposure.js` passed. This closes the deployed retired-route absence proof for `/onboarding` and `/api/media/upload`; it does not prove authenticated customer workflows, public-offer content cleanup, Performance launch-scope readiness, storage derivative posture, recovery/settlement lifecycle, authenticated operator behavior, or credit-consuming generation.

June 21 public offer payload hardening: production-safe HTML checks showed the public header no longer visibly renders the old `tsting teseting` offer card, but `/` and anonymous `/dashboard` still serialize those placeholder strings in the public HTML payload. Copperknot tightened `readActiveDashboardOffers` so placeholder/test-like active offers are filtered before public static props are built, not only hidden at render time. Focused validation passed `tests/api/admin-offers.test.ts`, `tests/pages/dashboard.guest-route.test.tsx`, `npm -C frontend run type-check:touched`, scoped ESLint, docs check, diff check, and `npm -C frontend run build`; the local built SSG HTML for `/` and `/dashboard` no longer contains `tsting` or `teseting`. This is local source/build hardening only; the deployed payload cleanup remains unproven until after deploy.

June 21 media rendering guardrail refresh: while checking the hard no-Supabase-transform invariant, `npm -C frontend run validate:media-rendering-guardrails` was initially blocked by `frontend/features/ai-studio/components/MediaLibraryPanel.tsx` exceeding the enforced media-rendering size budget. Copperknot kept the current Media Library UI/UX and behavior intact, extracted the prompt-section rendering into `MediaLibraryPanelPromptsSection.tsx`, and brought `MediaLibraryPanel.tsx` to `1498` lines. Focused Media Library panel validation passed at `81` tests / `8` skipped, scoped ESLint and touched-file type-check passed, and `npm -C frontend run validate:media-rendering-guardrails` passed end-to-end, including size budget, architecture boundary, Supabase transform guard, and docs checks. This is local source/guardrail proof, not authenticated production Media Library readiness.

June 21 AI Studio task-submission modularization: the remaining size-budget warning was `frontend/features/ai-studio/hooks/useAiStudioTaskSubmission.ts` at `1023` lines, a central Create/Video/Sound/Generation dispatch seam. Copperknot preserved current behavior, extracted pure submission input helpers into `taskSubmission/submissionInputHelpers.ts`, moved the generation-record input type into the existing task-submission contract file, and brought `useAiStudioTaskSubmission.ts` to `999` lines. Focused task-submission validation passed at `56` tests, scoped ESLint and touched-file type-check passed, and `npm -C frontend run check:size-budget` now passes with no warnings. This is local source-hardening proof only; it does not prove authenticated production Create, Video, Sound, or provider generation behavior.

June 21 local guardrail and Media Library checkpoint sweep: Copperknot ran the clean launch guardrails after the task-submission and media-panel hardening. Generate CTA contract, Fal route wrappers, architecture boundary, model doctor, generation legacy-path, legacy naming, and npm-script path checks passed. Media checkpoint validation also passed for `count-hot-path`, `panel-runtime-churn`, and `preview-authority`, covering media-list, resolve-preview, signing-controller, panel-controller, media-runtime store/runtime, and focused Media Library panel behavior slices. This strengthens local P2/P3 confidence around non-blocking counts, bounded preview signing, transform-free preview authority, and panel churn, but remains local source/contract evidence only. Authenticated production Media Library behavior, hosted derivative backlog, variant coverage, storage-scope drift SQL, and object-delivery checks remain open.

June 21 billing readiness refresh: production billing launch readiness passed against `https://www.shortpulse.ai` at `9 pass / 1 warn / 0 fail`. The pass covered the production Vercel env contract, billing route parity, signup callback plan intent, public pricing catalog, production Supabase catalog, hidden free tier, signup trigger, and billing renewal worker fail-closed behavior. The single warning remains Stripe webhook endpoint event proof because `STRIPE_SECRET_KEY` is unavailable locally; Vercel production env presence for Stripe remains covered by the env-contract pass. A direct production env-contract refresh also passed with undeclared-key warnings; current source search found the retired media-upload env keys only in historical docs/retained artifacts, not active runtime reads.

June 21 production SQL security and media storage gate refresh: Copperknot used the ignored local production DB URL as the hosted read-only SQL target and did not print the URL. `sql/check_runtime_sql_security_audit.sql` passed against production with `353 / 353` checks and `failing_checks = 0`. `scripts/media_storage_deploy_gate.sh` also passed against production: media storage-path drift counts were all `0`, and required storage constraints were present and validated. This refresh strengthens Security/ownership and Storage/delivery production-checked confidence, but it is still diagnostic proof. It does not prove authenticated customer Media Library save/browse/reuse behavior, actual object-delivery success for every user row, hosted derivative backlog health, or production generation/recovery lifecycle success.

June 21 production QoE recheck: the active deployment is still `shortpulse-kbam1m8m2-kirk-artmans-projects.vercel.app`. `/` and anonymous `/dashboard` return `200` with `ShortPulse · Home`, but still serialize `tsting` and `teseting` once each in public HTML. `/onboarding` remains retired with `404`. `/terms`, `/privacy`, and `/refund-policy` still return `404`, so the public legal/policy gate remains blocked. `/ai-studio` now returns `ShortPulse · AI Studio`, while `/profile`, `/report-issue`, `/admin`, `/performance`, and `/performance-soon` still return `ShortPulse · Loading` on unauthenticated production probes. This confirms the public-offer payload cleanup and most protected-title cleanup are not deployed on the active production surface, and Performance/legal decisions remain explicit product/approval gates.

June 21 recovery/project-scope convergence hardening: Copperknot ran read-only production generation diagnostics against the hosted production database. Scheduler health is green: recovery, media derivatives, and admin health cron jobs are present, active, and had `0` failed runs in the last `6h`; scheduler function contracts include the Vercel bypass header and `60000ms` timeout. Settlement integrity returned `0` missing generation charges and `0` duplicate charge keys. Recent `telemetry.generation.recovery.media_visible` rows showed `4` Kie user-reconcile recoveries with roughly `4-5s` provider-terminal-to-visible latency. The live convergence defect scan found one concrete class: `127` successful generations have project metadata but missing project-scoped projection. Copperknot hardened `frontend/lib/server/api/generationOutputConvergence.ts` so owned-output convergence passes project/workspace scope from generation metadata into `upsertGenerationProjection`, with focused output-convergence, projection-repair, and control-plane tests passing. This moves P1 Recovery from pure handoff blockage to local source-hardened status, but production remains below floor until the fix is deployed and non-credit convergence/backfill proof shows project-scoped projections are repaired without credit-consuming generation smoke.

June 21 post-deploy recovery refresh: after the user reported a deploy, strict production route parity still resolved `https://www.shortpulse.ai` to the same deployment, `shortpulse-kbam1m8m2-kirk-artmans-projects.vercel.app`, created `2026-06-21T14:01:59.680Z`, so the local convergence source fix is not proven deployed. Non-mutating route-surface checks still passed: strict route parity passed, protected internal generation recovery/user health/media derivatives failed closed with unauthenticated `401`, and secret exposure checks passed. A follow-up read-only production Supabase aggregate at `2026-06-21T15:14:49.180Z` showed the hosted historical project-scope data is now clean: `2410` successful generations with outputs, `0` outputs without projection, `0` terminal successes missing projection, `0` published outputs without projection, `0` missing or mismatched projection project scope, `0` missing project-generation associations, and `0` missing project-media associations. This upgrades the P1 row's evidence to production-checked for hosted data/backfill posture, but does not move Recovery above below-floor because the app-code convergence fix still needs deploy proof and the broader accepted-job lifecycle remains unproven without credit-consuming smoke.

June 21 storage/delivery hosted health refresh: Copperknot ran a read-only production Supabase aggregate plus transform-free signed delivery sample without printing user ids, object ids, storage paths, signed URLs, or raw error text. Production currently has `3468` media rows, `2817` image rows, `276` video rows, and `6036` media asset variant rows. All `2817` image rows have ready thumb variants; there are `0` pending, processing, failed, or terminal-failed image rows without thumbs. Variant rows are ready for `thumb_240`, `thumb_480`, `preview_loop_360p`, and `poster_720`. Signed delivery samples passed for originals (`8/8`), image thumb variants (`5/5`), and video preview/poster variants (`5/5`), all returning `206` range responses. Together with the earlier zero storage-path drift and validated constraint gate, this moves Storage/delivery to `Floor With Watch` / `Production Checked` for hosted derivative/object-delivery posture. The watch boundary remains authenticated production media API sign/list/resolve behavior and customer-surface preview behavior, which stays coupled to the broader Media Library workflow proof.

June 21 generation runtime/pricing watch refresh: Copperknot refreshed the P6 generation runtime lane after the user reported a deploy, without touching dirty Gear Ball-owned app files or spending credits. Production-safe checks passed for the Vercel env contract, Fal route wrappers, and model doctor. Read-only production app-error aggregation found the only recent `api.generation_billing_missing_canonical_video_price` events were `2` historical `kie-ai/kling-3.0` failures from `2026-06-20T17:09:29.157428Z`, with `0` in the last `6h`, `2h`, `1h`, and `30m`. The failed production params were `durationSeconds=10`, `resolution=720p`, `mode=720p`, `audio=true`, and `aspect=16:9` under active pricing policy version `5`; current source plus that active policy resolves the same variant to `default|res:720p|aspect:16:9|audio:on` at `52` credits. Copperknot therefore did not patch billing or provider code. P6 moves to production-checked posture evidence but remains below floor until approval-gated generation smoke and authenticated lifecycle behavior are proven; P7 remains launchable-with-watch rather than blocked.

## Board Rules

- `Blocked` and `Below Floor` systems drive the queue unless fresh evidence reranks them.
- `Launchable With Watch` systems still need final proof before launch, but they should not outrank a known blocker unless fresh incidents reopen them.
- No system may move to `Launch Ready` from this board without production evidence or a clearly justified waiver.
- This board supersedes the old July 2 score-first posture for launch prioritization.
- The `/10` catalog score is a secondary maturity index. It cannot override launch state, human risk, evidence level, or next proof.
