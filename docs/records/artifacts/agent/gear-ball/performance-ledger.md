# Gear Ball Performance Ledger

Purpose: keep a concise scored ledger of Gear Ball SOP runs and the minimal per-run training loop.

## Entries

| Date       | Run                                                                                                                                                                                                              | Score | Confidence | What went right                                                                                                                                                                                                                                                                                                                                      | What went wrong                                                                                                                                                                                                                                                                        | Smallest next improvement                                                                                                                                                                                                                                      |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----: | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-20 | Production SOP run with canonical video billing lookup alignment for provider-carried Kling Motion Control resolution                                                                                            |   9.4 | high       | The full live tree collapsed immediately into one honest five-file billing lane, one shared proof pass cleared preflight plus 45 targeted tests and a production build, and the lane converged on `production` with no late tails after commit                                                                                                       | The wrapper still paid the routine docs drift checks even though the lane was pure runtime and test work, which is honest but slightly broader than the seam itself needed                                                                                                             | On compact server/runtime billing lanes, keep the same focused test set but consider whether the manual ladder is cheaper than wrapper docs gates when no docs or shared indexes are touched                                                                   |
| 2026-06-20 | Production SOP run with AI Studio detail-media continuity, canonical video billed credits, Pulse title truncation, and Expert Edit timing test follow-through                                                    |   9.1 | high       | The full 20-file mixed tree was classified cleanly into four honest lanes, one shared proof pass cleared preflight plus 417 targeted tests, docs drift checks, the pricing drift gate, and a production build, and the final commit set converged on `production` without late same-lane tails                                                       | The first preflight still burned a preventable formatting-only cycle, and one new detail-selection idempotence guard escaped the shared preflight and only surfaced as a build-time union-type seam before the Git phase                                                               | On mixed AI Studio runtime runs, do a touched-file Prettier sweep and a quick union-narrowing scan around new selection or guard helpers before the first shared proof so the opening ladder stays on real behavior and contract signal                        |
| 2026-06-20 | Production SOP run with dashboard account-menu unification plus AI Studio text-edit and remove-background interaction hardening                                                                                  |   9.2 | high       | The full live tree was classified cleanly into two honest frontend lanes, one shared pre-commit proof cleared preflight plus 27 targeted tests and a production build, and both product commits converged on `production` without late same-lane tails                                                                                               | The first preflight still burned one avoidable formatting-only cycle before the real proof started, and both product commits still paid the expected hook rewrite cost even though the validated content was already stable                                                            | On mixed frontend-only runs, do a touched-file Prettier sweep before the first shared preflight so the opening ladder starts on contract proof and the only remaining extra cost is the intentional hook-adjusted commit flow                                  |
| 2026-06-19 | Production SOP run with Seedance reference-image admission, workflow-reload/audio-companion convergence repair, canvas tear-out fanout, and Gear Ball CI recovery training writeback                             |   9.0 | medium     | The full live tree was classified honestly into one broad product/runtime batch plus one small Gear Ball training-doc batch, focused owning suites cleared 18 files and 419 tests on the exact committed product tree, `type-check:touched`, `docs:check`, and the production build passed, and the final branch push landed cleanly on `production` | The first docs check exposed a missed migration inventory entry, one new Seedance admission test was too expensive for its initial noisy fixture and timed out, and the hook-adjusted product commit still required the expected exact-tree proof rerun before push                    | On broad product batches that add both a new migration and new heavy image-admission tests, do one early migration-doc inventory pass and keep synthetic compression fixtures as small as possible before the first full targeted proof                        |
| 2026-06-19 | Production SOP run with Pulse composer row-layout contract coverage plus Gear Ball context-trim and self-audit prompt tightening                                                                                 |   9.2 | medium     | The live tree was reclassified honestly into one tiny product lane plus one Gear Ball docs lane, the owning composer suites and production build passed before and after commit, the docs integrity gate stayed green, and the final exact-tree proof was rerun on the committed content before push                                                 | The run started with a docs-only hunch that turned out to be incomplete once the full worktree surfaced two frontend tails, so the first classification pass still needed one corrective widen-before-stage step                                                                       | On small post-maintenance SOP runs, always assume the tree may have one adjacent product tail and do the full `git status --short` classification before forming the first run-profile guess                                                                   |
| 2026-06-19 | Production SOP run with Kie Motion Control provider-admission hardening plus AI Studio create/edit/runtime/layout follow-through                                                                                 |   9.0 | medium     | The full live tree stayed honestly collapsed into one broad shared-runtime lane, the corrected preflight cleared formatting, lint, docs drift, and the preview/signing contract fan-out, `type-check:touched` stayed clean after the storage-schema seam fix, and the production build passed on the validated tree                                  | I lost time to one avoidable wrapper-input mistake on the first preflight command and had to recover the final proof state after the earlier session handle was gone, which added a small extra verification cycle before the Git phase                                                | On broad shared-runtime runs, start `gear-ball:preflight` with an explicit `--files` manifest on the first command and keep the closing proof ladder serialized so wrapper/input mistakes cannot add a second confirmation loop                                |
| 2026-06-16 | Production SOP run with AI Studio low-zoom canvas media-action suppression and canvas shell left-rail clearance                                                                                                  |   9.3 | high       | The live tree collapsed immediately into one honest shared-runtime lane, focused preflight and the owning canvas suites passed on the first proof pass, the production build passed before commit, and the exact committed tree re-cleared preflight, tests, and build without late tails                                                            | The hook-adjusted product commit still forced the expected exact-tree rerun before push, so the run paid one extra final proof cycle even though no new lane surfaced                                                                                                                  | On compact shared-runtime lanes that clearly trigger hook rewrites, budget the exact-tree rerun up front and keep the closeout path fully mechanical so the run flows straight from first commit to final push                                                 |
| 2026-06-16 | Production SOP run with AI Studio canvas zoom badge, canvas workspace badge styling, and right-rail shell background continuity                                                                                  |   9.3 | high       | The live tree collapsed immediately into one honest shared-runtime lane, focused preflight and the two owning canvas suites passed on the first proof pass, the production build passed before commit, and the exact committed tree re-cleared preflight, tests, and build without late tails                                                        | The hook-adjusted product commit still forced the expected exact-tree rerun before push, so the run paid one extra final proof cycle even though no new lane surfaced                                                                                                                  | On compact shared-runtime lanes that clearly trigger hook rewrites, budget the exact-tree rerun up front and keep the closeout path fully mechanical so the run flows straight from first commit to final push                                                 |
| 2026-06-16 | Production SOP run with AI Studio shell overlap sizing, right-rail visual-width continuity, matching layout contract hardening, and a late snapshot-control modal tail                                           |   8.8 | high       | The initial shell lane collapsed honestly, focused preflight plus owning tests passed, changed-file type-check and both production builds were clean, and the late modal CSS tail was caught, validated, committed, and pushed instead of being hand-waved away after the first push                                                                 | Control quality slipped because I wrote and pushed the score row before the final exact-tree check, and that exact-tree check then exposed a real one-file modal tail that forced another validation, commit, ledger correction, and repush                                            | Do the last live `git status --short` and full tail classification before writing the score row or making the first final push-ready claim so closeout artifacts never get ahead of real convergence                                                           |
| 2026-06-16 | Production SOP run with suspicious-account deletion hardening, signup/hidden-free launch gates, and AI Studio workflow-reload/audio stabilization                                                                |   7.8 | high       | The run did keep the security/signup lane and the AI Studio lane separated, every final owning proof passed, the billing launch-readiness gate stayed green, and the exact final worktree eventually converged cleanly before push                                                                                                                   | The AI Studio lane was materially undercounted several times after commit, one score-loop commit happened before the lane had truly converged, and repeated hook-restored tails forced multiple extra proof and commit cycles before the final exact tree was real                     | Before the first AI Studio commit, force one broader sibling sweep across the full workflow-reload/detail-modal/reference-grid cluster and do not write the score loop until a post-commit `git status --short` check proves the lane is truly clean           |
| 2026-06-16 | Production SOP run with closed-signup and hidden-free billing hardening plus reference-grid workflow-reload visibility                                                                                           |   8.9 | high       | The mixed tree collapsed cleanly into two honest lanes, touched-file type-check plus 5 focused suites passed, the full frontend production build passed, and both manifests committed cleanly on `production` before the exact-tree proof                                                                                                            | The first touched-file type-check still surfaced one avoidable test-only `null` vs `undefined` seam, and the first wrapper preflight was a no-op because it had no explicit manifest input                                                                                             | Start by locking manifest-aware preflight inputs and do one quick touched-test type seam scan before the first validation rung so the shared-runtime ladder stays one-shot clean                                                                               |
| 2026-06-15 | Production SOP run with Standard agent web-search transport control, AI Studio large-project media-runtime hardening, dashboard tutorial deferred autoplay, and billing-security retained audit/test-key hygiene |   8.8 | high       | The mixed tree collapsed into four honest lanes, touched-file type-check and docs checks passed up front, the exact committed tree finished with all owning suites green plus a clean production build, and every non-temp worktree change was shipped on `production` without late unrelated tails                                                  | I burned one avoidable runner-wiring cycle by starting the browser/jsdom suites through the wrong Vitest path, and the AI Studio responsiveness lane hid one real `react-hooks/refs` lint violation that only surfaced during the first commit attempt                                 | For frontend browser-owned lanes, start focused proof with `npm -C frontend run test -- <paths>` and do one cheap staged lint sanity pass on touched hooks before the first commit so runner shape and hook rules stop earlier                                 |
| 2026-06-15 | Production SOP run with workspace canvas storage-authority recovery from trusted media URLs and preview-trust policy extraction hardening                                                                        |   8.9 | high       | The live tree collapsed immediately into one honest shared-runtime lane, the two owning suites re-cleared 59 tests on the exact committed tree, and the production build passed before and after the hook-adjusted product commit                                                                                                                    | The first wrapper preflight was a no-op because it had no manifest input, and the first two commit attempts were blocked by the secret-exposure hook until the new signed-media test fixtures were rewritten into scanner-safe assembled URLs                                          | When adding signed-media fixtures, use a small URL builder from the first pass and skip the wrapper preflight when it has no locked file manifest so the opening ladder stays on real contract proof                                                           |
| 2026-06-13 | Production SOP run with Expert Edit restore continuity, non-passive stage wheel capture, Video workflow slot reload authority, and canonical provider image staging                                              |   9.1 | high       | The live tree collapsed honestly into one Expert Edit and video-runtime lane, focused suites passed with 309 tests, and the exact committed tree re-cleared preflight plus the production build before push                                                                                                                                          | The first proof pass still paid a four-file Prettier-only stop, and the hook-adjusted commit required the expected exact-tree rerun because validated TS and TSX files were rewritten during commit                                                                                    | On broad AI Studio runtime lanes, do a touched-file Prettier sweep before the first preflight so the opening ladder starts on contract proof and the only remaining rerun is the exact-tree check after hook writes                                            |
| 2026-06-13 | Production SOP run with AI Studio reference-grid browser-load capping, media preview preload pressure, quota-session priming, and reconcile fail-open hardening                                                  |   9.0 | high       | The live tree collapsed cleanly into one honest shared-runtime AI Studio lane, the focused 12-file Vitest set passed before and after the hook-adjusted commit, and the exact committed tree re-cleared preflight plus the production build before push                                                                                              | The first proof pass still paid one formatter-only stop in `ReferenceAudioPlayer`, and the hook-driven commit rewrote staged TS/TSX files so the final exact-tree proof had to be rerun before the run was honestly push-ready                                                         | On shared-runtime AI Studio lanes, do one touched-file Prettier sweep before the first preflight so the opening ladder starts on contract proof and the only expected reproof cost is the exact-tree rerun after hook writes                                   |
| 2026-06-12 | Production SOP run with AI Studio workflow-reload continuity, trusted provider media handling, and deferred toolbar retirement                                                                                   |   8.9 | high       | The live tree collapsed honestly into two planned lanes plus one small hook-restored toolbar tail, the whole dirty tree stayed touched-typecheck clean, all three targeted proof sets passed, and the final exact committed tree passed the production build cleanly before push                                                                     | The run paid two formatter-only recovery cycles before the real proofs could speak, and a late two-file AI Studio toolbar tail resurfaced after the second commit, forcing an extra validation and commit loop before convergence was truly complete                                   | On mixed AI Studio plus server/config SOP runs, do one quick post-commit live-tree sweep before starting the next lane so hook-restored UI tails are caught and folded earlier instead of appearing after the main runtime batches already look finished       |
| 2026-06-12 | Production SOP run with Motion Control primary-image staging through canonical reference upload plus AI Studio data/blob upload helper hardening                                                                 |   9.2 | high       | The live tree collapsed immediately into one honest AI Studio Motion Control lane, the file-backed manifest matched the 10 intended files exactly, preflight plus touched-file type-check passed before commit, and the exact committed tree re-cleared preflight and production build cleanly after hook rewrites                                   | The post-commit `type-check:touched` rerun fell back to zero touched frontend paths because the clean tree no longer exposed the original TS manifest through live status, so that rung was less informative than the pre-commit type check                                            | When hook-driven reproof is needed on a clean committed tree, rerun TypeScript against the locked frontend file manifest instead of the live dirty-file detector so the post-commit type proof stays lane-specific and explicit                                |
| 2026-06-12 | Production SOP run with AI Studio transient restore messaging, expert-edit stack breakpoint hardening, and one-pass Kling saved-element refresh normalization                                                    |   9.1 | high       | The live tree collapsed immediately into one coherent AI Studio lane, the touched-file type-check stayed clean, 5 focused suites passed with 111 tests before and after commit, and the production build passed on both the pre-commit tree and the exact committed tree after hook rewrites                                                         | The first targeted Vitest pass used repo-root paths against the frontend-local runner, so the opening test command burned one cheap cycle before the real proof ladder started                                                                                                         | Derive focused Vitest paths from the frontend workdir before the first run so compact AI Studio lanes reach behavioral proof on the first command instead of paying a path-shape retry                                                                         |
| 2026-06-11 | Production SOP run with dashboard tutorial modal launch flow plus AI Studio reference download and media-library authority hardening                                                                             |   8.9 | high       | The live tree split honestly into one dashboard/tutorial lane and one AI Studio media lane, both exact-manifest preflights passed before commit, the final exact-tree preflight cleared 9 focused suites with 149 tests, and the production build passed before and after the commit phase on the committed tree                                     | The first dashboard preflight still paid one formatter-only stop, both product commits required the expected hook-driven exact-tree reproof, and the existing dashboard `act(...)` warning noise remained visible in a passing suite                                                   | On mixed two-lane runs, do a touched-file Prettier sweep on both manifests before the first preflight so the opening ladders stay on contract proof and the only remaining rerun cost is the intentional exact-tree post-commit check                          |
| 2026-06-11 | Production SOP run with admin dashboard tutorial drag-reorder, auto-derived thumbnail metadata, and simplified tutorial management controls                                                                      |   9.0 | high       | The live tree collapsed immediately to one coherent 4-file admin lane, exact-manifest `gear-ball:preflight` passed before and after commit, the owning admin announcements page suite passed with 4 tests on both proofs, and the production build passed before commit and again on the committed tree                                              | The first preflight still paid one formatter-only stop, and the hook-driven commit required the expected exact-tree reproof before the batch was honestly push-ready                                                                                                                   | On compact admin lanes, do one touched-file Prettier sweep before the opening preflight so the only remaining rerun cost is the intentional post-commit proof when hooks rewrite files                                                                         |
| 2026-06-11 | Production SOP run with Lip Sync Fal staging timeout and media-kind hardening plus dashboard tutorial grid-slot layout tightening                                                                                |   9.1 | high       | The live tree split cleanly into one shared-runtime lane and one dashboard/admin lane, both manifest preflights passed, the final exact-tree preflight cleared 6 focused suites with 198 passing tests, and the production build passed before and after the commit phase on the committed tree                                                      | The first runtime preflight still paid one formatter-only stop, both product commits required the expected hook-driven exact-tree reproof, and the standing `DetailModal` act-warning noise remained visible even though the suite passed                                              | On mixed runtime plus dashboard runs, do one touched-file Prettier sweep before the first preflight so the opening ladder stays on contract proof and the only remaining rerun is the intentional exact-tree post-commit check                                 |
| 2026-06-10 | Production SOP run with AI Studio credit-fraction display, resolved billing-plan credit authority, auth-guard session-hint gating, and Lip Sync reference-layout cleanup                                         |   9.2 | high       | The live tree stayed collapsed to one coherent shared-runtime production batch, `gear-ball:preflight` passed on the exact 13-file manifest with 77 passing tests, the production build passed before and after the hook-adjusted commit, and the committed tree converged cleanly without late same-lane tails                                       | The first focused proof still exposed two stale Seedance tab-label expectations, and the hook-driven commit required the expected exact-tree preflight/build rerun before the batch was honestly ready to push                                                                         | When product wording changes rename a compact mode toggle, run the owning focused suite immediately after the code edit so stale label assertions are repaired before the first full preflight ladder                                                          |
| 2026-06-01 | Production SOP run with Standard create warning cleanup, billing API error sanitization, internal media-ref path hardening, and pricing-drift validator refresh                                                  |   8.7 | high       | The final shared-runtime lane stayed honest once the full tail set was pulled in, `gear-ball:preflight` cleared 8 focused suites with 58 passing tests, `check:generate-cta-contract` and pricing-drift validation both passed, and the final production build passed on the exact committed tree                                                    | The first manifest undercounted same-lane tails after the first build pass, and one required validator was stale enough to fail until its source-of-truth path was refreshed                                                                                                           | When a lane touches pricing-adjacent AI Studio surfaces plus media preview/signing boundaries, run the support validators early and re-audit the live tree before the first staging step so stale checker paths and sibling API test tails cannot appear late. |
| 2026-06-01 | Production SOP run with Dave launch-readiness contract tightening plus media-ownership, pricing-policy refresh, and workspace-preview hardening                                                                  |   9.2 | high       | The live tree collapsed cleanly into one Dave docs lane plus one shared-runtime lane, `docs:check` passed, `gear-ball:preflight` cleared 11 focused suites with 159 passing tests, and the final production build passed before the commit path went live                                                                                            | No repo-local blocker surfaced after the first focused validation ladder; only the standing remote branch-rule bypass note remained outside the repo                                                                                                                                   | Keep using the two-lane collapse for mixed docs-plus-runtime runs when the validation seams are this clear so the first pass can stay this direct.                                                                                                             |
| 2026-06-01 | Production SOP run with Standard chat formatting refinement, session-bootstrap unification, and AI Studio image-billing/bootstrap hardening                                                                      |   8.6 | high       | The live tree converged into three honest lanes, focused route/auth/runtime suites plus pricing and dashboard checks cleared with 357 total tests, `gear-ball:preflight` passed on the exact full manifest, and the final production build passed on the exact pushed tree                                                                           | The first validation ladder still paid avoidable recovery cycles: formatting drift, unused-import lint tails, one hanging `supabaseClient` localStorage test seam, one pricing-logo `next/image` parity regression, and two late build-only typing seams before the tree fully settled | Build the first full-manifest ladder from live status plus an upfront touched-file hygiene pass and one quick type-boundary scan on new shared pricing/auth helpers so the first preflight and final build act more like confirmation than seam discovery.     |
| 2026-06-01 | Production SOP run with Gear Ball lane helpers plus auth-bootstrap, pricing-route, dashboard, and terminal-convergence hardening                                                                                 |   8.4 | high       | The tree converged cleanly into one Gear Ball docs/helper lane plus one broad shared-runtime lane, 12 focused suites cleared with 98 passing tests, `docs:check` passed, and the final production build passed on the exact committed tree                                                                                                           | The first manifest still underreached: late sibling files surfaced during the live-tree audit before commit, and the lane also paid two formatting sweeps plus one unused-import cleanup before the real validation signal was fully clean                                             | Build the first product manifest from full live status plus one explicit seed sweep around every touched route/helper/test cluster so new sibling tests and helper files are included before the first validation pass.                                        |
| 2026-05-30 | Production SOP run with generated-output aspect hydration, GPT Image 2 resolution clamp hardening, and Ako/Money Stuff docs follow-through                                                                       |   8.9 | high       | The live tree stayed collapsed to one broad shared-runtime lane plus one smaller docs/agent-support lane, `docs:check` passed, 19 focused suites cleared with 424 passing tests, and the final production build passed on the exact converged tree                                                                                                   | The first final build surfaced two narrow type-only seams in the new `displayAspectRatio.ts` helper before the lane could close                                                                                                                                                        | When introducing new shared helpers on broad runtime lanes, run one quick local type-seam scan on the new file before the final build rung so nullability edges do not become the only remaining blocker.                                                      |
| 2026-05-30 | Production SOP run with Expert Edit submit-intent hardening, Fal inpaint retirement, and AKO/Gutan docs follow-through                                                                                           |   8.6 | high       | The live tree stayed split into one compact runtime lane plus one small docs lane, `docs:check` passed, 7 focused suites cleared with 138 passing tests, and the final production build passed on the exact converged tree with the retired Fal wrappers absent from the route inventory                                                             | The first broad test manifest still overreached into the stale monolithic `ExpertEditPanelView.test.tsx` surface, which produced unrelated failures and noise before I narrowed back to the honest changed seams                                                                       | When a changed assertion inside a stale broad suite is already skipped or cold, validate the changed contract through its focused helper/runtime suites first and do not let the monolithic file become the lane authority by default.                         |
| 2026-05-30 | Production SOP run with admitted-image rollout, project-bootstrap hardening, and Gutan/Holomony/Nuclo image-admission follow-through                                                                             |   8.7 | high       | The live tree stayed split into one broad product lane plus one honest docs/support lane, `docs:check` passed, 15 focused suites cleared with 92 passing tests, and the final production build passed on the exact converged tree with the Fal route family restored                                                                                 | The lane still lost time to mixed-manifest cleanup friction, and the first real parity failure came from active Fal wrapper routes disappearing even though the model catalog, docs, and route expectations still required them                                                        | When model-catalog or route-parity checks fail on active Fal models, verify `frontend/pages/api/fal` wrapper coverage and wrapper-sync ownership before touching docs or assuming the route family is intentionally gone.                                      |
| 2026-05-29 | Production SOP run with curated-reference authority hardening and OpenAI image route docs refresh                                                                                                                |   8.9 | high       | The live tree stayed collapsed to one honest AI Studio/reference-ingress lane, `docs:check` passed, 11 focused suites cleared with 199 passing tests, and the final production build passed on the exact converged tree                                                                                                                              | The lane still lost time to two stale blob-clone test expectations that assumed permissive internal drag fallback behavior even after the runtime had tightened to fail closed without authoritative resolution                                                                        | When a shared-runtime lane hardens authority or ingress policy, treat stale permissive tests as the first likely failure mode and recheck the contract before assuming the product code needs to reopen fallback behavior.                                     |
| 2026-05-29 | Production SOP run with Expert Edit ingress hardening, image/video routing cleanup, and stage-control simplification                                                                                             |   8.6 | high       | The live tree stayed collapsed to one honest AI Studio lane, docs checks plus focused runtime/server/integration tests cleared, and the final production build passed on the exact converged tree                                                                                                                                                    | The run still lost time to an initial repo-root path miss, one migration-doc parity miss, a build-only page-to-hook contract seam around `motionReferenceVideoError`, and the fact that the first tracked-diff lint manifest skipped two new source files until the final hygiene pass | When commands derive from tracked diffs, build the first lint manifest from `git status --short` instead so new source files and broad page-runtime call-site seams get checked before the final build rung.                                                   |
| 2026-05-29 | Production SOP run with Reference Grid density pressure hardening and motion recorder settings recovery                                                                                                          |   9.0 | high       | The live tree stayed collapsed to one compact AI Studio lane, preflight passed, 6 focused suites cleared with 123 passing tests, and the final production build passed on the exact converged tree                                                                                                                                                   | The first pass still paid one small Prettier sweep on two touched files before the lane was fully clean, and the focused Reference Grid suite continues to emit its known `act(...)` warning noise without failing                                                                     | On compact runtime lanes, start with a tiny touched-file Prettier sweep and keep the known warning-bearing suite in the focused manifest so the first pass stays one-shot clean while the noise remains visible.                                               |
| 2026-05-29 | Production SOP run with project-folder-authority retirement, Kie upload diagnostics, and Create surface authority cleanup                                                                                        |   8.8 | high       | The live tree stayed collapsed to one broad shared-runtime lane, `docs:check` plus hosted SQL lint passed, 18 focused test files cleared with 422 passing tests, and the final production build passed on the exact converged tree                                                                                                                   | The first ladder still lost time to two small formatting sweeps, one lint-only `Function` type cleanup in `useControlPlaneCatalog.ts`, and a noisy `gear-ball:preflight` wrapper pass because `next.config.js` is ignore-matched by ESLint                                             | When `frontend/next.config.js` or another ignore-matched config file is in the manifest, skip the wrapper immediately and start with one repo-root manifest formatting sweep so the first direct ladder reaches the real behavioral signal faster.             |
| 2026-05-28 | Production SOP run with global media-folder authority restore, signed voice-changer upload staging, and AI Studio media/workspace hardening                                                                      |   8.1 | high       | The live tree stayed collapsed to one broad shared-runtime lane, preflight plus docs checks plus hosted SQL lint plus 33 focused test files with 514 passing tests all cleared, and the final production build passed on the exact converged tree                                                                                                    | The lane still paid two formatting sweeps, a small unused-vars cleanup round, and multiple build-only contract seams across Standard Create, Video panel, media folder canvas compatibility, and server upload constants before the tree fully converged                               | On broad shared-runtime lanes, do one explicit build-minded seam scan right after focused tests go green, especially for recently changed prop/helper signatures, so the final build confirms the lane instead of discovering late contract drift.             |
| 2026-05-28 | Production SOP run with Standard/Pulse create control hardening, internal video-frame drops, and embedded elements panel polish                                                                                  |   7.9 | high       | The final production result converged cleanly: exact preflight, focused UI/runtime suites, docs checks, and multiple final production builds all passed on the exact tree that reached `production`                                                                                                                                                  | The first pass surfaced three unused Pulse bindings, and the convergence loop then kept uncovering legitimate sibling AI Studio tails across reference-grid, motion-recorder, video panel, and elements-manager seams after the first product commit path                              | Before the first product commit on broad shared-runtime lanes, run one explicit sibling-seam sweep (`git diff --name-only` plus adjacent test ownership scan) so the first manifest absorbs the whole lane instead of serial tail-chasing it later.            |
| 2026-05-28 | Production SOP run with agent-surface pruning and AI Studio audio metadata hardening                                                                                                                             |   8.8 | high       | The live tree stayed collapsed to one broad docs/governance lane plus one coherent AI Studio runtime lane, `docs:check` passed, product preflight cleared 5 focused files with 84 passing tests, and the final production build passed on the exact converged tree                                                                                   | The run still lost avoidable cycles to one shell-safe manifest issue around spaced paths and one build-only missing import in `useAiStudioProjectWorkspacePersistenceController.ts`                                                                                                    | On mixed docs lanes with spaced paths, use shell-safe staging/formatting loops from the start and do one quick import-seam scan on persistence hooks before the final build rung.                                                                              |
| 2026-05-28 | Production SOP run with Copperknot dispatch guidance and AI Studio media restore/capture hardening                                                                                                               |   9.0 | high       | The live tree collapsed cleanly into one Copperknot docs lane plus one broad AI Studio/runtime lane, docs checks plus exact preflight plus the final production build all passed, and the tree converged cleanly before push                                                                                                                         | The run still lost one avoidable cycle on a macOS `xargs` flag mismatch during the initial formatting sweep                                                                                                                                                                            | On macOS runs, use BSD-safe `xargs` form or a tiny shell loop for manifest-wide formatting so the first cleanup pass stays one-shot clean.                                                                                                                     |
| 2026-05-27 | Production SOP run with generated-media authority and element-create follow-through                                                                                                                              |   8.9 | high       | The live tree stayed collapsed to one compact AI Studio lane, the exact preflight cleared 3 focused test files with 70 passing tests, and the final production build passed on the exact converged tree                                                                                                                                              | The first build surfaced one real async contract mismatch in `ElementsManagerShell.tsx` where `onCreateElement` was treated as promise-returning when the view-state surface exposes `void`                                                                                            | On compact UI/runtime lanes, do one quick build-minded callback-contract scan after focused tests go green so page-local async mismatches surface before the final build rung.                                                                                 |
| 2026-05-27 | Production SOP run with startup-governance hardening and AI Studio embedded media-library rollout                                                                                                                |   8.8 | high       | The live tree stayed collapsed to one docs/governance lane plus one broad product lane, the product preflight cleared 24 focused test files with 247 passing tests, `supabase db lint --linked --schema public --fail-on warning` passed, and the final production build passed on the exact converged tree                                          | The product lane still paid two formatting sweeps, one bracketed-route shell glob miss, and a small lint cleanup round before the broad manifest could fully clear                                                                                                                     | On very large mixed frontend/docs/sql lanes, start with one manifest-wide Prettier sweep and quote any bracketed route paths in the first command block so the first pass reaches real signal faster.                                                          |
| 2026-05-27 | Production SOP run with video-panel motion intake and reference-card styling cleanup                                                                                                                             |   9.2 | high       | The live tree stayed collapsed to one compact AI Studio lane, the exact preflight cleared 2 focused test files with 33 passing tests, and the final production build passed on the exact converged tree                                                                                                                                              | No product or validation regressions surfaced once the lane entered the ladder                                                                                                                                                                                                         | Keep starting compact visual/runtime lanes with one upfront touched-file Prettier sweep so the first pass stays this clean.                                                                                                                                    |
| 2026-05-26 | Production SOP run with detail-preview authority and workspace-restore hardening                                                                                                                                 |   8.7 | high       | The live tree stayed collapsed to one coherent AI Studio lane, the exact preflight cleared 10 focused files with 113 passing tests, and the final production build passed on the exact converged tree                                                                                                                                                | The run still paid repeated formatting cleanup cycles, and `DetailModal.tsx` needed an explicit `react-hooks/set-state-in-effect` contract alignment before the lane could clear                                                                                                       | On modal-heavy authority lanes, do one upfront Prettier sweep and one quick effect-state lint scan before the first preflight so the first pass reaches the real behavioral signal faster.                                                                     |
| 2026-05-26 | Production SOP run with motion-recorder intake, style-drop hardening, and character reference drag cleanup                                                                                                       |   8.2 | high       | The live tree stayed collapsed to one coherent AI Studio lane, the focused preflight cleared 28 files with 130 passing tests, and the final production build passed on the exact converged tree                                                                                                                                                      | The run still paid several formatting sweeps, one motion-recorder cleanup lint warning, two style-drop regressions, and one project-scoped generated-media authority bug before the lane fully cleared                                                                                 | On broad drag/drop and media-authority lanes, do one upfront touched-file Prettier sweep and one hook-to-boundary contract scan before the first preflight so the real signal appears sooner.                                                                  |
| 2026-05-26 | Production SOP run with reference-grid delete cleanup and AI Studio media-panel hardening                                                                                                                        |   8.3 | high       | The live tree stayed collapsed to one coherent AI Studio lane, the exact current-manifest preflight cleared 23 files and 44 focused tests, and the final production build passed on the exact pushed tree                                                                                                                                            | The first pass still paid a formatting-only cleanup, the new reference-grid delete path shipped with a missing `normalizeId` helper, and the final build exposed a page-shell callback contract mismatch before the lane converged                                                     | On shared media-delete/runtime lanes, do one quick callback-seam scan after hook signature changes so page-shell adapters and delete-flow helpers stay aligned before the final build.                                                                         |
| 2026-05-26 | Production SOP run with launch-trust doctrine docs and project workspace/media rail hardening                                                                                                                    |   8.8 | high       | The live tree stayed collapsed to one governance lane plus one broad project-workspace/runtime lane, docs checks plus shared-runtime preflight plus Generate CTA contract plus final build all passed, and the final tree converged cleanly before score writeback                                                                                   | The first product pass still paid a formatting sweep, and one dead import warning surfaced in `VideoPropertiesPanel.tsx` before the lane could clear                                                                                                                                   | On medium shared-runtime lanes, run a tiny dead-import scan alongside the first formatting sweep so the first preflight speaks only to real behavioral regressions.                                                                                            |
| 2026-05-26 | Production SOP run with Abismia/Babineaux onboarding and AI Studio shell modularization                                                                                                                          |   8.4 | high       | The live tree stayed collapsed to one governance lane plus one broad AI Studio/runtime lane, docs checks plus shared-runtime preflight plus Generate CTA contract plus final build all passed, and the final tree converged cleanly before score writeback                                                                                           | The first product pass paid a large formatting sweep, four lint warnings surfaced after that, and the DetailModal preview-lock contract regressed once before the lane fully cleared                                                                                                   | On very large shared-runtime lanes, run a manifest-wide formatting sweep plus a tiny extracted-runtime/effect lint scan before the main preflight so the first pass catches only real behavioral regressions.                                                  |
| 2026-05-25 | Production SOP run with internal media-ref submit authority and Holomony reference-grid baseline                                                                                                                 |   8.5 | high       | The live tree stayed split into one Holomony baseline-doc lane and one broad AI Studio submission/workspace lane, the focused suite cleared 24 files with 539 passing tests, and the final build plus docs checks passed before publish                                                                                                              | The first product-lane ESLint pass used the repo root instead of `frontend/`, the build surfaced one real session-snapshot union type hole, and `ExpertEditPanelView.test.tsx` still emitted its recurring `act(...)` warning noise                                                    | On large frontend-only runtime lanes, start from the `frontend/` workdir and do one quick union-access scan around new snapshot helpers before the full build so the first pass catches only real behavioral risk.                                             |
| 2026-05-25 | Production SOP run with character-mode bundle recovery and Holomony reference-grid onboarding                                                                                                                    |   8.8 | high       | The live tree split cleanly into one Holomony docs lane and one compact character-mode runtime lane, focused tests cleared 56 checks, and the final build plus docs checks passed before publish                                                                                                                                                     | The first product-lane ESLint pass used the repo root instead of `frontend/`, which cost one avoidable rerun before the real validation signal                                                                                                                                         | On frontend-only runtime lanes, derive ESLint commands directly from the `frontend/` workdir up front so the first validation pass stays clean.                                                                                                                |
| 2026-05-25 | Production SOP run with Holomony media-library baseline and AI Studio workspace/session hardening                                                                                                                |   8.1 | high       | The live tree still collapsed cleanly into one Holomony baseline-doc lane plus one broad AI Studio runtime lane, the focused suite cleared 28 files with 499 passing tests, and the final docs checks plus build passed on the exact pushed tree                                                                                                     | The run paid too many recovery cycles: one stale media-library mock, two Expert Edit history regressions, one keyboard-contract drift, and two build-caught type holes in preview/loading-shell components before the tree converged                                                   | When a lane introduces a new shared history or keyboard-routing seam, pull its owning hook tests into the first manifest and do the final build before drafting any score-loop closeout so UI-local type holes surface earlier.                                |
| 2026-05-25 | Production SOP run with reference-grid ordering and provider-error cleanup                                                                                                                                       |   8.9 | high       | The live tree stayed collapsed to one coherent AI Studio/media-ingestion lane, the focused suite cleared 9 files with 89 tests, and the final build plus docs checks passed on the exact pushed tree                                                                                                                                                 | The first validation pass still surfaced one avoidable lint warning from an unused import in `ReferenceGridCard.tsx` before the lane could finish                                                                                                                                      | On compact single-lane runs, do one final dead-import scan immediately after the initial formatting sweep so the first validation pass stays fully clean.                                                                                                      |
| 2026-05-25 | Production SOP run with reference-grid ownership hardening and Create model-context alignment                                                                                                                    |   8.8 | high       | The live tree stayed collapsed to one coherent AI Studio/reference-grid lane, the focused suites plus docs checks passed, and the final build cleared on the exact pushed tree                                                                                                                                                                       | The first cheap cleanup pass used the wrong working-directory path shape, which cost one avoidable recovery cycle before the real validation ladder started                                                                                                                            | On repo-root mixed lanes, derive the initial formatting sweep from the repo root and include untracked files in that first manifest so the first ladder pass stays clean.                                                                                      |
| 2026-05-25 | Production SOP run with character-mode fail-closed guards and admin/pulse governance polish                                                                                                                      |   8.1 | high       | The live tree still collapsed to one coherent AI Studio/admin governance lane, the focused suites plus docs checks passed, and the final build cleared before publish                                                                                                                                                                                | The first pass paid a broad formatting-only cleanup across 18 files, the Reference Grid suite still emitted recurring `act(...)` warning noise, and the ADR index tail surfaced after the first score-loop writeback so convergence needed one more correction step                    | On very large mixed runtime/docs lanes, run a manifest-wide Prettier sweep before the main ladder and re-check doc index tails before writing the score loop so the run closes in one shot.                                                                    |
| 2026-05-25 | Production SOP run with global right-rail authority, Pulse catalog hardening, and signed-in issue reporting                                                                                                      |   8.5 | high       | The live tree stayed collapsed to one coherent AI Studio runtime/governance lane, the focused suites plus docs checks passed, and the final build cleared on the exact pushed tree                                                                                                                                                                   | The first pass still paid a broad formatting-only cleanup across 18 files before the lane could finish                                                                                                                                                                                 | On large mixed runtime/docs lanes, do one manifest-wide Prettier sweep before the main ladder so the first validation pass speaks only to real failures.                                                                                                       |
| 2026-05-25 | Production SOP run with Pulse runtime parking and preset docs refresh                                                                                                                                            |   8.7 | high       | The live tree stayed collapsed to one coherent AI Studio runtime lane, focused tests plus docs checks passed, and the final build cleared on the exact pushed tree                                                                                                                                                                                   | The first pass still paid two cheap cleanup cycles: five files needed Prettier and one workspace test had an unused `container` binding                                                                                                                                                | On larger mixed docs-plus-runtime lanes, do one up-front Prettier sweep on touched runtime files and scan the changed tests for dead locals before starting the main ladder.                                                                                   |
| 2026-05-25 | Production SOP run with fixed character split host and workspace chrome                                                                                                                                          |   8.8 | high       | The live tree stayed collapsed to one compact character-panel lane, the focused UI tests passed cleanly, and the final build cleared on the exact pushed tree                                                                                                                                                                                        | The first pass still paid one tiny formatting cycle on `CharacterPanelSplitHost.test.tsx` before the lane could finish                                                                                                                                                                 | On compact UI-only lanes, start with a one-file Prettier sweep on the newest changed test file when it was edited alongside structural component changes.                                                                                                      |
| 2026-05-25 | Production SOP run with character panel split and rail polish                                                                                                                                                    |   8.9 | high       | The live tree stayed collapsed to one compact character-panel lane, the focused UI tests passed cleanly, and the final build cleared on the exact pushed tree                                                                                                                                                                                        | The first pass still paid one tiny formatting cycle on `CharacterPanelSplitHost.test.tsx` before the lane could finish                                                                                                                                                                 | On compact UI-only lanes, run a one-file Prettier sweep on the newest touched test file before the rest of the ladder so the lane stays fully one-pass.                                                                                                        |
| 2026-05-24 | Production SOP run with Generate CTA guardrail, audio route limits, and character rail tuning                                                                                                                    |   8.7 | high       | The live tree stayed collapsed to one coherent shared-runtime lane and the focused tests, docs checks, Generate CTA guard, and final build all cleared on the exact pushed tree                                                                                                                                                                      | The first pass still lost one cycle to a bad repo-relative Prettier invocation, and the new guard script itself needed formatting before the lane could finish                                                                                                                         | On mixed repo-root plus `frontend/` lanes, derive the first formatting command from the actual working directory up front so the validation ladder starts cleanly.                                                                                             |
| 2026-05-20 | Production mixed-run closeout after repeated manifest drift                                                                                                                                                      |   8.0 | medium     | Validation stayed honest on the final tree                                                                                                                                                                                                                                                                                                           | Commit and leftover discipline was noisy                                                                                                                                                                                                                                               | Rebuild manifests once from live `git status --short` when new files appear mid-run, then stop absorbing repeated tails.                                                                                                                                       |
| 2026-05-22 | Production SOP run with repeated mixed-lane rereads and blocked visual QA                                                                                                                                        |   7.0 | high       | The shipped lanes were still validated safely                                                                                                                                                                                                                                                                                                        | Time-to-clean-push efficiency dragged                                                                                                                                                                                                                                                  | Split mixed trees earlier, verify optional browser QA tooling before attempting smoke, and treat stash-restored unrelated files as a new lane by default.                                                                                                      |
| 2026-05-22 | Supervised closeout-scope correction                                                                                                                                                                             |   7.4 | high       | The correction sharpened role boundaries                                                                                                                                                                                                                                                                                                             | Closeout scope drifted out of Gear Ball                                                                                                                                                                                                                                                | Restrict suggested next steps to SOP/process/self-scoring improvements unless broader repo recommendations are explicitly requested.                                                                                                                           |
| 2026-05-22 | Supervised timer-creation false claim                                                                                                                                                                            |   6.2 | high       | The failure mode is now explicit and durable                                                                                                                                                                                                                                                                                                         | Completion language outran tool evidence                                                                                                                                                                                                                                               | Do not use completion language for timers, automations, commits, pushes, or similar side effects until the tool has succeeded and returned confirmation.                                                                                                       |
| 2026-05-22 | Supervised full-worktree SOP correction                                                                                                                                                                          |   7.1 | high       | The user clarified the true SOP promise                                                                                                                                                                                                                                                                                                              | Scope control missed full-tree accounting                                                                                                                                                                                                                                              | Add a hard pre-commit classification pass for every live non-temp worktree file and recheck classification before any push-ready claim.                                                                                                                        |
| 2026-05-23 | Production SOP run with stale first closeout snapshot                                                                                                                                                            |   8.1 | high       | The corrected tree was still pushed cleanly                                                                                                                                                                                                                                                                                                          | The first closeout snapshot was stale                                                                                                                                                                                                                                                  | Rerun `git status --short` after the last build and draft the final report from the actual pushed commits, not from an earlier mental snapshot.                                                                                                                |
| 2026-05-23 | Production SOP run with voices retune and character workspace polish                                                                                                                                             |   8.8 | high       | Lane boundaries stayed clean through push                                                                                                                                                                                                                                                                                                            | Cheap blockers forced avoidable reruns                                                                                                                                                                                                                                                 | Catch formatting drift and stale size/test expectations before the first preflight pass so targeted lanes clear in one shot.                                                                                                                                   |
| 2026-05-23 | Production shared-runtime run with voices defaults and character resizing                                                                                                                                        |   8.7 | high       | One honest shared-runtime batch cleared clean                                                                                                                                                                                                                                                                                                        | Formatter drift forced extra preflight                                                                                                                                                                                                                                                 | Scan likely touched tests for stale style/typing assumptions before the first broad preflight so the shared-runtime lane clears in one pass.                                                                                                                   |
| 2026-05-23 | Production SOP run with Gear Ball prompt storage and panel-surface tuning                                                                                                                                        |   8.9 | high       | Full-tree accounting and batch boundaries held                                                                                                                                                                                                                                                                                                       | Preflight still burned on format/lint cleanup                                                                                                                                                                                                                                          | Run a quick cheap-pass sweep for touched callback deps and single-file formatting drift before the first shared-runtime preflight.                                                                                                                             |
| 2026-05-23 | Production SOP run with runtime-load trim and entry-surface polish                                                                                                                                               |   8.9 | high       | Docs and product lanes stayed cleanly split                                                                                                                                                                                                                                                                                                          | First preflight still burned on CSS formatting drift                                                                                                                                                                                                                                   | Run a tiny CSS/Prettier sweep on touched style files before the first mixed docs-plus-UI preflight.                                                                                                                                                            |
| 2026-05-23 | Scheduled production SOP run with create-entry unification                                                                                                                                                       |   8.8 | high       | Full-tree accounting and shared-runtime validation stayed honest                                                                                                                                                                                                                                                                                     | First preflight still burned on dead hook deps and an unused page binding                                                                                                                                                                                                              | Do one quick touched-file dead-binding/dependency sweep before the first shared page-shell preflight.                                                                                                                                                          |
| 2026-05-23 | Production SOP run with rail visibility and panel polish                                                                                                                                                         |   8.8 | high       | One coherent shared-runtime lane stayed intact through push                                                                                                                                                                                                                                                                                          | First preflight still paid avoidable format/lint cleanup                                                                                                                                                                                                                               | Add a tiny preflight sweep for touched docs/routes formatting plus dead constants on shared UI polish lanes.                                                                                                                                                   |
| 2026-05-23 | Production SOP run with AI Studio preview polish and deferred Copperknot lane                                                                                                                                    |   8.6 | high       | The validated AI Studio lane stayed coherent through push                                                                                                                                                                                                                                                                                            | Preflight paid small cleanup cycles and a new unrelated docs lane surfaced post-commit                                                                                                                                                                                                 | Re-audit for unrelated docs tails immediately after the first commit so deferred leftovers are named sooner and cost less attention.                                                                                                                           |
| 2026-05-23 | Production SOP run with voice-sample persistence and security retention cleanup                                                                                                                                  |   8.2 | high       | Full-tree accounting held and both real lanes were validated and pushed cleanly                                                                                                                                                                                                                                                                      | A staged-file spill crossed the first commit boundary, and the new secret scan needed a missing-file fix mid-run                                                                                                                                                                       | Before every commit, compare `git diff --cached --name-only` against the intended manifest so pre-staged files and hook-restored tails cannot slip across boundaries.                                                                                          |

| 2026-05-23 | Production SOP run with character save progress and Gottspan dispatch clarification | 9.0 | high | Two lean lanes stayed separate and validated cleanly | One cheap formatting rerun delayed the first preflight | Keep the fast whole-tree split and run a tiny touched-file Prettier sweep before the first frontend preflight. |
| 2026-05-23 | Production SOP run with runtime-prompt hardening and agent workspace rehome | 8.8 | high | Two large but coherent lanes stayed stable and validated honestly | The product lane still paid repeated cheap formatting reruns before the real signal | Keep the 2-lane collapse, but do one broad touched-file Prettier sweep up front on large shared-runtime batches so preflight clears in one pass. |
| 2026-05-23 | Production SOP run with handoff filing, preference scoping, and workspace preview sanitizing | 8.9 | high | Three honest lanes stayed separate and the final build passed on the exact pushed tree | First-pass validation still burned on formatting-only drift across docs and shared-runtime files | Run one manifest-wide Prettier sweep before the first preflight whenever the live tree mixes docs and shared-runtime lanes. |
| 2026-05-23 | Production SOP run with account hardening, panel polish, and Gottspan prompt filing | 8.8 | high | The live tree collapsed cleanly into one product lane plus one docs lane, and the final build passed on the exact pushed tree | First-pass validation still burned on formatting drift plus one tiny unused-catch lint miss before the real checks could speak | Run one touched-manifest Prettier plus dead-binding sweep before the first broad preflight on mixed product/docs runs. |
| 2026-05-23 | Production SOP run with character-panel hardening planning and embedded-media drop fixes | 9.0 | high | The live tree collapsed cleanly into one docs/governance lane plus one coherent product lane, and every focused check plus the final build cleared on the exact pushed tree | The run still paid one recovery cycle because the first focused Vitest command used the wrong repo-relative paths | Keep the lean two-lane collapse and derive focused test commands directly from the frontend workdir so path mistakes do not steal time before the real validation signal. |
| 2026-05-24 | Production SOP run with planning archive cleanup and QuickSwap retirement | 8.9 | high | The live tree collapsed cleanly into one docs/archive lane plus one coherent character-panel runtime lane, and the focused checks plus final build all cleared on the exact pushed tree | The product lane still paid one cheap formatting recovery cycle before the real validation signal could speak | Keep the lean two-lane collapse and run one tiny touched-file Prettier sweep before the first character-panel preflight so broad removals clear in one pass. |
| 2026-05-24 | Production SOP run with loading-skeleton polish and blocked-loop closeout | 8.9 | high | The live tree collapsed to one small character-panel lane, the focused checks stayed honest, and the final build cleared on the exact pushed tree | The first preflight still lost one cheap cycle to formatting drift in the loading skeleton file | Keep these micro-lanes tight and run one single-file Prettier sweep before the first preflight when a lane is this small. |
| 2026-05-24 | Production SOP run with Standard prompt bootstrap hardening and planning closeout | 8.8 | high | The live tree collapsed cleanly into one docs/control-plane lane plus one small character-panel lane, and both focused checks plus the final build cleared on the exact pushed tree | The docs lane still paid one cheap recovery cycle because `docs/api/api-internal-routes.md` needed formatting before the real validation signal could speak | Keep the two-lane collapse and run a tiny docs formatting sweep before the first mixed docs/control-plane preflight. |
| 2026-05-24 | Production SOP run with Standard selection isolation and looks-rail overflow controls | 8.8 | high | The live tree collapsed cleanly into one docs/control-plane lane plus one small UI/runtime polish lane, and both focused checks plus the final build cleared on the exact pushed tree | The docs lane still paid one cheap recovery cycle because `docs/api/api-internal-routes.md` needed formatting before the real validation signal could speak | Keep the two-lane collapse and run a tiny docs formatting sweep before the first mixed docs/control-plane preflight. |
| 2026-05-24 | Production SOP run with Standard selection isolation, billing rate limits, preparing image previews, and looks-rail overflow controls | 7.7 | high | The final shipped tree was still validated honestly, including both restored billing test seams and the restored Standard attachment-preview seam, and the combined lane cleared the final build | Three stash-restored tails surfaced after the first commit path, forcing multiple follow-up commits before push | Before final push, compare the live worktree against the just-validated manifest repeatedly until the tree is truly clean so restored tails get folded into the main lane instead of becoming serial patch commits. |
| 2026-05-24 | Production SOP run with voices mode cleanup and API mutation rate limits | 8.0 | high | The final shipped tree was validated honestly and the post-commit convergence loop kept working until the live tree was truly clean before the score loop and push | Three related API test tails still surfaced after the first runtime commit, forcing extra follow-up commits before convergence completed | Keep using the convergence loop, but broaden the first manifest to include all sibling API tests whenever a rate-limit/sanitized-error seam touches adjacent routes. |
| 2026-05-24 | Production SOP run with Dave lane-discipline docs, derivative-preserving voice outputs, and media-copy rate limiting | 8.4 | high | The run stayed converged: the related media/API tail was pulled into the live lane before commit, the focused tests plus final build cleared on the exact pushed tree, and the score loop waited for a clean tree | The first scoped wrapper underreached because `next.config.js` tripped an ignored-file ESLint warning, and the surfaced rate-limit test had a false 500 caused by reusing the same `Response` body across looped requests | When a lane touches ignored config files like `next.config.js`, skip the wrapper immediately and run the manual validation ladder from the outset so the first pass stays honest and fast. |
| 2026-05-24 | Production SOP run with custom-voice ownership authority, transcript propagation, and AI Studio loading/ordering polish | 8.7 | high | The tree stayed collapsed to one honest product lane, the focused test suite covered 29 files / 422 tests, and the final docs checks plus build passed on the exact pushed tree | The manifest wrapper failed on the long file list, and the build surfaced one real type-narrowing bug in `AiStudioProjectEntryState.tsx` before the lane could finish | On very large single-lane runs, skip the wrapper immediately and go straight to the manual ladder so the only remaining time cost is real code correction, not tooling friction. |
| 2026-05-24 | Production SOP run with voice-design preview tokens and authenticated admin instruction fetches | 8.9 | high | The tree collapsed cleanly to one shared voice/admin lane, the focused suite cleared 6 files / 88 tests, and the final build plus docs checks passed on the exact pushed tree | The first pass still lost a cheap cycle to two files that only needed Prettier | On compact shared-runtime lanes, start with a tiny Prettier sweep on obviously touched new/edited files so the rest of the ladder can stay one-pass clean. |
| 2026-05-24 | Production SOP run with signed-in issue reports, Ophestivus docs/tool rehome, and custom-voice quarantine follow-through | 8.8 | high | The whole tree stayed collapsed to one coherent product-and-governance lane, 17 focused test files / 154 tests passed, and the final build plus docs checks cleared on the exact pushed tree | The first pass still paid a small formatting cycle on three new admin-report files before the direct ladder was fully green | For medium-size mixed UI/docs/tooling lanes, run a tiny Prettier sweep on brand-new files before the rest of the ladder so the direct pass stays one-shot clean. |
| 2026-05-24 | Production SOP run with AI Studio prompt-control polish, custom voice-name guardrails, and object URL recovery | 8.8 | high | The tree stayed collapsed to one broad but coherent AI Studio lane, the direct test/build ladder cleared, and the final product commit landed cleanly on the exact validated tree | I initially let the score-loop writeback fall out of the commit path and had to catch that after the tree was already clean | After every main product commit, verify immediately whether the score-loop files changed this turn and commit them before moving on to the push step. |
| 2026-05-25 | Production SOP run with style-drop server-copy fallback and stable detail-preview selection | 9.1 | high | The tree stayed collapsed to one compact AI Studio lane, the targeted lint/tests all cleared, and the final production build passed on the exact pushed tree | The first pass still paid one cheap formatting cycle on `styleSourceNormalization.ts` before the ladder was fully clean | On compact single-lane UI/runtime runs, start with a one-file Prettier sweep when one touched file clearly dominates the diff so the first validation pass stays one-shot clean. |
| 2026-05-26 | Production SOP run with workflow-local restore authority and curated quick-slot drop precedence | 9.0 | high | The tree stayed collapsed to one coherent AI Studio lane, focused lint/tests plus docs checks passed, and the final production build cleared on the exact pushed tree | The first pass still paid one cheap formatting cycle on the workflow-settings files before the ladder was fully clean | On compact workflow-state lanes, run a small Prettier sweep on the touched hook and test cluster before the main ladder so the first pass stays one-shot clean. |
| 2026-05-29 | Production SOP run with elements-manager drop handling and create-surface camera permissions | 9.2 | high | The tree stayed collapsed to one coherent Elements Manager lane, the exact focused tests passed cleanly, and the final production build cleared on the exact pushed tree | No repo-local blocker surfaced after the direct ladder started; only the standing `production` branch-bypass remote note remained | Keep using the direct manual ladder immediately when `frontend/next.config.js` is in the lane so the first pass reaches real behavioral signal without wrapper noise. |
| 2026-05-29 | Production SOP run with reference-grid adaptive preview compaction and audio-cover transforms | 9.2 | high | The tree stayed collapsed to one coherent reference-grid lane, the focused runtime/adaptive suites passed cleanly, and the final production build cleared on the exact pushed tree | No repo-local blocker surfaced after the direct ladder started; only the standing `production` branch-bypass remote note remained | Keep broad reference-grid and adaptive-preview changes collapsed when they share one validation seam so the first build confirms the lane instead of reopening taxonomy work. |
| 2026-05-29 | Production SOP run with create-mode authority cleanup and quickswap preference retirement | 8.8 | high | The live tree stayed collapsed to one broad but coherent product-plus-schema lane, focused tests plus docs checks passed, and the final production build cleared on the exact pushed tree | The first cleanup pass still burned time on a formatter sweep that wrongly included raw SQL, and three tiny unused-type warnings delayed the real ladder | On mixed frontend/docs/sql lanes, keep the lane collapsed but split the cleanup pass by tool ownership: format frontend/docs first and leave raw SQL out unless a dedicated SQL formatter is part of the lane. |
| 2026-05-29 | Production SOP run with agent-surface pruning and video-upload authority hardening | 8.8 | high | The live tree stayed collapsed to one docs/governance lane plus one broad AI Studio runtime lane, `docs:check` passed, 25 focused test files / 327 tests passed, and the final production build cleared on the exact converged tree | The first cleanup pass lost time on mixed-manifest command friction: spaced doc paths needed shell-safe handling, raw SQL did not belong in the generic Prettier sweep, and the final build still surfaced one missing-import seam | On mixed docs/frontend/sql lanes, start with quote-safe repo-root loops and split the cleanup pass by tool ownership so the first real validation signal arrives before the build rung. |
| 2026-05-29 | Production SOP run with motion-reference lease cleanup, internal media-ref authority hardening, and Expert Edit launch-lock alignment | 8.3 | high | The live tree stayed collapsed to one honest product lane, docs checks plus the focused runtime/server/integration suites cleared, and the final production build passed on the exact converged tree | The run lost time to an initial repo-root path miss in the cleanup pass, a migration-doc parity miss, one build-caught `selectedRailTool` call-site seam, and stale broad `ExpertEditPanelView.test.tsx` expectations that were too wide for the actual changed contract | On broad shared-runtime lanes that also touch one large legacy UI suite, keep the lane collapsed but validate the exact touched expectations plus the modern integration/launch-lock seams instead of reopening the whole stale suite unless product code truly points there. |
| 2026-05-30 | Production SOP run with embedded media library authority, adaptive-preview delivery, and Expert Edit stage control cleanup | 8.6 | high | The live tree stayed collapsed to one honest shared-runtime lane, `docs:check` passed, 17 focused media-library/runtime suites plus 3 focused Expert Edit integration suites cleared, and the final production build passed on the exact converged tree | The run still lost time to early manifest cleanup friction, a build-caught null-guard seam in `mediaUploadService`, and a stale monolithic `ExpertEditPanelView.test.tsx` file that was no longer the honest authority for the changed contract | When a broad shared-runtime lane brushes a stale monolithic UI suite, prove the lane with focused modern integration seams plus the exact touched runtime/media tests, and only keep the legacy file in authority when the product evidence still points there. |
| 2026-05-30 | Production SOP run with Holomony Reference Grid onboarding and detail-preview authority polish | 9.0 | high | The live tree split cleanly into one Holomony docs lane plus one compact AI Studio runtime lane, focused tests cleared the exact preview/repair/render seams, and the final production build plus docs checks passed on the exact pushed tree | The only repo-local blocker was a single Prettier drift in the Reference Grid controller test file before the final build rung | Keep using the two-lane split for small docs-plus-runtime runs and start with a tiny touched-file Prettier sweep so the rest of the ladder stays one-pass clean. |
| 2026-05-30 | Production SOP run with Gutan onboarding, Holomony Canvas scope expansion, and Elements reference upload-state polish | 9.0 | high | The live tree split cleanly into one docs/tooling lane plus one compact Elements runtime lane, `docs:check` passed, the focused Elements layout suite cleared, and the final production build passed on the exact pushed tree | The only repo-local blocker was a first-pass formatting sweep across the new Gutan docs and one Elements test file, plus a harmless shell-script parser mismatch from using Prettier check too broadly | On mixed docs/tooling lanes with one small runtime seam, keep the two-lane split and aim the first cheap pass only at files the formatter actually owns so the rest of the ladder stays one-pass clean. |
| 2026-05-31 | Production SOP run with billed-credit authority docs, launch follow-through, and AI Studio generated-media pricing hardening | 8.9 | high | The live tree collapsed to one docs/agent-support lane plus one broad shared-runtime lane, `docs:check` passed, 18 focused suites cleared with 333 passing tests, and the final production build passed on the exact converged tree | The first cleanup pass still cost time on a broad mixed-manifest sweep, and the final build was the first place a type-shape seam in `pricingGridBilledCredits.ts` surfaced | On broad pricing/runtime lanes, do a quick local type-shape scan on new shared helper adapters before the final build rung so the last pass confirms the lane instead of discovering a narrow contract mismatch. |
| 2026-05-31 | Production SOP run with pricing-grid variant normalization and signed storage-url recovery in Reference Grid | 8.9 | high | The live tree stayed collapsed to one compact shared-runtime lane, focused pricing/reference-grid suites passed, `docs:check` passed, and the final production build passed on the exact converged tree | The final build was the first place a narrow `PricingStrategyId` typing seam surfaced between the admin pricing model row and the new shared variant-rule helper | When pulling admin-grid rules into shared runtime helpers, normalize the type boundary up front so the final build confirms behavior instead of discovering a small adapter mismatch. |
| 2026-05-31 | Production SOP run with Copperknot fix-shape guidance and Create billed-resolution preservation | 9.0 | high | The live tree split cleanly into one Copperknot docs lane plus one compact AI Studio runtime lane, `docs:check` passed, the focused view-model suite cleared with 38 passing tests, and the final production build passed on the exact converged tree | The only real blocker was a narrow build-time typing seam where the new shared pricing-resolution helper expected a stricter strategy-id shape than the admin model row exposed | When moving shared pricing helpers across admin/runtime boundaries, normalize permissive string inputs at the helper boundary immediately so the final build stays a confirmation pass. |
| 2026-05-31 | Production SOP run with Copperknot post-deploy queue refresh and AI Studio prompt/layout normalization | 9.0 | high | The live tree split cleanly into one Copperknot post-deploy docs lane plus one compact shared-runtime lane, `docs:check` passed, 9 focused suites cleared with 152 passing tests, and the final production build passed on the exact converged tree | The only repo-local friction was the initial formatter sweep across the mixed docs/runtime manifest before the focused ladder | Keep this two-lane collapse for post-deploy follow-through plus compact runtime hardening, and start with one quote-safe mixed-manifest formatter pass so the focused suites stay one-shot clean. |
| 2026-05-31 | Production SOP run with Copperknot user-load discipline tightening | 9.2 | high | The live tree stayed collapsed to one tiny docs-only lane, `docs:check` passed cleanly, and the lane closed in one straightforward commit path plus the score writeback | No repo-local blocker surfaced after the direct docs ladder started | Keep docs-only Copperknot maintenance lanes collapsed and use the direct docs ladder immediately so the run stays almost frictionless. |
| 2026-05-31 | Production SOP run with Datserok agent onboarding | 9.2 | high | The live tree stayed collapsed to one tiny docs-only lane, `docs:check` passed cleanly, and the new agent surfaces plus indexes landed in one straightforward commit path | No repo-local blocker surfaced after the direct docs ladder started | Keep new-agent onboarding as one docs-only lane when the work is purely contract and artifact scaffolding so the run stays almost frictionless. |
| 2026-05-31 | Production SOP run with projection-backed project generation ownership | 9.1 | high | The live tree stayed collapsed to one compact shared-runtime lane, the focused project-generation/workspace suites passed with 43 tests, and the final production build passed on the exact converged tree | No repo-local blocker surfaced after the direct lane ladder started | Keep server-only ownership/persistence fixes as one compact runtime lane and go straight to the exact focused suites plus final build. |
| 2026-05-31 | Production SOP run with project-workspace persistence hardening, billed-image policy materialization, and launch-authority follow-through | 8.6 | high | The live tree converged into one docs/authority lane plus one shared-runtime lane, `docs:check` passed, 9 focused runtime suites cleared with 144 passing tests, and the final production build passed on the exact pushed tree | The run still paid two test-expectation drift fixes, two build-caught type-narrowing seams in `useAiStudioPageMediaReferenceRuntime.ts`, and one related backlog tail that had to be folded in after the runtime commit | When a shared-runtime lane changes canonical billing and restored-canvas identity together, do one upfront expectation pass on the touched pricing/view-model and workspace-read tests so the final build is less likely to discover branch-narrowing seams late. |
| 2026-05-31 | Production SOP run with auth-authority hardening, upload rate limits, and persistence/ownership convergence | 8.9 | high | The live tree converged into one docs/authority lane plus one shared-runtime lane, `docs:check` passed, 24 focused suites cleared with 175 passing tests, and the final production build passed on the exact pushed tree | The only real repo-local friction was an upfront mixed-manifest formatter pass and harmless recurring macOS libgio/libvips duplicate-class warnings during some media-route tests | Keep broad auth/upload/runtime lanes collapsed when they share one API-hardening seam, and start with one quote-safe formatter pass so the focused ladder stays one-shot clean. |
| 2026-05-31 | Production SOP run with Copperknot launch-control refresh and media/Stripe authority hardening | 8.4 | high | The live tree split cleanly into one Copperknot docs lane plus one broad shared-runtime lane, `docs:check` passed, focused runtime suites cleared with 301 passing tests plus targeted rechecks, and the final production build passed on the exact converged tree | The run still paid for a repo-root path miss, tracked-diff manifests that would have skipped a new file without the full-tree re-audit, a hook-blocking secret-pattern fixture, a stale error-logging allowlist line anchor, and two build-time type seams in the Stripe subscription boundary | Build frontend manifests from live `git status --short`, not tracked diffs alone, and scan shared helper/type boundaries plus hook-sensitive fixture strings before the first commit path so the final build stays a confirmation rung instead of a seam finder. |
| 2026-06-01 | Production SOP run with planning-archive rehome, Copperknot/Holomony media authority docs, and media-display/workspace restore hardening | 8.5 | high | The live tree converged into one docs/archive lane plus one shared-runtime lane, `docs:check` passed, focused preview/signing/workspace suites and the Expert Edit parity core all cleared, and the final production build passed on the exact pushed tree | The run still paid for broad formatting drift, one small lint-only import fix, two late build-time type seams (`useAiStudioProjectWorkspacePersistenceController.ts` and `mediaLibraryDragGhost.ts`), and one post-commit docs tail where the old `docs/planning/*` copies had to be deleted in a final follow-through commit | When archive moves replace tracked planning docs with `docs/archive/` copies, explicitly stage both the new archive files and the old-path deletions in the first docs commit, and keep broad legacy UI suites out of lane authority when the touched expectations can be proven by smaller modern seams plus the final build. |
| 2026-06-01 | Production SOP run with protected-route bootstrap unification, GPT Image 2 naming cleanup, and media preview-field authority hardening | 7.9 | high | The live tree converged into one docs/agent-support lane plus one broad shared-runtime lane, `docs:check` passed, 25 focused suites cleared with 298 passing tests, and the final production build passed on the exact converged tree | The run still paid for one docs-check drift after route-helper extraction, a compiler/lint seam after simplifying autosave snapshot preparation, one late dashboard lazy-import lint tail, and one hook test that had to be realigned to the new computation behavior | When moving canonical route metadata or removing manual memoization, update the supporting repo checks and any optimization-sensitive tests in the same first pass so docs drift and stale performance assertions do not become the last blockers. |
| 2026-06-01 | Production SOP run with Gear Ball scoring rebase and project-restore bootstrap hardening | 9.1 | high | The live tree converged into one Gear Ball docs lane plus one shared-runtime lane, exact lint passed, 8 focused suites cleared with 51 passing tests, `docs:check` passed, and the final production build passed on the exact converged tree | No repo-local blocker surfaced after the focused ladder started | Keep shared project-restore/loading-surface hardening collapsed to one runtime lane and prove it with the exact hook, component, and dashboard suites before the final build rung. |
| 2026-06-01 | Production SOP run with dashboard auth bootstrap hardening, Standard chat formatting refinement, generation recovery stage timing follow-through, and Datserok load pruning | 7.4 | high | The final pushed tree converged cleanly: focused dashboard/auth/runtime suites passed, generation control-plane suites passed, `docs:check` passed twice, and the final production build passed on the exact converged tree before closeout | The first manifest underreached badly: multiple stash-restored tails surfaced after the first product commit path, the build discovered a real `authGuard.ts` webpack seam, and related dashboard route docs plus separate generation and Datserok lanes had to be folded in late before the tree truly settled | Build the first manifest from full live status plus one aggressive sibling sweep around touched route, auth, and prompt-runtime surfaces, and do not trust a seemingly clean post-commit tree until stash-restored tails have been explicitly re-audited. |
| 2026-06-01 | Production SOP run with auth-scoped media URL hardening and post-restore workspace autosave recovery | 8.0 | high | The final pushed tree converged into one auth/media route lane plus one workspace-persistence lane, 10 focused suites cleared with 85 passing tests, `gear-ball:preflight` passed on both lanes, and the final production build passed on the exact converged tree | The first manifest undercounted related auth-route tails multiple times, one render-time ref fix had to be reworked to the repo-approved microtask state pattern, and one focused hook test run initially used the wrong workdir | Do one harder first-pass sibling/tail sweep around touched auth/helper/route surfaces, and run focused Vitest suites from the exact owning workdir by default so late tail discovery and command-shape noise do not steal the first commit path. |
| 2026-06-01 | Production SOP run with Dave threat-first guidance, ElevenLabs voice ownership filtering, and project autosave unlock refinement | 8.2 | high | The final pushed tree converged into one docs lane plus two compact product lanes, the exact hook suite and 4 focused ElevenLabs route suites passed, `gear-ball:preflight` passed on both product lanes, `docs:check` passed, and the final production build passed on the exact converged tree | The initial lane map undercounted the live ElevenLabs tail cluster, so the run was not honestly ready to stage after the first validation pass even though the final tree recovered cleanly | After the first validation rung, always re-check live `git status --short` before planning the first commit message so hidden same-session tails are folded in before the commit phase starts. |
| 2026-06-01 | Production SOP run with fail-closed auth and billing hardening, storage-entitlement grant tightening, and workspace autosave/reference hydration refinement | 8.9 | high | The live tree converged cleanly into one security lane plus one workspace-runtime lane, both `gear-ball:preflight` manifests passed, 8 focused suites plus 6 focused security/runtime suites cleared with 94 passing tests, `docs:check` passed, and the final production build passed on the exact pushed tree | The run still paid one cheap formatting cycle in the security lane, one migration-doc parity miss after the new SQL migration landed, and one expired build session that had to be rerun before the commit phase could close | On mixed security/sql/runtime runs, add new migrations to the docs inventory immediately, do one tiny touched-file formatter sweep before the first preflight, and treat any lost build tail as a prompt to rerun the final build once rather than delaying the commit split. |
| 2026-06-01 | Production SOP run with Dave account-isolation guidance refresh and library quick-slot workspace ownership preservation | 8.8 | high | The live tree converged into one compact docs lane plus one compact AI Studio/runtime lane, `docs:check` passed on the final docs batch, `gear-ball:preflight` passed, 3 focused suites cleared with 77 passing tests, and the final production build passed on the exact converged tree | A new Dave audit report surfaced only after the first validation pass, so the docs lane needed one extra convergence check before the first commit even though the run stayed clean after that | Re-run `git status --short` immediately after validation and before the first commit on mixed docs-plus-runtime runs so fresh audit artifacts are folded into the docs lane before the first commit message is chosen. |
| 2026-06-01 | Production SOP run with hosted storage-entitlement grant findings and project-workspace quick-slot telemetry tracing | 8.9 | high | The live tree converged into one compact Dave docs lane plus one compact project-workspace runtime lane, `docs:check` passed, `gear-ball:preflight` passed, 2 focused hook suites cleared with 34 passing tests, and the final production build passed on the exact converged tree | The only repo-local friction was one cheap formatting cycle in `useAiStudioProjectWorkspaceRestoreHydration.ts` before the shared-runtime preflight could finish | On compact shared-runtime lanes, run one tiny touched-file formatter sweep before the first preflight whenever a newly edited hook file is likely to trip formatting-only noise. |
| 2026-06-02 | Production SOP run with Dave account-isolation audit packets, project-route error sanitization, media-list companion-art scope guarding, and prepared quick-slot library insertion | 9.0 | high | The live tree converged into one compact Dave docs lane plus one coherent shared project/reference-runtime lane, `docs:check` passed, `gear-ball:preflight` passed, 3 focused suites cleared with 68 passing tests, and the final production build passed on the exact converged tree | No late tails or build-time seams surfaced after the first honest manifest, and the only notable cost was the normal hook formatter/lint pass during commit | Keep broad project/reference lanes collapsed when API disclosure hardening, media-scope regression coverage, and quick-slot persistence prep all share one validation seam so the first build stays a confirmation rung. |
| 2026-06-01 | Production SOP run with Pulse ownership/workspace realignment and project-route media autosave bootstrap gating | 8.5 | high | The final pushed tree converged into one compact docs lane plus one shared-runtime AI Studio lane, `docs:check` passed, `gear-ball:preflight` passed, 2 focused autosave suites cleared with 18 passing tests, and the final production build passed on the exact runtime tree before the docs-only close | The run paid one cheap formatting cycle in the new helper file, one first-pass Vitest workdir miss, and a live Pulse docs lane surfaced only at the final worktree audit so batching finished later than it should have | Re-run full `git status --short` immediately before the first validation rung on small runtime runs so any simultaneous docs lane is collapsed into the plan before validation starts. |
| 2026-06-01 | Production SOP run with Pulse operating-package expansion and Standard response plus restore-authority hardening | 8.4 | high | The final pushed tree converged into one compact Pulse docs lane plus one shared Standard/restore-runtime lane, `docs:check` passed, `gear-ball:preflight` passed, 7 focused suites cleared with 108 passing tests plus a 3-suite Standard rerun with 49 passing tests, and the final production build passed on the exact converged tree | The first runtime preflight paid two formatting fixes, ESLint exposed one dead import after the refactor, and the first full build discovered a real shared-contract seam where Standard transport results no longer satisfied the shared `workflowSession` type | When shared transport contracts move, check the binding/interface seam in the first pass and run one tiny formatter sweep before preflight so build-only and formatting-only tails do not steal the commit path. |
| 2026-06-01 | Production SOP run with Standard memory-window extraction, Standard message presentation modularization, and restore-safe reference projection | 8.6 | high | The live tree stayed collapsed to one coherent Standard/runtime lane, `gear-ball:preflight` passed, the focused Standard send/runtime and projection suites cleared, and the final production build passed on the converged tree | The build still surfaced two real shared-contract seams because the new Standard session-memory send path had not been kept compatible with both the direct runtime caller and the older orchestration hook | When narrowing a shared Standard send contract, update both call-site families in the same first pass so the final build remains a confirmation rung instead of finding compatibility drift. |
| 2026-06-02 | Production SOP run with restored quick-slot membership recovery from pinned outputs | 9.2 | high | The live tree stayed collapsed to one compact AI Studio restore/runtime lane, `gear-ball:preflight` passed, 3 focused owner suites cleared with 83 passing tests, and the final production build passed on the exact converged tree | The only repo-local friction was one cheap formatting pass in the Reference Grid selector-store test before the shared-runtime preflight could finish | On compact shared-runtime test lanes, run one tiny touched-file formatter sweep before the first preflight when a newly edited UI test is likely to trip formatting-only noise. |
| 2026-06-02 | Production SOP run with passive-wheel audit docs, Standard memory/detail-modal modularization, AI Studio header project-name display, and Pulse capability-plan docs | 7.2 | high | The final pushed tree did converge into four honest lanes, `docs:check` passed, the focused Standard/runtime suites cleared with 121 passing tests, the header regression test passed, repeated `gear-ball:preflight` runs cleared the final 37-file manifest, and the production build passed on the converged tree | The first manifest undercounted several live tails, multiple build-only and lint-only type/import seams in the shared detail-modal extraction forced extra reruns, and the closeout only stabilized after a late full-tree re-audit | Before the first product commit on broad shared-runtime work, do one explicit sibling sweep around every new extracted helper directory and one final live-status sweep before staging so late tails and trivial seam fixes stop stealing the commit path. |
| 2026-06-02 | Production SOP run with Datserok startup-load tightening and Standard memory plus shared media-detail expansion | 8.4 | high | The live tree converged into one compact Datserok docs lane plus one coherent shared-runtime AI Studio lane, `docs:check` passed, `gear-ball:preflight` cleared the final 45-file manifest, 10 focused suites passed with 155 tests, and the production build passed on the converged tree | The first runtime manifest still missed three related detail-modal helpers, so one late same-lane fold-back plus another formatter pass were needed before the batch was honestly ready to stage | When broad runtime work extracts shared presentation helpers, force one pre-commit sibling sweep over the whole new helper cluster and add every touched helper file to the first manifest so the last clean-tree audit stays boring. |
| 2026-06-02 | Production SOP run with Standard memory expansion, shared media-detail unification, canvas detail-modal wiring, and Kie upload auth hardening | 9.0 | high | The live tree stayed collapsed to one coherent AI Studio/runtime lane, `docs:check` passed, `gear-ball:preflight` cleared the full touched manifest, 22 focused/fan-out suites passed with 334 tests, and the final production build passed on the converged tree | The only repo-local friction was one small lint seam in `DetailModal`, one dead helper in `referenceDownload`, and one build-only wrapper-type drift after adding `shortpulseRetryAuth401` to the authenticated fetch contract | When adding a new shared fetch-helper option, widen the thin wrapper type in the same first pass and do one tiny formatter/lint seam sweep before the first preflight so the build stays a confirmation rung. |
| 2026-06-02 | Production SOP run with staged transient reference-image uploads, larger project-workspace autosave budgets, and visible Standard Create guardrail reasons | 9.2 | high | The live tree stayed collapsed to one coherent AI Studio/runtime lane, `docs:check` passed, `gear-ball:preflight` cleared the full touched manifest, 7 focused suites passed with 85 tests, and the final production build passed on the converged tree | The only repo-local friction was one tiny Prettier seam in the new staged image-upload helpers, and the focused workspace-persistence suite still emitted the standing `act(...)` warnings during the pass | On browser-direct upload lanes, run one tiny formatter sweep on the new client upload helper and its tests before the first preflight so the final path stays purely confirmatory. |
| 2026-06-02 | Production SOP run with admin pricing custom rows, AI Studio snapshot freshness/media authority hardening, and Nuclo Kie Vercel env audit docs | 8.9 | high | The live tree stayed collapsed to one broad product lane plus one compact Nuclo docs lane, `docs:check` passed before batching, `gear-ball:preflight` cleared the 70-file frontend manifest with preview-contract fan-out, 27 focused suites passed with 345 tests, and the final production build passed on the converged tree | The run still paid a small dead-prop/lint cleanup round before preflight and one build-only TypeScript nullability seam in `useAiStudioSessionAutosave.ts` before the final build could close | When a touched hook rewires retry or failure refs, make nullability explicit in the first pass and let one owning hook-suite rerun plus the final build prove the fix instead of rediscovering the seam late. |
| 2026-06-02 | Production SOP run with Standard `gpt-5.5` default promotion, Pulse decision-packet docs, and Create composer focus-restore hardening | 9.4 | high | The live tree stayed collapsed to one coherent Standard/Pulse runtime lane, `docs:check` passed, `gear-ball:preflight` cleared the 7-file frontend manifest, the locked Standard proof surface plus the changed PromptStep action suite passed with 125 tests, and the final production build passed on the converged tree | No related tails or build-time seams surfaced after the first honest manifest; the only real cost was carrying a slightly broader proof set because the model-policy lane and focus-restore lane shared one publish surface | When this exact Standard model-policy lane repeats, keep reusing the locked proof command plus the owning composer-action suite so the first validation rung stays both narrow and sufficient. |
| 2026-06-02 | Production SOP run with Standard Responses transport gating, first-send composer-shell focus continuity, and isolated admin pricing input layout cleanup | 9.3 | high | The live tree collapsed honestly into one Standard runtime/docs lane plus one tiny admin CSS lane, `docs:check` passed, `gear-ball:preflight` cleared the 7-file frontend manifest, the targeted Standard route/gateway/panel proof surface passed with 125 tests, and the final production build passed on the exact converged tree | The only real drag was one cheap staging path typo and the fact that an unrelated admin CSS change was live in the worktree, which forced a second tiny commit instead of a one-batch close | When a compact shared-runtime lane is already proven and one unrelated UI-only file remains live, split it early and keep the first staged manifest lane-pure so the commit phase stays one-shot clean. |
| 2026-06-02 | Production SOP run with Enate Ende workspace bootstrap, Standard Responses continuity, and AI Studio video restore authority | 8.7 | high | The live tree converged into one compact Enate Ende docs lane plus one broad but coherent AI Studio/runtime lane, `docs:check` passed, `gear-ball:preflight` cleared the final 57-file frontend manifest, 26 focused suites passed with 529 tests on the exact committed tree, and the final production build passed on the exact committed tree | The first manifest missed untracked files, the broad preflight lost one formatter-only cycle, and the product close needed one build-only ref-typing fix plus an exact-tree reproof after hook rewrites during commit | Build the first manifest from live `git status --short`, not only tracked diffs, and assume broad hook rewrites on large staged TypeScript batches so untracked files and post-hook exact-tree proof are planned up front instead of discovered late. |
| 2026-06-03 | Production SOP run with Edit workflow rerate docs, Canvas launch planning, Canvas restore/detail proof, and Standard chat dragging | 9.0 | high | The live tree collapsed cleanly into one docs/rerate lane plus one compact AI Studio/runtime lane, `docs:check` passed, `gear-ball:preflight` cleared the 12-file frontend manifest, 10 focused suites passed with 175 tests on the exact committed tree, and the final production build passed on the exact committed tree | The only real drag was one formatter-only preflight recovery in `AgentChatPanel.tsx`, and the product close still needed one exact-tree reproof because the commit hooks ran `eslint --fix` and `prettier` across the staged TypeScript batch | When a medium TypeScript batch is headed for hook-driven commit cleanup, run one tiny touched-file formatter sweep first and budget the exact-tree reproof up front so the close stays fast without guessing about hook-side diffs. |
| 2026-06-03 | Production SOP run with Pulse artifact consolidation plus Kie video duration, hidden shot-mode, Seedance reference-mode, and canvas prompt-anchor/runtime refinements | 8.8 | high | The live tree converged into one compact Pulse docs lane plus one broad but coherent AI Studio video/runtime lane, `docs:check` passed, `gear-ball:preflight` cleared the refreshed 31-file frontend manifest, 12 focused canvas/video/runtime suites passed with 225 tests, and the final production build passed on the exact pre-score tree | The first manifest undercounted same-lane canvas tails, preflight still had to burn one formatter cycle and two cheap lint seams, and one mid-run manifest regeneration from `frontend/` accidentally stripped the `frontend/` prefixes before I rebuilt it from repo root | Rebuild any status-backed manifest from repo root after subdirectory test work, and do one last full live-status sweep before the final preflight so same-lane tails and path-shape mistakes do not survive into closeout. |
| 2026-06-03 | Production SOP run with Enate Ende drag-design notes, project-owned Pulse chats, Standard/Kie video restore hardening, and ElevenLabs provider retry normalization | 8.6 | high | The live tree converged honestly into one tiny Enate docs lane plus one broad AI Studio/runtime lane, `gear-ball:preflight` cleared the exact 70-file frontend manifest, 22 focused suites passed with 466 tests on the exact committed tree, and the final production build passed on the exact committed tree | The run still paid several late type-boundary seams across Pulse chat hydration and project snapshot patching, the manifest had to be rebuilt as new touched runtime files surfaced, and the product commit required an exact-tree reproof after hook-driven `eslint --fix` and `prettier` rewrites | When a new runtime seam introduces snapshot or hydration helpers, widen the shared contract and every caller in the same first pass, then rebuild the live-status manifest immediately after any new touched file appears so the first exact-tree build becomes confirmation instead of discovery. |
| 2026-06-03 | Production SOP run with agent-governance refresh, media-folder-canvas retirement, project-output display records, and scheduler hardening | 8.9 | high | The live tree converged into one docs/governance lane plus one broad AI Studio/runtime/sql lane, `docs:check` passed, `gear-ball:preflight` cleared both the pre-commit and exact committed frontend manifests, 22 focused suites passed with 530 tests, and the final production build passed on the exact committed tree | The first preflight still paid one formatting-only recovery, the browser-bound Playwright audit scripts and release checks were not runnable in this SOP because they required `PLAYWRIGHT_AUDIT_EMAIL` and a valid browser target, and the product commit needed an exact-tree reproof after hook-driven `eslint --fix` and `prettier` rewrites | Classify browser-auth-gated audit scripts before the main proof plan so production SOP validation does not spend cycles discovering missing Playwright credentials or localhost-only assumptions mid-run. |
| 2026-06-04 | Production SOP run with plan-based concurrency controls, AI Studio persistence/presets/runtime hardening, Kie GPT Image 2 route expansion, and launch-planning/doc updates | 8.8 | high | The live tree converged into one already-validated billing commit plus one broad AI Studio/runtime commit and one docs/planning commit, `gear-ball:preflight` cleared the final 104-file frontend manifest, 33 focused suites passed with 607 tests, `docs:check` passed, and the runtime build passed both before and after the hook-adjusted runtime commit | The first runtime manifest still missed route-sync tails and one submission-matrix suite, preflight burned several formatter-only recoveries, and the hook-driven runtime commit required an exact-tree rebuild pass before the lane was honestly ready to push | When route-inventory work is live, run the wrapper sync before the first manifest lock and immediately rebuild the touched-file/test manifests after that sync so generated route tails and their owning tests do not surface during closeout. |
| 2026-06-04 | Production SOP run with AI Studio media-library telemetry filtering and telemetry/concurrency docs refresh | 9.3 | high | The live tree split cleanly into one compact runtime lane plus one docs/research lane, `gear-ball:preflight` passed on the runtime manifest before and after the hook-adjusted commit, 4 focused suites passed with 42 tests, `docs:check` passed, and no late same-lane tails surfaced after the first lane split | The only real drag was the required exact-tree rerun after the runtime commit hooks rewrote staged TypeScript files | Keep compact runtime lanes collapsed and budget one exact-tree rerun immediately after hook-driven TypeScript commits instead of treating that recheck as surprise churn. |
| 2026-06-04 | Production SOP run with Canvas live-props isolation, pan and wheel responsiveness hardening, video poster authority recovery, and Canvas/archive planning notes | 9.1 | high | The live tree split cleanly into one compact Canvas runtime lane plus one docs/archive lane, `gear-ball:preflight` passed before and after the hook-adjusted runtime commit, 4 focused suites passed with 68 tests on both passes, `docs:check` passed, and the runtime build passed both before and after the exact-tree rerun | The only meaningful friction was one cheap Prettier miss in the touched manifest and one real `react-hooks/refs` lint seam in the new live-props bridge before the runtime lane was ready to commit | When adding stable live-props bridges for shared Canvas surfaces, initialize the stable store/object through lazy state instead of ref writes during render, and keep the exact-tree rerun budgeted whenever hook-driven TypeScript commits will rewrite the staged lane. |
| 2026-06-04 | Production SOP run with Canvas interaction backpressure, Reference Grid suspension wiring, and Standard assistant-to-prompt handoff | 9.2 | high | The live tree stayed collapsed to one coherent AI Studio/runtime lane plus its owning Enate Ende evidence update, `gear-ball:preflight` passed before and after the hook-adjusted commit, 5 focused suites passed with 95 tests on both passes, `docs:check` passed before and after commit, and the runtime build passed before and after the exact-tree rerun | The only real drag was two cheap Prettier misses in the first preflight and the expected exact-tree rerun after the commit hooks rewrote the staged TypeScript batch | When a broad AI Studio lane spans Canvas and Standard chat/composer wiring, keep it as one honest runtime batch and add the direct surface-threading suite before the first build so the final build confirms the lane instead of discovering a missing handoff seam. |
| 2026-06-04 | Production SOP run with Canvas media render recovery through output and media-id authority refresh | 9.3 | high | The live tree stayed collapsed to one compact Canvas/runtime lane, `gear-ball:preflight` passed before and after the hook-adjusted commit, 2 focused suites passed with 62 tests on both passes, and the runtime build passed before and after the exact-tree rerun | The only real drag was the expected exact-tree rerun after commit hooks rewrote the staged TypeScript batch, while the focused suites still emitted the standing jsdom `HTMLMediaElement.pause()` warnings without failing | On compact restore-signing lanes, keep the proof surface anchored to the owning Canvas drop and page-media-runtime suites and accept the hook-driven exact-tree rerun as the only expected duplicate cost. |
| 2026-06-04 | Production SOP run with large-project AI Studio responsiveness, reference-grid media authority recovery, project-open gating, Kie GPT Image 2 safety defaults, and Enate Ende latency reclassification | 8.7 | high | The live tree converged as one broad but coherent AI Studio/runtime lane plus its owning Enate Ende and Kie docs follow-through, `gear-ball:preflight` passed on the full 39-file frontend manifest, 16 focused suites passed with 269 tests, and the production build passed before the score loop | The first pass still paid three formatter-only misses, two cheap hook-dependency seams, and one false-red focused Vitest run because it was launched from repo root instead of the frontend-owned jsdom context | On broad frontend lanes, keep the first manifest frozen from live status, but always launch focused Vitest from `frontend/` or with the explicit frontend config so DOM suites do not waste a cycle failing in a plain Node environment. |
| 2026-06-05 | Production SOP run with Canvas tear-out to Create composer routing, create-runtime helper extractions, and launch governance/latency doc refresh | 9.0 | high | The live tree converged into one broad frontend/runtime lane plus one docs/governance lane, `gear-ball:preflight` caught the needed preview and character-layout fan-out early, 26 changed-suite frontend tests passed with 474 passing and 41 skipped tests, `docs:check` passed, and the production build passed before and after the hook-adjusted frontend commit | The first frontend preflight still paid ten formatter-only misses, the temporary manifest readback path was annoyingly inconsistent twice, and the exact-tree rerun was still required after hook-driven TypeScript rewrites | On broad frontend lanes, do one touched-file Prettier sweep before the first preflight and use a single known-good manifest path pattern from the start so the first proof pass stays purely about real contract risk. |
| 2026-06-05 | Production SOP run with media preview modal hardening, latency planning refresh, AI Studio video pricing parity, and converged Canvas tear-out drag ghost lanes | 7.9 | high | The final pushed tree converged honestly into five shipped commits, `gear-ball:preflight` cleared the media-preview lane, pricing lane, and final Canvas lane, focused suites passed with 95 + 115 + 41 tests, the pricing drift check passed, `docs:check` passed, and the final production build passed on the converged tree | The run lost a lot of time to late same-lane tail discovery: pricing tests surfaced only after the pricing source pass, and the Canvas lane widened from hook-plus-contract into renderer plus CSS only after two follow-up commits, forcing repeated reclassification and reruns | When a lane starts inside a deep Canvas or Create subtree, sweep the adjacent state hook, renderer, shared contract, styles, and owning tests together before the first commit so hook stash restore cannot keep revealing the rest of the same lane after closeout already looked near-done. |
| 2026-06-05 | Production SOP run with ElevenLabs audio pricing-grid UI display and server billing parity | 9.1 | high | The live tree stayed collapsed to one coherent shared-runtime audio lane, `gear-ball:preflight` passed before and after the hook-adjusted commit, 9 focused suites passed with 160 tests, the pricing drift check passed, docs parity checks passed, and the runtime build passed before and after the exact-tree rerun | The only real drag was one formatter-only recovery in the audio hook and drift script plus one callback-dependency seam in the audio property panels before the first preflight could clear | When a pricing-display migration adds `pricingPolicyReady` to submit payloads, update the owning callback dependency lists in the same first pass so the React Compiler checks stay confirmatory instead of becoming the first red rung. |
| 2026-06-07 | Production SOP run with AI Studio workflow reload restore metadata plus a separate Copperknot July 7 launch-method refresh | 9.0 | high | The live tree collapsed cleanly into one broad AI Studio shared-runtime lane plus one docs-only Copperknot lane, `gear-ball:preflight` passed before and after the hook-adjusted runtime commit, 25 focused suites passed with 434 tests, the runtime build passed before and after the exact-tree rerun, and the Copperknot docs lane cleared `docs:check` cleanly before closeout | The only preventable drag was a formatter-only first preflight pass and one bad `git add --pathspec-from-file` staging attempt before switching back to the simpler file-fed staging path, while the expected exact-tree rerun still remained necessary after hook rewrites | On broad runtime lanes, do one touched-file Prettier sweep before the first preflight and prefer the simplest staged-file feed path over clever pathspec wrappers so the first pass stays about product risk instead of tool friction. |
| 2026-06-07 | Production SOP run with Expert Edit prompt-reference expansion, bounded plain-session generated-output restore, product-image staging/finalize routes, and shared AI Studio runtime/docs/sql follow-through | 8.8 | high | The live tree stayed collapsed to one broad but coherent AI Studio/runtime/docs/sql lane, `gear-ball:preflight` cleared the exact 177-file manifest with 49 passing test files and 833 passing tests, repo-wide `type-check` passed after the stale fixture contracts were updated, and the production build passed cleanly on the same runtime tree | The run still paid multiple formatter-only preflight stops up front, and repo-wide `type-check` surfaced three stale test-fixture builders outside the first focused test manifest after the main runtime proof was already green | When a broad shared-runtime lane changes required view-model or panel-contract inputs, sweep the companion fixture-builder tests into the first type-check audit and do one touched-file Prettier pass before the first preflight so late repo-wide test-fixture drift cannot become the last blocker. |
| 2026-06-07 | Production SOP run with a redesigned `/profile` account workspace plus a late Expert Edit secondary-rail CSS tail | 8.7 | high | The profile lane stayed coherent as one account-workspace batch, file-scoped preflight passed after a small formatter recovery, 4 focused dashboard/profile suites passed with 24 tests before and after the hook-adjusted commit, docs parity checks passed, the production build passed on the committed profile tree, and the late CSS tail converged cleanly with its 2 owning Expert Edit contract suites green | The run still paid one avoidable path-context miss during the formatter fix, then lost time when an unrelated Expert Edit CSS file resurfaced only after the profile commit and required a second classification pass plus a stale CSS-contract test repair before the tree could go clean | When a hook-restored tail collapses to one style-owned file, search for the owning contract tests immediately and validate that tiny lane on its own before assuming the run is finished or budgeting a broader rerun. |
| 2026-06-07 | Production SOP run with generation reconcile route wiring, workflow-reload hardening, Expert Edit secondary-slot/layout updates, and a late profile stylesheet import tail | 8.8 | high | The live tree still converged into one broad AI Studio/runtime/docs lane plus one tiny related profile-style tail, `gear-ball:preflight` caught the required character-layout contract fan-out, focused suites passed with 14 files, 301 passing tests, and 41 skipped tests on the exact committed runtime tree, the profile tail cleared file-scoped preflight plus `profile.account-settings`, and the production build passed before and after the exact-tree rerun as well as again on the profile-style tail | The run still paid a cheap first-pass Prettier stop, one repo-root binary path miss while applying the formatter fix, and a late profile stylesheet import tail that surfaced only after the score-loop commit and forced one more classification and proof pass before the tree was truly finished | On broad AI Studio lanes, trust the helper fan-out signals early, clear formatter-only seams before spending another proof pass, and recheck the live tree after score-loop writeback so any tiny global-style tails get folded in before closeout is declared final. |
| 2026-06-07 | Production SOP run with voice workflow-reload restore hardening, Expert Edit secondary-reference slot support, and ElevenLabs voices timeout/auth tightening | 9.2 | high | The live tree stayed collapsed to one coherent shared-runtime lane, `gear-ball:preflight` cleared the 11-file frontend manifest after one formatter recovery, 4 focused suites passed with 85 tests on the exact committed tree, and the production build passed cleanly on the exact committed tree | The only real drag was one formatter-only preflight stop in the ElevenLabs voices route, and the expected exact-tree rerun still had to be replayed manually after the original proof sessions expired during closeout | When a compact runtime lane is already hook-clean and `git status` is empty after commit, keep the exact-tree reproof limited to the owning focused suites and build instead of reopening broader validation rungs. |
| 2026-06-08 | Production SOP run with canonical generated-output shell rehydration and maintenance refresh for id-only restored project outputs | 9.4 | high | The live tree stayed collapsed to one compact shared-runtime AI Studio lane, `gear-ball:preflight` passed before and after the hook-adjusted commit, 2 focused suites passed with 22 tests on both passes, and the production build passed before and after the exact-tree rerun | The only repeat cost was the expected exact-tree rerun after the commit hooks rewrote the staged files; there were no late tails, formatter misses, or seam-expansion surprises | When a restored-output lane only changes hydration identity matching plus maintenance refresh criteria, keep the proof surface anchored to the owning hydration and maintenance suites and let the build serve as the single shared-runtime confirmation rung. |
| 2026-06-08 | Production SOP run with shared feedback-message normalization, AI Studio drag-drop and Expert Edit hardening, Copperknot launch-board refresh, and workflow planning docs | 9.0 | high | The live tree stayed collapsed to one broad frontend/runtime lane plus one docs-only lane, `gear-ball:preflight` passed before and after the hook-adjusted frontend commit, 20 focused suites passed with 546 tests and 39 skipped on the exact committed tree, the production build passed before and after the exact-tree rerun, and both docs commits cleared `docs:check` cleanly | The only preventable drag was one formatter-only preflight stop plus two ad hoc helper mistakes during manual test invocation and staging, while the expected exact-tree rerun still remained necessary after hook rewrites on the frontend batch | On broad frontend lanes, trust the manifest-backed preflight more than improvised side commands, and keep any manual Vitest reruns path-correct to the `frontend/` working directory so helper mistakes do not add noise to an otherwise clean ladder. |
| 2026-06-08 | Production SOP run with prompt-only Styles Library preview generation, Canvas text-reference paste commit hardening, and provider-size runtime contract cleanup | 8.8 | high | The live tree stayed collapsed to one broad but coherent AI Studio shared-runtime lane, 14 focused suites passed with 178 tests before and after the hook-adjusted commit, `docs:check` passed on both passes, and the production build passed before and after the exact-tree rerun on the committed tree | The first pass still paid formatter-only and hook-dependency cleanup misses, the new preview route's auth-guard regression was discovered and added mid-run instead of being in the first manifest, and the hook rewrite forced the expected exact-tree rerun before closeout | When a broad AI Studio runtime lane adds a new authenticated route, sweep the auth-guard regression file into the first manifest and do a touched-file Prettier pass before the opening proof so the first ladder stays about contract risk instead of recoverable hygiene misses. |
| 2026-06-08 | Production SOP run with Motion Control prompt-optional generate enablement, Canvas tear-out and output-cleanup hardening, and shared text-detail modal unification | 7.1 | high | The final shipped tree converged into four coherent AI Studio product commits: the Motion Control generate gate passed its owning `VideoPropertiesPanel` suite twice with Generate CTA and docs contract checks; the recovered Canvas/Create tear-out lane passed 53 focused tests before and after its hook-adjusted commit; the output-cleanup runtime lane passed 83 focused tests on the exact committed tree; and the final detail-modal/delete-output cleanup bundle passed 83 focused tests on the exact committed tree with the new delete-output owner test included | Control quality was substantially worse than the final outcome: same-surface AI Studio tails resurfaced across Create panels, Canvas, runtime hooks, delete-output behavior, and DetailModal styling after multiple supposed closeout points, the score loop was committed too early more than once, and the run paid repeated classification, proof, and ledger-rewrite cycles before the real final tree was finally complete | On mixed AI Studio runs, freeze the live tree before any score-loop attempt by rerunning `git diff --name-only` after each product commit, grouping every resurfaced file into the same surface lane immediately, and only touching the ledger once a post-commit status check stays empty for product files. |
| 2026-06-10 | Production SOP run with Fal CDN Lip Sync staging, dashboard/auth contract cleanup, dashboard announcement test-settlement repair, and a small Gottspan planning-prompt docs batch | 8.9 | high | The live tree stayed honestly split into one broad shared-runtime production batch plus one tiny docs-only prompt batch, the product manifest cleared `gear-ball:preflight` with 18 files and 354 passing tests after the one real test-harness repair, the production build passed, and both commits pushed cleanly to `production` | The first proof pass still paid one formatter-only stop across seven files, and the dashboard announcements suite exposed a late dynamic-import settlement seam that had to be diagnosed and repaired after the broad preflight had already run once | On dashboard/auth lanes that mount eager lazy-import boundaries, sweep the import-settlement test contract into the first manifest and do a touched-file Prettier pass before the opening preflight so the first proof pass stays focused on runtime risk instead of hygiene and harness noise. |
| 2026-06-10 | Production SOP run with Lip Sync Fal upload staging authority, session/workflow reload persistence, Motion Control drop-source normalization, AI Studio header/layout follow-through, and the owning ADR/API/SOP/doc updates | 9.2 | high | The live tree stayed collapsed to one honest broad production lane, `docs:check` passed, `gear-ball:preflight` passed before and after the hook-adjusted commit with 157 focused tests, and the production build passed before and after the exact-tree rerun on the committed tree | The first proof pass still paid six formatter-only stops, and the hook-driven commit required the expected exact-tree rerun before the lane was honestly ready to push | On broad shared-runtime lanes, do one touched-file Prettier sweep before the first preflight so the opening ladder stays about contract risk instead of cheap formatting recovery. |
| 2026-06-10 | Production SOP run with Motion Control upload-source hardening, Lip Sync OmniHuman regenerate routing, credit-coin spent-angle correction, Fal route inventory parity, and Copperknot post-deploy evidence refresh | 9.1 | high | The live tree stayed collapsed to one honest production lane, `fal:routes:check`, `docs:check`, `gear-ball:preflight`, and `build` all passed before and after the hook-adjusted commit, and the committed tree converged cleanly except for the intentional Copperknot scratchpad noise | The first proof pass still paid one formatter-only stop in the media preview signing hook, and the hook-adjusted commit required the expected exact-tree rerun before the lane was honestly ready to push | On mixed runtime plus proof-doc lanes, do a quick touched-file Prettier sweep before the first preflight so the opening ladder stays focused on contract risk instead of cheap formatter recovery. |
| 2026-06-10 | Production SOP run with built-in Styles control plane, style-creator runtime wiring, admin instruction editors, ElevenLabs music route catalog alignment, and the owning SQL/schema/doc refresh | 8.9 | high | The live tree stayed collapsed to one honest production-broad lane, `docs:check`, the isolated admin instructions page suite, `gear-ball:preflight` with 184 focused tests, and `build` all passed before and after the hook-adjusted commit, and the committed tree converged cleanly except for the intentional Copperknot scratchpad noise | The first proof pass still paid eleven formatter-only stops and one cheap unused-parameter lint seam before the real contract proof could run, and the hook-driven commit required the expected exact-tree rerun before the lane was honestly ready to push | On broad control-plane lanes with many new files, do a touched-file Prettier sweep and a quick lint scan on new helpers before the first full preflight so the opening ladder stays about contract risk instead of cheap hygiene recovery. |
| 2026-06-10 | Production SOP run with durable AI Studio media drops, Expert Edit transform hardening, admin built-in Style preview uploads, and ElevenLabs music display-title persistence | 8.8 | high | The live tree stayed collapsed to one honest broad AI Studio/runtime lane, `docs:check`, the isolated admin instructions page suite, `gear-ball:preflight` with 460 focused tests, and `build` all passed before and after the hook-adjusted commit, and the committed tree converged cleanly except for the intentional Copperknot scratchpad noise | The first proof pass still paid four formatter-only stops plus three cheap lint seams before the real contract proof could run, and the hook-adjusted commit required the expected exact-tree rerun before the lane was honestly ready to push | On broad frontend runtime lanes, do one touched-file Prettier sweep plus a cheap touched-file ESLint pass before the first manifest preflight so the opening ladder stays about contract risk instead of hygiene recovery. |
| 2026-06-10 | Production SOP run with AI Studio project-entry loading/error surface convergence and legacy project display-title read fallback | 8.7 | high | The live tree stayed collapsed to one compact shared-runtime lane, the exact manifest `gear-ball:preflight` passed before and after the hook-adjusted commit with 56 focused tests, the pre-commit production build passed, and the committed lane files reconverged cleanly against `HEAD` before push | The first pass still paid one formatter-only stop, and the commit-hook stash restore surfaced an unrelated admin built-in-styles tail that had to be explicitly classified and deferred before the run could close honestly | When a small shared-runtime lane touches files that are likely to trip hook stash restore, classify any resurfaced tail immediately and keep the rerun scope pinned to the committed lane manifest instead of widening into unrelated admin work. |
| 2026-06-10 | Production SOP run with dashboard tutorial hub delivery, built-in Styles canonical IDs, stale video-submit recovery, Kie upload fallback hardening, and the owning SQL/docs/admin updates | 8.8 | high | The live tree stayed collapsed to one honest production-broad lane, `docs:check` passed, `gear-ball:preflight` passed before and after the hook-adjusted commit with 144 focused tests, the production build passed before and after the exact-tree rerun, and the unrelated post-commit Expert Edit tails were explicitly classified and deferred instead of leaking into the batch | The first proof pass still paid nine formatter-only stops, the hook-driven commit required the expected exact-tree rerun before push, and stash restore resurfaced two unrelated Expert Edit files late in the closeout loop | On broad mixed admin/dashboard/runtime lanes, do one touched-file Prettier sweep before the first preflight and check for likely stash-restore tails immediately after the product commit so the first proof pass stays focused on contract risk. |
| 2026-06-10 | Production SOP run with AI Studio image-ingress timeout hardening, OpenAI GPT Image 2 transport ceilings, provider submit-timeout alignment, Kie explicit-failure handling, and dashboard tutorial/style control-plane tightening | 8.9 | high | The worktree collapsed cleanly into two honest lanes, both exact-manifest preflights passed, `fal:routes:check` passed before and after the commit phase, the production build passed on the validated and final committed trees, and the only final live residue was the intentional Copperknot scratchpad noise | The run still paid two preventable formatter-only stops before real contract proof, and the hook-adjusted commit path forced the expected full exact-tree rerun before push | When a mixed runtime-plus-control-plane run already looks like two seams, do a touched-file Prettier pass on both manifests before the first preflight so the first ladder acts like proof instead of cleanup. |
| 2026-06-10 | Production SOP run with Expert Edit transform/runtime hardening plus dashboard tutorial thumbnail uploads, hosted schema contract checks, and admin/docs/sql follow-through | 9.0 | high | The live tree collapsed cleanly into two honest lanes, both exact-manifest `gear-ball:preflight` passes cleared, the combined production build passed before the commit phase, both product commits landed without late same-lane tails, and the final live residue stayed limited to the intentional untracked Copperknot scratchpad noise | The first validation passes still paid two formatter-only recovery stops and one small AI Studio lint cleanup before the real contract proof could run | On mixed two-lane runs, do a touched-file Prettier sweep on both manifests plus one cheap lint scan on newly edited helpers before the first preflight so the opening ladder stays purely about product risk. |
| 2026-06-10 | Production SOP run with Lip Sync Fal CDN guardrails, OmniHuman result-media settlement hardening, reference-grid detail snapshot stability, output-display title fallback retirement, and dashboard tutorial storage compatibility | 9.1 | high | The live tree collapsed cleanly into two honest lanes, both exact-manifest preflights passed, the final exact-tree rerun stayed green with 174 focused tests total, and the committed production build passed cleanly before push | The first AI-runtime proof pass still paid one stale lineage expectation repair, the admin lane paid one small formatter recovery, and the hook-driven commits required the expected exact-tree rerun before the run was honestly push-ready | When a mixed runtime-plus-admin run adds one new lineage field and a few schema-compat fallbacks, sweep the owning helper tests and do a touched-file Prettier pass on both manifests before the first preflight so the opening ladder stays purely about contract proof. |
| 2026-06-11 | Production SOP run with dashboard tutorial auth-only delivery, guest-surface gating, payload normalization, and dashboard API/docs/header alignment | 8.8 | high | The live tree stayed collapsed to one coherent dashboard/auth lane, the focused dashboard/tutorial suites and doc drift checks passed before and after the product commit, and the production build cleared on both the validated tree and the exact committed tree before push | The first wrapper preflight stopped on the known ignored-file `next.config.js` ESLint warning, and one untracked dashboard helper had to be pulled back into the manifest before closeout | When a lane touches ignored config files plus one new helper, skip the wrapper immediately, validate with the manual exact-tree ladder, and scan for untracked same-surface files before staging so the first pass stays fully converged. |
| 2026-06-11 | Production SOP run with AI Studio workflow-reload slot fidelity, adaptive properties-rail layout hardening, OmniHuman Fal image compatibility normalization, dashboard tutorial media polish, and admin offer bulk-save controls | 8.9 | high | The live tree collapsed cleanly into two honest lanes, both exact-manifest preflights passed with 195 focused tests total, one production build passed on the full live tree before the commit phase, both product commits landed without late same-lane tails, and the final live tree was clean before closeout | The first AI Studio lane still paid one formatter-only recovery pass, and the dashboard/admin lane paid one avoidable lint stop because a new tutorial thumbnail image did not start on the repo's existing `next/image` pattern | On mixed runtime-plus-dashboard runs, do a touched-file Prettier sweep and a quick existing-pattern scan for newly added image elements before the first preflight so the first ladder stays about product risk instead of mechanical cleanup. |
| 2026-06-11 | Production SOP run with Expert Edit center-stage expansion plus dashboard guest tutorial hydration and inactive-offer bulk-save cleanup | 9.2 | high | The worktree collapsed immediately into two honest lanes, both exact-manifest preflights passed on the first try, the production build passed before the commit phase and again on the exact committed tree, and the final live tree stayed clean through closeout | The hook-adjusted commit path still required the expected exact-tree rerun before push, so the run paid one extra final-proof loop even though no product tails resurfaced | When two small lanes are already isolated and green, keep using the split-manifest plus one shared pre-commit build pattern, then rerun only the exact owning tests and build after hook-adjusted commits. |
| 2026-06-11 | Production SOP run with guest dashboard tutorial header simplification and matching public-route contract cleanup | 9.4 | high | The live tree collapsed immediately into one honest public-dashboard lane, the focused guest-route suite passed before and after the product commit, the production build passed before and after the exact-tree rerun, and no related tails resurfaced during convergence | The hook-adjusted commit path still forced the expected exact-tree rerun, so the closeout paid one duplicate proof cycle even though the batch never widened | When a tiny single-surface lane is already obviously coherent, keep the ladder exactly this small: one owning test, one production build, then only the affected rerun set after the hook-adjusted commit. |
| 2026-06-12 | Production SOP run with dashboard tutorial display-derivative delivery/backfill plus AI Studio shell-bound and reference-grid widening hardening | 9.1 | high | The live tree collapsed cleanly into two honest lanes, both exact-manifest preflights passed after one tiny formatter repair, docs drift checks and remote SQL lint both cleared, the shared production build passed before commit, and the exact committed tree re-cleared 105 focused tests plus docs and build before push | The first AI Studio preflight still paid one preventable Prettier-only stop, and both hook-adjusted product commits required the expected exact-tree rerun because validated files were rewritten during commit | On mixed runtime-plus-SQL lanes, do a touched-file Prettier sweep on both manifests before the first preflight so the opening ladder stays pure proof and the only remaining rerun is the intentional exact-tree check after hook writes. |
| 2026-06-12 | Production SOP run with dashboard thumbnail derivative docs/schema parity plus AI Studio compact-layout recentering | 9.0 | high | The live tree collapsed cleanly into two honest lanes, both exact-manifest preflights passed after one tiny formatter repair, the shared production build passed before commit, and the exact committed tree re-cleared 193 focused tests plus docs and build before push | The first AI Studio preflight still paid one preventable Prettier-only stop, and both hook-adjusted commits required the expected exact-tree rerun because validated files were rewritten during commit | On mixed docs-plus-layout runs, do a touched-file Prettier sweep on both manifests before the first preflight so the first ladder stays about contract proof and the only remaining rerun is the exact-tree check after hook writes. |
| 2026-06-12 | Production SOP run with AI Studio built-in restore persistence plus Kie storage-ref upload hardening | 9.0 | high | The live tree collapsed cleanly into two honest AI Studio lanes, both exact-manifest preflights passed, the shared production build and docs checks passed before commit, and the exact committed tree re-cleared 18 focused suites with 240 tests plus docs and build before push | The first proof pass still paid one formatter-only stop and one cheap React hook lint seam in the reference-grid signing controller, and both hook-adjusted commits required the expected exact-tree reproof before the run was honestly push-ready | On mixed AI Studio runtime-plus-schema runs, do a touched-file Prettier sweep on both manifests and a quick touched-file lint scan on newly edited controllers before the first preflight so the opening ladder stays about contract proof and the only remaining rerun is the exact-tree check after hook writes. |
| 2026-06-12 | Production SOP run with AI Studio reference-grid active-workset capping plus dashboard tutorial backfill regeneration controls | 8.5 | high | The worktree collapsed quickly into one honest AI Studio runtime lane plus one small dashboard backfill lane, touched-file type checks and targeted suites passed, the shared production build passed, and both product commits landed cleanly on `production` without post-commit product tails | The first AI Studio preflight still paid a five-file Prettier-only stop, and I nearly closed the run after the product push before catching that the required Gear Ball ledger writeback still needed its own tiny docs-only closeout commit | Run the Gear Ball closeout checklist before the first push-ready claim so formatter hygiene and the required ledger writeback are both settled before the main product push sequence starts |
| 2026-06-13 | Production SOP run with AI Studio style-extraction prompt concurrency, project-workspace bootstrap caching, autosave serialization reuse, and reference-grid/media-shell load shedding | 9.1 | high | The live tree stayed collapsed to one honest AI Studio shared-runtime lane, the isolated admin instructions page suite plus the final exact-tree 14-suite pass cleared 188 focused tests, scoped lint and Prettier checks passed, and the production build passed before and after the hook-adjusted product commit | The first proof pass still paid one two-file Prettier-only stop, and the hook stack required the expected exact-tree reproof before the run was honestly push-ready | On broad shared-runtime lanes, do one touched-file Prettier sweep before the first proof ladder and assume the product commit will need an exact-tree recheck whenever hook writes are part of the path. |
| 2026-06-13 | Production SOP run with style-preview error sanitization plus Fal FLUX Klein alias-status compatibility hardening | 9.2 | high | The live tree collapsed immediately into one honest 9-file shared-runtime lane, shared-contract fan-out pulled the downstream preview API suites into the first manifest, the focused exact-tree set cleared 81 tests before and after the hook-adjusted commit, `gear-ball:preflight` passed, and the production build passed before and after commit | The first wrapper pass still paid one preventable three-file Prettier-only stop, and the hook stack forced the expected exact-tree reproof before push even though the lane stayed stable | On compact shared-runtime lanes, do a touched-file Prettier sweep before the first wrapper run so the opening pass stays pure proof and the only remaining duplicate cost is the intentional post-hook exact-tree recheck. |
| 2026-06-13 | Production SOP run with AI Studio video style-prompt composition, media workflow reload/detail enrichment, provider-credit failure normalization, and compact split shell sizing follow-through | 8.4 | high | The live tree correctly collapsed into one broad AI Studio/runtime lane, `gear-ball:preflight` passed, the exact-tree broad suite set re-cleared 21 files with 545 tests, the production build passed on the converged live tree, and all same-lane tails were ultimately shipped on `production` | Control quality was worse than the final outcome: the first pass paid a seven-file Prettier stop plus two stale `ReferenceGrid` expectations, then the post-commit convergence loop surfaced three separate same-lane tail commits (`useAiStudioShellResize.ts`, `ai-studio-layout.css`, and two owning tests) before the lane was truly complete | On broad AI Studio lanes, search for the owning hook/layout tests before the first commit whenever shell sizing or layout CSS changes are adjacent, so same-lane stash-restore tails get absorbed into the first manifest instead of leaking into extra convergence commits. |
| 2026-06-13 | Production SOP run with ElevenLabs generated-lyrics persistence, project-workspace display-materialization fail-open fallback, and video Styles action-cluster repositioning | 9.0 | high | The live tree collapsed honestly into one shared-runtime lane, touched-file type-check passed, the owning server suites cleared 57 tests, the owning UI/hook suites cleared 85 tests once run through the actual frontend Vitest config, and the docs contract checks passed before staging | The first focused test pass used the wrong Vitest invocation shape from the repo root, so I paid two cheap reruns before the frontend config, `jsdom`, and setup file were all loaded correctly | When a batch mixes server and UI suites, start UI reruns from the `frontend/` workdir or pass the frontend Vitest config on the first command so proof time goes to behavior instead of runner wiring. |
| 2026-06-13 | Production SOP run with project-workspace minimal save responses, reference-grid/media-preview fallout alignment, and the owning restore/account test refresh | 9.3 | high | The live tree collapsed cleanly into one honest shared-runtime lane, the separate Copperknot prompt and scratchpad changes were explicitly deferred, repo type-check passed, 22 owning Vitest files cleared 193 tests on the first targeted pass, and the production build passed before staging | The product lane still carried two harmless one-line formatting tails into the staged set instead of starting from a fully normalized touched-file tree, and the existing dashboard `act(...)` warnings stayed visible in one passing suite | On mixed runtime-plus-test fallout lanes, do one touched-file Prettier sweep before the proof ladder so the commit set starts visually normalized and the only remaining noise is known passing-suite warnings. |
| 2026-06-13 | Production SOP run with generation-projection reliability hardening, AI Studio error/detail recovery alignment, dashboard/tutorial pagination and public-fetch cleanup, billing readiness helper addition, and SQL/docs/control-plane follow-through | 9.2 | high | The live tree collapsed into one honest production-broad lane with the Copperknot prompt/board/queue and scratchpad paths explicitly deferred, `git diff --check` passed, repo type-check passed, the changed-test fanout cleared 33 files with 366 tests on the first run, `docs:check` passed, the production build passed, and the new billing/shell helpers both cleared lightweight smoke | The lane was broad enough that I relied on docs parity and build/type/test proof instead of a deeper SQL- or workflow-specific executable check, and the Vitest run still surfaced harmless local native-library warnings that add noise to broad proofs | On broad mixed runtime-plus-SQL lanes, add one cheap dedicated SQL/workflow smoke rung when new migrations or GitHub workflow files are part of the batch so the proof ladder covers those operational seams as directly as the app code. |
| 2026-06-13 | Production SOP run with Expert Edit flatten-footer gating, matching test-contract updates, and toolbar motion polish | 8.9 | medium | The live tree stayed collapsed to one honest Expert Edit lane, `git diff --check` passed, touched-file repo type-check passed, and the owning Expert Edit suite cleared 208 tests with 169 passing and 39 expected skips once run through the correct frontend Vitest harness | I burned two avoidable reruns on the first test command by invoking Vitest from the wrong workdir/config shape, and the real product delta still needed two stale flatten-button expectations repaired before the suite matched the shipped contract | For frontend-only SOP runs, start the first owning test from `frontend/` with the repo's Vitest config so the first red, if any, reflects the product seam rather than runner wiring. |
| 2026-06-14 | Production SOP run with generated-output project convergence hardening, toolbar/audio contract follow-through, public-route auth-bootstrap stabilization, and reliability diagnostic coverage expansion | 9.0 | high | The full worktree was classified into two honest product lanes, touched-file repo type-check passed, the five owning frontend suites cleared 50 tests, the exact committed product tree re-cleared those same suites plus the production build, and both product commits converged cleanly before the score writeback | I still paid one avoidable false-red cycle by starting the first multi-file Vitest command from the repo root instead of `frontend`, and the changed diagnostics SQL could only be syntax/coverage-checked here because `SUPABASE_DB_URL` and `psql` were unavailable for a live read-only run | For mixed frontend/shared-runtime proof sets, start the first owning Vitest pass from `frontend/` and check DB-diagnostics prerequisites up front so the only remaining uncertainty is genuine environment availability, not runner wiring. |
| 2026-06-14 | Production SOP run with Nuclo production-proof handoff capture plus AI Studio shell/detail/audio/runtime hardening, project preview convergence, and pricing auth-bootstrap alignment | 9.0 | high | The branch proof stayed explicit on local `production` with `shortpulse.allowedBranch=production`, the full live tree was classified into two honest lanes, full-tree `gear-ball:preflight` passed, the production build passed before commit, the hook-adjusted product commit got an exact-tree reproof with the same targeted suite set and another clean build, and the final convergence loop stayed clean before score writeback | The first ladder still paid one preventable Prettier-only stop, and the hook-driven product commit forced the expected exact-tree reproof before the run was honestly push-ready | On mixed docs-plus-shared-runtime runs, do a touched-file Prettier sweep before the first preflight and assume any hook-adjusted product commit on shared runtime will need an immediate exact-tree rerun budgeted in from the start. |
| 2026-06-16 | Production SOP run with Standard assistant URL linkification in shared rich-message rendering plus matching style and renderer tests | 9.3 | medium | The live tree collapsed immediately into one honest shared-runtime lane, focused `gear-ball:preflight` passed once the touched files were normalized, the owning Standard renderer suite cleared 7 tests, the production build passed, and no late same-lane tails surfaced before the Git phase | The first wrapper pass still paid one preventable Prettier-only stop, so the opening ladder spent a cycle on formatting instead of pure contract proof | On compact shared-runtime UI lanes, run a touched-file Prettier sweep before the first wrapper preflight so the first pass stays about behavior proof and the rest of the ladder can flow straight to build, commit, and push. |
| 2026-06-16 | Production SOP run with Standard runtime browser-link guidance plus bare-domain assistant linkification coverage | 9.4 | medium | The live tree collapsed immediately into one honest shared-runtime lane, focused `gear-ball:preflight` passed on the first try with 58 targeted tests green, the production build passed, and the full worktree stayed limited to the intended runtime plus renderer seam before the Git phase | I still relied on one shared rich-message component change without expanding into broader UI surface tests, so the proof stayed intentionally narrow even though the seam is reused across adjacent prompt surfaces | On compact mixed runtime-plus-renderer lanes, keep doing one explicit sibling sweep first so narrow proof stays honest without drifting into a broader suite by habit. |
| 2026-06-16 | Production SOP run with split production lanes for Standard runtime browser guidance, media-list storage-path hardening, sticky AI Studio rail panels, and protected video footer requirement layout | 9.2 | high | The full worktree was classified into four real seams instead of being force-fit into one batch, the first exact-manifest proof cleared 98 targeted tests plus docs drift and a production build, each product seam landed as its own commit without late same-lane tails, and the final exact-tree rerun on the committed product set re-cleared the same focused proof before score writeback | The hooks rewrote staged files on every product commit, so the run still paid the expected final exact-tree rerun, and the first whole-tree preflight was a little broader than each individual lane needed because proving the mixed tree once was cheaper than per-lane wrapper churn | When a mixed tree already splits into several small honest product lanes, keep using one shared exact-manifest proof before the commit phase, then budget only one final exact-tree rerun after the last product commit instead of reflexively revalidating after every intermediate batch. |
| 2026-06-16 | Production SOP run with AI Studio video snapshot capture, canvas media actions, deferred video workflow reload hydration, AI Studio interaction test ratchets, shared assistant-link polish, billing-readiness diagnostic redaction, and a late Copperknot startup-doc cleanup tail | 9.2 | high | The live tree was classified into four honest primary batches, the broad AI Studio runtime lane cleared `gear-ball:preflight` with 125 targeted tests plus preview/layout contract fan-out, the smaller frontend lanes each cleared focused preflight, the production frontend build passed once across the converged shipped tree, and the late Copperknot docs tail was caught by the exact-tree proof, validated with `docs:check`, then shipped as its own final cleanup commit | The first stage command paid a cheap Git pathspec mistake, the big runtime preflight still emitted pre-existing passing-suite `act(...)` warnings in `DetailModal.test.tsx`, and a concurrent late docs tail forced one extra repush after the first branch-close attempt | On mixed frontend-plus-script trees with one broad runtime seam and several tiny tails, keep doing one heavy proof pass up front, then trust the final exact-tree proof enough to treat any new post-push dirt as a fresh batch instead of hand-waving it away. |
| 2026-06-16 | Production SOP run with shared video Frame-shot relabel plus AI Studio canvas/shell overlap and panel-edge threshold tightening | 9.3 | medium | The live tree collapsed quickly into two honest AI Studio lanes, 96 focused tests and a production build passed before Git writes, both product commits converged cleanly without late same-lane tails, and the final tree was clean before closeout | `gear-ball:preflight` could not usefully run without a prepared manifest, and the focused proof still inherited pre-existing passing `act(...)` warnings from `DetailModal.test.tsx` that add noise to the run | On small mixed frontend lanes, either prepare the manifest for `gear-ball:preflight` up front or skip straight to the manual ladder so the opening rung does not spend a cycle on wrapper setup noise. |
| 2026-06-16 | Production SOP run with Canvas zoom badge migration into the Reference Grid header plus AI Studio shell-divider background hardening | 8.8 | medium | The final lane stayed coherent once the whole diff surfaced, the committed tree re-cleared 70 focused AI Studio shell/canvas/reference tests plus a production build, and the product batch converged cleanly on `production` before closeout | I initially undercounted the lane twice because only part of the live tree was visible in early checks, which forced two extra proof cycles and late stale-test repairs before the full canvas/reference badge move was explicit | On compact shared-runtime UI lanes, rebuild the live manifest immediately when the tree widens and search for every moved UI affordance owner before the first commit so the first proof set already matches the whole lane. |
| 2026-06-17 | Production SOP run with Create character-mode desktop-width guardrail in responsive properties CSS | 9.4 | low | The live tree collapsed immediately into one honest two-file lane, the owning contract tests re-cleared 23 assertions before and after the hook-adjusted commit, the production build passed before and after commit, and no late same-lane tails surfaced during convergence | I still paid the expected exact-tree rerun after hook writes even though the lane stayed tiny and stable | On compact CSS-contract lanes like this, keep using one narrow contract rung plus build, then reserve the second pass strictly for hook-adjusted exact-tree proof. |
| 2026-06-17 | Production SOP continuation with saved media workflow identity reload hardening plus its missing legacy-video contract | 9.0 | low | A post-push tree check caught two real same-seam AI Studio reload edits instead of closing on a false clean tree, the owning `mediaLibraryWorkflowReload` suite stayed green across both product commits and the final committed tree, and the lane split cleanly into logic then contract batches without widening into broader validation | I wrote the score row too early for the first push and had to treat the surfaced reload seam as a fresh continuation, which cost two extra tiny commit/push cycles even though the validation stayed cheap | Keep the final exact-tree proof authoritative enough to reopen the SOP when a same-area tail appears after push, but delay the scoring writeback until the tree has stayed clean through the last post-push check. |
| 2026-06-17 | Production SOP continuation with media-panel legacy-video reload coverage surfacing only in the final exact-tree proof | 8.9 | low | The final tree check caught a real UI-level companion test instead of letting the run end on a false clean tree, the bounded `MediaLibraryPanel` suite passed before and after the companion commit, and the lane stayed isolated to one test file without forcing a broader AI Studio rerun | I still wrote the prior continuation score row one beat too early, so this same saved-media seam needed one more tiny docs tail after the panel contract shipped | When a long SOP continuation keeps surfacing same-area tests after push, keep honoring the exact-tree proof, but wait to score until one clean post-push pass lands with no further tails. |
| 2026-06-17 | Production SOP run with one large runtime consolidation batch plus a launch-control docs tail | 9.2 | medium | The live tree was classified quickly into one broad product lane and one docs-only tail, a single shared pre-commit ladder cleared 15 focused test files plus the Fal route gate, `docs:check`, and a production build, the hook-rewritten runtime commit was fully reproven before the docs commit, and the tree stayed clean through the final score writeback | I still had to pay the full targeted rerun after the runtime hooks rewrote 59 staged source files, and the first clean-tree expectation was wrong because a large mixed packet had appeared between SOP runs | On big mixed production trees that already collapse into one runtime lane and one docs tail, keep using one shared front-loaded ladder, but pre-plan the hook-rerun cost as soon as staged TypeScript volume is high enough that lint-staged will almost certainly rewrite the commit tree. |
| 2026-06-17 | Production SOP run with retired upload-route removal, auth-boundary hardening, deployment-route parity coverage, and launch checkpoint scratchpad refresh | 9.1 | high | A surprise 299-file packet was caught by the final exact-tree proof instead of being waved through, the lane was split honestly into one runtime batch plus one launch-doc tail, the changed-test sweep cleared 118 files before commit and 125 files after the hook-rewritten runtime commit, and both `npm run build` plus the Fal/docs gates passed before the final docs closeout | I nearly closed on the first pushed `HEAD` match before the final status check exposed the fresh packet, and the runtime hook rewrite forced a full second proof ladder across a very broad API/test surface | When `HEAD` and `origin/production` match after a push, treat `git status --short` as the real closeout gate before any victory lap, then assume a broad staged TypeScript batch will require one intentional post-hook reproof rather than hoping to avoid it. |
| 2026-06-17 | Production SOP run with canvas native-video preview rendering, overlap-safe shell resize bounds, and recorder MIME normalization for voice-changer staging | 9.3 | low | The live tree collapsed immediately into one honest 10-file AI Studio/runtime lane, the owning five-file proof set cleared 121 tests before and after the hook-adjusted commit, the production build passed before and after commit, and the final tree stayed clean before score writeback | I still had to pay the expected rerun because lint-staged rewrote every staged TypeScript file even though the lane stayed compact and stable | On compact shared-runtime SOP runs like this, keep the proof ladder narrow, but assume one post-hook recheck as soon as all staged files are TypeScript and the hooks are active. |
| 2026-06-18 | Production SOP run with AI Studio runtime-authority extraction, chronological media-library modal flow, Pulse preference warm-start, and Voice Changer large-video normalization | 9.1 | medium | The live worktree collapsed into one honest product/runtime lane plus one explicitly deferred out-of-lane Gutan plan note, the owning six-file test ladder cleared 84 tests before and after the hook-adjusted commit, `docs:check` passed, and the production build passed before and after commit | The lane mixed client runtime authority, media-library display behavior, and server media normalization in one batch, so even though the validation stayed focused the hook rewrite still forced a full second proof pass across tests, docs, and build | When one product lane spans both AI Studio state and server media ingestion, keep the out-of-lane planning artifacts deferred early and assume the full focused ladder will need one post-hook replay once TypeScript hooks rewrite the committed tree. |
| 2026-06-18 | Production SOP run with Expert Edit secondary-reference preview hardening and media-duration cache continuity | 9.0 | low | The lane stayed collapsed to one honest 10-file AI Studio batch, touched-file type-check passed, the final preflight cleared 5 focused suites with 223 passing tests, and the production build passed on the validated tree | The first proof pass paid one avoidable Prettier stop, then the new secondary-preview helper hung one hook-contract test until I made the preview generation fail open when blob decoding stalls | When a lane adds optional blob-to-image preview work, run the owning hook suite before the broader preflight so any decode-timeout seam is caught before the larger ladder starts. |
| 2026-06-18 | Production SOP run with create-mode reference-selection restore for AI Studio workflow reload | 9.4 | low | The lane stayed collapsed to one honest four-file hook batch, `gear-ball:preflight` cleared the two owning hook suites with 37 passing tests on the first pass, touched-file type-check passed, and the production build passed without needing a post-hook replay | The only real drag was paying the full build rung for a compact wiring-and-test lane that otherwise stayed stable and hook-clean | On tiny workflow-reload hook lanes, keep the proof set this narrow and expect a one-pass closeout unless hooks actually rewrite the validated tree. |
| 2026-06-18 | Production SOP run with grouped AI Studio failure-banner dismissal, motion-recorder reference persistence, and drop-source fallback hardening | 9.1 | low | The live tree stayed collapsed to one honest 11-file AI Studio layout/runtime lane, touched-file type-check passed, `gear-ball:preflight` cleared 5 owning suites with 151 passing tests, and the production build passed on the validated tree | The first proof pass still paid one cheap Prettier stop in `AiStudioPageContent.tsx` before the real contract checks could run | On compact mixed UI-plus-hook lanes like this, do one touched-file Prettier sweep before the first preflight so the opening ladder stays about runtime behavior instead of formatting recovery. |
| 2026-06-20 | Production SOP run with style-creator prompt-budget hardening across admin built-in Styles, extraction/runtime normalization, preview API limits, and Kling single-shot visible-budget tightening | 9.1 | medium | The live tree stayed collapsed to one coherent AI Studio/runtime lane, `git diff --check` passed, the targeted owning sweep cleared 7 files with 89 passing tests, the production build passed, and the batch was ready to ship on `production` without late same-lane tails | The first proof command burned one avoidable rerun because I passed repo-prefixed test paths into the frontend Vitest runner instead of frontend-relative paths | On frontend-targeted SOP runs, start the first owning Vitest command with frontend-relative paths from the repo root or run it directly inside `frontend/` so the first pass spends time on product proof rather than runner path wiring. |
| 2026-06-20 | Production SOP run with video pricing-grid materialization for active video variants plus AI Studio view-model resolution alignment for Generate-button pricing | 9.4 | low | The live tree collapsed immediately into one honest four-file shared-runtime lane, `git diff --check` passed, the two owning suites cleared 60 tests before and after the hook-adjusted commit, the production build passed before and after commit, and no related tails resurfaced during convergence | The only extra cost was the expected exact-tree replay after lint-staged rewrote the committed TypeScript files | On compact TypeScript runtime lanes, assume one post-hook replay up front and keep the first proof ladder this narrow unless the sibling sweep exposes a real downstream contract seam. |
| 2026-06-20 | Production SOP run with duration-aware video pricing materialization, shared detail-modal action hardening plus its planning-doc/index refresh, and a late admin style-prompt clamp test ratchet | 9.2 | high | The mixed tree was split honestly into three lanes, one shared pre-commit proof set cleared 241 tests plus `docs:check` and a production build, the new late test-only file was caught and classified before any push-ready claim, and the final exact-tree replay on the committed content re-cleared 241 tests plus docs and build with a clean worktree | The run still paid one midstream manifest refresh because the user added a new test after the first proof pass, and every product commit triggered the expected hook rewrite path that forced the final replay | On mixed-lane SOP runs, keep using one shared proof pass up front, but budget a fast manifest refresh checkpoint right before the first commit whenever the worktree may still be changing. |
| 2026-06-20 | Production SOP run with pricing-grid/runtime convergence rollback plus admin built-in Styles grid layout hardening and matching pricing drift checks | 9.3 | medium | The live tree collapsed to one honest pricing/admin lane, `git diff --check` passed, the pricing drift script passed, 5 owning suites cleared 34 tests before and after the hook-adjusted commit, and the production build passed before and after commit with a clean final worktree | The only real drag was the expected exact-tree replay after lint-staged rewrote the staged files, and the lane still mixed pricing-runtime rollback work with a small admin layout tweak instead of being a pure single-seam packet | When a pricing-runtime lane also carries one tiny admin surface tweak, keep the proof ladder this small but call the mixed seam out early so the closeout score reflects that broader ownership surface honestly. |
| 2026-06-21 | Production SOP run with broad media-intake/dashboard/AI Studio launch-surface hardening plus Copperknot launch-control doc refresh | 8.8 | medium | The full live tree was classified into two honest lanes, the product lane cleared targeted preflight plus the shared-runtime build, the docs lane cleared its own docs-only preflight, and both commits converged to a clean `production` tree before score writeback | The run paid two avoidable Prettier-only stops, one suite-hot adjacent billing test surfaced a stale contract after the main preflight, and the first staging attempt failed because the manifest parser trimmed away the leading porcelain status space on the first path | Build SOP manifests from `git status --porcelain` with a trim-safe parser before the first staging step so the opening validation and first Git write both operate on the exact same tree snapshot. |
| 2026-06-21 | Production SOP run with AI Studio/runtime hardening, public legal policy control plane shipping, and Bactuo/Nuclo startup-surface pruning | 9.0 | high | The full live tree was classified into one broad product lane, one docs-only agent lane, and one explicitly deferred export lane; `gear-ball:preflight` passed, 33 changed test files cleared with 672 passing tests, the production build passed before and after the hook-adjusted product commit, and both commits pushed cleanly on `production` | The run still paid one real hook-dependency lint seam in the new admin legal controller, the first manifest lock stumbled over untracked directory expansion plus local shell-tool availability, and the product commit required the expected exact-tree rerun after hook-driven rewrites | When mixed worktrees include untracked directories, normalize the first manifest to concrete file paths with a known fallback path-expander before preflight so the first proof pass and first staging step stay aligned. |
| 2026-06-21 | Production SOP run with AI Studio style-order and interaction hardening, Kling staged character-media submit allowance, and auth paid-intent signup gating | 9.2 | medium | The mixed tree was collapsed into three honest product lanes, 9 touched suites re-cleared 313 tests before and after the hook-adjusted commits, `docs:check` and the production build both passed on the final committed content, and the only remaining live files were explicitly classified as deferred unreferenced SVG assets | The run still paid one full focused re-proof cycle because lint-staged rewrote committed files on all three commits, and the unreferenced SVG tail was only called out after the first inventory sweep instead of being explicitly deferred at manifest time | When the tree includes new assets that are not wired to any shipped surface, classify and defer them explicitly before the first validation pass so the commit plan and final leftover story are locked from the start. |
| 2026-06-21 | Production SOP run with AI Studio preset/reference hardening, closed-signup dashboard/pricing/auth polish, ElevenLabs audio-route tightening, and legal-expert/create-panel report refresh | 9.3 | high | The live tree was classified cleanly into one broad shipped-product lane plus one docs/report lane, 21 focused suites re-cleared 517 tests before and after the hook-adjusted commit, `docs:check` passed before and after the docs commit, and the production build passed before and after the exact-tree replay with a clean final worktree | The product lane still mixed several launch surfaces together, and the hook-rewritten product commit forced the expected full focused replay even though no late same-lane tails resurfaced | When a resumed SOP run opens to a much larger mixed tree than expected, lock the two-lane manifest immediately after the first grouped status pass so validation, staging, and closeout stay visibly aligned from the start. |
| 2026-06-22 | Production SOP run with AI Studio voice-source runtime extraction, paid-signup launch controls, and dashboard homepage auth-surface polish | 9.1 | high | The live tree collapsed into three honest production lanes, one shared proof pass cleared touched-file type-check, 183 focused tests, and a production build before commit phase, each hook-rewritten commit got its own narrow rerun before push, and all three batches landed cleanly on `origin/production` | Two transient stale `index.lock` interruptions hit staging boundaries, every product commit paid the expected hook-adjusted rerun, and the final score-ledger writeback still needed its own tiny docs tail | When a mixed production tree is already headed for multiple batches, assume one hook-rerun per commit and reserve one tiny final docs batch for the score ledger so the product pushes and exact-tree closeout stay predictable. |
| 2026-06-23 | Production SOP run with GPT Image 2 multi-ref edit billing authority restoration across runtime, API reservation, and AI Studio pricing tests | 9.5 | high | The full live tree collapsed immediately into one honest six-file shared-runtime lane, `gear-ball:preflight` passed on the first try, the exact-tree replay re-cleared 90 owning tests after hook rewrites, and both production builds passed with no late same-lane tails | The only extra cost was the expected hook-driven eslint/prettier rewrite, which forced one narrow post-commit replay before the lane was honestly push-ready | On compact billing/runtime lanes with a stable sibling sweep, start with the exact owning test quartet and keep any required post-commit replay to that same quartet plus the build when hooks rewrite files. |
| 2026-06-25 | Production SOP run with AI Studio persistence/auth boundary hardening plus deferred Datserok/Copperknot/brainstorming docs follow-through | 8.8 | high | The live tree was ultimately classified into one broad product/runtime lane plus one docs-only lane, the corrected exact product manifest cleared `gear-ball:preflight`, 19 focused suites with 423 passing tests, and a production build both before and after the hook-adjusted commit, and the shipped product commit pushed cleanly to `production` before the docs lane continued | The first product manifest was built from `git diff` instead of full live status, so it silently missed untracked source files and forced a corrective manifest rebuild; the run also paid two preventable Prettier-only stops and one unrelated suite-hot guardrail failure before the manual ladder settled on the honest proof set | Build every first manifest from full `git status --short` including untracked files, then do a touched-file Prettier sweep before the opening preflight so the first proof pass starts on the real exact tree. |
| 2026-06-25 | Production SOP run with AI Studio media delete authority, generated-audio presentation mirroring, and preset/project modal follow-through | 9.2 | high | The live tree collapsed cleanly into one honest shared-runtime lane, `gear-ball:preflight` cleared scoped formatting, lint, docs checks, and 15 focused Vitest suites with 261 passing tests, and the production build passed on the validated tree without late same-lane tails | The opening proof pass still paid one preventable Prettier-only stop across eight touched files because I did not sweep formatting before the first preflight | On broad shared-runtime lanes, do one touched-file Prettier sweep before the first preflight so the opening ladder stays pure contract proof and the only remaining heavy rung is the required production build. |
| 2026-06-26 | Production SOP run with AI Studio insufficient-credit recovery, Stripe checkout return-path hardening, reference/detail modal follow-through, and shared runtime preview/protected-route seams | 9.2 | high | The full live tree stayed one honest shared-runtime lane, `gear-ball:preflight` cleared scoped formatting, lint, docs checks, and 17 focused Vitest suites with 323 passing tests, the pricing drift audit passed, and the production build passed on the validated tree before Git writeback | The run still paid one preventable Prettier-only stop and one tiny unused-parameter lint seam before the main proof ladder was green | On broad shared-runtime lanes, do a touched-file Prettier sweep before the first preflight and trim obvious lint-only callback tails during the opening diff sample so the first heavy proof pass is closer to final. |
| 2026-06-26 | Production SOP run with AI Studio internal-drop prompt recovery, media-library root-drop dedupe, detail-modal reference polish, and video audio-slot preview controls | 9.3 | high | The live tree collapsed immediately into one honest AI Studio shared-runtime lane, `gear-ball:preflight` passed after fan-out with 13 focused Vitest files clearing 256 tests plus 7 expected skips, and the production build passed on the validated tree before Git writeback | The opening proof pass still paid one preventable Prettier-only stop on the new audio-slot presentation seam before the real ladder was green | On compact shared-runtime AI Studio lanes, do a touched-file Prettier sweep right after manifest lock so the first preflight pass is pure contract proof instead of spending a cycle on formatting. |
| 2026-06-26 | Production SOP run with AI Studio internal-drop recovery plus a hook-restored direct OpenAI image retirement lane across pricing, model-runtime, protected paths, and test contracts | 8.7 | high | The run ultimately shipped as two coherent production commits: the first AI Studio shared-runtime batch cleared preflight, 13 focused Vitest files with 256 passing tests plus 7 skips, and a production build; the surfaced second retirement batch re-cleared full-manifest preflight with 21 focused Vitest files and 238 passing tests, the pricing drift check passed, and the production build passed on the retirement source tree before final staging | Hook stash restore surfaced a large same-root second lane after the first commit, I undercounted that lane more than once before locking the true 40-file manifest, and the follow-up proof paid multiple avoidable test-contract correction cycles before the final exact tree was honestly green | When a post-commit stash restore exposes even a plausible same-root retirement or catalog lane, stop and rebuild the full live manifest immediately before any more narrow proofs so the next validation pass targets the real final tree instead of a smaller snapshot. |
| 2026-06-26 | Production SOP run with AI Studio generated-media replay/context preservation plus Gear Ball runtime-load self-pruning rules | 9.2 | high | The full live tree was classified into one coherent AI Studio generated-media context lane plus one Gear Ball process-doc lane, the focused owning Vitest set cleared 5 files with 63 tests before and after the hook-adjusted commit, `docs:check` passed, and the production build passed on the committed tree | The run still paid a small duplicate proof cycle because touched-file Prettier happened after the first test/build pass, and the score-ledger tail required its own final docs commit | Run touched-file Prettier immediately after manifest lock on mixed product-plus-process lanes, then do one focused proof and reserve only a tiny final score-row docs check before push. |
| 2026-06-27 | Production SOP run with AI Studio media detail navigation, generated detail loading continuity, reference-grid delete guard, and credit-modal presentation polish | 9.4 | high | The live tree collapsed into one coherent AI Studio modal/media-library lane, touched-file Prettier was clean before proof, the focused owning Vitest set cleared 5 files with 117 tests before and after the hook-adjusted commit, `docs:check` passed, and the production build passed on the committed tree | The run still required the expected post-hook exact proof replay, and the lane included visible credit-modal/preset-card polish alongside runtime navigation hardening instead of being a purely behavioral seam | Keep using the early Prettier sweep plus exact owning-suite replay after hook commits; call out mixed visual/runtime seams early so the proof stays bounded without pretending the lane is narrower than it is. |
| 2026-06-27 | Production SOP run with AI Studio archived-output project snapshots, autosave unlock signatures, audio companion-art media mirroring, and media-delete route coverage | 9.4 | high | The live tree was fully classified including two untracked tests, touched-file Prettier ran before proof, the focused owning Vitest set cleared 7 files with 144 tests before and after the hook-adjusted commit, `docs:check` passed, and the production build passed on the committed tree | The lane still combined project persistence, audio companion-art server processing, media-delete API coverage, and small credit/modal styling polish under one broad AI Studio/runtime proof | Keep full-status manifests with untracked tests included up front, then use one shared proof ladder when the seams all support the same media persistence/runtime outcome. |
| 2026-06-27 | Production SOP run with AI Studio detail navigation key reuse, Reference Grid document-delete command ownership, reference overflow archiving, and credit purchase card actions | 9.3 | high | The full live tree was classified including two untracked controller files, touched-file Prettier ran before proof, the focused owning Vitest set cleared 8 files with 287 tests before and after the hook-adjusted commit, `docs:check` passed, and the production build passed on the committed tree | The lane was broad and the first Prettier sweep still changed multiple files after diff sampling, so the validated manifest was larger than ideal even though it stayed coherent | Keep doing the manifest-first Prettier sweep before tests, but for lanes this broad consider splitting visible credit/profile polish from deep Reference Grid/runtime behavior when the worktree already presents that boundary. |
| 2026-06-27 | Production SOP run with audio companion-art claim scoping, telemetry scope tagging, and near-white image persistence telemetry | 9.5 | medium | The live tree collapsed cleanly into one four-file server lane, touched-file Prettier ran before proof, the focused audio companion-art Vitest set cleared 2 files with 9 tests before and after the hook-adjusted commit, `docs:check` passed, and the production build passed on the committed tree | The only drag was the expected hook replay after commit; otherwise the manifest and proof stayed tight | Keep compact server lanes on this path: manifest, Prettier, owning tests, docs/build, hook replay, then score tail. |
| 2026-06-27 | Production SOP run with AI Studio pricing/admin Motion Control billing, media/canvas runtime persistence, and Copperknot launch-restart handoff capture | 9.1 | high | The full live tree was classified into a 44-file manifest including two untracked files, touched-file Prettier ran before proof, the focused owning Vitest set cleared 18 files with 438 passing tests plus 7 skips, `docs:check` passed, and the production build passed before Git writeback | The lane was very broad across pricing/admin, AI Studio media/canvas runtime, media-library runtime, generation billing, admin CSS, and Copperknot handoff context, and the hook/post-commit doc normalization left one handoff wording tail to fold into the score commit | When a manifest spans pricing plus AI Studio runtime plus handoff docs, split only if validation seams diverge; otherwise keep one broad proof ladder, budget hook/doc normalization, and score the scope breadth honestly. |
| 2026-06-28 | Production SOP run with AI Studio media-library bulk selection, Fal CDN upload/runtime extraction, provider-returned status URLs, perf gates, and Copperknot launch docs | 9.1 | high | The full live tree was classified into a 43-file manifest including three untracked helpers, a sibling sweep pulled in adjacent AI Studio/reference/media-library risk, focused Vitest cleared 23 files with 491 passing tests plus 7 skips, `gear-ball:preflight` passed with formatting, ESLint, docs checks, and 17-file Vitest coverage at 441 passing tests plus 7 skips, `git diff --check` passed, and the production build passed before Git writeback | The lane was broad across AI Studio media-library UI/runtime, Fal server upload plumbing, provider URL derivation, admin styling, and Copperknot docs; the first formatting pass used the wrong Prettier binary and preflight caught one lint-only React effect seam in the new bulk-selection hook | Use the repo-local Prettier binary for manifest sweeps on broad runs, then keep the fast lint/test correction loop bounded to the exact failed file before replaying only the affected proof rungs. |
| 2026-06-28 | Production SOP run with AI Studio drag/reference ingestion hardening, protected-route auth recovery, Copperknot launch metrics, and Dave auth-granting-user audit retention | 9.3 | high | The full live tree was classified into a 42-file manifest including one untracked Dave report, repo-local Prettier ran before proof, `gear-ball:preflight` passed with formatting, ESLint, docs checks, and 11-file Vitest coverage at 253 passing tests, the sibling-expanded focused Vitest set cleared 17 files with 331 passing tests, `git diff --check` passed, the production build passed, and the post-commit convergence check found no hook-restored tails | The lane was broad across AI Studio drag/drop ownership, Reference Grid ingestion, Media Library item interactions, protected-route recovery, Copperknot docs, and a Dave retained report, so the validation ladder had to be heavier than a narrow publish run | Keep using manifest-first repo-local formatting plus one related-sweep expansion for AI Studio drag/reference lanes; when the post-commit tree is clean, skip duplicate replay and move straight to the score tail. |
| 2026-06-28 | Production SOP run with AI Studio workflow reload availability, media-library persistence, prompt-text drops for Sound/Music/Voice panels, and Reference Grid card/canvas presentation | 9.4 | high | The full live tree was classified into a 40-file manifest including two untracked helpers, repo-local Prettier ran before proof, `gear-ball:preflight` passed with formatting, ESLint, docs checks, media preview fan-out, and 21-file Vitest coverage at 655 passing tests, the sibling-expanded focused Vitest set cleared 10 files with 124 passing tests, `git diff --check` passed, the production build passed, and the post-commit convergence check found no tails | The lane stayed broad across workflow reload, generated-output hydration, persistence, prompt-drop helpers, Reference Grid card rendering, Canvas styling, and Media Library preview behavior, so the validation profile remained heavier than a narrow component run | Keep broad AI Studio workflow/persistence lanes on this path: file-backed manifest, repo-local formatting, preflight with fan-out, one sibling expansion, build, then no duplicate replay when hooks leave the tree clean. |
| 2026-06-28 | Production SOP run with Create/Pulse preset surface hardening, AI Studio view-model alignment, Copperknot launch/UX audit artifacts, and simulated tester profiles | 9.4 | medium | The full live tree was classified into a 17-file manifest including new Copperknot, UX evidence, and tester-profile docs, repo-local Prettier ran before proof, `gear-ball:preflight` passed with formatting, ESLint, docs checks, and 3-file Vitest coverage at 109 passing tests, the sibling-expanded Create/Pulse Vitest set cleared 8 files with 64 passing tests, `git diff --check` passed, the production build passed, and the post-commit convergence check found no tails | The lane mixed a small Create/Pulse runtime surface with several retained audit/tester docs and shared `docs/README.md`, so the validation had to include docs gates and build rather than staying pure component-test scope | Keep compact mixed product-plus-evidence lanes together when the manifest is small and proof seams stay green; use expanded untracked-directory manifests so docs/tester folders cannot hide behind a directory-level status row. |
| 2026-06-28 | Production SOP run with launch runtime hardening, media detail/prompt-copy polishing, billing eligibility/auth recovery, deployment/docs validation tooling, and Maya/Gottspan/Copperknot evidence updates | 9.1 | high | The full live tree was classified into an 89-file manifest including untracked docs, public assets, runtime helpers, and validation scripts; repo-local Prettier normalized supported files, `gear-ball:preflight` passed with formatting, ESLint, docs checks, preview-delivery fan-out, and 29 Vitest files clearing 653 tests; `git diff --check`, script syntax checks, size-budget checks, and the production build all passed before Git writeback | The run still hit one preflight tooling false-red because ignored JS config files emitted ESLint warnings under `--max-warnings 0`, requiring a narrow `--no-warn-ignored` preflight fix, and one parallel manifest refresh caused a harmless formatter recheck race | Keep manifest-first validation, but when config files are touched, ensure tooling treats ignored files as neutral before the first heavy preflight so the proof ladder stays about product risk rather than validation-script noise. |
| 2026-06-28 | Production SOP run with AI Studio shared audio playback mirroring, hidden video shot-mode prompt visibility cleanup, and Maya/Copperknot tester evidence updates | 9.2 | high | The live tree was classified into a 31-file manifest including two untracked files, repo-local Prettier ran before proof, `gear-ball:preflight` passed with formatting, ESLint, docs checks, preview-delivery fan-out, and 8 Vitest files clearing 180 tests; a sibling-focused run cleared 5 additional files with 83 tests, `git diff --check` passed, the production build passed, and the post-commit tree was clean | The run had one avoidable command-context miss when an extra sibling Vitest run was launched from repo root instead of `frontend/`, causing jsdom and relative-path failures that were immediately corrected without product edits | Keep extra sibling test runs frontend-relative, and continue treating root-cwd Vitest failures as command-harness noise when the same files pass from the repo-standard `frontend/` cwd. |
| 2026-06-29 | Production SOP run with Google auth callback code exchange hardening, AI Studio perf/media polling audit coverage, audio companion-art prompt safeguards, public-surface drift checks, and Maya orientation evidence assets | 9.2 | high | The live tree was classified into a 22-file manifest including six untracked PNG evidence assets, repo-local Prettier was clean before proof, `gear-ball:preflight` passed with formatting, ESLint, docs checks, and 6 Vitest files clearing 61 tests; `git diff --check`, script syntax checks, and the production build passed before Git writeback | The only execution drag was another harmless parallel manifest/Prettier race during the verification recheck, which was corrected by rerunning the formatter check after the manifest write completed | Stop parallelizing commands where one consumes a just-written manifest from another; refresh the manifest first, then run dependent checks in a separate command so the proof ladder stays noise-free. |
| 2026-06-29 | Production SOP run with Google signup account-chooser independence, dashboard media-compliance gate wiring, server-side upload/generation/project consent guards, and ADR/docs alignment | 9.1 | high | The live tree was classified into a 33-file manifest including new ADR and media-compliance guard files, repo-local Prettier ran before proof, `gear-ball:preflight` passed with formatting, ESLint, docs checks, and 13 Vitest files clearing 168 tests; the related upload-route sibling run cleared 6 files with 32 tests, `git diff --check` passed, and the production build passed before Git writeback | The sibling run exposed one adjacent stale motion-reference upload test that needed the new accepted-consent mock, and the first proof pass still paid a few formatter rewrites before the final manifest was locked | When adding a shared server guard, proactively sweep same-family route tests that call the guarded service indirectly so stale mocks are updated before the sibling proof rung turns red. |
| 2026-06-29 | Production SOP run with shared adaptive-media pressure sampling, AI Studio stability telemetry/quarantine, auth recovery fail-closed cleanup, prompt-length preservation, and Dave security audit evidence | 9.1 | high | The live tree was classified into a 55-file manifest with six new files, repo-local Prettier ran before proof, `gear-ball:preflight` passed with formatting, ESLint, docs checks, and 15 Vitest files clearing 458 tests plus 39 skips; a sibling Reference Grid/media run cleared 5 files with 209 tests plus 7 skips, `git diff --check` passed, and the production build passed before Git writeback | The first preflight caught two React Compiler warnings from synchronous disabled-state resets in new pressure hooks, and the lane remained broad across media pressure, auth recovery, AI Studio panels, telemetry, and security docs | For shared hook refactors, scan for `setState` inside effect bodies before the first preflight, and keep the sibling proof limited to the highest-risk rendered surfaces instead of chasing the full filename fan-out. |
| 2026-06-29 | Production SOP run with auth OAuth loading reset on browser restore for Google sign-in and signup | 9.6 | low | The live tree was a clean two-file auth lane, repo-local Prettier was clean, `gear-ball:preflight` passed with formatting, ESLint, docs checks, and the auth route suite clearing 26 tests; `git diff --check` and the production build passed before Git writeback | The lane was tiny and did not need sibling expansion; the only cost was the standard build guard for an auth page change | Keep compact auth UX recovery lanes on the simple manifest-preflight-plus-build path when the owning route test directly covers both login and signup modes. |
| 2026-06-29 | Production SOP run with compliance gate recovery, missing-account Google callback handling, billing entitlement copy, and AI Studio crash-resilience pressure restoration | 9.3 | high | The live tree was classified into a 38-file manifest, repo-local Prettier was clean, `gear-ball:preflight` passed with formatting, ESLint, docs checks, and 14 Vitest files clearing 200 tests; the focused AI Studio crash-resilience suite cleared 9 files with 121 tests, `git diff --check` passed, the production build passed before commit, and the hook-rewritten committed content re-cleared preflight plus production build | The batch was broad across auth/compliance, billing/profile, AI Studio media cards, telemetry, CSS, route docs, and a new test script, so one post-hook replay was necessary even though the first proof pass was clean | Keep broad mixed-runtime SOP runs on one manifest-first proof ladder, but when lint-staged rewrites staged TypeScript/CSS, replay the compact committed-content preflight and build once rather than chasing redundant sibling sweeps. |
| 2026-06-29 | Production SOP run with AI Studio generation-access CTAs, pricing signup-closed routing, media-library pressure-aware budgets, preview-signing cleanup, and active-workset perf gates | 9.3 | high | The live tree was classified into a 66-file manifest, repo-local Prettier normalized the changed files, `gear-ball:preflight` passed with formatting, ESLint, docs checks, preview-delivery fan-out, and 21 Vitest files clearing 284 tests; the focused crash-resilience suite cleared 12 files with 141 tests, Generate CTA contract and AI Studio pricing display drift checks passed, `git diff --check` passed, the production build passed, and the hook-rewritten committed content re-cleared the same compact proof ladder | The lane was broad across AI Studio create/edit/video/music/sound/voice panels, pricing/billing/profile copy, media-library pressure/signing policy, perf runtime, E2E audit script, CSS, and docs, so the proof ladder was necessarily heavier than a narrow component lane | Keep this exact profile for broad generation-access plus media-pressure batches: manifest preflight with preview fan-out, the explicit CTA/pricing/media-performance gates, build, then a single committed-content replay after lint-staged instead of expanding into speculative sibling sweeps. |
| 2026-06-29 | Production SOP run with shared account-menu chrome, project-restore perf audit gates, dashboard media-compliance test coverage, and plan concurrency diagnostics | 9.4 | medium | The live tree was classified into a 16-file manifest, repo-local Prettier normalized the changed files, `gear-ball:preflight` passed with formatting, ESLint, docs checks, and 5 Vitest files clearing 57 tests; the focused crash-resilience suite cleared 12 files with 143 tests, Generate CTA contract passed, SQL diagnostic sanity and `git diff --check` passed, the production build passed, and the hook-rewritten committed content re-cleared the same compact ladder | The lane included visible account-menu CSS and perf audit runtime changes, so it still needed a build plus post-hook replay even though the file count was modest | For compact chrome/perf/SQL-diagnostic batches, keep the manifest proof narrow, add only the active contract gates and a lightweight SQL read-only sanity check, then stop after one committed-content replay when the hook leaves no tails. |
| 2026-06-30 | Production SOP run with Reference Grid 300-active cap, full project display prompt preservation, 300-active restore perf gates, audio CTA sizing, pricing style polish, and concurrency diagnostic snapshot semantics | 9.3 | high | The live tree was classified into a 26-file manifest after catching one late CSS tail, repo-local Prettier normalized touched files, `gear-ball:preflight` passed with formatting, ESLint, docs checks, and 4 Vitest files clearing 105 tests; the focused crash-resilience suite cleared 12 files with 144 tests, Generate CTA contract passed, SQL diagnostic sanity and `git diff --check` passed, the production build passed, and the hook-rewritten committed content re-cleared the same compact proof ladder | One extra `landing-sections.css` style change appeared after the first manifest, so I refreshed the manifest and reran preflight before committing instead of shipping a stale proof set | Keep manifest refresh mandatory after formatter/status changes, especially when CSS tails appear mid-run; for project-persistence plus Reference Grid cap changes, pair targeted project/server tests with crash-resilience, CTA, SQL sanity, and build, then stop after one committed-content replay. |
| 2026-06-30 | Production SOP run with AI Studio plan-access upload gating, protected-route restore hardening, Gear Ball runtime-memory pruning, and matching tests/styles | 9.1 | high | The live tree was classified into one broad product-plus-Gear-Ball-doc lane, manifest preflight passed with formatting, ESLint, docs checks, and 4 Vitest files clearing 125 tests plus 7 skips after a bounded formatting correction; Generate CTA contract, `git diff --check`, and the production build passed before commit, and the hook-rewritten committed content re-cleared the compact preflight plus build | I launched one helper sweep in parallel before its manifest file existed, and the first preflight found three files that should have been formatted before the validation rung | Serialize manifest-producing commands before any manifest consumer, then run a repo-local touched-file Prettier sweep before the first preflight on mixed product-plus-process lanes. |
| 2026-06-30 | Production SOP run with prefab agent tile height tightening | 9.2 | medium | The live tree collapsed to one CSS-only file, the manual CSS ladder passed Prettier, `git diff --check`, and the production build before commit, and the hook-adjusted committed content re-cleared Prettier plus build with no restored tails | The wrapper preflight spent one avoidable cycle trying to run Vitest against a stylesheet-only manifest, which correctly produced no test files | For CSS-only SOP runs, skip `gear-ball:preflight` and use the manual ladder immediately: touched-file Prettier, `git diff --check`, and build when the style is production-facing. |
| 2026-06-30 | Production SOP run with AI Studio viewport-adjusted perf gates, project-restore instrumentation contract checks, and Media Library plan-access drop handling | 9.1 | high | The 12-file AI Studio perf/media batch was classified cleanly, sibling sweep identified broad Media Library fan-out without widening proof unnecessarily, manifest preflight passed formatting, ESLint, docs checks, and 3 Vitest files with 119 passing tests plus 7 skips after one bounded formatting correction; `git diff --check` and production build passed before commit and again on the hook-rewritten committed content | The first preflight caught two test/audit files that should have been formatted before validation, and authenticated perf-audit smoke stopped at the expected missing `PLAYWRIGHT_AUDIT_EMAIL` credential gate | Run touched-file Prettier before first preflight on E2E/page-test batches, and keep authenticated perf audit proof classified separately from local unit/build proof when credentials are unavailable. |
| 2026-06-30 | Production SOP run with Media Library card action layout, selection marker alignment, AI Studio lifecycle telemetry breadcrumbs, and Nuclo tab-return handoff | 9.1 | high | The live tree was classified into one 8-file AI Studio media/telemetry/docs batch including two untracked files, manifest preflight passed formatting, ESLint, docs checks, and 2 Vitest files with 21 passing tests after one bounded formatting correction, and the production build passed before commit and again on the hook-rewritten committed content | I again let first preflight find formatting in a new hook/test/handoff trio instead of running touched-file Prettier before the validation rung | Run touched-file Prettier immediately after manifest lock whenever the batch includes new tests or handoff markdown, then use the same compact preflight/build replay only if hooks rewrite committed content. |
| 2026-06-30 | Production SOP run with AI Studio tab-return auth revalidation opt-out | 9.5 | high | The live tree collapsed to a three-file protected-route/auth-restore lane, manifest preflight passed formatting, ESLint, and 12 focused hook tests on the first pass, the broader AI Studio/protected-route gate set passed 31 tests, and production build passed before commit and again on the hook-rewritten committed content | The sibling sweep showed broad route/auth neighbors, so the run added one focused page-level test pass beyond the wrapper instead of relying only on the changed hook suite | Keep compact auth-route lanes on this profile: manifest preflight, route/bootstrap gate tests, build, then one committed-content replay only when hooks rewrite the staged files. |
| 2026-06-30 | Production SOP run with 400-active Reference Grid gates, AI Studio protected-route runtime preservation, and media auth outage logging | 9.2 | high | The full live tree was classified into one 24-file shared-runtime manifest including the untracked auth logging helper, related sweep ran before validation, manifest preflight passed formatting, ESLint, docs checks, and 10 Vitest files with 155 passing tests after one bounded formatting correction, `git diff --check` passed, and production build passed before commit and again on hook-rewritten committed content | The first preflight still caught five API test files that should have been formatted immediately after manifest lock, creating one avoidable red validation pass | Run repo-local touched-file Prettier right after locking broad API/runtime manifests, then use preflight plus build once and replay only when lint-staged rewrites committed content. |
| 2026-06-30 | Production deployment recovery for Vercel static generation timeout on `/`, `/dashboard`, and `/pricing` | 9.4 | high | The Vercel failure was correctly classified as a static-generation timeout rather than a compile error, the fix stayed at the build-time data-loading boundary with existing degraded props instead of UI/UX changes, manifest preflight passed formatting, ESLint, docs checks, and 2 focused Vitest files with 5 tests, the broader dashboard/pricing route set passed 4 files with 28 tests, `git diff --check` passed, and production build passed with affected static pages generating in about 642ms | The first broader sibling test run exposed a stale root-route Supabase mock, requiring a small test-harness correction before the route set went green | For Vercel build failures that occur after compile during static-page generation, inspect shared `getStaticProps` data loaders first and bound optional upstream reads before chasing route rendering or UI behavior. |
| 2026-07-01 | Production SOP run with Google OAuth handoff preflight, protected-route session preservation, AI Studio perf audit route flags, and database I/O posture SQL/docs | 9.5 | high | The full live tree was classified into one 32-file auth/database-reliability lane including untracked API, tests, migrations, rollback scripts, diagnostics, ops SQL, and evidence; repo-local Prettier ran before validation, related sweep completed, manifest preflight passed formatting, ESLint, docs checks, migration/doc parity, and 7 Vitest files with 63 tests, `git diff --check` passed, and production build passed before commit and again on hook-rewritten committed content | The lane was broad enough that SQL syntax could only be locally checked through docs/parity/text validation and build/test coverage, not hosted execution, because applying SQL is outside Gear Ball's default push lane | Keep broad auth-plus-SQL posture lanes on this path: full-status manifest, early Prettier, related sweep, preflight with migration/doc parity, build, one hook replay, and clearly separate local proof from hosted SQL apply proof. |
| 2026-07-01 | Production SOP run with AI Studio media-library reroll/reload controls, runtime scope-key preservation, profile/subscription styling, and worker-runs retention SQL/docs | 9.2 | high | The full live tree was classified into one 37-file AI Studio/profile/SQL lane including untracked runtime-scope tests, storage cleanup diagnostics, migration/rollback SQL, and evidence; repo-local Prettier was clean, related sweep ran, manifest preflight passed formatting, ESLint, docs checks, migration/doc parity, and 8 Vitest files with 175 passed plus 7 skipped, `git diff --check` passed, and production build passed before commit and again on hook-rewritten committed content | The first preflight caught one React Compiler ref-read violation in the new runtime scope-key route code, requiring a small canonical state-backed fix before validation was green | For runtime remount-boundary work, avoid reading `ref.current` during render; keep prior render authority in state or effects so React Compiler proof stays green on the first pass. |
| 2026-07-01 | Production SOP run with media storage lifecycle diagnostics, signup-seed credit retirement, storage add-on no-stacking controls, admin storage economics, and profile/billing/API coverage | 9.4 | high | The 110-file production tree was captured from full status including untracked migrations, rollback SQL, admin/runtime helpers, handoffs, and tests; repo-local formatting was already normalized, `gear-ball:preflight` passed formatting, ESLint, docs checks, migration/doc parity, and 31 Vitest files with 313 passed plus 7 skipped, `git diff --check` passed, and production build passed before commit and again on the hook-rewritten committed content with `/`, `/dashboard`, and `/pricing` static generation under one second locally | The lane was very broad across billing, profile/admin surfaces, AI Studio media runtime, provider routes, and SQL, and hosted SQL apply/production deploy proof remained outside the local commit/push SOP | For broad billing-plus-storage-lifecycle lanes, keep the manifest-first preflight and exact hook replay, but do not add speculative extra tests once the related suite and build are green; separate local proof from hosted SQL/deployment proof in the closeout. |
| 2026-07-01 | Production SOP run with profile billing/storage presentation contracts, storage add-on eligibility updates, AI Studio shell resize reset extraction, SQL no-stack idempotence, and Nuclo/Dave retained reports | 9.4 | high | The 34-file tree was captured from full status including untracked reports, repo-local Prettier ran before validation, related sweep confirmed broad AI Studio header adjacency without widening the lane, `gear-ball:preflight` passed formatting, ESLint, docs checks, migration/doc parity, and 13 Vitest files with 200 passing tests, `git diff --check` passed, and production build passed before commit and again on hook-rewritten committed content | The lane still mixed visible profile/CSS presentation with billing/SQL contract work and retained agent reports, and hosted SQL/deploy proof remained outside the local commit/push SOP | Keep this profile for medium mixed profile/billing lanes: full-status manifest, early Prettier, related sweep, preflight, build, one exact hook replay, then stop without speculative extra AI Studio fan-out once owning shell/header tests are green. |
| 2026-07-01 | Production SOP run with compact profile credits, storage, and subscription section layout polish | 9.5 | medium | The 8-file profile/CSS/test tree was captured from full status, related sweep confirmed the expected profile page-test neighborhood without adding same-lane tails, Prettier was clean, `gear-ball:preflight` passed formatting, ESLint, and 4 focused Vitest files with 31 passing tests, `git diff --check` passed, and production build passed before commit and again on hook-rewritten committed content | The run still paid the standard exact replay because lint-staged touched the staged files, and this was visible profile polish so local tests/build do not replace human visual review on production | For compact profile presentation lanes, keep the ladder exactly this small: full manifest, related sweep, preflight, build, one hook replay, score tail, push. |
| 2026-07-01 | Production SOP run with admin storage economics catalog rows, AI Studio persistence/runtime debug gates, reference-grid archive semantics, and migration 176 admin-stats telemetry index | 9.4 | high | The 25-file mixed runtime/SQL tree was captured from full status including untracked forward/rollback migrations, repo-local Prettier ran before validation, related sweep confirmed broad AI Studio header adjacency without expanding beyond owning tests, `gear-ball:preflight` passed formatting, ESLint, docs checks, migration/doc parity, and 8 Vitest files with 194 passing tests, `git diff --check` passed, and production build passed before commit and again on hook-rewritten committed content | The lane still crossed admin analytics, AI Studio persistence, billing catalog/profile storage, CSS, and SQL; hosted SQL apply/lint and production deployment proof remained outside the local commit/push SOP | For medium shared-runtime plus SQL lanes, keep manifest-first Prettier, preflight with migration parity, build, and one exact hook replay; call out hosted SQL proof separately instead of adding local duplicate test fan-out. |
| 2026-07-01 | Production SOP run with admin global-stats RPC slimming, storage economics catalog rows, and AI Studio perf-audit URL stabilization | 8.9 | high | The 14-file mixed admin/runtime/SQL tree was captured from full status, related sweep ran, repo-local Prettier was clean, `gear-ball:preflight` passed formatting, ESLint, docs checks, migration/doc parity, and 3 Vitest files with 8 passing tests, `git diff --check` passed, and production build passed after the plan-row contract tail was fixed | A post-commit hook/stash tail exposed two uncommitted storage-economics contract lines and the build then caught the API plan-row payload missing the matching fields, forcing an extra corrective product commit before the score row | After any hook-restored tail, rerun `git status` and build before scoring; if parser/type contracts changed, inspect the producing API object before assuming the first green build still describes the final tree. |
| 2026-07-01 | Production SOP tail recovery with plan pricing and contract MRR in admin storage economics | 8.4 | high | The post-push dirty tail was correctly reopened instead of being ignored, then the five-file storage-economics pricing/MRR batch cleared repo-local Prettier, `gear-ball:preflight` with 3 Vitest files and 8 passing tests, docs checks, migration parity, `git diff --check`, and a production build before commit | I found this tail only after an initial push and score-row commit, so the run required an avoidable second product commit plus a corrective ledger row | Do not push immediately after a score-row commit until a final `git status --short --branch -uall` is clean; if it is dirty, treat that as active work and validate before any closeout. |
| 2026-07-01 | Production SOP run with standalone admin storage page, generated-column admin stats SQL, lifecycle summary optimization, and motion-reference retirement | 9.4 | high | The 25-file route/admin/SQL/media tree was captured from full status including untracked page, test, migration, and rollback files; repo-local Prettier was clean before validation, related sweep identified expected admin and media neighbors, `gear-ball:preflight` passed formatting, ESLint, docs checks, migration/doc parity, and 4 Vitest files with 18 passing tests, `git diff --check` passed, and production build passed with `/admin/storage` included | The lane still includes visible Character/Elements action styling plus two SQL migrations, and hosted SQL apply/lint or production browser proof were not part of the local commit/push SOP | Keep this profile for broad admin route plus SQL lanes: full manifest including untracked files, early Prettier, preflight with route/docs/migration parity, build, clean post-commit status, then score and push without duplicate replay when hooks leave no tail. |
| 2026-07-01 | Production SOP run with AI Studio pressure conservation, local crash evidence snapshots, and deferred quota/credit/prestage refreshes | 9.5 | high | The 15-file AI Studio pressure-conservation tree was captured from full status including the new pressure helper and tests, repo-local Prettier was clean, related sweep identified expected AI Studio hook/upload neighbors without requiring broader fan-out, `gear-ball:preflight` passed formatting, ESLint, docs checks, and 6 Vitest files with 34 passing tests, `git diff --check` passed, production build passed, and post-commit status was clean | The lane still touches shared AI Studio route/runtime hooks, so local proof does not replace production-browser evidence in an affected profile | Keep this exact profile for compact pressure/perf lanes: full manifest, early Prettier, related sweep, preflight, build, clean post-commit status, score row, and push without duplicate replay when hooks leave no tail. |
| 2026-07-01 | Production SOP tail recovery with Motion Control prestage resource release | 8.5 | high | The post-push dirty tail was reopened immediately, the two-file Motion Control prestage helper/test batch cleared repo-local Prettier, `gear-ball:preflight` with the owning video upload suite at 15 passing tests, `git diff --check`, and a production build before commit | The original score row was committed and pushed before the final post-push clean-state check caught this same-lane tail, so the run required a corrective product commit and score row | Move the final `git status --short --branch -uall` check before the score-row commit and before every push, not only after push, so same-lane hook/restored tails cannot slip behind the ledger. |
| 2026-07-01 | Production SOP run with AI Studio Media Library folder-open motion and Reference Grid section transition polish | 9.5 | medium | The 7-file AI Studio component/CSS/test tree was captured from full status, the branch guard and `shortpulse.allowedBranch=production` were confirmed, the related sweep stayed in expected Media Library and Reference Grid neighbors, repo-local Prettier and `git diff --check` passed, `gear-ball:preflight` passed formatting, targeted ESLint, and 2 Vitest files with 10 passing tests, and production build passed with `/`, `/dashboard`, and `/pricing` generated successfully before commit | This was visible motion/layout polish, so local tests and build prove compile/regression safety but do not replace production visual review after deployment | For compact visible AI Studio polish lanes, keep manifest-first status, targeted related sweep, preflight, one build, clean post-commit status, score row, and pre-push clean check; avoid broad same-directory fan-out unless the sweep exposes a real owner seam. |
| 2026-07-01 | Production SOP run with cost-performance diagnostics workflow and AI Studio Reference Grid audit progress reporting | 9.4 | high | The 7-file tree was captured from full status including untracked workflow/script files, branch guard and `shortpulse.allowedBranch=production` were confirmed, related sweep stayed in the owning AI Studio hook-test neighborhood, repo-local Prettier and `git diff --check` passed, the diagnostics script passed `bash -n`, the workflow parsed as YAML, `gear-ball:preflight` passed formatting, targeted ESLint, docs checks, and 1 Vitest file with 7 passing tests, and production build passed with `/`, `/dashboard`, and `/pricing` generated successfully before commit | This mixed hosted workflow/script/docs with AI Studio runtime/CSS changes, and the hosted diagnostics workflow itself was not executed against staging or production secrets in this local SOP | For workflow-plus-runtime lanes, keep full-status manifests with untracked files, add cheap script/YAML sanity before preflight, build once for shared frontend code, then score only after post-commit status is clean. |
| 2026-07-01 | Production SOP run with admin error-event aggregate RPC, scheduler timeout parity, and aggregate-only derivative diagnostics | 9.2 | high | The 15-file SQL/API/docs/report tree was captured from full status including untracked migration, rollback, and Dave handoff files; branch guard and `shortpulse.allowedBranch=production` were confirmed; SQL migration/security docs were loaded; the related sweep stayed in expected admin/Dave-report adjacency; repo-local Prettier, `git diff --check`, workflow YAML parsing, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with docs/migration parity and 1 admin API Vitest file with 8 passing tests, and production build all passed before commit | The first preflight correctly caught one TypeScript ESLint warning and required a focused code cleanup before green, and the hosted migration/RPC apply plus hosted SQL lint remain outside this local commit/push SOP | For SQL/API/security lanes, keep full manifest with untracked migrations, run cheap YAML/secret checks, accept one focused lint correction when needed, build once, and clearly separate local compile/test proof from hosted SQL apply proof. |
| 2026-07-02 | Production SOP run with media agreement admission hardening, aggregate-only reliability diagnostics, control-plane grant migration 181, admin error-event handling, and AI Studio restore/detail cleanup | 9.3 | high | The 42-file tree was captured from full status including untracked migration, rollback, diagnostics, SQL, and test files; branch guard and `shortpulse.allowedBranch=production` were confirmed; SQL/security/route docs were loaded; targeted scans checked the media agreement, row-detail gating, and RPC grant seams; repo-local Prettier, `git diff --check`, workflow YAML parsing, shell syntax checks, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with docs/migration parity and 8 Vitest files clearing 158 tests, and production build all passed before commit | The lane mixed SQL/security diagnostics, admin API state, AI Studio runtime/detail behavior, route docs, workflow config, and reports; hosted SQL apply/lint and production browser proof remained outside this local commit/push SOP | For broad launch-hardening batches, keep the full-status manifest including untracked SQL/tests, run cheap workflow/script/secret checks before preflight, build once, and report hosted SQL proof separately instead of expanding into duplicate local reruns. |
| 2026-07-02 | Production SOP run with Voice Changer staged-audio lifecycle tracking, media copy/extract safeguards, AI Studio shell scroll/persistence polish, and migration 182 docs/workflows | 9.4 | high | The 40-file tree was captured from full status including untracked migration, rollback, and scrollbar contract test files; branch guard and `shortpulse.allowedBranch=production` were confirmed; SQL/security docs and Gear Ball runtime policy were loaded; related sweep stayed in expected AI Studio adjacency; targeted scans checked staged-audio lifecycle and shell scrollbar/persistence seams; repo-local Prettier, `git diff --check`, workflow YAML parsing, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with docs/migration parity and 11 Vitest files clearing 235 tests, and production build passed before commit and again after lint-staged rewrote committed content | The lane mixed SQL migration/RPC diagnostics, media API routes, AI Studio layout/CSS, workflows, and docs; hosted SQL apply/lint and production browser proof remained outside this local commit/push SOP | For media lifecycle plus AI Studio shell batches, keep the full manifest, run one related sweep plus targeted seam scans, use preflight/build, and replay only the affected committed-content rung set when lint-staged reports rewrites. |
| 2026-07-02 | Production SOP run with media-library bucket MIME/size guardrails, preview resolver hardening, admin error filters, AI Studio perf gates, and migration 184 docs/schema | 9.4 | high | The 46-file tree was captured from full status including untracked migration and rollback files; branch guard and `shortpulse.allowedBranch=production` were confirmed; SQL/security docs were loaded; related sweep stayed in expected AI Studio adjacency; targeted scans checked bucket guardrails, preview resolver signing, and perf/admin filter seams; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with preview-delivery fan-out, docs/migration parity, and 15 Vitest files clearing 251 tests, and production build passed before commit and again after lint-staged rewrote committed content | The lane mixed storage SQL/schema docs, media server helpers, preview signing, admin error-event filtering, AI Studio perf/CSS, and tests; hosted SQL apply/lint and production browser proof remained outside this local commit/push SOP | For broad media-storage guardrail lanes, keep the full manifest with untracked SQL, accept preflight fan-out for preview delivery, build once, and replay only the committed-content preflight/build when lint-staged reports rewrites. |
| 2026-07-02 | Production SOP run with Voice Changer extracted-audio source-name privacy copy simplification | 9.5 | medium | The two-file Voice Changer component/test tree was captured from full status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; related sweep stayed in expected AI Studio panel-test adjacency without widening the lane; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with owning Voices panel coverage at 84 tests, and production build passed before commit and again after the commit output was truncated | This was a visible copy/privacy polish in AI Studio, so local tests and build do not replace production visual review after deployment | For compact UI-copy privacy lanes, keep the ladder tight: full manifest, related sweep, owning panel preflight, one build, clean post-commit status, score row, and push. |
| 2026-07-02 | Production SOP run with media storage basename preview resolver RPC, preview repair runtime, generation projection hydration, admin error filters, and migration 185 docs/schema | 9.4 | high | The 18-file tree was captured from full status including untracked forward/rollback migration files; branch guard and `shortpulse.allowedBranch=production` were confirmed; SQL/security docs were loaded; related sweep stayed in expected AI Studio hydration-test adjacency; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with preview-delivery fan-out, docs/migration parity, and 6 Vitest files clearing 80 tests, and production build passed before commit and again after lint-staged rewrote committed content | The lane crossed SQL/security posture, media preview runtime, generation projection hydration, admin error-event filtering, AI Studio Media Library presentation, docs, and schema snapshots; hosted SQL apply/lint and production browser proof remain outside the local commit/push SOP | For medium media-preview repair lanes, keep the manifest exact, let preflight add preview fan-out, build once, and replay only the committed-content preflight/build when lint-staged rewrites staged files. |
| 2026-07-02 | Production SOP run with durable prepared media upload moves, Media Library folder-open timing polish, and requested Vercel forced rebuild | 9.4 | high | The four-file tree was captured from full status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Vercel CLI/project linkage was checked before deployment; related sweep added only the highest-risk `mediaUploadService.voiceChangerSource` sibling; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `gear-ball:preflight`, focused 2-file media upload Vitest coverage with 10 tests, and production build passed before commit and again after lint-staged rewrote committed content | The lane mixes shared media upload storage behavior with visible Media Library folder-open timing, and the requested Vercel forced rebuild remains a separate post-push deployment action from local proof | For compact upload-service plus visible polish lanes, keep one manifest, add only the top shared-service sibling test, build once, replay after lint-staged rewrites, then push before running `vercel deploy --prod --force --yes`. |
| 2026-07-03 | Production SOP run with admin storage usage snapshots, telemetry retention controls, storage add-on eligibility, AI Studio upload/blob-pressure helpers, and migrations 186/187 | 9.1 | high | The 45-file tree was captured from full status including untracked provider/test and forward/rollback migration files; branch guard and `shortpulse.allowedBranch=production` were confirmed; SQL/security docs were loaded; related sweep stayed in expected AI Studio upload-test adjacency; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with docs/migration parity and 14 Vitest files clearing 96 tests, focused 5-file upload sibling coverage with 54 tests, and production build passed before commit and again after lint-staged rewrote committed content | The first preflight caught one avoidable unused destructuring warning in `appErrorLogs.ts`, and hosted SQL apply/lint plus production browser/admin proof remain outside this local commit/push SOP | For broad admin-storage/telemetry/upload batches, keep the full manifest and one upload sibling set, but scan new destructuring discard patterns before preflight so ESLint does not catch a cheap warning after the expensive docs/load step. |
| 2026-07-03 | Production SOP run with Media Library folder row affordance and collapsed-folder polish | 9.5 | medium | The four-file Media Library component/test/CSS tree was captured from full status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; related sweep stayed in expected AI Studio component-test adjacency without widening the lane; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with owning folder-section coverage at 3 tests, and production build passed before commit | This was visible Media Library UI polish, so local tests and build do not replace production visual review after deployment | For compact visible Media Library component lanes, keep the ladder tight: full manifest, related sweep, owning component preflight, one build, clean post-commit status, score row, and push. |
| 2026-07-03 | Production SOP run with launch storage quota helper, billing/profile account-summary hardening, AI Studio preset/media/video controllers, generation recovery persistence, and migration 188 docs | 9.0 | high | The 133-file tree was captured from full status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; docs/SQL/security scope was loaded; related sweep identified expected AI Studio sibling tests without widening by reflex; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `check:generate-cta-contract`, `gear-ball:preflight` with docs/migration parity and 49 Vitest files clearing 757 tests, and production build passed before commit and again after lint-staged rewrote committed content | The first manifest build trimmed the leading status space on `README.md`, causing an avoidable Prettier miss, and preflight caught the missing migration 188 inventory docs before commit | For broad production trees, generate manifests without trimming raw porcelain output, and scan new migration numbers against `docs/database-migrations.md` and `docs/sops/sop_sql_migration_operations.md` before the expensive preflight. |
| 2026-07-03 | Production SOP run with AI Studio reference-property modularization, Expert Edit canvas/runtime extraction, lazy-modal trace hardening, admin/media API tests, and migration 189 baseline-access copy | 9.0 | high | The 53-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; docs/SQL/security and pricing-audit instructions were loaded; related sweep stayed in expected Expert Edit and AI Studio modal adjacency; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `check:generate-cta-contract`, `gear-ball:preflight` with preview-delivery fan-out and 18 Vitest files clearing 244 tests, and production build passed before commit and again after lint-staged rewrote committed content | The first formatting attempt used text porcelain paths, which broke on `Money Stuff` paths and the deleted file, and preflight caught one new audit-script CLI secret contract mismatch before commit | For mixed trees with spaces or deletions, build both a full staging manifest and an existing-file validation manifest from `git status --porcelain=v1 -z`; for new helper CLIs, run their focused contract test from `frontend/` before the broad preflight. |
| 2026-07-03 | Production SOP run with paid Media Library insert gating, admin catalog/pricing consolidation, AI Studio layout/runtime polish, and Maya authenticated-orientation evidence | 9.2 | high | The 82-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; docs/SQL/security and pricing-audit instructions were loaded; related sweep stayed in expected AI Studio/media/admin adjacency; repo-local Prettier ran before validation; `git diff --check`, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with preview-delivery and character-panel fan-out plus 21 Vitest files clearing 402 tests, and production build all passed before commit | The lane was broad across SQL/RLS, admin pricing/catalog, AI Studio layout/runtime, media upload admission, tester evidence, and docs, and Prettier still touched a few files after the first manifest lock | Keep using null-delimited full and existing-file manifests for broad mixed trees, run touched-file Prettier before preflight, accept repo helper fan-out for preview/character contracts, then build once and score only after post-commit status is clean. |
| 2026-07-03 | Production SOP run with credit top-up package rename, admin error visibility filters, audio credit messaging, Generate CTA contract coverage, and migration 191 docs/schema | 9.4 | high | The 39-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; docs/SQL/security and pricing-audit instructions were loaded; related sweep stayed in expected AI Studio modal adjacency without widening the lane; repo-local Prettier ran before validation; `git diff --check`, `node scripts/check_secret_exposure.js`, `check:generate-cta-contract`, `gear-ball:preflight` with docs/migration parity and 18 Vitest files clearing 267 tests, and production build all passed before commit | The lane mixed customer-facing credit copy, SQL/catalog docs, admin error telemetry, and audio-generation credit handling, but the post-commit tree was clean and did not justify duplicate validation | Keep broad credit-copy plus audio/admin lanes on this profile: null-delimited manifest, early Prettier, explicit CTA contract, preflight with migration parity, one build, then skip post-commit reruns when hooks leave no tail and no content fix occurred. |
| 2026-07-03 | Production SOP run with launch billing entitlement copy, admin error triage/runtime consolidation, authenticated fetch/media URL hardening, AI Studio panel/layout refinements, and route/docs/SQL parity | 9.2 | high | The 73-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; route/docs/SQL/security and pricing-audit instructions were loaded; related sweep added expected AI Studio create and character-panel adjacency without splitting the lane; repo-local Prettier ran before validation; `git diff --check`, `node scripts/check_secret_exposure.js`, `check:generate-cta-contract`, `gear-ball:preflight` with character-panel fan-out and 28 Vitest files clearing 677 tests plus 39 skips, and production build all passed before commit | The lane was broad across docs, SQL, admin, billing/profile, AI Studio, media/auth fetch helpers, and shared CSS; Prettier touched several files after manifest lock but before validation, and post-commit status was clean | Keep production-broad launch/runtime batches on one honest manifest when the live tree couples docs, SQL, API, and UI tests; use related sweep plus preflight fan-out, one build, and no duplicate post-commit rerun when hooks leave no tail. |
| 2026-07-03 | Production SOP run with admin error UI consolidation, AI Studio project/media persistence hardening, Voice Changer source preview updates, Kie/provider recovery contracts, and route/docs parity | 9.4 | high | The 43-path tree was captured from null-delimited status on `production`, including two tracked admin error component deletions; branch guard and `shortpulse.allowedBranch=production` were confirmed; route/docs/frontend and Gear Ball instructions were loaded; related sweep stayed in expected AI Studio modal-test adjacency; repo-local Prettier ran before validation; `git diff --check`, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with preview-delivery fan-out and 20 Vitest files clearing 353 tests, and production build all passed before commit | The lane mixed admin error UI/API consolidation, AI Studio project/media persistence, Voice Changer preview behavior, Kie/provider recovery persistence, shared preview signing, and docs, but hooks left a clean post-commit tree so duplicate validation was not warranted | For deletion-bearing mixed runtime lanes, keep null-delimited full/existing manifests, run one related sweep plus targeted preflight/build, and skip post-commit reruns when lint-staged leaves no tail and no content fix occurred. |
| 2026-07-03 | Production SOP run with admin product catalog naming, storage workspace Supabase usage framing, pricing status compacting, and Projects modal copy/test parity | 9.5 | high | The 17-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; route/docs/frontend, Gear Ball, and pricing-audit instructions were loaded; related sweep stayed in expected AI Studio modal-test adjacency without widening the lane; repo-local Prettier ran before validation; `git diff --check`, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with 4 Vitest files clearing 29 tests, and production build all passed before commit | The lane touched shared admin CSS, admin catalog/storage pages, docs indexes, pricing-page state, and a Projects modal pair, but hooks left a clean post-commit tree and no rerun trigger | For compact admin workspace/docs batches, keep one null-delimited manifest, run pricing-audit routing only as a scope check when no billable formula changed, use preflight plus build, and avoid duplicate post-commit validation when lint-staged leaves no tail. |
| 2026-07-03 | Production SOP run with Projects modal selected-card open-label affordance polish | 9.6 | medium | The one-path CSS module tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs and Gear Ball instructions were loaded; related sweep showed only generic AI Studio stylesheet adjacency; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with the owning Projects modal test clearing 10 tests, and production build all passed before commit | This was a visible AI Studio modal micro-interaction, so local proof does not replace production visual review after deployment, but hooks left a clean post-commit tree and no rerun trigger | For one-file visible CSS lanes, keep the manifest exact, add the owning component test explicitly, build once, and skip duplicate post-commit validation when lint-staged leaves no tail. |
| 2026-07-03 | Production SOP run with Expert Edit post-stage overlay layering and Projects modal hover-only affordance correction | 9.5 | medium | The three-path AI Studio CSS/layout-contract tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs and Gear Ball instructions were loaded; related sweep showed broad test-directory adjacency without a same-lane tail; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `gear-ball:preflight` with the Expert Edit layout contract clearing 7 tests, and production build all passed before commit | This was visible AI Studio layout/CSS work, so local proof does not replace production visual review after deployment, but hooks left a clean post-commit tree and no rerun trigger | For compact visible layout lanes, keep the exact manifest, rely on the owning layout-contract test plus build, and avoid broad sibling-test fan-out when related sweep only returns generic directory adjacency. |
| 2026-07-04 | Production SOP run with ElevenLabs sound-effects explicit-duration pricing variants, admin pricing policy display, generation billing params, Expert Edit transform chrome proof, and Projects modal hover timing | 9.1 | high | The initial 20-path pricing/runtime tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs, Gear Ball, pricing-audit, and pricing-wiring instructions were loaded; related sweep showed broad Expert Edit adjacency without a same-lane pricing tail; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `node scripts/check_ai_studio_pricing_display_drift.js`, `node scripts/print_ai_studio_pricing_action_inventory.js`, `gear-ball:preflight` with 8 Vitest files clearing 205 tests plus 39 skips, and production build passed before the pricing commit; after a restored one-path Projects modal CSS tail, targeted Projects modal preflight cleared 10 tests and a second production build passed before the tail commit | The first preflight caught preventable unused bindings in the admin pricing files, and a post-commit Projects modal tail required a second compact commit/build before score writeback | For pricing/runtime lanes, keep the pricing drift checks plus manifest preflight/build, but scan new helper imports/derived values before preflight and re-run build only once for any post-commit visible CSS tail that was not in the original manifest. |
| 2026-07-04 | Production SOP run with credit top-up ladder repricing, admin tester reports intake, AI Studio audio/runtime metadata hardening, media upload policy extraction, admin diagnostics extraction, and docs/SQL parity | 9.3 | high | The 104-file tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; docs/SQL/security, Gear Ball, pricing-audit, and pricing-wiring instructions were loaded; repo-local Prettier ran before validation; related sweep stayed in expected AI Studio adjacency; `git diff --check`, `node scripts/check_secret_exposure.js`, pricing drift/inventory checks, Generate CTA contract, `gear-ball:preflight` with docs/migration parity and 38 Vitest files clearing 272 tests, and production build all passed before commit | The first preflight caught four unused-binding lint warnings in the audio companion art extraction, requiring a tiny dead-code cleanup before validation was green | For broad pricing/admin/media-runtime SOP runs, keep the null-delimited manifest, early Prettier, pricing drift checks, preflight with migration parity, and one production build; scan extraction seams for unused helper/test parameters before the first preflight to avoid a preventable lint loop. |
| 2026-07-04 | Production SOP run with AI Studio sound-effects duration default, project-switch flush blocking, canvas/reference-grid runtime bounds, auth callback marker coverage, and dashboard/profile presentation parity | 9.5 | high | The 21-file tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs, Gear Ball, pricing-audit, and pricing-wiring instructions were loaded; repo-local Prettier, related sweep, `git diff --check`, `node scripts/check_secret_exposure.js`, pricing drift/inventory checks, Generate CTA contract, `gear-ball:preflight` with docs checks and 8 Vitest files clearing 97 tests, and production build all passed before commit | The lane included visible dashboard/profile CSS and AI Studio runtime changes, so local proof does not replace deployed visual/manual review after Vercel serves the pushed build | Keep this compact mixed-runtime profile: exact null-delimited manifest, early Prettier, pricing/CTA guards when Sound Effects pricing display is touched, one related sweep, manifest preflight, one build, then skip duplicate post-commit validation when hooks leave no tail. |
| 2026-07-04 | Production SOP run with tester-agent relocation/report artifacts, migration 194 request-id lookup index, AI Studio canvas/media/reference-grid runtime hardening, Stripe webhook retry release, size-budget CI controls, and profile/testing docs parity | 9.0 | high | The 153-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/SQL/security and Gear Ball instructions were loaded; repo-local Prettier ran before validation; related sweep stayed in expected canvas/style/reference-grid adjacency; `git diff --check`, `node scripts/check_secret_exposure.js`, Generate CTA contract, size-budget warn-mode check, focused canvas rerun, manifest preflight with 26 Vitest files clearing 383 tests, and production build passed before commit | The first full preflight exposed a jsdom viewport-size harness gap in the new canvas virtualization/drop tests, and one attempted focused rerun used the repo-root Vitest cwd before the correct frontend cwd rerun | For broad canvas/media/tester-report lanes, keep exact null-delimited manifests and early Prettier, but run canvas-focused suites from `frontend/` immediately after viewport/virtualization test changes and avoid `npm -C frontend exec` because it can preserve repo-root cwd noise. |
| 2026-07-05 | Production SOP run with browser crash-session ingest/admin surface, migrations 195/196 docs/schema parity, AI Studio stability telemetry, persistence/output proof, and admin error/crash route coverage | 9.3 | high | The 71-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/SQL/security and Gear Ball instructions were loaded; repo-local Prettier ran before validation; related sweep showed broad but expected AI Studio/reference-grid adjacency; `git diff --check`, `node scripts/check_secret_exposure.js`, Generate CTA contract, size-budget warn-mode check, manifest preflight with docs/migration parity and 26 Vitest files clearing 592 tests, and production build passed before commit | The first preflight caught one preventable unused heartbeat parameter in the new browser-session monitor before green validation | For crash-session/admin-plus-AI-Studio lanes, keep the exact manifest, early Prettier, and one broad preflight/build; scan new monitor/controller helper signatures for unused state parameters before the first preflight. |
| 2026-07-05 | Production SOP run with crash-session recovery hardening, chunk-load error handling, admin crash-log refinements, AI Studio Reference Grid/canvas media runtime proof, and dashboard stale-session recovery | 9.5 | high | The 28-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs and Gear Ball instructions were loaded; repo-local Prettier was clean; related sweep showed expected AI Studio/reference-grid adjacency without a concrete tail; `git diff --check`, `node scripts/check_secret_exposure.js`, Generate CTA contract, size-budget warn-mode check, manifest preflight with docs checks and 12 Vitest files clearing 121 tests, and production build passed before commit | The lane still mixed admin crash-session surfaces, shared browser recovery, dashboard auth recovery, and AI Studio media runtime, but the manifest captured all live paths and hooks left no tail | Keep this profile for compact crash-recovery plus AI Studio runtime follow-ups: exact manifest, early Prettier, one related sweep, preflight, build, then skip duplicate post-commit validation when hooks leave the tree clean. |
| 2026-07-05 | Production SOP run with crash-session review status controls, admin crash-status API, migration 197 docs/schema parity, dashboard route polish, profile storage add-on eligibility controls, and Projects modal async test hardening | 9.2 | high | The final 37-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/SQL/security and Gear Ball instructions were loaded; repo-local Prettier ran before validation; related sweep was rerun after a stale-manifest correction; `git diff --check`, `node scripts/check_secret_exposure.js`, Generate CTA contract, size-budget warn-mode check, manifest preflight with docs/migration parity and 12 Vitest files clearing 76 tests, and production build passed before commit | The first related sweep used an older manifest, the live tree expanded twice before staging, and the first preflight caught one Projects modal test that awaited the shell instead of loaded project-card state | Rebuild the null-delimited manifest immediately before every broad validation/stage step, and for async modal tests, await loaded content state before asserting status text. |
| 2026-07-05 | Production SOP run with admin Customer Support nav grouping, user-health SOP copy, generated-media provider request-id fallback, and dashboard hero spacing tail | 9.3 | medium | The final 7-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs and Gear Ball instructions were loaded; related sweep showed broad AI Studio test-directory adjacency without a concrete same-lane tail; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, manifest preflight with docs checks and 2 Vitest files clearing 52 tests, and production build passed before commit | A visible dashboard CSS tail appeared after the first six-file manifest, requiring a manifest refresh and one repeated preflight/build before staging | Refresh the manifest immediately before staging even on compact lanes, and treat late visible CSS tails as part of the live tree unless the user explicitly defers them. |
| 2026-07-05 | Production SOP run with simplified admin pricing catalog display, baseline-access pricing status, retired credit-package filtering, SQL payload projection refactor, and dashboard hero text polish | 9.4 | high | The 14-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs, Gear Ball, and pricing-audit instructions were loaded; related sweep showed broad AI Studio media-persistence adjacency without a concrete tail; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, AI Studio pricing display drift check, pricing action inventory print, manifest preflight with docs checks and 4 Vitest files clearing 57 tests, and production build passed before commit | The lane touched shared `admin.module.css` and visible dashboard CSS, but the manifest stayed stable after validation and hooks left no tail | For compact admin pricing display lanes, keep pricing drift checks plus manifest preflight/build; treat shared admin CSS as acceptable when the dominant batch is admin pricing and the owning page/API tests are included. |
| 2026-07-05 | Production SOP run with dashboard hero announcement title color polish | 9.5 | low | The one-path CSS tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs and Gear Ball instructions were loaded; related sweep showed generic stylesheet adjacency without a concrete tail; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | `gear-ball:preflight` is not useful for CSS-only manifests because it runs Vitest with no matching test files, so the lane switched to the manual CSS ladder after confirming formatting was clean | For one-file visible CSS lanes, use Prettier, diff/secret checks, related sweep, and build; skip or manually replace `gear-ball:preflight` when the manifest has no runnable tests. |
| 2026-07-05 | Production SOP run with Agent Tester Reports naming, admin nav ordering, tester report route copy, and dashboard announcement positioning | 9.4 | medium | The 6-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs and Gear Ball instructions were loaded; related sweep showed admin report/test adjacency, so the focused `AdminTesterReportsPanel` sibling test was added; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, manifest preflight with docs checks and 2 Vitest files clearing 7 tests, focused sibling test with 2 tests, and production build passed before commit | This was visible admin/dashboard copy and layout polish, so local proof does not replace deployed visual review, but hooks left no tail | For compact admin route copy lanes, include the touched page/header tests plus the directly changed panel sibling test, then build once and skip broad admin-page fan-out when related sweep only returns generic route-test adjacency. |
| 2026-07-05 | Production SOP run with project workspace equal-timestamp freshness guard, saved Pulse chat restore rejection, media detail preview URL safety, admin pricing dirty-state, and Stripe webhook claim-release logging | 9.3 | high | The 16-path tree was captured from null-delimited status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/SQL/security, Gear Ball, and pricing-audit instructions were loaded; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, pricing drift/inventory checks, focused Stripe webhook coverage, manifest preflight with docs/migration parity and preview-delivery fan-out clearing 106 tests, and production build all passed before commit | The first preflight used a null-delimited manifest where the script expected newline-delimited input, so it passed docs/parity checks but failed before Vitest until rerun with a newline manifest; hosted SQL apply/lint and production runtime proof remain outside local commit/push proof | For mixed SQL plus AI Studio runtime lanes, keep null-delimited manifests for staging but feed preflight newline manifests; add only concrete sibling coverage like webhook exception logging, then stop after preflight/build when hooks leave no tail. |
| 2026-07-05 | Production SOP run with Kie GPT Image 2 reference admission/staging, admin storage provider snapshot capture, docs/API/SOP parity, and related AI Studio compliance/reference tests | 9.4 | high | The 32-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/Gear Ball/doc-index/route/API/SOP context was loaded; related sweep showed broad AI Studio hook-test adjacency without a concrete tail; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `docs:check`, Generate CTA contract, manifest preflight with 10 files clearing 113 tests, focused Kie/admin storage suites clearing 81 tests, and production build all passed before commit | The lane touched shared docs indexes and `admin.module.css`, but those were paired with route/docs parity and admin storage tests; hosted provider upload behavior and production admin manual capture remain outside local proof | For provider-admission plus admin route/docs lanes, keep one newline manifest, rely on preflight plus focused route/provider tests and build, and avoid broad AI Studio hook fan-out when the sweep only returns generic same-directory adjacency. |
| 2026-07-05 | Production SOP run with Maya find-generated-image report artifacts, Media Library findability/persistence hardening, project-card open affordance, dashboard announcement fallback handling, media-list duration metadata, and Reference Grid/archive polish | 9.2 | high | The 69-path tree was captured from live status on `production`, including staged Maya report/workspace artifacts plus unstaged product runtime changes; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/Gear Ball/Maya/AI Studio Media Library context was loaded; related sweep showed broad generic AI Studio component adjacency without a concrete tail; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `docs:check`, manifest preflight with 18 files clearing 334 tests, focused owning suites clearing 334 tests, focused persistence rerun clearing 33 tests, and production build passed before commit | The first build caught one preventable TypeScript contract miss in the new canonical-upload save path (`previewPosterStoragePath`), requiring a narrow fix and rerun; local proof does not replace production browser validation of the Maya-reported workflow | For broad Maya-report plus media-runtime lanes, keep the staged/unstaged manifest unified, but scan new calls to strict delivery helpers before build so type-contract misses are caught before the slow rung. |
| 2026-07-05 | Production SOP run with Maya Agent Tester Reports publish status, report index/status corrections, admin-publish checklist naming, and training-history follow-up | 9.5 | low | The 8-path docs-only tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/Gear Ball/Maya workspace instructions were loaded; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, and `docs:check` passed before commit | `gear-ball:preflight` passed Prettier and docs checks but ended on the known docs-only Vitest no-test-files path, so it was treated as wrapper mismatch rather than a product/docs failure | For docs-only Maya/reporting lanes, use Prettier, diff/secret checks, and `docs:check`; do not pay build or product-test cost when the manifest contains no runtime code. |
| 2026-07-05 | Production SOP run with Maya prompt-detail recovery reports, Agent Tester Reports markdown rendering, Pulse built-in draft validation, AI Studio prompt/detail recovery metadata, project workspace persistence extraction, Reference Grid/detail modal polish, browser crash evidence, and dashboard/pricing route refinements | 9.3 | high | The final 74-path mixed tree was captured from live status on `production`, including staged Maya report artifacts plus unstaged runtime/docs changes; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/Gear Ball/Maya/Pulse route context was loaded; related sweep added only concrete sibling Detail Modal full-quality and Reference Grid sections tests; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `docs:check`, Generate CTA contract, manifest preflight with preview-delivery fan-out clearing 284 tests, focused owning/sibling suites clearing 261 tests, two late related tail tests clearing 57 tests, and production build all passed before commit | The lane was broad across tester reports, admin UI, Pulse validation, AI Studio detail/reference/persistence, dashboard, crash-session, docs, and CSS; two related unstaged tails appeared before commit and were folded in with focused proof rather than ignored | For broad tester-report plus AI Studio runtime lanes, keep one exact manifest, allow preflight contract fan-out, add only concrete sibling tests from related sweep, rerun late-tail focused tests from `frontend/`, and avoid splitting when staged docs and runtime fixes are live-coupled by the same customer recovery issue. |
| 2026-07-05 | Production SOP run with AI Studio video element slot ordering, Video Properties slot-drop tests, Projects modal label/style polish, video payload coverage, and admin storage snapshot test type cleanup | 9.5 | high | The 11-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/Gear Ball instructions were loaded; repo-local Prettier refreshed the manifest; related sweep returned broad generic AI Studio adjacency, so no unrelated fan-out was added; `git diff --check`, `node scripts/check_secret_exposure.js`, manifest preflight clearing 130 tests, repo-wide `type-check`, and production build all passed before commit | This lane includes visible Video Properties and Projects modal changes, so local proof does not replace production visual review after deploy, but the final tree was clean and type-check specifically verified the prior admin storage test blocker is gone | For compact video-panel runtime lanes, use exact manifest preflight plus repo-wide type-check when the tree includes a test-contract type cleanup, then build once and avoid broad modal/payload fan-out when related sweep only surfaces generic same-directory adjacency. |
| 2026-07-05 | Production SOP run with admin storage economics payment-exempt reporting, hidden internal-comp billing SOP parity, Fal persisted-result transient-provider fallback, admin storage UI visibility labels, and tester-report markdown spacing | 9.3 | high | The 12-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/Gear Ball instructions were loaded; pricing-audit routing was checked and did not require AI Studio pricing/debit drift validation; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `docs:check`, manifest preflight clearing 54 tests, focused Fal persisted-results sibling test clearing 18 tests, repo-wide `type-check`, and production build all passed before commit | The first preflight caught two preventable redundant `Boolean(...)` lint issues in new visibility-label branches, requiring a narrow fix/rerun; visible admin CSS/report spacing changes still need deployed admin review if the owner wants visual confirmation | For admin storage/billing reporting lanes, keep docs parity plus exact-manifest preflight, add only concrete helper sibling tests from related sweep, and scan new ternary/boolean label code for lint-clean shape before the first preflight. |
| 2026-07-05 | Production SOP run with Reference Grid text-card hover affordance and Maya second-image-variant evidence/report/admin-publish packet | 8.7 | high | The first live manifest captured Reference Grid runtime/style changes, the owning card test, Maya live notes, and five PNG evidence assets; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/Gear Ball instructions were loaded; non-PNG Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, `docs:check`, manifest preflight clearing 72 tests, and production build all passed before the first commit; post-commit convergence found the related Maya report/index/memory/ledger tail and passed docs packet formatting, `docs:check`, diff check, and secret scan before the second content commit; final post-push status then exposed a related admin-publish status tail, which was deduped, docs-validated, and repushed | Initial manifest undercounted the completed Maya report packet, Prettier refused ignored PNGs when passed explicitly, and final status was dirty after the first push, requiring a non-PNG validation manifest, multiple related tail commits, and a post-push correction | For tester-evidence plus small runtime lanes, separate validation manifests from staging manifests when binary assets are present, and do not commit or push until the full report/index/memory/ledger/admin-publish packet is checked for same-run tails. |
| 2026-07-05 | Production SOP run with Reference Grid hover helper accent polish | 9.2 | low | The 2-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/Gear Ball instructions were loaded; repo-local Prettier, `git diff --check`, `node scripts/check_secret_exposure.js`, owning `ReferenceGridArchiveControls` Vitest suite clearing 3 tests, and production build passed before commit | `gear-ball:preflight` passed format/eslint but hit the known no-test-files wrapper path because the changed component/style manifest did not include the owning test file, so the manual focused-test ladder replaced that rung | For tiny component/style follow-ups, run the owning test directly when preflight cannot infer a test file from the changed manifest, then build once for shared CSS/runtime confidence. |
| 2026-07-05 | Production SOP run with Pulse built-in catalog repair, recovered starter-message fallback, Reference Grid prompt pinning, admin payment-exempt reporting, and route-error noise filtering | 9.3 | high | The 49-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/SQL/Gear Ball instructions were loaded; repo-local Prettier formatting was repaired for three test files; `git diff --check`, `node scripts/check_secret_exposure.js`, manifest preflight with docs/migration parity and 14 Vitest files clearing 292 tests, and production build all passed before commit | The first green-path attempt correctly stopped on missing migration 199 inventory docs before build, requiring a narrow parity fix; hosted SQL apply/lint and production browser/admin proof remain outside local commit/push proof | For mixed Pulse SQL/runtime plus admin telemetry lanes, scan new migration numbers against both migration inventories before preflight, then rely on one manifest preflight, one build, and no duplicate broad reruns when the focused gates are green. |
| 2026-07-06 | Production SOP run with credit grant-lot expiration, paid Media Library access authority, admin credit/crash/storage surfaces, AI Studio detail/session persistence, route/docs/schema parity, and Maya tester evidence | 9.0 | high | The 170-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/SQL/Gear Ball instructions were loaded; repo-local Prettier repaired formatting before proof; `git diff --check`, `node scripts/check_secret_exposure.js`, Generate CTA contract, repo-wide `type-check`, manifest preflight with preview/character fan-out clearing 46 files and 540 tests, focused type-fix tests clearing 19 tests, and production build all passed before commit | The first preflight caught 23 formatting misses, the first type-check caught two preventable test-contract mismatches, and the first focused Vitest rerun used the wrong cwd before the correct frontend-cwd rerun; hosted SQL apply/lint and production browser/admin proof remain outside local commit/push proof | For very broad billing/admin/AI Studio/tester-evidence lanes, run early Prettier before preflight, run focused type-sensitive tests from `frontend/`, and keep one exact manifest preflight plus build rather than splitting when docs, SQL, API, UI, and reports are live-coupled by launch proof. |
| 2026-07-06 | Production SOP run with Hybervees tester-report review surface, Maya Reference Grid evidence, admin tester report access review, credit-lot reservation hardening, and admin/storage/docs parity | 9.2 | high | The 78-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; frontend/docs/SQL/Gear Ball instructions were loaded; repo-local Prettier repaired formatting before proof; `git diff --check`, `node scripts/check_secret_exposure.js`, repo-wide `type-check`, manifest preflight with docs/migration parity and 10 files clearing 121 tests, and production build all passed before commit | The first preflight caught 15 formatting misses before green validation; hosted SQL apply/lint, deployed production proof, and live admin review remain outside local commit/push proof | For medium-broad admin tester-report plus SQL/docs lanes, keep one exact manifest, run early Prettier before the wrapper, rely on preflight/type-check/build, and do not split Hybervees docs from the API/admin route when they are the same access-review lane. |
| 2026-07-06 | Production SOP run with Hybervees Maya orientation reports, AI Studio project workspace persistence/runtime recovery, and dashboard bootstrap polish | 9.4 | high | The 22-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; focused Gear Ball preflight passed Prettier, ESLint, docs checks, and 4 Vitest files clearing 64 tests; `git diff --check`, `node scripts/check_secret_exposure.js`, repo-wide `type-check`, and production build all passed before commit | The lane mixed Hybervees report docs, AI Studio workspace persistence/runtime hooks, dashboard page/style changes, and tests; local proof does not replace deployed production browser review after Vercel serves the pushed build | For compact mixed docs/runtime/dashboard lanes, use one exact manifest, run preflight plus type-check/build once, and skip duplicate reruns when the validation path is green and no formatter/hook tail appears. |
| 2026-07-06 | Production SOP run with unchanged-project switching persistence coverage and Projects modal gradient alignment | 9.5 | medium | The 2-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; related sweep showed only generic AI Studio hook-test adjacency; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, and the owning persistence suite with 50 tests; `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | This included a visible Projects modal CSS polish, so local proof does not replace deployed visual review after the pushed build; no related tail or hook rewrite had appeared before staging | For tiny test-plus-CSS lanes, keep the exact manifest, trust the owning test plus build, and do not fan out into broad AI Studio hook suites when related sweep returns only generic same-directory noise. |
| 2026-07-06 | Production SOP run with AI Studio generated-media duration hydration, workflow reload duration carry-through, detail/media modal metadata coverage, and Hybervees report status updates | 9.0 | high | The 33-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Prettier repaired three files before proof; exact-manifest `gear-ball:preflight` passed docs checks plus preview-delivery fan-out with 15 test files clearing 287 tests; focused generated-media reruns cleared 46 tests; `git diff --check`, `node scripts/check_secret_exposure.js`, repo-wide `type-check`, and production build all passed before commit | The first preflight caught formatting, the next caught a real empty-workflow metadata hydration bug in `resolveWorkflowReloadVideoDurationMs`, and two temporary test-mock corrections were needed before type-check/test parity was green | For media-duration hydration lanes, treat empty workflow metadata as a canonical fixture, run the generated-media suite before broad preflight after helper edits, and avoid broad mock patching by checking which test actually owns each fallback table. |
| 2026-07-06 | Production SOP run with credit grant-lot RPC ambiguity hardening, runtime SQL audit coverage, billing docs parity, and optimistic credit-balance guard | 9.5 | high | The 9-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; SQL/credit/security docs were loaded; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs/migration parity, and 3 test files clearing 22 tests; `git diff --check`, `node scripts/check_secret_exposure.js`, repo-wide `type-check`, and production build all passed before commit | Hosted Supabase apply/lint and production billing replay proof remain outside this local commit/push SOP; the app code change was a small balance-display guard, so local build does not replace deployed billing review after migration apply | For compact SQL hotfix plus frontend guard lanes, keep one exact manifest, require migration inventory parity and runtime-audit tests, run type-check/build once, and report hosted SQL apply as a separate approved operation. |
| 2026-07-06 | Production SOP run with admin reports/crash metadata, Hybervees training/report updates, Pulse preset authoring/runtime repair, billing launch-readiness checks, credit grant summary RPC, and Prompt Modifier catalog repair | 8.9 | high | The final 86-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; exact-manifest `gear-ball:preflight` passed docs/migration parity and 22 test files clearing 168 tests; focused fleet-scan coverage passed 6 tests after a type fixture repair; repo-wide `type-check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The first preflight caught formatting, the second caught missing migration inventory entries for 205/206, and type-check caught a missing `user_id` in a grant-summary test fixture; hosted SQL apply/lint and production admin/Pulse proof remain outside local commit/push proof | For very broad admin/Pulse/billing/docs lanes, expect migration inventory drift and type-contract fixture misses; run Prettier early, refresh the manifest after every correction, and keep one broad preflight/build rather than splitting once the live tree is clearly coupled. |
| 2026-07-07 | Production SOP run with browser-delivered crash-report ingest, CrashReportContext correlation, admin Pulse authoring simplification, security header coverage, and docs parity | 9.4 | high | The 17-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; targeted ESLint, docs checks, Prettier, 5 focused Vitest files clearing 42 tests, `git diff --check`, `node scripts/check_secret_exposure.js`, repo-wide `type-check`, and production build all passed before commit | The first Prettier attempt used the wrong cwd for repo-root paths, then correctly exposed four format fixes; local proof does not replace deployed production confirmation that Vercel serves the crash-report endpoint and `Reporting-Endpoints` header | For next-config plus crash-report lanes, run the manual ladder from the repo root, include header and route tests, run type-check/build once, and avoid relying only on preflight when changed paths include mixed root docs and frontend runtime files. |
| 2026-07-07 | Production SOP run with Hybervees Maya context reports, admin tester-report review/access controls, Pulse preset authoring/runtime validation, browser crash-report refinements, and AI Studio library copy/controls | 9.1 | high | The 50-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs instructions were loaded; related sweep showed broad generic AI Studio adjacency without a concrete extra suite; refreshed `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 15 Vitest files clearing 129 tests; `git diff --check`, `node scripts/check_secret_exposure.js`, repo-wide `type-check`, and production build all passed before commit | The first preflight caught six avoidable formatting misses, so the manifest had to be refreshed and preflight rerun before the build; local proof does not replace deployed admin/tester/Pulse/browser-crash review after Vercel serves the pushed build | For mixed Hybervees/admin/Pulse/crash-report lanes, run early Prettier before the wrapper, keep one exact manifest, trust preflight plus type-check/build when the related sweep only returns generic AI Studio adjacency, and avoid splitting unless validation seams actually diverge. |
| 2026-07-07 | Production SOP run with AI Studio Pulse preset audio/reference handling, adaptive local-transcode yield/release hardening, object URL memory cap tightening, admin crash page cleanup, and Gear Ball fast-path compression | 9.2 | high | The 25-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs instructions were loaded; related sweep showed broad AI Studio adjacency but no concrete extra suite beyond manifest-owned create/audio/upload/runtime tests; adaptive change gate passed 7 files and 251 tests; refreshed `gear-ball:preflight` passed docs checks and 8 Vitest files clearing 136 tests; Generate CTA contract, repo-wide `type-check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The first preflight caught three avoidable formatting misses after the process-doc and runtime edits, requiring one Prettier correction and rerun; local proof does not replace deployed AI Studio audio/reference/Pulse review after Vercel serves the pushed build | For AI Studio adaptive/reference lanes, run the adaptive gate plus exact-manifest preflight, add Generate CTA guard when create/Pulse generation surfaces are touched, and run early Prettier before the wrapper to avoid avoidable validation loops. |
| 2026-07-07 | Production SOP run with Hybervees/Maya supervised-learning tooling, AI Studio workspace/reference-grid persistence simplification, Seedance pricing variant cleanup, auth route behavior, and package script helpers | 9.0 | high | The 81-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs and pricing-audit instructions were loaded; related sweep showed broad AI Studio hook adjacency without a concrete extra suite beyond the manifest-owned hook/runtime tests; pricing display drift and pricing action inventory checks passed; refreshed `gear-ball:preflight` passed docs checks and 21 Vitest files clearing 455 tests; Generate CTA contract, repo-wide `type-check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The first preflight caught 15 formatting misses and the second caught one hook dependency warning in `useAiStudioReferenceGridProps`, requiring a stable empty archived-output constant before proof was green; local proof does not replace deployed AI Studio workspace/auth/pricing review after Vercel serves the pushed build | For broad AI Studio persistence plus pricing/docs/tooling lanes, run early Prettier before preflight, include pricing drift/inventory and Generate CTA guards, and scan new memo fallback arrays for stable dependencies before paying the full wrapper pass. |
| 2026-07-07 | Production SOP run with Hybervees/Maya report packets, AI Studio toolbar/Pulse runtime and session hardening, workflow entitlement guards, ElevenLabs/Fal support messaging, dashboard footer/account links, and media-performance docs | 9.3 | high | The 68-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; related sweep showed broad AI Studio adjacency without a concrete extra suite beyond manifest-owned runtime/API/dashboard tests; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 15 Vitest files clearing 216 tests; Generate CTA contract, repo-wide `type-check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The first preflight caught 12 formatting misses, requiring one Prettier correction and rerun before proof was green; local proof does not replace deployed AI Studio/dashboard/provider-route review after Vercel serves the pushed build | For broad Hybervees plus AI Studio entitlement/support lanes, run early Prettier before the wrapper, keep one exact manifest, include Generate CTA/type-check/build, and avoid broad sibling fan-out when related sweep returns generic toolbar/modal adjacency rather than a concrete owner test. |
| 2026-07-07 | Production SOP run with AI Studio workflow entitlement split: baseline/free Video/Sound exploration stays available while generation remains plan-gated, Starter rail navigation stays blocked, and docs/style affordances match the launch contract | 9.6 | medium | The 6-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs instructions were loaded; related sweep showed generic AI Studio/docs/style adjacency without a concrete extra test beyond the manifest-owned entitlement suite; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 1 Vitest file clearing 4 tests; Generate CTA contract, repo-wide `type-check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The run had no validation-fix loop; local proof does not replace deployed production review that baseline/free users can explore Video/Sound and still see `View plans` at generation time | For compact entitlement/navigation split lanes, keep one manifest preflight plus type-check/CTA/build, and do not fan out into broad style/SOP adjacency unless the related sweep names a concrete same-contract owner test. |
| 2026-07-07 | Production SOP run with Starter-restricted AI Studio workflow rail cleanup: Video/Sound buttons collapse into one `View plans` CTA while toolbar tests and launch styling stay aligned | 9.6 | medium | The 3-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend instructions were loaded; related sweep returned generic toolbar adjacency without a concrete extra owner test beyond the manifest-owned toolbar suite; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, and 1 Vitest file clearing 21 tests; Generate CTA contract, repo-wide `type-check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The run had no validation-fix loop; this is visible toolbar/layout behavior, so local proof does not replace deployed production review after Vercel serves the pushed build | For tiny visible toolbar lanes, keep exact-manifest preflight plus type-check/CTA/build, and avoid broad component-test fan-out when related sweep only returns generic same-directory adjacency. |
| 2026-07-07 | Production SOP run with billing/pricing status policy hardening, SQL migrations 207/208, Stripe failed-payment recovery docs, AI Studio pricing/telemetry/runtime updates, media-library virtualization/runtime fixes, admin/profile/dashboard support surfaces, and Hybervees/Dave reports | 8.8 | high | The 124-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball, SQL, security, pricing-audit, and pricing-wiring instructions were loaded; related sweep found expected media-library and AI Studio adjacency; pricing display drift and action inventory passed; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs/migration parity, and 43 Vitest files clearing 499 tests; Generate CTA contract, repo-wide `type-check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The first broad check caught formatting misses, React Compiler ref-access warnings in media masonry virtualization, a stale dependency/import warning pair, a migration-doc checker false positive on `align_business_plan_8000_credits.sql`, and two expected helper catch blocks that needed precise telemetry-guard allowlist entries; one focused guard rerun used repo-root cwd before the wrapper reran it correctly from `frontend/` | For very broad billing/pricing/SQL/media/runtime batches, run early parseable-file Prettier, expect SQL utility filenames to stress migration-doc parsing, keep React Compiler hook purity fixes canonical, and run focused guard tests from `frontend/` rather than `npm -C frontend exec` when a test assumes frontend cwd. |
| 2026-07-07 | Production SOP run with Pulse built-in control-plane/admin/runtime fallback hardening, AI Studio toolbar and Pulse SOP parity, route/internal API docs, and model-pricing policy sequence repair docs/check/test updates | 9.4 | high | The 25-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/SQL instructions were loaded; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs drift/parity checks, and 6 Vitest files clearing 59 tests; repo-wide `type-check`, Generate CTA contract, pricing drift/inventory checks, `git diff --check`, `node scripts/check_secret_exposure.js`, `docs:check`, and production build all passed before commit | The first preflight caught one avoidable formatting miss in `docs/api/api-internal-routes.md`; local proof does not replace hosted SQL apply/lint or deployed admin/Pulse review after Vercel serves the pushed build | For compact Pulse control-plane plus SQL sequence lanes, run early Prettier on docs/CSS/SQL tests, keep exact-manifest preflight, include docs/migration parity plus type-check/build, and report hosted SQL apply as separate from local commit proof. |
| 2026-07-07 | Production SOP run with customer-support mail fallback behavior, dashboard/admin/AI Studio support-dialog tests, and admin Pulse-card action cleanup | 9.1 | medium | The 6-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend instructions were loaded; related sweep returned generic AI Studio adjacency without a concrete extra owner suite; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, and 4 Vitest files clearing 64 tests; repo-wide `type-check`, corrected frontend-cwd admin page isolation clearing 12 tests, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The first preflight caught one formatting miss in `CustomerSupportDialog.tsx`, and I briefly invoked the admin isolation test from repo root, producing a false jsdom failure before rerunning correctly from `frontend`; this visible support/admin behavior still needs deployed production review after Vercel serves the pushed build | For compact support/admin UI behavior lanes, run early Prettier, trust exact-manifest preflight for mapped suites, and run suite-hot page isolation from `frontend/` directly instead of `npm -C frontend exec` to avoid wrong-cwd noise. |
| 2026-07-07 | Production SOP run with customer-support dialog portal placement, dashboard footer support visibility, authenticated-dashboard support access, and AI Studio no-snapshot project autosave unlock coverage | 9.2 | medium | The 7-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend instructions were loaded; related sweep showed broad AI Studio hook adjacency but no concrete owner suite beyond the changed persistence-controller test; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, and 3 Vitest files clearing 81 tests; repo-wide `type-check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The first preflight caught two formatting misses before proof was green; this visible dashboard/support behavior and AI Studio autosave lane still need deployed production review after Vercel serves the pushed build | For compact dashboard/support plus AI Studio persistence lanes, run early Prettier on changed UI/tests, keep exact-manifest preflight plus type-check/build, and avoid broad hook-suite fan-out when the related sweep only reports generic same-directory adjacency. |
| 2026-07-07 | Production SOP run with July 7 storage add-on ladder, billing subscription/storage change guards, profile/admin support surfaces, legal copy parity, chunk-load recovery, SQL migration 209, and billing docs/schema updates | 8.9 | high | The 47-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/SQL/pricing instructions were loaded; related sweep added concrete profile and billing sibling suites; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs/migration parity checks, and 15 Vitest files clearing 120 tests; sibling suites passed 8 files and 45 tests; repo-wide `type-check`, AI Studio pricing drift/inventory checks, `git diff --check`, `node scripts/check_secret_exposure.js`, `docs:check`, and production build all passed before commit | The first preflight caught five formatting misses; an adjacent profile transactions test had stale two-heading expectations and was corrected to the canonical single transaction-history section; I also twice chained root-level checks after `cd frontend`, causing wrong-cwd noise before rerunning from repo root; hosted SQL apply/lint and deployed billing/profile/legal review remain separate from local commit proof | For broad billing/storage/legal SQL batches, run early Prettier before preflight, use a subshell for frontend sibling suites so root checks keep the right cwd, include pricing drift/inventory plus docs/migration parity, and update adjacent tests only when they contradict the canonical route contract. |
| 2026-07-07 | Production SOP run with admin support queue rendering cleanup, AI Studio recovery/error presentation, dashboard community-link consolidation, profile storage copy, route/docs parity, and admin user credit metrics coverage | 9.0 | high | The 41-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend instructions were loaded; related sweep returned broad AI Studio same-directory adjacency without concrete extra suites beyond manifest owners; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 13 Vitest files clearing 159 tests; Generate CTA contract, AI Studio pricing drift, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed; after the test typing fix, exact preflight, repo-wide `type-check`, diff check, and secret scan passed again | The first preflight caught six formatting misses plus a CSS syntax error in `ai-studio-layout.css`; repo-wide type-check then caught one self-referential admin-users test mock type issue, fixed with an explicit chainable mock type; local proof does not replace deployed AI Studio/dashboard/admin/profile review after Vercel serves the pushed build | For broad AI Studio/admin/dashboard surface batches, run early Prettier before preflight, scan moved CSS variable blocks for selector ownership, and treat test-only type fixes after build as affected-rung reruns instead of replaying the full build. |
| 2026-07-07 | Production SOP run with admin generation-breakdown stats SQL/API/UI, AI Studio reference-download actions, browser crash/session health enrichment, docs/schema parity, and migration 210 | 9.1 | high | The 35-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/SQL instructions were loaded; related sweep added one concrete `DetailModal` sibling suite while the rest was generic AI Studio adjacency; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs/migration parity checks, and 7 Vitest files clearing 114 tests; concrete `DetailModal` sibling passed 80 tests; repo-wide `type-check`, `git diff --check`, `node scripts/check_secret_exposure.js`, `docs:check`, and production build passed before commit | The first preflight caught three formatting misses; I again let a `cd frontend` sibling-test command leak into later root checks, causing wrong-cwd noise before rerunning root gates correctly; hosted SQL apply/lint and deployed admin stats/download/crash review remain separate from local commit proof | For admin stats plus SQL migration lanes, keep docs/migration parity in the exact preflight, add only concrete sibling suites, and run frontend sibling tests in a subshell so root checks do not inherit the frontend cwd. |
| 2026-07-07 | Production SOP run with admin first-value funnel stats SQL/API/UI, billing immediate-upgrade payment handling, project workspace persistence freshness, security header/docs parity, and migration 211 | 9.0 | high | The 45-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/SQL/pricing instructions were loaded; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs/migration parity checks, and 11 Vitest files clearing 211 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, AI Studio pricing drift check, and production build all passed before commit | The first preflight caught five formatting misses, and the second caught one React Compiler state-in-effect warning in `useAiStudioProjectWorkspacePersistenceController`, fixed by deferring the cleanup state update without changing product behavior; hosted SQL apply/lint and deployed admin/billing/project review remain separate from local commit proof | For admin stats plus billing/project persistence lanes, run early Prettier before preflight, keep the exact manifest plus type-check/build, and scan changed effects for React Compiler synchronous state warnings before paying the wrapper pass. |
| 2026-07-07 | Production SOP run with per-user generation-admission limit copy cleanup, admin error queue filtering, and image/video SOP plus route/docs parity | 9.6 | medium | The 9-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs instructions were loaded; related sweep returned only generic AI Studio hook adjacency; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 3 Vitest files clearing 43 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | No validation-fix loop occurred; local proof does not replace deployed confirmation that admin error filtering and AI Studio admission messaging are served by the pushed build | For compact admission-message/docs lanes, keep one exact manifest, trust the owning Fal/admin/API tests plus type-check/build, and skip broad hook fan-out when related sweep only reports generic same-directory adjacency. |
| 2026-07-07 | Production SOP run with admin user cycle-spend helper extraction, Stripe subscription-credit grant reconciliation hardening, media-compliance tab-return recovery, AI Studio autosave media detection, admin style/docs/backlog parity | 9.0 | high | The 14-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/billing/security/pricing instructions were loaded; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 5 Vitest files clearing 61 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, AI Studio pricing drift check, and production build all passed before commit | The first preflight caught four formatting misses, and repo-wide type-check caught one self-referential mock type in `admin-user-cycle-spend.test.ts`, fixed with an explicit mock query type before rerunning affected preflight/type-check/build gates; deployed admin/billing/compliance proof remains separate from local commit proof | For billing/admin helper extraction lanes, run early Prettier before preflight, expect fluent Supabase mocks to need explicit types, keep exact owner tests, and rerun only affected gates after test-contract fixes. |
| 2026-07-07 | Production SOP run with monthly-to-annual higher-plan immediate paid upgrade handling, Stripe pending-update-applied webhook sync, billing docs/backlog parity, and subscription/webhook tests | 9.6 | high | The 8-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/billing/pricing instructions were loaded; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 2 Vitest files clearing 51 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, AI Studio pricing drift check, and production build all passed before commit | No validation-fix loop occurred; deployed Stripe/webhook proof remains separate from local commit proof | For compact billing route/webhook lanes, keep exact owner tests plus type-check/build/pricing drift, and avoid broad Stripe-neighbor fan-out when the changed route tests directly cover the new contract. |
| 2026-07-07 | Production SOP run with dashboard `Open AI Studio` launch copy, neutral billing checkout progress copy, and admin billing diagnostics annual-offer interval matching | 9.4 | medium | The 15-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/pricing instructions were loaded; related sweep showed mostly generic admin API neighbors while the changed diagnostics owner test was in-manifest; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 3 Vitest files clearing 35 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, AI Studio pricing drift check, and production build all passed before commit | The first preflight caught two avoidable formatting misses before the rerun passed; visible dashboard/profile/AI Studio copy and admin diagnostics behavior still need deployed production review after Vercel serves the pushed build | For compact visible-copy plus billing diagnostics lanes, run early Prettier on changed UI/server files, keep the exact owner tests, and do not fan out across broad admin API neighbors when the related sweep is directory-generic rather than contract-specific. |
| 2026-07-07 | Production SOP run with admin support queue natural ordering, collapsible Stripe billing details, admin CSS/test coverage, and signup-funnel backlog capture | 9.7 | medium | The 4-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs instructions were loaded; related sweep returned generic admin page adjacency without a concrete extra owner suite; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 1 Vitest file clearing 9 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | No validation-fix loop occurred; deployed admin support-queue review remains separate from local commit proof | For compact admin UI/backlog lanes, keep exact-manifest preflight plus type-check/build, and do not add broad admin-page fan-out when the related sweep is generic and the owning test already covers the changed behavior. |
| 2026-07-08 | Production SOP run with report-issue screenshot upload/admin review, admin error watch metadata, billing full-price upgrade proof, AI Studio/profile navigation and credit messaging, Badu agent onboarding, SQL migrations 212-214, and docs/schema parity | 8.8 | high | The final 138-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/SQL/security/billing/pricing/agent-setup instructions were loaded; related sweep showed broad AI Studio and agent-runtime adjacency while the manifest contained the owner suites; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs/migration parity checks, and 41 Vitest files clearing 535 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, AI Studio pricing drift, Generate CTA contract, and production build all passed before commit | The first preflight caught 10 formatting misses; eslint caught three intentional screenshot-preview `<img>` warnings that required narrow lint comments; one JSX fix briefly introduced invalid map syntax before being corrected with a keyed `Fragment`; migration parity caught missing 214 entries in the ordered docs; type-check caught a stale `ProfileSubscriptionSection` test fixture; `billing:portal-upgrade-config:verify` could not run because `STRIPE_BILLING_PORTAL_FULL_PRICE_UPGRADE_CONFIG_ID` was absent from shell and ignored local env | For very broad admin/report-issue/billing/SQL/agent batches, run early Prettier, expect migration-inventory and test-fixture drift, keep screenshot previews on direct `<img>` with documented lint rationale, and treat Stripe Portal config verification as an env-gated proof that must be rerun where the config id exists. |
| 2026-07-08 | Production SOP run with billing scheduled-change projection, Pulse built-in catalog repairs, media upload/recovery hardening, profile navigation/billing state, Badu training updates, SQL migrations 215-217, and docs/schema parity | 9.1 | high | The final 65-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/SQL/security/billing/pricing/agent instructions were loaded; related sweep returned broad generic AI Studio and agent-runtime adjacency while manifest owner suites were present; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs/migration parity checks, and 20 Vitest files clearing 219 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, AI Studio pricing drift, Generate CTA contract, and production build all passed before commit | The first preflight caught four formatting misses; dashboard announcement coverage needed a test-contract wait for the authenticated dynamic route; repo-wide type-check caught two stale billing summary fixtures and one self-referential Stripe webhook mock type; `billing:portal-upgrade-config:verify` remained env-gated because `STRIPE_BILLING_PORTAL_FULL_PRICE_UPGRADE_CONFIG_ID` was absent from shell and ignored local env; hosted SQL apply/lint and deployed production review remain separate from local commit proof | For broad billing/Profile/AI Studio/SQL/agent lanes, run early Prettier, expect required summary fields to ripple into quota fixtures, keep dynamic-route tests aligned to bootstrap timing instead of changing product behavior, and rerun Stripe Portal config verification only where the config id exists. |
| 2026-07-08 | Production SOP run with Pulse custom-vs-built-in runtime simplification, built-in control-plane contract cleanup, Pulse session/preset tests, admin agent-instruction surface cleanup, billing return-sync helper, Nuclo Vercel health report, and Badu training updates | 9.0 | high | The final 57-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/billing/Pulse instructions were loaded; related sweep returned broad Pulse/agent-runtime sibling candidates while the manifest carried the owner suites; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 17 Vitest files clearing 218 tests; suite-hot `admin.agent-instructions` isolation passed 12 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The first preflight caught 10 formatting misses before the same manifest gate passed; broad related-sweep candidates were mostly directory-generic Pulse/runtime neighbors, so I did not add extra fan-out beyond the exact manifest and suite-hot admin isolation; deployed production proof remains separate from local commit proof, especially while Vercel build initialization is incident-prone | For broad Pulse/admin-agent/doc batches, run early Prettier before preflight, trust exact-manifest owner suites plus one suite-hot isolation, and avoid large generic neighbor fan-out when the sweep does not identify a concrete uncovered contract. |
| 2026-07-08 | Production SOP run with Pulse built-in catalog fail-closed coverage, Pulse preset start/session hydration alignment, Stripe subscription schedule webhook projection, billing account summary tests, and Badu billing run-log notes | 9.4 | medium | The final 13-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/billing/Pulse instructions were loaded; related sweep returned broad generic AI Studio hook neighbors while manifest owner suites covered the changed contracts; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 6 Vitest files clearing 86 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The first preflight caught two formatting misses before the same manifest gate passed; related-sweep fan-out stayed intentionally bounded because the extra candidates were generic hook-neighbor matches rather than concrete uncovered contract owners; deployed production and live Stripe webhook proof remain separate from local commit proof | For compact Pulse catalog plus Stripe webhook lanes, run early Prettier on changed server/docs files, keep exact owner API/runtime tests, and avoid expanding into broad AI Studio hook fan-out when the manifest tests directly exercise the contract. |
| 2026-07-08 | Production SOP run with profile subscription action copy/disablement alignment and profile subscription action tests | 9.5 | low | The final 3-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/profile/billing instructions were loaded; related sweep returned mostly generic profile page-neighbor candidates while the manifest carried the owner component and page-action tests; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, and 2 Vitest files clearing 22 tests; repo-wide `type-check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The first preflight caught one formatting miss in `ProfileSubscriptionSection.tsx`; no behavior-changing test workaround was made; deployed profile review remains separate from local commit proof | For compact profile subscription UI lanes, run exact owner component/page tests plus type-check/build, and avoid broad profile-page fan-out when the changed files and tests directly cover the affected subscription action contract. |
| 2026-07-08 | Production SOP run with Badearsai agent onboarding/artifact records, Badu tool/training additions, planning-doc relocation into retained report records, billing plan-card/candidate catalog hardening, and Kie upload-url admission cleanup | 8.9 | high | The final 41-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/billing/pricing/agent-teaching instructions were loaded; related sweep returned concrete billing/Kie neighbors plus broad API noise while manifest owner tests covered the changed billing and Kie contracts; widened exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 3 Vitest files clearing 35 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, AI Studio pricing drift smoke, Badu helper `node --check`, and production build all passed before commit | A late manifest tail added Badu tool/training/report files after the first validation, requiring a widened manifest and rerun; the widened preflight then caught three formatting misses in Badu files; deleted planning doc was intentionally replaced by the Badearsai retained report path and docs index updates; live Kie/provider proof and deployed production review remain separate from local commit proof | For mixed agent onboarding plus billing/Kie batches, refresh the final manifest immediately before ledger, keep docs indexes in the same manifest, run exact owner billing/Kie tests plus type/build/docs, and rerun exact preflight whenever agent artifact tails appear late. |
| 2026-07-08 | Production SOP run with Badu monthly cadence, provider account map/checklist, and provider-vs-Stripe timing guardrails in Badu docs | 9.5 | low | The final 9-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/docs/Badu instructions were loaded; corrected docs-only `gear-ball:preflight` passed Prettier and docs checks; `docs:check`, `git diff --check`, and `node scripts/check_secret_exposure.js` passed before commit | The first exact preflight used the dirty markdown manifest as both files and tests, so Vitest correctly found no test files and exited non-zero; the widened manifest then caught two formatting misses before final proof; no product build/type-check was run because the manifest was docs-only and no runtime/UI files changed | For compact agent-doc lanes, refresh the manifest before ledger, run exact-manifest preflight without a tests manifest unless real test files are present, then use docs checks/diff/secret scan as the commit bar instead of widening into product validation. |
| 2026-07-08 | Production SOP run with Pulse seeded-repair guardrails and billing bonus-credit plan-card display coverage | 9.5 | medium | The final 10-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/SQL/pricing instructions were loaded; related sweep returned broad generic admin API neighbors while manifest owner tests covered the changed Pulse/billing contracts; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 3 Vitest files clearing 19 tests; repo-wide `type-check`, `docs:check`, AI Studio pricing display drift, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | No validation-fix loop occurred; hosted SQL apply/lint and deployed production review remain separate from local commit proof; the bonus-credit card change is visible billing copy/layout and was validated by the owner component test rather than changed as a test workaround | For compact SQL repair plus billing-display lanes, keep exact owner SQL/API/component tests, include type-check/build/pricing drift, and avoid broad admin API fan-out when related sweep is directory-generic instead of contract-specific. |
| 2026-07-08 | Production SOP run with billing catalog bonus-credit feature parity and plan-card annotation alignment | 9.7 | medium | The final 5-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/pricing instructions were loaded; related sweep returned mostly generic billing/admin neighbors while the manifest owner tests covered catalog and card behavior; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, and 2 Vitest files clearing 11 tests; repo-wide `type-check`, AI Studio pricing display drift, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | No validation-fix loop occurred; this visible billing display change still needs deployed production review after Vercel serves the pushed build | For compact billing catalog/card lanes, trust exact owner tests plus type-check/build/pricing drift, and do not fan out into broad billing/admin neighbors when the sweep is directory-generic rather than contract-specific. |
| 2026-07-08 | Production SOP run with admin cancellation-state visibility, admin users API projection, billing diagnostics mapper, and pricing bonus-credit copy cleanup | 9.4 | medium | The final 13-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/pricing instructions were loaded; related sweep returned broad generic admin/billing/page-neighbor candidates while manifest owner tests covered the changed admin users, support queue, billing catalog/card, and pricing route behavior; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 5 Vitest files clearing 38 tests; repo-wide `type-check`, `docs:check`, AI Studio pricing display drift, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | No validation-fix loop occurred; this visible admin/pricing behavior still needs deployed production review after Vercel serves the pushed build | For mixed admin support queue plus pricing-copy lanes, keep exact owner API/page/component tests plus type-check/build/pricing drift, and avoid broad admin-route fan-out when the sweep is directory-generic rather than contract-specific. |
| 2026-07-09 | Production SOP run with admin growth cohort stats, conversion target UI/API wiring, runtime SQL security coverage, docs parity, and Pulse built-in save/delete hardening | 9.4 | high | The 20-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/SQL/security instructions were loaded; related sweep returned mostly generic admin/API neighbors while manifest owner tests covered admin stats, Pulse built-ins, and page behavior; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs/migration parity checks, and 5 Vitest files clearing 41 tests after one formatting repair; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build all passed before commit | The only validation-fix loop was Prettier formatting in four frontend files; hosted SQL apply/lint and deployed `/admin/stats`/Pulse review remain separate from local commit proof | For admin stats plus SQL/RPC lanes, keep exact API/page/logic tests plus type-check/docs/build, report hosted migration apply as a separate boundary, and do not fan out into generic admin-neighbor tests when the owner suites cover the changed contracts. |
| 2026-07-09 | Production SOP run with AI Studio no-plan landing/access-resolving handoff, custom-GPT Pulse built-in normalization, admin user subscription summaries, support/stats copy alignment, and Badearsai evidence updates | 9.1 | high | The 31-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/Pulse/AI Studio instructions were loaded; related sweep returned broad generic AI Studio hook/component adjacency while manifest owner tests covered Standard Create, AI Studio runtime pass-through, Pulse built-ins, admin users, admin stats, and admin agent-instructions behavior; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 9 Vitest files clearing 130 tests; suite-hot `admin.agent-instructions` isolation passed 14 tests from the correct frontend test environment; repo-wide `type-check`, `docs:check`, Generate CTA contract, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught two formatting misses; type-check caught one test-only Promise resolver typing issue, fixed without product behavior changes; one initial suite-hot isolation command used the wrong cwd/jsdom environment and was replaced by the correct frontend-cwd run | For mixed AI Studio/Pulse/admin/docs lanes, run exact-manifest preflight plus Generate CTA/type/build, rerun suite-hot page tests from `frontend/`, and treat test-harness invocation failures separately from product regressions before editing. |
| 2026-07-09 | Production SOP run with project Pulse Chats hydration guard, Profile paid-plan action affordances, Admin Agent Instructions section subtabs, shared message action styling, and Badearsai run-log update | 9.4 | high | The 24-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/Pulse/Profile instructions were loaded; related sweep returned broad generic AI Studio component adjacency while manifest owner tests covered Pulse chat runtime, AI Studio page/header state, Profile storage/billing actions, and admin agent-instructions behavior; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 8 Vitest files clearing 104 tests; suite-hot `admin.agent-instructions` isolation passed 14 tests; repo-wide `type-check`, `docs:check`, Generate CTA contract, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught four formatting misses; local proof does not replace deployed review of Profile callouts, Admin subtabs, and project Pulse Chats in production | For mixed AI Studio/Profile/admin UI lanes, keep exact owner tests plus type-check/build, add Generate CTA guard when Create runtime props are touched, and avoid broad AI Studio component fan-out when the sweep is directory-generic. |
| 2026-07-09 | Production SOP run with admin subscription summary source cleanup and Badearsai recovery evidence update | 9.7 | medium | The 10-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/admin instructions were loaded; related sweep returned generic admin/API neighbors only; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 3 Vitest files clearing 17 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | No validation-fix loop occurred; deployed admin stats/support review and Badearsai production watch remain separate from local proof | For compact admin summary cleanup lanes, keep exact owner API/page tests plus type-check/build, and skip broad admin-neighbor fan-out when the related sweep is directory-generic rather than a concrete uncovered contract. |
| 2026-07-09 | Production SOP run with media storage lifecycle cleanup guardrails, admin storage economics/funnel visibility, admin user credit-expiry detail, AI Studio generated-autosave gating, Profile storage telemetry, Admin Agent Instructions tab slimming, and Badearsai watch-index updates | 9.2 | high | The 29-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/admin/storage/security instructions were loaded; related sweep returned broad generic AI Studio hook-neighbor candidates while manifest owner tests covered admin storage, admin users, profile storage, AI Studio autosave, admin header, and admin agent-instructions behavior; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 8 Vitest files clearing 62 tests; suite-hot `admin.agent-instructions` isolation passed 15 tests; new `media_storage_lifecycle_cleanup.mjs` passed `node --check`; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught four formatting misses before the same manifest gate passed; local proof does not replace deployed review of admin storage/profile/admin-instructions surfaces or any hosted dry-run execution of the cleanup wrapper | For broad storage/admin/Profile/AI Studio lanes, run early Prettier on large TSX/docs files, keep exact owner tests plus one suite-hot isolation, syntax-check new operator scripts, and avoid broad AI Studio fan-out when related sweep is generic same-directory adjacency. |
| 2026-07-09 | Production SOP run with admin storage intelligence expansion, account storage ownership proof SQL/RPCs and operator wrapper, Badearsai crash-log triage tooling, storage docs/index parity, and Admin Agent Instructions/detail styling refinements | 9.1 | high | The 46-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/SQL/security/storage instructions were loaded; related sweep added concrete `admin-storage-usage-snapshots` sibling coverage while the rest was broad generic admin API adjacency; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs/migration parity checks, and 7 Vitest files clearing 40 tests; suite-hot `admin.agent-instructions` plus sibling `admin-storage-usage-snapshots` passed 20 tests; new account-storage and crash-log operator scripts passed `node --check`; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught six formatting misses before the same manifest gate passed; hosted SQL apply/lint, live account-storage proof execution, deployed `/admin/storage` review, and Badearsai live crash-log triage remain separate from local commit proof | For broad SQL/admin-storage/agent-tool lanes, run early Prettier on TSX, tests, and new scripts; include migration/doc parity plus exact owner tests; add only concrete sibling tests from the sweep; and keep hosted SQL/apply proof separate from local build proof. |
| 2026-07-09 | Production SOP run with admin customer analytics API/accordion, selected-account analytics tests/docs, Nuclo account-storage handoff docs, and Pulse built-in system-instruction publication | 9.3 | high | The 22-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs instructions were loaded; related sweep was broad AI Studio same-directory noise with no concrete uncovered sibling target; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 6 Vitest files clearing 43 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught six formatting misses before the same manifest gate passed; deployed admin customer analytics and Pulse built-in modal/catalog behavior still need production review after Vercel serves the pushed build | For mixed admin API/UI plus Pulse catalog lanes, trust exact owner tests plus type-check/build, treat broad AI Studio related-sweep output as noise unless it names a concrete uncovered contract, and run early Prettier on large TSX/tests/docs before preflight. |
| 2026-07-09 | Production SOP run with customer analytics relocation to Admin Stats, Admin Support analytics handoff link, selected-customer analytics extraction, and Prompt Preset inspect-only modal simplification | 9.4 | high | The 18-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/admin/AI Studio instructions were loaded; related sweep was broad AI Studio same-directory noise with no concrete uncovered sibling target; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 3 Vitest files clearing 29 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught five formatting misses before the same manifest gate passed; deployed review of `/admin/stats?customerId=...`, Admin Support analytics handoff, and Prompt Preset built-in detail modal remains separate from local proof | For relocation/refactor lanes, keep old-surface and new-surface owner tests together, trust exact manifest plus type/build when the route contract is covered, and continue running early Prettier on large refactor files before first preflight. |
| 2026-07-09 | Production SOP run with parked Admin Storage route/API/docs alignment, Admin Support status layout tightening, video poster/preview backfill apply guardrails, and Badearsai storage watch evidence | 9.2 | high | The 28-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/SQL/security/storage instructions were loaded; related sweep was generic admin/API adjacency, while exact-manifest preflight added concrete preview-delivery contract fan-out for `media-sign-batch` and `media-resolve-previews`; refreshed `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 10 Vitest files clearing 84 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, `node --check` for both video backfill scripts, and production build passed before commit | The first preflight caught six formatting misses before the same manifest gate passed; `/admin/storage` remains parked at the route boundary and any future reactivation, hosted SQL proof, or live backfill apply execution remains separate from local commit proof | For parked-route/storage and operator-script lanes, accept concrete wrapper-added contract fan-out, keep route/API/page/tests/docs together, syntax-check touched scripts, and run early Prettier on large TSX/CSS/test files before first preflight. |
| 2026-07-09 | Production SOP run with Admin Support queue column ordering, selected-account snapshot/status compaction, and matching admin user-credit test coverage | 9.6 | medium | The 3-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/front-end operating instructions were loaded; related sweep returned generic admin/page adjacency with no concrete uncovered sibling target; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, and the admin users/credits Vitest file clearing 9 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught one test formatting miss before the same manifest gate passed; deployed admin support visual review remains separate from local proof | For compact admin UI/test lanes, keep exact owner test plus type-check/build, run early Prettier when TSX/CSS/test files are already dirty, and skip broad admin-neighbor fan-out when related sweep is generic. |
| 2026-07-09 | Follow-up SOP correction for Admin Support collapsed Credits & access disclosure and tests | 8.8 | medium | Post-commit validation caught that the first commit omitted an unstaged component/test pair for the collapsed Credits & access disclosure; follow-up exact-manifest `gear-ball:preflight` passed Prettier, ESLint, and the admin users/credits Vitest file clearing 9 tests before the follow-up commit | The first commit should not have proceeded with an unstaged hook-adjacent product diff still present; this was caught before push, so no bad remote state was created | After any commit whose hook runs fixers, immediately re-check `git status` before calling the commit final; if anything remains dirty, validate and commit the follow-up before push. |
| 2026-07-10 | Production SOP run with Badearsai queue-readback rules, Admin Support sorting/ledger-cycle summaries, admin users/credits API expansion, and AI Studio audio/voice-reference local blob hardening | 9.1 | high | The 22-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs instructions were loaded; related sweep returned mostly broad AI Studio hook adjacency while the manifest owner tests covered admin users/credits, AI Studio media reference runtime, session canvas snapshots, audio upload, and voice changer source assets; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 5 Vitest files clearing 87 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught six formatting misses and the second caught shared CSS formatting before the same manifest gate passed; deployed review of Admin Support sorting/ledger presentation and Badearsai production queue-readback behavior remains separate from local proof | For broad admin/API/AI Studio media/docs lanes, run early manifest Prettier before first preflight, trust exact owner tests plus type/build, and avoid broad AI Studio hook fan-out unless the sweep names a concrete uncovered contract. |
| 2026-07-10 | Production SOP run with AI Studio Create Safe Completion contract, Standard/Pulse safety convergence, ten-image composer attachments, agent request-size guardrails, docs/ADR/planning updates, and admin support style alignment | 9.2 | high | The 87-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs instructions were loaded; related sweep named broad agent-runtime and AI Studio attachment neighbors while the manifest already included owner tests for safe completion, safety finalization, transport, attachment strip/capacity, composer orchestration, route outcomes, OpenAI compatibility, agent prompts, runtime API paths, and admin users; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 24 Vitest files clearing 325 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught 25 formatting misses before the same manifest gate passed; deployed review of Create agent safe-completion behavior and ten-image attachment UX remains separate from local proof | For large agent-runtime/docs lanes, run manifest Prettier before first preflight, keep exact owner tests plus type/build, and do not add every broad related-sweep neighbor when the manifest already carries the concrete contract tests. |
| 2026-07-10 | Production SOP run with Safe Completion refinement, Pulse/Badearsai operating docs, safety-sim matrix tests, composer task/attachment coverage, admin support polish, and agent runtime fixture expansion | 9.3 | high | The 53-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs instructions were loaded; related sweep returned broad agent-runtime and AI Studio hook adjacency while the manifest owner tests covered safe completion, route outcomes, fast path turns, vision summaries, agent transport, composer attachments/orchestration/tasks, safety-sim matrix, runtime API paths, standard evals, and admin users; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 17 Vitest/script test files clearing 338 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught nine formatting misses before the same manifest gate passed; deployed review of Create agent safe-completion and attachment UX remains separate from local proof | For follow-on agent-runtime lanes, keep exact owner tests plus the dedicated script test, run manifest Prettier before preflight, and ignore broad related-sweep neighbors unless they identify a concrete missing contract. |
| 2026-07-10 | Production SOP run with broad admin crash/kanban observability, AI Studio media/runtime and voice changer recovery, billing/pricing policy handshakes, SQL migrations, security/docs reports, and Maya tester evidence | 9.1 | high | The 245-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs instructions were loaded; status grouping classified the live tree across frontend features/lib/pages/tests, docs/agents/planning/SOPs/ADR/API, scripts, and SQL migrations; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 69 Vitest files clearing 991 tests with wrapper-added media preview contract fan-out; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught 58 formatting misses and required one manifest-refresh rerun; SQL files were intentionally left outside Prettier because no parser is configured; hosted SQL apply, deployed production review, and any Vercel deployment proof remain separate from local/pushed proof | For very broad mixed product/docs/SQL/agent lanes, run manifest Prettier before the first preflight, accept concrete wrapper fan-out, keep one coherent commit only when the live tree is already cross-linked, and do not claim deployed production until Vercel proof exists. |
| 2026-07-10 | Production SOP run with Maya tester ingest-status corrections and AI Studio Create blocked-media Chat Mode guidance | 9.4 | medium | The 9-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend instructions were loaded; status grouping classified Maya tester report/training docs plus the Standard Create panel, owner test, and composer CSS; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and the StandardCreatePanelView Vitest file clearing 12 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught three Maya markdown formatting misses before the same manifest gate passed; the AI Studio change is a visible guidance affordance and still needs deployed production review after Vercel serves the pushed build | For compact mixed docs plus visible component/CSS lanes, run exact owner tests plus type/build, keep docs checks in the same gate, and run manifest Prettier before first preflight when markdown rows were hand-edited. |
| 2026-07-11 | Production SOP run with launch-week operations governance, all-batch changelog policy, agent/tester report updates, Hybervees insight packets, OpenAI capacity admission, generation output media-link route, provider guardrails, AI Studio/runtime reliability, dashboard coverage, and SQL/security docs | 9.2 | high | The 163-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/SQL instructions were loaded; status grouping classified agent docs, tester reports, Hybervees records, launch-week docs, API/runtime/test changes, SQL migration/check files, and the required `docs/change_log.md` update; refreshed exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 17 Vitest files clearing 214 tests; repo-wide `type-check`, `docs:check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | The first preflight caught 29 formatting misses before the same manifest gate passed; one manual Prettier pass hit quoted space-path noise for Babineaux files, so the wrapper remained the authoritative proof; hosted SQL apply and deployed production review remain separate from local/pushed proof | For broad launch-week batches, update the changelog before validation, trust exact wrapper preflight for space-path manifests, keep SQL/docs/runtime tests together, and do not claim deployed production until Vercel proof exists. |
| 2026-07-11 | Production SOP run with prompt-only Seedance 2 no-video-input billing evidence normalization and reservation coverage | 9.6 | medium | The 4-path tree was captured from live status on `production`; branch guard and `shortpulse.allowedBranch=production` were confirmed; Gear Ball/frontend/docs/pricing-audit instructions were loaded; the manifest classified one canonical runtime helper, two billing/model tests, and the required `docs/change_log.md` entry; exact-manifest `gear-ball:preflight` passed Prettier, ESLint, docs checks, and 2 Vitest files clearing 36 tests; targeted billing/pricing Vitest passed 5 files clearing 53 tests; `node scripts/check_ai_studio_pricing_display_drift.js`, repo-wide `type-check`, `git diff --check`, `node scripts/check_secret_exposure.js`, and production build passed before commit | No user-facing UI/UX or behavior workaround was made; related sweep returned broad billing/model neighbors but no concrete missing same-lane file beyond the targeted owner tests; deployed production proof remains separate from local/pushed proof | For compact billing-pricing lanes, use pricing-audit guidance, update the changelog even for internal billing correctness, run exact manifest preflight plus targeted owner tests and pricing drift, then avoid broad reruns unless a concrete adjacent contract is implicated. |
