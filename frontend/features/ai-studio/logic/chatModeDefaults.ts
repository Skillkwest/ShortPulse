/**
 * AI Studio chat-mode defaults.
 * Centralizes the Standard default-off and Pulse forced-on contracts so
 * runtime, snapshot, and restore paths do not drift independently.
 */

export const STANDARD_CREATE_DEFAULT_CHAT_MODE_ENABLED = false;
export const PULSE_CREATE_FORCED_CHAT_MODE_ENABLED = true;
