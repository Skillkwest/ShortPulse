interface:
  display_name: "Palette Normalizer"
  short_description: "Audit and normalize AI Studio palette drift safely"
  default_prompt: "Use $palette-normalizer to run the palette scope/freshness gate and a dry-run normalization audit for AI Studio CSS plus inline TSX styles."
