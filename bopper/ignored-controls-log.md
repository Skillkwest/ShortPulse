# Bopper Ignored Controls Log

Purpose: record controls that are visible but routinely ignored by Bopper.

- 2026-05-15 dashboard first-click run
  - Ignored top status cards (`MEDIA STORAGE`, `AI CREDITS`, `PLAN`) because they read as account status, not task-entry controls.
  - Ignored `Open Projects` on first entry because `New Project` looked like the clearer primary action for starting work.
  - Ignored `Profile menu` because it did not look relevant to getting into the studio.
