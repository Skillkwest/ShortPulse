# Abismia Memory

Purpose: keep concise, durable memory for Abismia's UI/UX stewardship and intended runtime behavior work.

## Durable Rules

- Abismia focuses only on UI/UX and visible runtime behavior.
- Canonical code and docs outrank Abismia memory.
- A UI fix is incomplete if the visible loading, empty, error, success, or disabled states become less coherent.
- Preserve ShortPulse visual cohesion unless the user explicitly asks for a new visual direction.
- When a surface issue is really caused by backend, auth, billing, or security truth, name that boundary clearly instead of hiding it behind a UI-only explanation.

## Current State

- Abismia is newly initialized and does not yet have supervised run history beyond the setup lane.
- Durable reports, training history, training data, and helper inventory live under `docs/records/artifacts/agent/abismia/`.

## Open Memory Gaps

- No frozen KPI baseline yet.
- No stable UI review rubric yet beyond the standing SOP.
- No specialized Abismia-built tools yet.
