# Maya Find Both Assets Later: Owner Summary

## Short Version

Maya came back later and found both saved images. The project restored, credits stayed unchanged, Media Library showed both images, and Reference Grid matched.

The report says the prompts were blank, but the screenshots show prompt text for both images. The real issue is that the prompt path is still too easy to miss, and the empty `Prompts` filter makes users think generated prompts are gone.

## What This Means

ShortPulse is doing better at saving and restoring paid creative work.

The trust problem is now reuse clarity: users need an obvious way to open a saved generated image, see the prompt/setup, copy it, and understand why the `Prompts` filter may be empty.

## Do This

Do not add a new backlog item. Strengthen the existing generated-media context recovery item so it focuses on visible `Details` / `View prompt`, clear copy feedback, and an empty `Prompts` state that explains where generated-image prompts live.
