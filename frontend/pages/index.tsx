/**
 * Home route entrypoint that forwards to the marketing landing page.
 */
import LandingPage from "./landing";

/**
 * Export landing page as the root route component.
 */
export default LandingPage;
