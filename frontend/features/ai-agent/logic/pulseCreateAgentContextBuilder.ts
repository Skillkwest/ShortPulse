/**
 * Pulse Create context builder.
 * Owns Pulse workflow context serialization for the Pulse agent transport.
 */
export { buildAgentContext as buildPulseCreateAgentContext } from "./contextBuilder";
