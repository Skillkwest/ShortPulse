# Maya Chen Scenario Backlog

Use this backlog when the user says `run test` without a specific scenario.

## Current Priority

1. Continue `Tiny Apartment Reset Kit` by finding the first paid generated image again.
2. Prompt/detail recovery run for the generated image metadata.
3. Create or verify the project home for `Tiny Apartment Reset Kit`.
4. Reference Grid understanding run.
5. Quick Slot Inventory understanding run.
6. Second image variant run with strict 4-8 credit limit.

## Durable UGC Goal

Maya is working toward `Tiny Apartment Reset Kit`, a UGC-style content package for five-minute reset routines for busy renters. Use `../ugc-content-goal.md` as the project arc. Each scenario should move Maya one small step toward a usable saved content package, not just random product coverage.

## Scenario Cards

### Find Yesterday's Generated Image

Customer goal: Maya wants to return later and find the image she paid credits to create.

Natural path:

- Open Chrome and sign in if needed.
- Start from dashboard or AI Studio.
- Look for the project `5-minute renter reset tests`.
- Try to find the generated image through Projects, Media, Reference Grid, and any generation history.
- Check whether the prompt is recoverable.
- Do not spend credits.

Success:

- Maya can find the project, image, and enough context to reuse the result.

Likely human errors:

- Check Media before Projects because "media" sounds like where images live.
- Assume the output is attached to the project if it was generated while the project was open.
- Use browser back or dashboard when the app's saved-work path is not obvious.

### Set Up Tiny Apartment Reset Kit Project

Customer goal: Maya wants one obvious home for the UGC package so she can come back next week.

Natural path:

- Open Chrome and sign in if needed.
- Start from dashboard.
- Look for existing project names before creating a duplicate.
- If no clear project exists, create `Tiny Apartment Reset Kit`.
- Open AI Studio from the project if that path exists.
- Do not spend credits.

Success:

- Maya can tell which project contains this content package and where she would resume.

Likely human errors:

- Create a slightly different project name if she cannot find the first one.
- Confuse dashboard-level projects with AI Studio session state.
- Look for saved media inside the project before understanding whether media is project-scoped.

### Understand Reference Grid

Customer goal: Maya wants to know whether Reference Grid is a saved library, a temporary tray, or a prompt reference area.

Natural path:

- Open existing project.
- Inspect Reference Grid.
- Try selecting existing generated media.
- Try adding/removing only if non-destructive and clear.
- Do not spend credits.

Success:

- Maya can explain what Reference Grid does in one sentence.

### Understand Quick Slot Inventory

Customer goal: Maya wants to know whether Quick Slot Inventory helps her generate or organize references.

Natural path:

- Open AI Studio.
- Inspect Quick Slot Inventory.
- Try drag/drop only if a visible affordance makes it feel safe.
- Check relationship to Reference Grid.
- Do not spend credits.

Success:

- Maya can tell whether this is useful for her next generation.

### Create A Second Variant

Customer goal: Maya wants one alternate image for `Tiny Apartment Reset Kit` without losing the first one.

Natural path:

- Open existing project.
- Find prior image and prompt if possible.
- Create one revised prompt.
- Confirm visible cost before generation.
- Generate once if cost is clear and monthly budget allows.
- Verify both old and new outputs remain findable.

Budget:

- Soft cap: 8 credits.
- Stop if the first generation fails after spending credits.

Likely human errors:

- Reuse a prompt that is too broad and then adjust after seeing the output.
- Forget to specify no text in the first draft.
- Try to find the first output in the wrong panel before generating the variant.

### Credits And Renewal Confidence

Customer goal: Maya wants to understand her remaining credits and whether she will be charged again.

Natural path:

- Visit account/profile/credits surfaces.
- Inspect plan, current balance, renewal language, and credit history if visible.
- Do not change billing.
- Do not cancel or upgrade.

Success:

- Maya can explain current balance, monthly budget implications, and whether anything feels risky.
