# System Catalog Agent Retained Namespace

Purpose: retain historical system-catalog artifacts that predate the current Copperknot stewardship model.

## Status

This is a legacy-retained namespace.

The active system catalog stewardship lane now lives under:

- `docs/agents/copperknot/`
- `docs/records/artifacts/agent/copperknot/`

## Usage Rule

Use this folder only when a task needs older system-catalog history or retained legacy artifacts for comparison.

Do not add new active catalog stewardship work here unless the task is explicitly about legacy history.
