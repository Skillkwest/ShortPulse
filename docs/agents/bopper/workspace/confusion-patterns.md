# Bopper Confusion Patterns

Purpose: record recurring wording, layout, or flow patterns that confuse Bopper.

- 2026-05-15: promise -> contradiction
  - `New Project` plus `Open the AI Studio` strongly implies a straight-through success path.
  - The next screen said `Project unavailable` / `Project not found.` even though the just-created project still appeared in `Open projects`.
- 2026-05-15: restore-progress UI on first entry
  - The full-page step list reads like a backend recovery screen, not a first-use creative workspace.
  - An average user is unlikely to understand which step failed or what action is safe.
- 2026-05-15: workspace wording -> pricing detour
  - On the public dashboard, `New Project` plus `Open the AI Studio` reads like direct workspace entry.
  - Actual behavior routes to pricing before auth or creation.
