# Lever Training History

Purpose: record supervised Lever runs, lessons, SOP/template updates, tool changes, remaining friction, and next training focus.

## 2026-05-11: Agent Setup

Task: establish Lever as the ShortPulse model maintenance manager.

Prompt summary:

```text
From now on your name is "Lever". My model maintenance manager...
```

Actions taken:

- Loaded the repo startup contract and core docs.
- Loaded the agent-teaching setup and maintenance references.
- Audited the existing model-platform SOPs, tooling, route inventories, and doc indexes.
- Created Lever's contract, repo-visible memory, retained artifact area, and a thin orchestration SOP.
- Wired Lever into the docs indexes and agent artifact indexes.

Training result:

- Lever is initialized at `Level 1: Supervised`.
- Lever has a durable contract, memory, KPI baseline, and retained artifact area.

Next training focus:

- Execute the next real model addition through the new Lever workflow.
- Decide whether a dated report template is needed after one more substantive run.

## 2026-05-11: First Real Retirement Lesson Captured

Task: absorb the first real retired-model workflow into Lever's durable operating memory.

Actions taken:

- Recorded `kie-ai/seedance-1.5-pro` as the first real retired picker-visible model in Lever memory.
- Captured the key lesson that visible app surfaces can retain stale model residue even after catalog lifecycle demotion.
- Captured the need for retirement-aware `model:doctor` compatibility allowances during the compatibility window.

Training result:

- Lever now starts with one real retirement lesson instead of a purely theoretical contract.

## 2026-05-11: Trigger-Language And Recordkeeping Hardening

Task: formalize simple trigger language and mandatory retained run records for Lever.

Actions taken:

- Updated Lever's contract so short commands like `add <model>` and `remove <model>` imply the full model workflow.
- Added a recordkeeping rule that every substantive Lever run must be preserved in the retained artifact area.
- Added a run log and a reusable report template so future model-maintenance work becomes explicit training data.

Training result:

- Lever now has a cleaner long-term job title and duty boundary.
- Future model-maintenance runs are expected to produce reusable retained training artifacts by default.

## 2026-05-13: Seedance 1.5 Residue Audit

Task: audit whether the first retired picker-visible model was still presented as active anywhere in the app or top-level docs.

Actions taken:

- Audited repo references for `Seedance 1.5` across app code, docs, and model-maintenance surfaces.
- Renamed the last compatibility-only app-facing settings title so it no longer advertised `Seedance 1.5` as an active workflow label.
- Removed remaining active wording from top-level README/routes/SOP/API docs while preserving compatibility-only runtime references.
- Wrote a retained Lever run report for the audit.

Training result:

- Lever now has a stronger retirement lesson: catalog demotion alone is not enough; active wording and compatibility UI labels need explicit residue review.

## 2026-05-13: Seedance 1.5 Hard Removal

Task: fully remove the first retired picker-visible model once compatibility support was no longer needed.

Actions taken:

- Deleted the active `kie-ai/seedance-1.5-pro` catalog/runtime/pricing/provider surfaces and removed the Kie Seedance 1.5 submit/status route layer.
- Removed remaining active tests and parity checks, replacing the last live 1.5 route-contract assertion with an active Seedance 2 parity test.
- Re-ran a residue scan and confirmed that the remaining 1.5 references are historical only: change log, planning snapshot, and Lever training artifacts.
- Wrote a retained Lever hard-removal report and updated Lever memory to distinguish retirement from full removal.

Training result:

- Lever now has a concrete full-removal lesson in addition to the earlier retirement lesson.
- Future `remove <model>` runs should treat historical records as intentional unless the user explicitly requests total historical erasure.

## 2026-05-13: Removal Workflow Hardening

Task: update Lever's formal operating docs so future `remove <model>` runs explicitly include app-surface audit and full active-residue removal.

Actions taken:

- Updated Lever's contract to treat full model removal as distinct from retirement and to require post-removal residue auditing.
- Updated the Lever orchestration SOP so `remove <model>` includes active repo residue scanning in addition to visible app audits.
- Updated the retirement SOP to document both the retirement branch and the hard-removal branch.
- Promoted the durable lesson into Lever memory and SOP notes/tools.

Training result:

- Lever's add/retire/remove behavior is now better canonicalized in docs rather than relying on chat memory from the first hard-removal run.
