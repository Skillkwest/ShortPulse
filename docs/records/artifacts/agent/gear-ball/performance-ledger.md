# Gear Ball Performance Ledger

Purpose: keep a concise scored ledger of Gear Ball SOP runs and the minimal per-run training loop.

## Entries

| Date       | Run                                                                       | Score | Confidence | What went right                               | What went wrong                            | Smallest next improvement                                                                                                                                 |
| ---------- | ------------------------------------------------------------------------- | ----: | ---------- | --------------------------------------------- | ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-05-27 | Production SOP run with video-panel motion intake and reference-card styling cleanup |   9.2 | high | The live tree stayed collapsed to one compact AI Studio lane, the exact preflight cleared 2 focused test files with 33 passing tests, and the final production build passed on the exact converged tree | No product or validation regressions surfaced once the lane entered the ladder | Keep starting compact visual/runtime lanes with one upfront touched-file Prettier sweep so the first pass stays this clean. |
| 2026-05-26 | Production SOP run with detail-preview authority and workspace-restore hardening |   8.7 | high | The live tree stayed collapsed to one coherent AI Studio lane, the exact preflight cleared 10 focused files with 113 passing tests, and the final production build passed on the exact converged tree | The run still paid repeated formatting cleanup cycles, and `DetailModal.tsx` needed an explicit `react-hooks/set-state-in-effect` contract alignment before the lane could clear | On modal-heavy authority lanes, do one upfront Prettier sweep and one quick effect-state lint scan before the first preflight so the first pass reaches the real behavioral signal faster. |
| 2026-05-26 | Production SOP run with motion-recorder intake, style-drop hardening, and character reference drag cleanup |   8.2 | high | The live tree stayed collapsed to one coherent AI Studio lane, the focused preflight cleared 28 files with 130 passing tests, and the final production build passed on the exact converged tree | The run still paid several formatting sweeps, one motion-recorder cleanup lint warning, two style-drop regressions, and one project-scoped generated-media authority bug before the lane fully cleared | On broad drag/drop and media-authority lanes, do one upfront touched-file Prettier sweep and one hook-to-boundary contract scan before the first preflight so the real signal appears sooner. |
| 2026-05-26 | Production SOP run with reference-grid delete cleanup and AI Studio media-panel hardening |   8.3 | high | The live tree stayed collapsed to one coherent AI Studio lane, the exact current-manifest preflight cleared 23 files and 44 focused tests, and the final production build passed on the exact pushed tree | The first pass still paid a formatting-only cleanup, the new reference-grid delete path shipped with a missing `normalizeId` helper, and the final build exposed a page-shell callback contract mismatch before the lane converged | On shared media-delete/runtime lanes, do one quick callback-seam scan after hook signature changes so page-shell adapters and delete-flow helpers stay aligned before the final build. |
| 2026-05-26 | Production SOP run with launch-trust doctrine docs and project workspace/media rail hardening |   8.8 | high | The live tree stayed collapsed to one governance lane plus one broad project-workspace/runtime lane, docs checks plus shared-runtime preflight plus Generate CTA contract plus final build all passed, and the final tree converged cleanly before score writeback | The first product pass still paid a formatting sweep, and one dead import warning surfaced in `VideoPropertiesPanel.tsx` before the lane could clear | On medium shared-runtime lanes, run a tiny dead-import scan alongside the first formatting sweep so the first preflight speaks only to real behavioral regressions. |
| 2026-05-26 | Production SOP run with Abismia/Babineaux onboarding and AI Studio shell modularization |   8.4 | high | The live tree stayed collapsed to one governance lane plus one broad AI Studio/runtime lane, docs checks plus shared-runtime preflight plus Generate CTA contract plus final build all passed, and the final tree converged cleanly before score writeback | The first product pass paid a large formatting sweep, four lint warnings surfaced after that, and the DetailModal preview-lock contract regressed once before the lane fully cleared | On very large shared-runtime lanes, run a manifest-wide formatting sweep plus a tiny extracted-runtime/effect lint scan before the main preflight so the first pass catches only real behavioral regressions. |
| 2026-05-25 | Production SOP run with internal media-ref submit authority and Holomony reference-grid baseline |   8.5 | high | The live tree stayed split into one Holomony baseline-doc lane and one broad AI Studio submission/workspace lane, the focused suite cleared 24 files with 539 passing tests, and the final build plus docs checks passed before publish | The first product-lane ESLint pass used the repo root instead of `frontend/`, the build surfaced one real session-snapshot union type hole, and `ExpertEditPanelView.test.tsx` still emitted its recurring `act(...)` warning noise | On large frontend-only runtime lanes, start from the `frontend/` workdir and do one quick union-access scan around new snapshot helpers before the full build so the first pass catches only real behavioral risk. |
| 2026-05-25 | Production SOP run with character-mode bundle recovery and Holomony reference-grid onboarding |   8.8 | high | The live tree split cleanly into one Holomony docs lane and one compact character-mode runtime lane, focused tests cleared 56 checks, and the final build plus docs checks passed before publish | The first product-lane ESLint pass used the repo root instead of `frontend/`, which cost one avoidable rerun before the real validation signal | On frontend-only runtime lanes, derive ESLint commands directly from the `frontend/` workdir up front so the first validation pass stays clean. |
| 2026-05-25 | Production SOP run with Holomony media-library baseline and AI Studio workspace/session hardening |   8.1 | high | The live tree still collapsed cleanly into one Holomony baseline-doc lane plus one broad AI Studio runtime lane, the focused suite cleared 28 files with 499 passing tests, and the final docs checks plus build passed on the exact pushed tree | The run paid too many recovery cycles: one stale media-library mock, two Expert Edit history regressions, one keyboard-contract drift, and two build-caught type holes in preview/loading-shell components before the tree converged | When a lane introduces a new shared history or keyboard-routing seam, pull its owning hook tests into the first manifest and do the final build before drafting any score-loop closeout so UI-local type holes surface earlier. |
| 2026-05-25 | Production SOP run with reference-grid ordering and provider-error cleanup |   8.9 | high | The live tree stayed collapsed to one coherent AI Studio/media-ingestion lane, the focused suite cleared 9 files with 89 tests, and the final build plus docs checks passed on the exact pushed tree | The first validation pass still surfaced one avoidable lint warning from an unused import in `ReferenceGridCard.tsx` before the lane could finish | On compact single-lane runs, do one final dead-import scan immediately after the initial formatting sweep so the first validation pass stays fully clean. |
| 2026-05-25 | Production SOP run with reference-grid ownership hardening and Create model-context alignment |   8.8 | high | The live tree stayed collapsed to one coherent AI Studio/reference-grid lane, the focused suites plus docs checks passed, and the final build cleared on the exact pushed tree | The first cheap cleanup pass used the wrong working-directory path shape, which cost one avoidable recovery cycle before the real validation ladder started | On repo-root mixed lanes, derive the initial formatting sweep from the repo root and include untracked files in that first manifest so the first ladder pass stays clean. |
| 2026-05-25 | Production SOP run with character-mode fail-closed guards and admin/pulse governance polish |   8.1 | high | The live tree still collapsed to one coherent AI Studio/admin governance lane, the focused suites plus docs checks passed, and the final build cleared before publish | The first pass paid a broad formatting-only cleanup across 18 files, the Reference Grid suite still emitted recurring `act(...)` warning noise, and the ADR index tail surfaced after the first score-loop writeback so convergence needed one more correction step | On very large mixed runtime/docs lanes, run a manifest-wide Prettier sweep before the main ladder and re-check doc index tails before writing the score loop so the run closes in one shot. |
| 2026-05-25 | Production SOP run with global right-rail authority, Pulse catalog hardening, and signed-in issue reporting |   8.5 | high | The live tree stayed collapsed to one coherent AI Studio runtime/governance lane, the focused suites plus docs checks passed, and the final build cleared on the exact pushed tree | The first pass still paid a broad formatting-only cleanup across 18 files before the lane could finish | On large mixed runtime/docs lanes, do one manifest-wide Prettier sweep before the main ladder so the first validation pass speaks only to real failures. |
| 2026-05-25 | Production SOP run with Pulse runtime parking and preset docs refresh |   8.7 | high | The live tree stayed collapsed to one coherent AI Studio runtime lane, focused tests plus docs checks passed, and the final build cleared on the exact pushed tree | The first pass still paid two cheap cleanup cycles: five files needed Prettier and one workspace test had an unused `container` binding | On larger mixed docs-plus-runtime lanes, do one up-front Prettier sweep on touched runtime files and scan the changed tests for dead locals before starting the main ladder. |
| 2026-05-25 | Production SOP run with fixed character split host and workspace chrome |   8.8 | high | The live tree stayed collapsed to one compact character-panel lane, the focused UI tests passed cleanly, and the final build cleared on the exact pushed tree | The first pass still paid one tiny formatting cycle on `CharacterPanelSplitHost.test.tsx` before the lane could finish | On compact UI-only lanes, start with a one-file Prettier sweep on the newest changed test file when it was edited alongside structural component changes. |
| 2026-05-25 | Production SOP run with character panel split and rail polish |   8.9 | high | The live tree stayed collapsed to one compact character-panel lane, the focused UI tests passed cleanly, and the final build cleared on the exact pushed tree | The first pass still paid one tiny formatting cycle on `CharacterPanelSplitHost.test.tsx` before the lane could finish | On compact UI-only lanes, run a one-file Prettier sweep on the newest touched test file before the rest of the ladder so the lane stays fully one-pass. |
| 2026-05-24 | Production SOP run with Generate CTA guardrail, audio route limits, and character rail tuning |   8.7 | high | The live tree stayed collapsed to one coherent shared-runtime lane and the focused tests, docs checks, Generate CTA guard, and final build all cleared on the exact pushed tree | The first pass still lost one cycle to a bad repo-relative Prettier invocation, and the new guard script itself needed formatting before the lane could finish | On mixed repo-root plus `frontend/` lanes, derive the first formatting command from the actual working directory up front so the validation ladder starts cleanly. |
| 2026-05-20 | Production mixed-run closeout after repeated manifest drift               |   8.0 | medium     | Validation stayed honest on the final tree    | Commit and leftover discipline was noisy   | Rebuild manifests once from live `git status --short` when new files appear mid-run, then stop absorbing repeated tails.                                  |
| 2026-05-22 | Production SOP run with repeated mixed-lane rereads and blocked visual QA |   7.0 | high       | The shipped lanes were still validated safely | Time-to-clean-push efficiency dragged      | Split mixed trees earlier, verify optional browser QA tooling before attempting smoke, and treat stash-restored unrelated files as a new lane by default. |
| 2026-05-22 | Supervised closeout-scope correction                                      |   7.4 | high       | The correction sharpened role boundaries      | Closeout scope drifted out of Gear Ball    | Restrict suggested next steps to SOP/process/self-scoring improvements unless broader repo recommendations are explicitly requested.                       |
| 2026-05-22 | Supervised timer-creation false claim                                     |   6.2 | high       | The failure mode is now explicit and durable  | Completion language outran tool evidence   | Do not use completion language for timers, automations, commits, pushes, or similar side effects until the tool has succeeded and returned confirmation.  |
| 2026-05-22 | Supervised full-worktree SOP correction                                   |   7.1 | high       | The user clarified the true SOP promise       | Scope control missed full-tree accounting  | Add a hard pre-commit classification pass for every live non-temp worktree file and recheck classification before any push-ready claim.                    |
| 2026-05-23 | Production SOP run with stale first closeout snapshot                     |   8.1 | high       | The corrected tree was still pushed cleanly   | The first closeout snapshot was stale      | Rerun `git status --short` after the last build and draft the final report from the actual pushed commits, not from an earlier mental snapshot.            |
| 2026-05-23 | Production SOP run with voices retune and character workspace polish      |   8.8 | high       | Lane boundaries stayed clean through push     | Cheap blockers forced avoidable reruns     | Catch formatting drift and stale size/test expectations before the first preflight pass so targeted lanes clear in one shot.                               |
| 2026-05-23 | Production shared-runtime run with voices defaults and character resizing |   8.7 | high       | One honest shared-runtime batch cleared clean | Formatter drift forced extra preflight     | Scan likely touched tests for stale style/typing assumptions before the first broad preflight so the shared-runtime lane clears in one pass.                |
| 2026-05-23 | Production SOP run with Gear Ball prompt storage and panel-surface tuning |   8.9 | high       | Full-tree accounting and batch boundaries held | Preflight still burned on format/lint cleanup | Run a quick cheap-pass sweep for touched callback deps and single-file formatting drift before the first shared-runtime preflight.                           |
| 2026-05-23 | Production SOP run with runtime-load trim and entry-surface polish        |   8.9 | high       | Docs and product lanes stayed cleanly split   | First preflight still burned on CSS formatting drift | Run a tiny CSS/Prettier sweep on touched style files before the first mixed docs-plus-UI preflight.                                                         |
| 2026-05-23 | Scheduled production SOP run with create-entry unification                |   8.8 | high       | Full-tree accounting and shared-runtime validation stayed honest | First preflight still burned on dead hook deps and an unused page binding | Do one quick touched-file dead-binding/dependency sweep before the first shared page-shell preflight.                                                        |
| 2026-05-23 | Production SOP run with rail visibility and panel polish                  |   8.8 | high       | One coherent shared-runtime lane stayed intact through push | First preflight still paid avoidable format/lint cleanup | Add a tiny preflight sweep for touched docs/routes formatting plus dead constants on shared UI polish lanes.                                                 |
| 2026-05-23 | Production SOP run with AI Studio preview polish and deferred Copperknot lane |   8.6 | high    | The validated AI Studio lane stayed coherent through push | Preflight paid small cleanup cycles and a new unrelated docs lane surfaced post-commit | Re-audit for unrelated docs tails immediately after the first commit so deferred leftovers are named sooner and cost less attention.                        |
| 2026-05-23 | Production SOP run with voice-sample persistence and security retention cleanup |   8.2 | high   | Full-tree accounting held and both real lanes were validated and pushed cleanly | A staged-file spill crossed the first commit boundary, and the new secret scan needed a missing-file fix mid-run | Before every commit, compare `git diff --cached --name-only` against the intended manifest so pre-staged files and hook-restored tails cannot slip across boundaries. |

| 2026-05-23 | Production SOP run with character save progress and Gottspan dispatch clarification |   9.0 | high       | Two lean lanes stayed separate and validated cleanly | One cheap formatting rerun delayed the first preflight | Keep the fast whole-tree split and run a tiny touched-file Prettier sweep before the first frontend preflight. |
| 2026-05-23 | Production SOP run with runtime-prompt hardening and agent workspace rehome |   8.8 | high       | Two large but coherent lanes stayed stable and validated honestly | The product lane still paid repeated cheap formatting reruns before the real signal | Keep the 2-lane collapse, but do one broad touched-file Prettier sweep up front on large shared-runtime batches so preflight clears in one pass. |
| 2026-05-23 | Production SOP run with handoff filing, preference scoping, and workspace preview sanitizing |   8.9 | high       | Three honest lanes stayed separate and the final build passed on the exact pushed tree | First-pass validation still burned on formatting-only drift across docs and shared-runtime files | Run one manifest-wide Prettier sweep before the first preflight whenever the live tree mixes docs and shared-runtime lanes. |
| 2026-05-23 | Production SOP run with account hardening, panel polish, and Gottspan prompt filing |   8.8 | high       | The live tree collapsed cleanly into one product lane plus one docs lane, and the final build passed on the exact pushed tree | First-pass validation still burned on formatting drift plus one tiny unused-catch lint miss before the real checks could speak | Run one touched-manifest Prettier plus dead-binding sweep before the first broad preflight on mixed product/docs runs. |
| 2026-05-23 | Production SOP run with character-panel hardening planning and embedded-media drop fixes |   9.0 | high       | The live tree collapsed cleanly into one docs/governance lane plus one coherent product lane, and every focused check plus the final build cleared on the exact pushed tree | The run still paid one recovery cycle because the first focused Vitest command used the wrong repo-relative paths | Keep the lean two-lane collapse and derive focused test commands directly from the frontend workdir so path mistakes do not steal time before the real validation signal. |
| 2026-05-24 | Production SOP run with planning archive cleanup and QuickSwap retirement |   8.9 | high       | The live tree collapsed cleanly into one docs/archive lane plus one coherent character-panel runtime lane, and the focused checks plus final build all cleared on the exact pushed tree | The product lane still paid one cheap formatting recovery cycle before the real validation signal could speak | Keep the lean two-lane collapse and run one tiny touched-file Prettier sweep before the first character-panel preflight so broad removals clear in one pass. |
| 2026-05-24 | Production SOP run with loading-skeleton polish and blocked-loop closeout |   8.9 | high       | The live tree collapsed to one small character-panel lane, the focused checks stayed honest, and the final build cleared on the exact pushed tree | The first preflight still lost one cheap cycle to formatting drift in the loading skeleton file | Keep these micro-lanes tight and run one single-file Prettier sweep before the first preflight when a lane is this small. |
| 2026-05-24 | Production SOP run with Standard prompt bootstrap hardening and planning closeout |   8.8 | high       | The live tree collapsed cleanly into one docs/control-plane lane plus one small character-panel lane, and both focused checks plus the final build cleared on the exact pushed tree | The docs lane still paid one cheap recovery cycle because `docs/api/api-internal-routes.md` needed formatting before the real validation signal could speak | Keep the two-lane collapse and run a tiny docs formatting sweep before the first mixed docs/control-plane preflight. |
| 2026-05-24 | Production SOP run with Standard selection isolation and looks-rail overflow controls |   8.8 | high       | The live tree collapsed cleanly into one docs/control-plane lane plus one small UI/runtime polish lane, and both focused checks plus the final build cleared on the exact pushed tree | The docs lane still paid one cheap recovery cycle because `docs/api/api-internal-routes.md` needed formatting before the real validation signal could speak | Keep the two-lane collapse and run a tiny docs formatting sweep before the first mixed docs/control-plane preflight. |
| 2026-05-24 | Production SOP run with Standard selection isolation, billing rate limits, preparing image previews, and looks-rail overflow controls |   7.7 | high       | The final shipped tree was still validated honestly, including both restored billing test seams and the restored Standard attachment-preview seam, and the combined lane cleared the final build | Three stash-restored tails surfaced after the first commit path, forcing multiple follow-up commits before push | Before final push, compare the live worktree against the just-validated manifest repeatedly until the tree is truly clean so restored tails get folded into the main lane instead of becoming serial patch commits. |
| 2026-05-24 | Production SOP run with voices mode cleanup and API mutation rate limits |   8.0 | high       | The final shipped tree was validated honestly and the post-commit convergence loop kept working until the live tree was truly clean before the score loop and push | Three related API test tails still surfaced after the first runtime commit, forcing extra follow-up commits before convergence completed | Keep using the convergence loop, but broaden the first manifest to include all sibling API tests whenever a rate-limit/sanitized-error seam touches adjacent routes. |
| 2026-05-24 | Production SOP run with Dave lane-discipline docs, derivative-preserving voice outputs, and media-copy rate limiting |   8.4 | high       | The run stayed converged: the related media/API tail was pulled into the live lane before commit, the focused tests plus final build cleared on the exact pushed tree, and the score loop waited for a clean tree | The first scoped wrapper underreached because `next.config.js` tripped an ignored-file ESLint warning, and the surfaced rate-limit test had a false 500 caused by reusing the same `Response` body across looped requests | When a lane touches ignored config files like `next.config.js`, skip the wrapper immediately and run the manual validation ladder from the outset so the first pass stays honest and fast. |
| 2026-05-24 | Production SOP run with custom-voice ownership authority, transcript propagation, and AI Studio loading/ordering polish |   8.7 | high       | The tree stayed collapsed to one honest product lane, the focused test suite covered 29 files / 422 tests, and the final docs checks plus build passed on the exact pushed tree | The manifest wrapper failed on the long file list, and the build surfaced one real type-narrowing bug in `AiStudioProjectEntryState.tsx` before the lane could finish | On very large single-lane runs, skip the wrapper immediately and go straight to the manual ladder so the only remaining time cost is real code correction, not tooling friction. |
| 2026-05-24 | Production SOP run with voice-design preview tokens and authenticated admin instruction fetches |   8.9 | high       | The tree collapsed cleanly to one shared voice/admin lane, the focused suite cleared 6 files / 88 tests, and the final build plus docs checks passed on the exact pushed tree | The first pass still lost a cheap cycle to two files that only needed Prettier | On compact shared-runtime lanes, start with a tiny Prettier sweep on obviously touched new/edited files so the rest of the ladder can stay one-pass clean. |
| 2026-05-24 | Production SOP run with signed-in issue reports, Ophestivus docs/tool rehome, and custom-voice quarantine follow-through |   8.8 | high       | The whole tree stayed collapsed to one coherent product-and-governance lane, 17 focused test files / 154 tests passed, and the final build plus docs checks cleared on the exact pushed tree | The first pass still paid a small formatting cycle on three new admin-report files before the direct ladder was fully green | For medium-size mixed UI/docs/tooling lanes, run a tiny Prettier sweep on brand-new files before the rest of the ladder so the direct pass stays one-shot clean. |
| 2026-05-24 | Production SOP run with AI Studio prompt-control polish, custom voice-name guardrails, and object URL recovery |   8.8 | high       | The tree stayed collapsed to one broad but coherent AI Studio lane, the direct test/build ladder cleared, and the final product commit landed cleanly on the exact validated tree | I initially let the score-loop writeback fall out of the commit path and had to catch that after the tree was already clean | After every main product commit, verify immediately whether the score-loop files changed this turn and commit them before moving on to the push step. |
| 2026-05-25 | Production SOP run with style-drop server-copy fallback and stable detail-preview selection |   9.1 | high       | The tree stayed collapsed to one compact AI Studio lane, the targeted lint/tests all cleared, and the final production build passed on the exact pushed tree | The first pass still paid one cheap formatting cycle on `styleSourceNormalization.ts` before the ladder was fully clean | On compact single-lane UI/runtime runs, start with a one-file Prettier sweep when one touched file clearly dominates the diff so the first validation pass stays one-shot clean. |
| 2026-05-26 | Production SOP run with workflow-local restore authority and curated quick-slot drop precedence |   9.0 | high       | The tree stayed collapsed to one coherent AI Studio lane, focused lint/tests plus docs checks passed, and the final production build cleared on the exact pushed tree | The first pass still paid one cheap formatting cycle on the workflow-settings files before the ladder was fully clean | On compact workflow-state lanes, run a small Prettier sweep on the touched hook and test cluster before the main ladder so the first pass stays one-shot clean. |
