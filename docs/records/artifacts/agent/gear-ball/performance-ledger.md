# Gear Ball Performance Ledger

Purpose: keep a concise scored ledger of Gear Ball SOP runs and the minimal per-run training loop.

## Entries

| Date       | Run                                                                       | Score | Confidence | What went right                               | What went wrong                            | Smallest next improvement                                                                                                                                 |
| ---------- | ------------------------------------------------------------------------- | ----: | ---------- | --------------------------------------------- | ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-01 | Production SOP run with Standard create warning cleanup, billing API error sanitization, internal media-ref path hardening, and pricing-drift validator refresh |   8.7 | high | The final shared-runtime lane stayed honest once the full tail set was pulled in, `gear-ball:preflight` cleared 8 focused suites with 58 passing tests, `check:generate-cta-contract` and pricing-drift validation both passed, and the final production build passed on the exact committed tree | The first manifest undercounted same-lane tails after the first build pass, and one required validator was stale enough to fail until its source-of-truth path was refreshed | When a lane touches pricing-adjacent AI Studio surfaces plus media preview/signing boundaries, run the support validators early and re-audit the live tree before the first staging step so stale checker paths and sibling API test tails cannot appear late. |
| 2026-06-01 | Production SOP run with Dave launch-readiness contract tightening plus media-ownership, pricing-policy refresh, and workspace-preview hardening |   9.2 | high | The live tree collapsed cleanly into one Dave docs lane plus one shared-runtime lane, `docs:check` passed, `gear-ball:preflight` cleared 11 focused suites with 159 passing tests, and the final production build passed before the commit path went live | No repo-local blocker surfaced after the first focused validation ladder; only the standing remote branch-rule bypass note remained outside the repo | Keep using the two-lane collapse for mixed docs-plus-runtime runs when the validation seams are this clear so the first pass can stay this direct. |
| 2026-06-01 | Production SOP run with Standard chat formatting refinement, session-bootstrap unification, and AI Studio image-billing/bootstrap hardening |   8.6 | high | The live tree converged into three honest lanes, focused route/auth/runtime suites plus pricing and dashboard checks cleared with 357 total tests, `gear-ball:preflight` passed on the exact full manifest, and the final production build passed on the exact pushed tree | The first validation ladder still paid avoidable recovery cycles: formatting drift, unused-import lint tails, one hanging `supabaseClient` localStorage test seam, one pricing-logo `next/image` parity regression, and two late build-only typing seams before the tree fully settled | Build the first full-manifest ladder from live status plus an upfront touched-file hygiene pass and one quick type-boundary scan on new shared pricing/auth helpers so the first preflight and final build act more like confirmation than seam discovery. |
| 2026-06-01 | Production SOP run with Gear Ball lane helpers plus auth-bootstrap, pricing-route, dashboard, and terminal-convergence hardening |   8.4 | high | The tree converged cleanly into one Gear Ball docs/helper lane plus one broad shared-runtime lane, 12 focused suites cleared with 98 passing tests, `docs:check` passed, and the final production build passed on the exact committed tree | The first manifest still underreached: late sibling files surfaced during the live-tree audit before commit, and the lane also paid two formatting sweeps plus one unused-import cleanup before the real validation signal was fully clean | Build the first product manifest from full live status plus one explicit seed sweep around every touched route/helper/test cluster so new sibling tests and helper files are included before the first validation pass. |
| 2026-05-30 | Production SOP run with generated-output aspect hydration, GPT Image 2 resolution clamp hardening, and Ako/Money Stuff docs follow-through |   8.9 | high | The live tree stayed collapsed to one broad shared-runtime lane plus one smaller docs/agent-support lane, `docs:check` passed, 19 focused suites cleared with 424 passing tests, and the final production build passed on the exact converged tree | The first final build surfaced two narrow type-only seams in the new `displayAspectRatio.ts` helper before the lane could close | When introducing new shared helpers on broad runtime lanes, run one quick local type-seam scan on the new file before the final build rung so nullability edges do not become the only remaining blocker. |
| 2026-05-30 | Production SOP run with Expert Edit submit-intent hardening, Fal inpaint retirement, and AKO/Gutan docs follow-through |   8.6 | high | The live tree stayed split into one compact runtime lane plus one small docs lane, `docs:check` passed, 7 focused suites cleared with 138 passing tests, and the final production build passed on the exact converged tree with the retired Fal wrappers absent from the route inventory | The first broad test manifest still overreached into the stale monolithic `ExpertEditPanelView.test.tsx` surface, which produced unrelated failures and noise before I narrowed back to the honest changed seams | When a changed assertion inside a stale broad suite is already skipped or cold, validate the changed contract through its focused helper/runtime suites first and do not let the monolithic file become the lane authority by default. |
| 2026-05-30 | Production SOP run with admitted-image rollout, project-bootstrap hardening, and Gutan/Holomony/Nuclo image-admission follow-through |   8.7 | high | The live tree stayed split into one broad product lane plus one honest docs/support lane, `docs:check` passed, 15 focused suites cleared with 92 passing tests, and the final production build passed on the exact converged tree with the Fal route family restored | The lane still lost time to mixed-manifest cleanup friction, and the first real parity failure came from active Fal wrapper routes disappearing even though the model catalog, docs, and route expectations still required them | When model-catalog or route-parity checks fail on active Fal models, verify `frontend/pages/api/fal` wrapper coverage and wrapper-sync ownership before touching docs or assuming the route family is intentionally gone. |
| 2026-05-29 | Production SOP run with curated-reference authority hardening and OpenAI image route docs refresh |   8.9 | high | The live tree stayed collapsed to one honest AI Studio/reference-ingress lane, `docs:check` passed, 11 focused suites cleared with 199 passing tests, and the final production build passed on the exact converged tree | The lane still lost time to two stale blob-clone test expectations that assumed permissive internal drag fallback behavior even after the runtime had tightened to fail closed without authoritative resolution | When a shared-runtime lane hardens authority or ingress policy, treat stale permissive tests as the first likely failure mode and recheck the contract before assuming the product code needs to reopen fallback behavior. |
| 2026-05-29 | Production SOP run with Expert Edit ingress hardening, image/video routing cleanup, and stage-control simplification |   8.6 | high | The live tree stayed collapsed to one honest AI Studio lane, docs checks plus focused runtime/server/integration tests cleared, and the final production build passed on the exact converged tree | The run still lost time to an initial repo-root path miss, one migration-doc parity miss, a build-only page-to-hook contract seam around `motionReferenceVideoError`, and the fact that the first tracked-diff lint manifest skipped two new source files until the final hygiene pass | When commands derive from tracked diffs, build the first lint manifest from `git status --short` instead so new source files and broad page-runtime call-site seams get checked before the final build rung. |
| 2026-05-29 | Production SOP run with Reference Grid density pressure hardening and motion recorder settings recovery |   9.0 | high | The live tree stayed collapsed to one compact AI Studio lane, preflight passed, 6 focused suites cleared with 123 passing tests, and the final production build passed on the exact converged tree | The first pass still paid one small Prettier sweep on two touched files before the lane was fully clean, and the focused Reference Grid suite continues to emit its known `act(...)` warning noise without failing | On compact runtime lanes, start with a tiny touched-file Prettier sweep and keep the known warning-bearing suite in the focused manifest so the first pass stays one-shot clean while the noise remains visible. |
| 2026-05-29 | Production SOP run with project-folder-authority retirement, Kie upload diagnostics, and Create surface authority cleanup |   8.8 | high | The live tree stayed collapsed to one broad shared-runtime lane, `docs:check` plus hosted SQL lint passed, 18 focused test files cleared with 422 passing tests, and the final production build passed on the exact converged tree | The first ladder still lost time to two small formatting sweeps, one lint-only `Function` type cleanup in `useControlPlaneCatalog.ts`, and a noisy `gear-ball:preflight` wrapper pass because `next.config.js` is ignore-matched by ESLint | When `frontend/next.config.js` or another ignore-matched config file is in the manifest, skip the wrapper immediately and start with one repo-root manifest formatting sweep so the first direct ladder reaches the real behavioral signal faster. |
| 2026-05-28 | Production SOP run with global media-folder authority restore, signed voice-changer upload staging, and AI Studio media/workspace hardening |   8.1 | high | The live tree stayed collapsed to one broad shared-runtime lane, preflight plus docs checks plus hosted SQL lint plus 33 focused test files with 514 passing tests all cleared, and the final production build passed on the exact converged tree | The lane still paid two formatting sweeps, a small unused-vars cleanup round, and multiple build-only contract seams across Standard Create, Video panel, media folder canvas compatibility, and server upload constants before the tree fully converged | On broad shared-runtime lanes, do one explicit build-minded seam scan right after focused tests go green, especially for recently changed prop/helper signatures, so the final build confirms the lane instead of discovering late contract drift. |
| 2026-05-28 | Production SOP run with Standard/Pulse create control hardening, internal video-frame drops, and embedded elements panel polish |   7.9 | high | The final production result converged cleanly: exact preflight, focused UI/runtime suites, docs checks, and multiple final production builds all passed on the exact tree that reached `production` | The first pass surfaced three unused Pulse bindings, and the convergence loop then kept uncovering legitimate sibling AI Studio tails across reference-grid, motion-recorder, video panel, and elements-manager seams after the first product commit path | Before the first product commit on broad shared-runtime lanes, run one explicit sibling-seam sweep (`git diff --name-only` plus adjacent test ownership scan) so the first manifest absorbs the whole lane instead of serial tail-chasing it later. |
| 2026-05-28 | Production SOP run with agent-surface pruning and AI Studio audio metadata hardening |   8.8 | high | The live tree stayed collapsed to one broad docs/governance lane plus one coherent AI Studio runtime lane, `docs:check` passed, product preflight cleared 5 focused files with 84 passing tests, and the final production build passed on the exact converged tree | The run still lost avoidable cycles to one shell-safe manifest issue around spaced paths and one build-only missing import in `useAiStudioProjectWorkspacePersistenceController.ts` | On mixed docs lanes with spaced paths, use shell-safe staging/formatting loops from the start and do one quick import-seam scan on persistence hooks before the final build rung. |
| 2026-05-28 | Production SOP run with Copperknot dispatch guidance and AI Studio media restore/capture hardening |   9.0 | high | The live tree collapsed cleanly into one Copperknot docs lane plus one broad AI Studio/runtime lane, docs checks plus exact preflight plus the final production build all passed, and the tree converged cleanly before push | The run still lost one avoidable cycle on a macOS `xargs` flag mismatch during the initial formatting sweep | On macOS runs, use BSD-safe `xargs` form or a tiny shell loop for manifest-wide formatting so the first cleanup pass stays one-shot clean. |
| 2026-05-27 | Production SOP run with generated-media authority and element-create follow-through |   8.9 | high | The live tree stayed collapsed to one compact AI Studio lane, the exact preflight cleared 3 focused test files with 70 passing tests, and the final production build passed on the exact converged tree | The first build surfaced one real async contract mismatch in `ElementsManagerShell.tsx` where `onCreateElement` was treated as promise-returning when the view-state surface exposes `void` | On compact UI/runtime lanes, do one quick build-minded callback-contract scan after focused tests go green so page-local async mismatches surface before the final build rung. |
| 2026-05-27 | Production SOP run with startup-governance hardening and AI Studio embedded media-library rollout |   8.8 | high | The live tree stayed collapsed to one docs/governance lane plus one broad product lane, the product preflight cleared 24 focused test files with 247 passing tests, `supabase db lint --linked --schema public --fail-on warning` passed, and the final production build passed on the exact converged tree | The product lane still paid two formatting sweeps, one bracketed-route shell glob miss, and a small lint cleanup round before the broad manifest could fully clear | On very large mixed frontend/docs/sql lanes, start with one manifest-wide Prettier sweep and quote any bracketed route paths in the first command block so the first pass reaches real signal faster. |
| 2026-05-27 | Production SOP run with video-panel motion intake and reference-card styling cleanup |   9.2 | high | The live tree stayed collapsed to one compact AI Studio lane, the exact preflight cleared 2 focused test files with 33 passing tests, and the final production build passed on the exact converged tree | No product or validation regressions surfaced once the lane entered the ladder | Keep starting compact visual/runtime lanes with one upfront touched-file Prettier sweep so the first pass stays this clean. |
| 2026-05-26 | Production SOP run with detail-preview authority and workspace-restore hardening |   8.7 | high | The live tree stayed collapsed to one coherent AI Studio lane, the exact preflight cleared 10 focused files with 113 passing tests, and the final production build passed on the exact converged tree | The run still paid repeated formatting cleanup cycles, and `DetailModal.tsx` needed an explicit `react-hooks/set-state-in-effect` contract alignment before the lane could clear | On modal-heavy authority lanes, do one upfront Prettier sweep and one quick effect-state lint scan before the first preflight so the first pass reaches the real behavioral signal faster. |
| 2026-05-26 | Production SOP run with motion-recorder intake, style-drop hardening, and character reference drag cleanup |   8.2 | high | The live tree stayed collapsed to one coherent AI Studio lane, the focused preflight cleared 28 files with 130 passing tests, and the final production build passed on the exact converged tree | The run still paid several formatting sweeps, one motion-recorder cleanup lint warning, two style-drop regressions, and one project-scoped generated-media authority bug before the lane fully cleared | On broad drag/drop and media-authority lanes, do one upfront touched-file Prettier sweep and one hook-to-boundary contract scan before the first preflight so the real signal appears sooner. |
| 2026-05-26 | Production SOP run with reference-grid delete cleanup and AI Studio media-panel hardening |   8.3 | high | The live tree stayed collapsed to one coherent AI Studio lane, the exact current-manifest preflight cleared 23 files and 44 focused tests, and the final production build passed on the exact pushed tree | The first pass still paid a formatting-only cleanup, the new reference-grid delete path shipped with a missing `normalizeId` helper, and the final build exposed a page-shell callback contract mismatch before the lane converged | On shared media-delete/runtime lanes, do one quick callback-seam scan after hook signature changes so page-shell adapters and delete-flow helpers stay aligned before the final build. |
| 2026-05-26 | Production SOP run with launch-trust doctrine docs and project workspace/media rail hardening |   8.8 | high | The live tree stayed collapsed to one governance lane plus one broad project-workspace/runtime lane, docs checks plus shared-runtime preflight plus Generate CTA contract plus final build all passed, and the final tree converged cleanly before score writeback | The first product pass still paid a formatting sweep, and one dead import warning surfaced in `VideoPropertiesPanel.tsx` before the lane could clear | On medium shared-runtime lanes, run a tiny dead-import scan alongside the first formatting sweep so the first preflight speaks only to real behavioral regressions. |
| 2026-05-26 | Production SOP run with Abismia/Babineaux onboarding and AI Studio shell modularization |   8.4 | high | The live tree stayed collapsed to one governance lane plus one broad AI Studio/runtime lane, docs checks plus shared-runtime preflight plus Generate CTA contract plus final build all passed, and the final tree converged cleanly before score writeback | The first product pass paid a large formatting sweep, four lint warnings surfaced after that, and the DetailModal preview-lock contract regressed once before the lane fully cleared | On very large shared-runtime lanes, run a manifest-wide formatting sweep plus a tiny extracted-runtime/effect lint scan before the main preflight so the first pass catches only real behavioral regressions. |
| 2026-05-25 | Production SOP run with internal media-ref submit authority and Holomony reference-grid baseline |   8.5 | high | The live tree stayed split into one Holomony baseline-doc lane and one broad AI Studio submission/workspace lane, the focused suite cleared 24 files with 539 passing tests, and the final build plus docs checks passed before publish | The first product-lane ESLint pass used the repo root instead of `frontend/`, the build surfaced one real session-snapshot union type hole, and `ExpertEditPanelView.test.tsx` still emitted its recurring `act(...)` warning noise | On large frontend-only runtime lanes, start from the `frontend/` workdir and do one quick union-access scan around new snapshot helpers before the full build so the first pass catches only real behavioral risk. |
| 2026-05-25 | Production SOP run with character-mode bundle recovery and Holomony reference-grid onboarding |   8.8 | high | The live tree split cleanly into one Holomony docs lane and one compact character-mode runtime lane, focused tests cleared 56 checks, and the final build plus docs checks passed before publish | The first product-lane ESLint pass used the repo root instead of `frontend/`, which cost one avoidable rerun before the real validation signal | On frontend-only runtime lanes, derive ESLint commands directly from the `frontend/` workdir up front so the first validation pass stays clean. |
| 2026-05-25 | Production SOP run with Holomony media-library baseline and AI Studio workspace/session hardening |   8.1 | high | The live tree still collapsed cleanly into one Holomony baseline-doc lane plus one broad AI Studio runtime lane, the focused suite cleared 28 files with 499 passing tests, and the final docs checks plus build passed on the exact pushed tree | The run paid too many recovery cycles: one stale media-library mock, two Expert Edit history regressions, one keyboard-contract drift, and two build-caught type holes in preview/loading-shell components before the tree converged | When a lane introduces a new shared history or keyboard-routing seam, pull its owning hook tests into the first manifest and do the final build before drafting any score-loop closeout so UI-local type holes surface earlier. |
| 2026-05-25 | Production SOP run with reference-grid ordering and provider-error cleanup |   8.9 | high | The live tree stayed collapsed to one coherent AI Studio/media-ingestion lane, the focused suite cleared 9 files with 89 tests, and the final build plus docs checks passed on the exact pushed tree | The first validation pass still surfaced one avoidable lint warning from an unused import in `ReferenceGridCard.tsx` before the lane could finish | On compact single-lane runs, do one final dead-import scan immediately after the initial formatting sweep so the first validation pass stays fully clean. |
| 2026-05-25 | Production SOP run with reference-grid ownership hardening and Create model-context alignment |   8.8 | high | The live tree stayed collapsed to one coherent AI Studio/reference-grid lane, the focused suites plus docs checks passed, and the final build cleared on the exact pushed tree | The first cheap cleanup pass used the wrong working-directory path shape, which cost one avoidable recovery cycle before the real validation ladder started | On repo-root mixed lanes, derive the initial formatting sweep from the repo root and include untracked files in that first manifest so the first ladder pass stays clean. |
| 2026-05-25 | Production SOP run with character-mode fail-closed guards and admin/pulse governance polish |   8.1 | high | The live tree still collapsed to one coherent AI Studio/admin governance lane, the focused suites plus docs checks passed, and the final build cleared before publish | The first pass paid a broad formatting-only cleanup across 18 files, the Reference Grid suite still emitted recurring `act(...)` warning noise, and the ADR index tail surfaced after the first score-loop writeback so convergence needed one more correction step | On very large mixed runtime/docs lanes, run a manifest-wide Prettier sweep before the main ladder and re-check doc index tails before writing the score loop so the run closes in one shot. |
| 2026-05-25 | Production SOP run with global right-rail authority, Pulse catalog hardening, and signed-in issue reporting |   8.5 | high | The live tree stayed collapsed to one coherent AI Studio runtime/governance lane, the focused suites plus docs checks passed, and the final build cleared on the exact pushed tree | The first pass still paid a broad formatting-only cleanup across 18 files before the lane could finish | On large mixed runtime/docs lanes, do one manifest-wide Prettier sweep before the main ladder so the first validation pass speaks only to real failures. |
| 2026-05-25 | Production SOP run with Pulse runtime parking and preset docs refresh |   8.7 | high | The live tree stayed collapsed to one coherent AI Studio runtime lane, focused tests plus docs checks passed, and the final build cleared on the exact pushed tree | The first pass still paid two cheap cleanup cycles: five files needed Prettier and one workspace test had an unused `container` binding | On larger mixed docs-plus-runtime lanes, do one up-front Prettier sweep on touched runtime files and scan the changed tests for dead locals before starting the main ladder. |
| 2026-05-25 | Production SOP run with fixed character split host and workspace chrome |   8.8 | high | The live tree stayed collapsed to one compact character-panel lane, the focused UI tests passed cleanly, and the final build cleared on the exact pushed tree | The first pass still paid one tiny formatting cycle on `CharacterPanelSplitHost.test.tsx` before the lane could finish | On compact UI-only lanes, start with a one-file Prettier sweep on the newest changed test file when it was edited alongside structural component changes. |
| 2026-05-25 | Production SOP run with character panel split and rail polish |   8.9 | high | The live tree stayed collapsed to one compact character-panel lane, the focused UI tests passed cleanly, and the final build cleared on the exact pushed tree | The first pass still paid one tiny formatting cycle on `CharacterPanelSplitHost.test.tsx` before the lane could finish | On compact UI-only lanes, run a one-file Prettier sweep on the newest touched test file before the rest of the ladder so the lane stays fully one-pass. |
| 2026-05-24 | Production SOP run with Generate CTA guardrail, audio route limits, and character rail tuning |   8.7 | high | The live tree stayed collapsed to one coherent shared-runtime lane and the focused tests, docs checks, Generate CTA guard, and final build all cleared on the exact pushed tree | The first pass still lost one cycle to a bad repo-relative Prettier invocation, and the new guard script itself needed formatting before the lane could finish | On mixed repo-root plus `frontend/` lanes, derive the first formatting command from the actual working directory up front so the validation ladder starts cleanly. |
| 2026-05-20 | Production mixed-run closeout after repeated manifest drift               |   8.0 | medium     | Validation stayed honest on the final tree    | Commit and leftover discipline was noisy   | Rebuild manifests once from live `git status --short` when new files appear mid-run, then stop absorbing repeated tails.                                  |
| 2026-05-22 | Production SOP run with repeated mixed-lane rereads and blocked visual QA |   7.0 | high       | The shipped lanes were still validated safely | Time-to-clean-push efficiency dragged      | Split mixed trees earlier, verify optional browser QA tooling before attempting smoke, and treat stash-restored unrelated files as a new lane by default. |
| 2026-05-22 | Supervised closeout-scope correction                                      |   7.4 | high       | The correction sharpened role boundaries      | Closeout scope drifted out of Gear Ball    | Restrict suggested next steps to SOP/process/self-scoring improvements unless broader repo recommendations are explicitly requested.                       |
| 2026-05-22 | Supervised timer-creation false claim                                     |   6.2 | high       | The failure mode is now explicit and durable  | Completion language outran tool evidence   | Do not use completion language for timers, automations, commits, pushes, or similar side effects until the tool has succeeded and returned confirmation.  |
| 2026-05-22 | Supervised full-worktree SOP correction                                   |   7.1 | high       | The user clarified the true SOP promise       | Scope control missed full-tree accounting  | Add a hard pre-commit classification pass for every live non-temp worktree file and recheck classification before any push-ready claim.                    |
| 2026-05-23 | Production SOP run with stale first closeout snapshot                     |   8.1 | high       | The corrected tree was still pushed cleanly   | The first closeout snapshot was stale      | Rerun `git status --short` after the last build and draft the final report from the actual pushed commits, not from an earlier mental snapshot.            |
| 2026-05-23 | Production SOP run with voices retune and character workspace polish      |   8.8 | high       | Lane boundaries stayed clean through push     | Cheap blockers forced avoidable reruns     | Catch formatting drift and stale size/test expectations before the first preflight pass so targeted lanes clear in one shot.                               |
| 2026-05-23 | Production shared-runtime run with voices defaults and character resizing |   8.7 | high       | One honest shared-runtime batch cleared clean | Formatter drift forced extra preflight     | Scan likely touched tests for stale style/typing assumptions before the first broad preflight so the shared-runtime lane clears in one pass.                |
| 2026-05-23 | Production SOP run with Gear Ball prompt storage and panel-surface tuning |   8.9 | high       | Full-tree accounting and batch boundaries held | Preflight still burned on format/lint cleanup | Run a quick cheap-pass sweep for touched callback deps and single-file formatting drift before the first shared-runtime preflight.                           |
| 2026-05-23 | Production SOP run with runtime-load trim and entry-surface polish        |   8.9 | high       | Docs and product lanes stayed cleanly split   | First preflight still burned on CSS formatting drift | Run a tiny CSS/Prettier sweep on touched style files before the first mixed docs-plus-UI preflight.                                                         |
| 2026-05-23 | Scheduled production SOP run with create-entry unification                |   8.8 | high       | Full-tree accounting and shared-runtime validation stayed honest | First preflight still burned on dead hook deps and an unused page binding | Do one quick touched-file dead-binding/dependency sweep before the first shared page-shell preflight.                                                        |
| 2026-05-23 | Production SOP run with rail visibility and panel polish                  |   8.8 | high       | One coherent shared-runtime lane stayed intact through push | First preflight still paid avoidable format/lint cleanup | Add a tiny preflight sweep for touched docs/routes formatting plus dead constants on shared UI polish lanes.                                                 |
| 2026-05-23 | Production SOP run with AI Studio preview polish and deferred Copperknot lane |   8.6 | high    | The validated AI Studio lane stayed coherent through push | Preflight paid small cleanup cycles and a new unrelated docs lane surfaced post-commit | Re-audit for unrelated docs tails immediately after the first commit so deferred leftovers are named sooner and cost less attention.                        |
| 2026-05-23 | Production SOP run with voice-sample persistence and security retention cleanup |   8.2 | high   | Full-tree accounting held and both real lanes were validated and pushed cleanly | A staged-file spill crossed the first commit boundary, and the new secret scan needed a missing-file fix mid-run | Before every commit, compare `git diff --cached --name-only` against the intended manifest so pre-staged files and hook-restored tails cannot slip across boundaries. |

| 2026-05-23 | Production SOP run with character save progress and Gottspan dispatch clarification |   9.0 | high       | Two lean lanes stayed separate and validated cleanly | One cheap formatting rerun delayed the first preflight | Keep the fast whole-tree split and run a tiny touched-file Prettier sweep before the first frontend preflight. |
| 2026-05-23 | Production SOP run with runtime-prompt hardening and agent workspace rehome |   8.8 | high       | Two large but coherent lanes stayed stable and validated honestly | The product lane still paid repeated cheap formatting reruns before the real signal | Keep the 2-lane collapse, but do one broad touched-file Prettier sweep up front on large shared-runtime batches so preflight clears in one pass. |
| 2026-05-23 | Production SOP run with handoff filing, preference scoping, and workspace preview sanitizing |   8.9 | high       | Three honest lanes stayed separate and the final build passed on the exact pushed tree | First-pass validation still burned on formatting-only drift across docs and shared-runtime files | Run one manifest-wide Prettier sweep before the first preflight whenever the live tree mixes docs and shared-runtime lanes. |
| 2026-05-23 | Production SOP run with account hardening, panel polish, and Gottspan prompt filing |   8.8 | high       | The live tree collapsed cleanly into one product lane plus one docs lane, and the final build passed on the exact pushed tree | First-pass validation still burned on formatting drift plus one tiny unused-catch lint miss before the real checks could speak | Run one touched-manifest Prettier plus dead-binding sweep before the first broad preflight on mixed product/docs runs. |
| 2026-05-23 | Production SOP run with character-panel hardening planning and embedded-media drop fixes |   9.0 | high       | The live tree collapsed cleanly into one docs/governance lane plus one coherent product lane, and every focused check plus the final build cleared on the exact pushed tree | The run still paid one recovery cycle because the first focused Vitest command used the wrong repo-relative paths | Keep the lean two-lane collapse and derive focused test commands directly from the frontend workdir so path mistakes do not steal time before the real validation signal. |
| 2026-05-24 | Production SOP run with planning archive cleanup and QuickSwap retirement |   8.9 | high       | The live tree collapsed cleanly into one docs/archive lane plus one coherent character-panel runtime lane, and the focused checks plus final build all cleared on the exact pushed tree | The product lane still paid one cheap formatting recovery cycle before the real validation signal could speak | Keep the lean two-lane collapse and run one tiny touched-file Prettier sweep before the first character-panel preflight so broad removals clear in one pass. |
| 2026-05-24 | Production SOP run with loading-skeleton polish and blocked-loop closeout |   8.9 | high       | The live tree collapsed to one small character-panel lane, the focused checks stayed honest, and the final build cleared on the exact pushed tree | The first preflight still lost one cheap cycle to formatting drift in the loading skeleton file | Keep these micro-lanes tight and run one single-file Prettier sweep before the first preflight when a lane is this small. |
| 2026-05-24 | Production SOP run with Standard prompt bootstrap hardening and planning closeout |   8.8 | high       | The live tree collapsed cleanly into one docs/control-plane lane plus one small character-panel lane, and both focused checks plus the final build cleared on the exact pushed tree | The docs lane still paid one cheap recovery cycle because `docs/api/api-internal-routes.md` needed formatting before the real validation signal could speak | Keep the two-lane collapse and run a tiny docs formatting sweep before the first mixed docs/control-plane preflight. |
| 2026-05-24 | Production SOP run with Standard selection isolation and looks-rail overflow controls |   8.8 | high       | The live tree collapsed cleanly into one docs/control-plane lane plus one small UI/runtime polish lane, and both focused checks plus the final build cleared on the exact pushed tree | The docs lane still paid one cheap recovery cycle because `docs/api/api-internal-routes.md` needed formatting before the real validation signal could speak | Keep the two-lane collapse and run a tiny docs formatting sweep before the first mixed docs/control-plane preflight. |
| 2026-05-24 | Production SOP run with Standard selection isolation, billing rate limits, preparing image previews, and looks-rail overflow controls |   7.7 | high       | The final shipped tree was still validated honestly, including both restored billing test seams and the restored Standard attachment-preview seam, and the combined lane cleared the final build | Three stash-restored tails surfaced after the first commit path, forcing multiple follow-up commits before push | Before final push, compare the live worktree against the just-validated manifest repeatedly until the tree is truly clean so restored tails get folded into the main lane instead of becoming serial patch commits. |
| 2026-05-24 | Production SOP run with voices mode cleanup and API mutation rate limits |   8.0 | high       | The final shipped tree was validated honestly and the post-commit convergence loop kept working until the live tree was truly clean before the score loop and push | Three related API test tails still surfaced after the first runtime commit, forcing extra follow-up commits before convergence completed | Keep using the convergence loop, but broaden the first manifest to include all sibling API tests whenever a rate-limit/sanitized-error seam touches adjacent routes. |
| 2026-05-24 | Production SOP run with Dave lane-discipline docs, derivative-preserving voice outputs, and media-copy rate limiting |   8.4 | high       | The run stayed converged: the related media/API tail was pulled into the live lane before commit, the focused tests plus final build cleared on the exact pushed tree, and the score loop waited for a clean tree | The first scoped wrapper underreached because `next.config.js` tripped an ignored-file ESLint warning, and the surfaced rate-limit test had a false 500 caused by reusing the same `Response` body across looped requests | When a lane touches ignored config files like `next.config.js`, skip the wrapper immediately and run the manual validation ladder from the outset so the first pass stays honest and fast. |
| 2026-05-24 | Production SOP run with custom-voice ownership authority, transcript propagation, and AI Studio loading/ordering polish |   8.7 | high       | The tree stayed collapsed to one honest product lane, the focused test suite covered 29 files / 422 tests, and the final docs checks plus build passed on the exact pushed tree | The manifest wrapper failed on the long file list, and the build surfaced one real type-narrowing bug in `AiStudioProjectEntryState.tsx` before the lane could finish | On very large single-lane runs, skip the wrapper immediately and go straight to the manual ladder so the only remaining time cost is real code correction, not tooling friction. |
| 2026-05-24 | Production SOP run with voice-design preview tokens and authenticated admin instruction fetches |   8.9 | high       | The tree collapsed cleanly to one shared voice/admin lane, the focused suite cleared 6 files / 88 tests, and the final build plus docs checks passed on the exact pushed tree | The first pass still lost a cheap cycle to two files that only needed Prettier | On compact shared-runtime lanes, start with a tiny Prettier sweep on obviously touched new/edited files so the rest of the ladder can stay one-pass clean. |
| 2026-05-24 | Production SOP run with signed-in issue reports, Ophestivus docs/tool rehome, and custom-voice quarantine follow-through |   8.8 | high       | The whole tree stayed collapsed to one coherent product-and-governance lane, 17 focused test files / 154 tests passed, and the final build plus docs checks cleared on the exact pushed tree | The first pass still paid a small formatting cycle on three new admin-report files before the direct ladder was fully green | For medium-size mixed UI/docs/tooling lanes, run a tiny Prettier sweep on brand-new files before the rest of the ladder so the direct pass stays one-shot clean. |
| 2026-05-24 | Production SOP run with AI Studio prompt-control polish, custom voice-name guardrails, and object URL recovery |   8.8 | high       | The tree stayed collapsed to one broad but coherent AI Studio lane, the direct test/build ladder cleared, and the final product commit landed cleanly on the exact validated tree | I initially let the score-loop writeback fall out of the commit path and had to catch that after the tree was already clean | After every main product commit, verify immediately whether the score-loop files changed this turn and commit them before moving on to the push step. |
| 2026-05-25 | Production SOP run with style-drop server-copy fallback and stable detail-preview selection |   9.1 | high       | The tree stayed collapsed to one compact AI Studio lane, the targeted lint/tests all cleared, and the final production build passed on the exact pushed tree | The first pass still paid one cheap formatting cycle on `styleSourceNormalization.ts` before the ladder was fully clean | On compact single-lane UI/runtime runs, start with a one-file Prettier sweep when one touched file clearly dominates the diff so the first validation pass stays one-shot clean. |
| 2026-05-26 | Production SOP run with workflow-local restore authority and curated quick-slot drop precedence |   9.0 | high       | The tree stayed collapsed to one coherent AI Studio lane, focused lint/tests plus docs checks passed, and the final production build cleared on the exact pushed tree | The first pass still paid one cheap formatting cycle on the workflow-settings files before the ladder was fully clean | On compact workflow-state lanes, run a small Prettier sweep on the touched hook and test cluster before the main ladder so the first pass stays one-shot clean. |
| 2026-05-29 | Production SOP run with elements-manager drop handling and create-surface camera permissions |   9.2 | high       | The tree stayed collapsed to one coherent Elements Manager lane, the exact focused tests passed cleanly, and the final production build cleared on the exact pushed tree | No repo-local blocker surfaced after the direct ladder started; only the standing `production` branch-bypass remote note remained | Keep using the direct manual ladder immediately when `frontend/next.config.js` is in the lane so the first pass reaches real behavioral signal without wrapper noise. |
| 2026-05-29 | Production SOP run with reference-grid adaptive preview compaction and audio-cover transforms |   9.2 | high       | The tree stayed collapsed to one coherent reference-grid lane, the focused runtime/adaptive suites passed cleanly, and the final production build cleared on the exact pushed tree | No repo-local blocker surfaced after the direct ladder started; only the standing `production` branch-bypass remote note remained | Keep broad reference-grid and adaptive-preview changes collapsed when they share one validation seam so the first build confirms the lane instead of reopening taxonomy work. |
| 2026-05-29 | Production SOP run with create-mode authority cleanup and quickswap preference retirement |   8.8 | high       | The live tree stayed collapsed to one broad but coherent product-plus-schema lane, focused tests plus docs checks passed, and the final production build cleared on the exact pushed tree | The first cleanup pass still burned time on a formatter sweep that wrongly included raw SQL, and three tiny unused-type warnings delayed the real ladder | On mixed frontend/docs/sql lanes, keep the lane collapsed but split the cleanup pass by tool ownership: format frontend/docs first and leave raw SQL out unless a dedicated SQL formatter is part of the lane. |
| 2026-05-29 | Production SOP run with agent-surface pruning and video-upload authority hardening |   8.8 | high       | The live tree stayed collapsed to one docs/governance lane plus one broad AI Studio runtime lane, `docs:check` passed, 25 focused test files / 327 tests passed, and the final production build cleared on the exact converged tree | The first cleanup pass lost time on mixed-manifest command friction: spaced doc paths needed shell-safe handling, raw SQL did not belong in the generic Prettier sweep, and the final build still surfaced one missing-import seam | On mixed docs/frontend/sql lanes, start with quote-safe repo-root loops and split the cleanup pass by tool ownership so the first real validation signal arrives before the build rung. |
| 2026-05-29 | Production SOP run with motion-reference lease cleanup, internal media-ref authority hardening, and Expert Edit launch-lock alignment |   8.3 | high       | The live tree stayed collapsed to one honest product lane, docs checks plus the focused runtime/server/integration suites cleared, and the final production build passed on the exact converged tree | The run lost time to an initial repo-root path miss in the cleanup pass, a migration-doc parity miss, one build-caught `selectedRailTool` call-site seam, and stale broad `ExpertEditPanelView.test.tsx` expectations that were too wide for the actual changed contract | On broad shared-runtime lanes that also touch one large legacy UI suite, keep the lane collapsed but validate the exact touched expectations plus the modern integration/launch-lock seams instead of reopening the whole stale suite unless product code truly points there. |
| 2026-05-30 | Production SOP run with embedded media library authority, adaptive-preview delivery, and Expert Edit stage control cleanup |   8.6 | high       | The live tree stayed collapsed to one honest shared-runtime lane, `docs:check` passed, 17 focused media-library/runtime suites plus 3 focused Expert Edit integration suites cleared, and the final production build passed on the exact converged tree | The run still lost time to early manifest cleanup friction, a build-caught null-guard seam in `mediaUploadService`, and a stale monolithic `ExpertEditPanelView.test.tsx` file that was no longer the honest authority for the changed contract | When a broad shared-runtime lane brushes a stale monolithic UI suite, prove the lane with focused modern integration seams plus the exact touched runtime/media tests, and only keep the legacy file in authority when the product evidence still points there. |
| 2026-05-30 | Production SOP run with Holomony Reference Grid onboarding and detail-preview authority polish |   9.0 | high       | The live tree split cleanly into one Holomony docs lane plus one compact AI Studio runtime lane, focused tests cleared the exact preview/repair/render seams, and the final production build plus docs checks passed on the exact pushed tree | The only repo-local blocker was a single Prettier drift in the Reference Grid controller test file before the final build rung | Keep using the two-lane split for small docs-plus-runtime runs and start with a tiny touched-file Prettier sweep so the rest of the ladder stays one-pass clean. |
| 2026-05-30 | Production SOP run with Gutan onboarding, Holomony Canvas scope expansion, and Elements reference upload-state polish |   9.0 | high       | The live tree split cleanly into one docs/tooling lane plus one compact Elements runtime lane, `docs:check` passed, the focused Elements layout suite cleared, and the final production build passed on the exact pushed tree | The only repo-local blocker was a first-pass formatting sweep across the new Gutan docs and one Elements test file, plus a harmless shell-script parser mismatch from using Prettier check too broadly | On mixed docs/tooling lanes with one small runtime seam, keep the two-lane split and aim the first cheap pass only at files the formatter actually owns so the rest of the ladder stays one-pass clean. |
| 2026-05-31 | Production SOP run with billed-credit authority docs, launch follow-through, and AI Studio generated-media pricing hardening |   8.9 | high       | The live tree collapsed to one docs/agent-support lane plus one broad shared-runtime lane, `docs:check` passed, 18 focused suites cleared with 333 passing tests, and the final production build passed on the exact converged tree | The first cleanup pass still cost time on a broad mixed-manifest sweep, and the final build was the first place a type-shape seam in `pricingGridBilledCredits.ts` surfaced | On broad pricing/runtime lanes, do a quick local type-shape scan on new shared helper adapters before the final build rung so the last pass confirms the lane instead of discovering a narrow contract mismatch. |
| 2026-05-31 | Production SOP run with pricing-grid variant normalization and signed storage-url recovery in Reference Grid |   8.9 | high       | The live tree stayed collapsed to one compact shared-runtime lane, focused pricing/reference-grid suites passed, `docs:check` passed, and the final production build passed on the exact converged tree | The final build was the first place a narrow `PricingStrategyId` typing seam surfaced between the admin pricing model row and the new shared variant-rule helper | When pulling admin-grid rules into shared runtime helpers, normalize the type boundary up front so the final build confirms behavior instead of discovering a small adapter mismatch. |
| 2026-05-31 | Production SOP run with Copperknot fix-shape guidance and Create billed-resolution preservation |   9.0 | high       | The live tree split cleanly into one Copperknot docs lane plus one compact AI Studio runtime lane, `docs:check` passed, the focused view-model suite cleared with 38 passing tests, and the final production build passed on the exact converged tree | The only real blocker was a narrow build-time typing seam where the new shared pricing-resolution helper expected a stricter strategy-id shape than the admin model row exposed | When moving shared pricing helpers across admin/runtime boundaries, normalize permissive string inputs at the helper boundary immediately so the final build stays a confirmation pass. |
| 2026-05-31 | Production SOP run with Copperknot post-deploy queue refresh and AI Studio prompt/layout normalization |   9.0 | high       | The live tree split cleanly into one Copperknot post-deploy docs lane plus one compact shared-runtime lane, `docs:check` passed, 9 focused suites cleared with 152 passing tests, and the final production build passed on the exact converged tree | The only repo-local friction was the initial formatter sweep across the mixed docs/runtime manifest before the focused ladder | Keep this two-lane collapse for post-deploy follow-through plus compact runtime hardening, and start with one quote-safe mixed-manifest formatter pass so the focused suites stay one-shot clean. |
| 2026-05-31 | Production SOP run with Copperknot user-load discipline tightening |   9.2 | high       | The live tree stayed collapsed to one tiny docs-only lane, `docs:check` passed cleanly, and the lane closed in one straightforward commit path plus the score writeback | No repo-local blocker surfaced after the direct docs ladder started | Keep docs-only Copperknot maintenance lanes collapsed and use the direct docs ladder immediately so the run stays almost frictionless. |
| 2026-05-31 | Production SOP run with Datserok agent onboarding |   9.2 | high       | The live tree stayed collapsed to one tiny docs-only lane, `docs:check` passed cleanly, and the new agent surfaces plus indexes landed in one straightforward commit path | No repo-local blocker surfaced after the direct docs ladder started | Keep new-agent onboarding as one docs-only lane when the work is purely contract and artifact scaffolding so the run stays almost frictionless. |
| 2026-05-31 | Production SOP run with projection-backed project generation ownership |   9.1 | high       | The live tree stayed collapsed to one compact shared-runtime lane, the focused project-generation/workspace suites passed with 43 tests, and the final production build passed on the exact converged tree | No repo-local blocker surfaced after the direct lane ladder started | Keep server-only ownership/persistence fixes as one compact runtime lane and go straight to the exact focused suites plus final build. |
| 2026-05-31 | Production SOP run with project-workspace persistence hardening, billed-image policy materialization, and launch-authority follow-through |   8.6 | high       | The live tree converged into one docs/authority lane plus one shared-runtime lane, `docs:check` passed, 9 focused runtime suites cleared with 144 passing tests, and the final production build passed on the exact pushed tree | The run still paid two test-expectation drift fixes, two build-caught type-narrowing seams in `useAiStudioPageMediaReferenceRuntime.ts`, and one related backlog tail that had to be folded in after the runtime commit | When a shared-runtime lane changes canonical billing and restored-canvas identity together, do one upfront expectation pass on the touched pricing/view-model and workspace-read tests so the final build is less likely to discover branch-narrowing seams late. |
| 2026-05-31 | Production SOP run with auth-authority hardening, upload rate limits, and persistence/ownership convergence |   8.9 | high       | The live tree converged into one docs/authority lane plus one shared-runtime lane, `docs:check` passed, 24 focused suites cleared with 175 passing tests, and the final production build passed on the exact pushed tree | The only real repo-local friction was an upfront mixed-manifest formatter pass and harmless recurring macOS libgio/libvips duplicate-class warnings during some media-route tests | Keep broad auth/upload/runtime lanes collapsed when they share one API-hardening seam, and start with one quote-safe formatter pass so the focused ladder stays one-shot clean. |
| 2026-05-31 | Production SOP run with Copperknot launch-control refresh and media/Stripe authority hardening |   8.4 | high       | The live tree split cleanly into one Copperknot docs lane plus one broad shared-runtime lane, `docs:check` passed, focused runtime suites cleared with 301 passing tests plus targeted rechecks, and the final production build passed on the exact converged tree | The run still paid for a repo-root path miss, tracked-diff manifests that would have skipped a new file without the full-tree re-audit, a hook-blocking secret-pattern fixture, a stale error-logging allowlist line anchor, and two build-time type seams in the Stripe subscription boundary | Build frontend manifests from live `git status --short`, not tracked diffs alone, and scan shared helper/type boundaries plus hook-sensitive fixture strings before the first commit path so the final build stays a confirmation rung instead of a seam finder. |
| 2026-06-01 | Production SOP run with planning-archive rehome, Copperknot/Holomony media authority docs, and media-display/workspace restore hardening |   8.5 | high       | The live tree converged into one docs/archive lane plus one shared-runtime lane, `docs:check` passed, focused preview/signing/workspace suites and the Expert Edit parity core all cleared, and the final production build passed on the exact pushed tree | The run still paid for broad formatting drift, one small lint-only import fix, two late build-time type seams (`useAiStudioProjectWorkspacePersistenceController.ts` and `mediaLibraryDragGhost.ts`), and one post-commit docs tail where the old `docs/planning/*` copies had to be deleted in a final follow-through commit | When archive moves replace tracked planning docs with `docs/archive/` copies, explicitly stage both the new archive files and the old-path deletions in the first docs commit, and keep broad legacy UI suites out of lane authority when the touched expectations can be proven by smaller modern seams plus the final build. |
| 2026-06-01 | Production SOP run with protected-route bootstrap unification, GPT Image 2 naming cleanup, and media preview-field authority hardening |   7.9 | high       | The live tree converged into one docs/agent-support lane plus one broad shared-runtime lane, `docs:check` passed, 25 focused suites cleared with 298 passing tests, and the final production build passed on the exact converged tree | The run still paid for one docs-check drift after route-helper extraction, a compiler/lint seam after simplifying autosave snapshot preparation, one late dashboard lazy-import lint tail, and one hook test that had to be realigned to the new computation behavior | When moving canonical route metadata or removing manual memoization, update the supporting repo checks and any optimization-sensitive tests in the same first pass so docs drift and stale performance assertions do not become the last blockers. |
| 2026-06-01 | Production SOP run with Gear Ball scoring rebase and project-restore bootstrap hardening |   9.1 | high       | The live tree converged into one Gear Ball docs lane plus one shared-runtime lane, exact lint passed, 8 focused suites cleared with 51 passing tests, `docs:check` passed, and the final production build passed on the exact converged tree | No repo-local blocker surfaced after the focused ladder started | Keep shared project-restore/loading-surface hardening collapsed to one runtime lane and prove it with the exact hook, component, and dashboard suites before the final build rung. |
| 2026-06-01 | Production SOP run with dashboard auth bootstrap hardening, Standard chat formatting refinement, generation recovery stage timing follow-through, and Datserok load pruning |   7.4 | high       | The final pushed tree converged cleanly: focused dashboard/auth/runtime suites passed, generation control-plane suites passed, `docs:check` passed twice, and the final production build passed on the exact converged tree before closeout | The first manifest underreached badly: multiple stash-restored tails surfaced after the first product commit path, the build discovered a real `authGuard.ts` webpack seam, and related dashboard route docs plus separate generation and Datserok lanes had to be folded in late before the tree truly settled | Build the first manifest from full live status plus one aggressive sibling sweep around touched route, auth, and prompt-runtime surfaces, and do not trust a seemingly clean post-commit tree until stash-restored tails have been explicitly re-audited. |
| 2026-06-01 | Production SOP run with auth-scoped media URL hardening and post-restore workspace autosave recovery |   8.0 | high       | The final pushed tree converged into one auth/media route lane plus one workspace-persistence lane, 10 focused suites cleared with 85 passing tests, `gear-ball:preflight` passed on both lanes, and the final production build passed on the exact converged tree | The first manifest undercounted related auth-route tails multiple times, one render-time ref fix had to be reworked to the repo-approved microtask state pattern, and one focused hook test run initially used the wrong workdir | Do one harder first-pass sibling/tail sweep around touched auth/helper/route surfaces, and run focused Vitest suites from the exact owning workdir by default so late tail discovery and command-shape noise do not steal the first commit path. |
| 2026-06-01 | Production SOP run with Dave threat-first guidance, ElevenLabs voice ownership filtering, and project autosave unlock refinement |   8.2 | high       | The final pushed tree converged into one docs lane plus two compact product lanes, the exact hook suite and 4 focused ElevenLabs route suites passed, `gear-ball:preflight` passed on both product lanes, `docs:check` passed, and the final production build passed on the exact converged tree | The initial lane map undercounted the live ElevenLabs tail cluster, so the run was not honestly ready to stage after the first validation pass even though the final tree recovered cleanly | After the first validation rung, always re-check live `git status --short` before planning the first commit message so hidden same-session tails are folded in before the commit phase starts. |
| 2026-06-01 | Production SOP run with fail-closed auth and billing hardening, storage-entitlement grant tightening, and workspace autosave/reference hydration refinement |   8.9 | high       | The live tree converged cleanly into one security lane plus one workspace-runtime lane, both `gear-ball:preflight` manifests passed, 8 focused suites plus 6 focused security/runtime suites cleared with 94 passing tests, `docs:check` passed, and the final production build passed on the exact pushed tree | The run still paid one cheap formatting cycle in the security lane, one migration-doc parity miss after the new SQL migration landed, and one expired build session that had to be rerun before the commit phase could close | On mixed security/sql/runtime runs, add new migrations to the docs inventory immediately, do one tiny touched-file formatter sweep before the first preflight, and treat any lost build tail as a prompt to rerun the final build once rather than delaying the commit split. |
| 2026-06-01 | Production SOP run with Dave account-isolation guidance refresh and library quick-slot workspace ownership preservation |   8.8 | high       | The live tree converged into one compact docs lane plus one compact AI Studio/runtime lane, `docs:check` passed on the final docs batch, `gear-ball:preflight` passed, 3 focused suites cleared with 77 passing tests, and the final production build passed on the exact converged tree | A new Dave audit report surfaced only after the first validation pass, so the docs lane needed one extra convergence check before the first commit even though the run stayed clean after that | Re-run `git status --short` immediately after validation and before the first commit on mixed docs-plus-runtime runs so fresh audit artifacts are folded into the docs lane before the first commit message is chosen. |
| 2026-06-01 | Production SOP run with hosted storage-entitlement grant findings and project-workspace quick-slot telemetry tracing |   8.9 | high       | The live tree converged into one compact Dave docs lane plus one compact project-workspace runtime lane, `docs:check` passed, `gear-ball:preflight` passed, 2 focused hook suites cleared with 34 passing tests, and the final production build passed on the exact converged tree | The only repo-local friction was one cheap formatting cycle in `useAiStudioProjectWorkspaceRestoreHydration.ts` before the shared-runtime preflight could finish | On compact shared-runtime lanes, run one tiny touched-file formatter sweep before the first preflight whenever a newly edited hook file is likely to trip formatting-only noise. |
| 2026-06-02 | Production SOP run with Dave account-isolation audit packets, project-route error sanitization, media-list companion-art scope guarding, and prepared quick-slot library insertion |   9.0 | high       | The live tree converged into one compact Dave docs lane plus one coherent shared project/reference-runtime lane, `docs:check` passed, `gear-ball:preflight` passed, 3 focused suites cleared with 68 passing tests, and the final production build passed on the exact converged tree | No late tails or build-time seams surfaced after the first honest manifest, and the only notable cost was the normal hook formatter/lint pass during commit | Keep broad project/reference lanes collapsed when API disclosure hardening, media-scope regression coverage, and quick-slot persistence prep all share one validation seam so the first build stays a confirmation rung. |
| 2026-06-01 | Production SOP run with Pulse ownership/workspace realignment and project-route media autosave bootstrap gating |   8.5 | high       | The final pushed tree converged into one compact docs lane plus one shared-runtime AI Studio lane, `docs:check` passed, `gear-ball:preflight` passed, 2 focused autosave suites cleared with 18 passing tests, and the final production build passed on the exact runtime tree before the docs-only close | The run paid one cheap formatting cycle in the new helper file, one first-pass Vitest workdir miss, and a live Pulse docs lane surfaced only at the final worktree audit so batching finished later than it should have | Re-run full `git status --short` immediately before the first validation rung on small runtime runs so any simultaneous docs lane is collapsed into the plan before validation starts. |
| 2026-06-01 | Production SOP run with Pulse operating-package expansion and Standard response plus restore-authority hardening |   8.4 | high       | The final pushed tree converged into one compact Pulse docs lane plus one shared Standard/restore-runtime lane, `docs:check` passed, `gear-ball:preflight` passed, 7 focused suites cleared with 108 passing tests plus a 3-suite Standard rerun with 49 passing tests, and the final production build passed on the exact converged tree | The first runtime preflight paid two formatting fixes, ESLint exposed one dead import after the refactor, and the first full build discovered a real shared-contract seam where Standard transport results no longer satisfied the shared `workflowSession` type | When shared transport contracts move, check the binding/interface seam in the first pass and run one tiny formatter sweep before preflight so build-only and formatting-only tails do not steal the commit path. |
| 2026-06-01 | Production SOP run with Standard memory-window extraction, Standard message presentation modularization, and restore-safe reference projection |   8.6 | high       | The live tree stayed collapsed to one coherent Standard/runtime lane, `gear-ball:preflight` passed, the focused Standard send/runtime and projection suites cleared, and the final production build passed on the converged tree | The build still surfaced two real shared-contract seams because the new Standard session-memory send path had not been kept compatible with both the direct runtime caller and the older orchestration hook | When narrowing a shared Standard send contract, update both call-site families in the same first pass so the final build remains a confirmation rung instead of finding compatibility drift. |
| 2026-06-02 | Production SOP run with restored quick-slot membership recovery from pinned outputs |   9.2 | high       | The live tree stayed collapsed to one compact AI Studio restore/runtime lane, `gear-ball:preflight` passed, 3 focused owner suites cleared with 83 passing tests, and the final production build passed on the exact converged tree | The only repo-local friction was one cheap formatting pass in the Reference Grid selector-store test before the shared-runtime preflight could finish | On compact shared-runtime test lanes, run one tiny touched-file formatter sweep before the first preflight when a newly edited UI test is likely to trip formatting-only noise. |
| 2026-06-02 | Production SOP run with passive-wheel audit docs, Standard memory/detail-modal modularization, AI Studio header project-name display, and Pulse capability-plan docs |   7.2 | high       | The final pushed tree did converge into four honest lanes, `docs:check` passed, the focused Standard/runtime suites cleared with 121 passing tests, the header regression test passed, repeated `gear-ball:preflight` runs cleared the final 37-file manifest, and the production build passed on the converged tree | The first manifest undercounted several live tails, multiple build-only and lint-only type/import seams in the shared detail-modal extraction forced extra reruns, and the closeout only stabilized after a late full-tree re-audit | Before the first product commit on broad shared-runtime work, do one explicit sibling sweep around every new extracted helper directory and one final live-status sweep before staging so late tails and trivial seam fixes stop stealing the commit path. |
| 2026-06-02 | Production SOP run with Datserok startup-load tightening and Standard memory plus shared media-detail expansion |   8.4 | high       | The live tree converged into one compact Datserok docs lane plus one coherent shared-runtime AI Studio lane, `docs:check` passed, `gear-ball:preflight` cleared the final 45-file manifest, 10 focused suites passed with 155 tests, and the production build passed on the converged tree | The first runtime manifest still missed three related detail-modal helpers, so one late same-lane fold-back plus another formatter pass were needed before the batch was honestly ready to stage | When broad runtime work extracts shared presentation helpers, force one pre-commit sibling sweep over the whole new helper cluster and add every touched helper file to the first manifest so the last clean-tree audit stays boring. |
| 2026-06-02 | Production SOP run with Standard memory expansion, shared media-detail unification, canvas detail-modal wiring, and Kie upload auth hardening |   9.0 | high       | The live tree stayed collapsed to one coherent AI Studio/runtime lane, `docs:check` passed, `gear-ball:preflight` cleared the full touched manifest, 22 focused/fan-out suites passed with 334 tests, and the final production build passed on the converged tree | The only repo-local friction was one small lint seam in `DetailModal`, one dead helper in `referenceDownload`, and one build-only wrapper-type drift after adding `shortpulseRetryAuth401` to the authenticated fetch contract | When adding a new shared fetch-helper option, widen the thin wrapper type in the same first pass and do one tiny formatter/lint seam sweep before the first preflight so the build stays a confirmation rung. |
| 2026-06-02 | Production SOP run with staged transient reference-image uploads, larger project-workspace autosave budgets, and visible Standard Create guardrail reasons |   9.2 | high       | The live tree stayed collapsed to one coherent AI Studio/runtime lane, `docs:check` passed, `gear-ball:preflight` cleared the full touched manifest, 7 focused suites passed with 85 tests, and the final production build passed on the converged tree | The only repo-local friction was one tiny Prettier seam in the new staged image-upload helpers, and the focused workspace-persistence suite still emitted the standing `act(...)` warnings during the pass | On browser-direct upload lanes, run one tiny formatter sweep on the new client upload helper and its tests before the first preflight so the final path stays purely confirmatory. |
| 2026-06-02 | Production SOP run with admin pricing custom rows, AI Studio snapshot freshness/media authority hardening, and Nuclo Kie Vercel env audit docs |   8.9 | high       | The live tree stayed collapsed to one broad product lane plus one compact Nuclo docs lane, `docs:check` passed before batching, `gear-ball:preflight` cleared the 70-file frontend manifest with preview-contract fan-out, 27 focused suites passed with 345 tests, and the final production build passed on the converged tree | The run still paid a small dead-prop/lint cleanup round before preflight and one build-only TypeScript nullability seam in `useAiStudioSessionAutosave.ts` before the final build could close | When a touched hook rewires retry or failure refs, make nullability explicit in the first pass and let one owning hook-suite rerun plus the final build prove the fix instead of rediscovering the seam late. |
| 2026-06-02 | Production SOP run with Standard `gpt-5.5` default promotion, Pulse decision-packet docs, and Create composer focus-restore hardening |   9.4 | high       | The live tree stayed collapsed to one coherent Standard/Pulse runtime lane, `docs:check` passed, `gear-ball:preflight` cleared the 7-file frontend manifest, the locked Standard proof surface plus the changed PromptStep action suite passed with 125 tests, and the final production build passed on the converged tree | No related tails or build-time seams surfaced after the first honest manifest; the only real cost was carrying a slightly broader proof set because the model-policy lane and focus-restore lane shared one publish surface | When this exact Standard model-policy lane repeats, keep reusing the locked proof command plus the owning composer-action suite so the first validation rung stays both narrow and sufficient. |
| 2026-06-02 | Production SOP run with Standard Responses transport gating, first-send composer-shell focus continuity, and isolated admin pricing input layout cleanup |   9.3 | high       | The live tree collapsed honestly into one Standard runtime/docs lane plus one tiny admin CSS lane, `docs:check` passed, `gear-ball:preflight` cleared the 7-file frontend manifest, the targeted Standard route/gateway/panel proof surface passed with 125 tests, and the final production build passed on the exact converged tree | The only real drag was one cheap staging path typo and the fact that an unrelated admin CSS change was live in the worktree, which forced a second tiny commit instead of a one-batch close | When a compact shared-runtime lane is already proven and one unrelated UI-only file remains live, split it early and keep the first staged manifest lane-pure so the commit phase stays one-shot clean. |
| 2026-06-02 | Production SOP run with Enate Ende workspace bootstrap, Standard Responses continuity, and AI Studio video restore authority |   8.7 | high       | The live tree converged into one compact Enate Ende docs lane plus one broad but coherent AI Studio/runtime lane, `docs:check` passed, `gear-ball:preflight` cleared the final 57-file frontend manifest, 26 focused suites passed with 529 tests on the exact committed tree, and the final production build passed on the exact committed tree | The first manifest missed untracked files, the broad preflight lost one formatter-only cycle, and the product close needed one build-only ref-typing fix plus an exact-tree reproof after hook rewrites during commit | Build the first manifest from live `git status --short`, not only tracked diffs, and assume broad hook rewrites on large staged TypeScript batches so untracked files and post-hook exact-tree proof are planned up front instead of discovered late. |
| 2026-06-03 | Production SOP run with Edit workflow rerate docs, Canvas launch planning, Canvas restore/detail proof, and Standard chat dragging |   9.0 | high       | The live tree collapsed cleanly into one docs/rerate lane plus one compact AI Studio/runtime lane, `docs:check` passed, `gear-ball:preflight` cleared the 12-file frontend manifest, 10 focused suites passed with 175 tests on the exact committed tree, and the final production build passed on the exact committed tree | The only real drag was one formatter-only preflight recovery in `AgentChatPanel.tsx`, and the product close still needed one exact-tree reproof because the commit hooks ran `eslint --fix` and `prettier` across the staged TypeScript batch | When a medium TypeScript batch is headed for hook-driven commit cleanup, run one tiny touched-file formatter sweep first and budget the exact-tree reproof up front so the close stays fast without guessing about hook-side diffs. |
