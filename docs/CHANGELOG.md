## Changelog

### Unreleased
- AI Studio: page restructuring (preview wrapped in create-style card, regenerate uses preview prompt, studio preview column hidden for now to give Reference Canvas more width).
- Image Regen: added third secondary dropzone plus per-dropzone clear buttons and header “Clear references”; prevents image drops into prompt preview; dropzones now show hover “×” to clear individually.
- Styling: reference canvas grid gap set to 1px; reference card corners sharpened; canvas scroll area taller; AI Studio-specific styles documented in `styles/workspace-ai-studio.css`.
- Git: rebased and pushed `main` after resolving conflicts with upstream.
