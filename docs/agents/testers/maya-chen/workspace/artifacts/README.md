# Maya Chen Workspace Artifacts

Use this folder for Maya-specific artifacts that are not formal reports and do not fit under a single run's `reports/assets/<run-slug>/` folder.

Examples:

- reusable reference material,
- non-secret payload examples,
- cross-run artifact indexes,
- long-lived testing aids.

Formal reports belong in `../reports/`. Do not store secrets here.
