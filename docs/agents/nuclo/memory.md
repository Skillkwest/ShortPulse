# Nuclo Memory

Purpose: keep repo-visible memory for Nuclo's version, environment, Vercel, and Supabase coordination work.

## Standing Preferences

- Formal name: Nuclo.
- Short name: Nuclo.
- Role: environment and version manager for ShortPulse's pre-launch `production` operating posture, Vercel topology, and Supabase project wiring.
- Default posture: map first, mutate second.
- Historical environment topology reference: `working-development` -> `staging-preview` -> `production`.
- Standing user branch rule: during the current pre-launch phase, work only on `production` unless the user explicitly changes that rule in the current thread.
- Branch safety rule: keep `git config --local shortpulse.allowedBranch` aligned before branch-affecting work.
- Env rule: local env files are convenience surfaces, not deployed source of truth.
- Secret rule: never store or restate raw secret values in memory or reports.
- Supabase rule: use CLI-first hosted targeting and keep environment/database intent explicit.
- Supabase manager rule: full-access Supabase management is allowed for inspection, migrations, parity, and environment work, but deleting auth users or user-owned data is out of bounds.
- Vercel rule: verify preview/production state with direct evidence before recommending cutover.

## Durable Lessons

- 2026-05-08: Nuclo was established as the repo-visible environment/version manager identity with a dedicated contract, retained artifact area, and owned workspace folder.
- 2026-05-08: At Nuclo setup time, the repo was linked locally to Vercel project `shortpulse` through `.vercel/project.json`.
- 2026-05-08: At Nuclo setup time, the repo's local Supabase link pointed to project ref `jwmcytzyhcvacjwqtynn` (`ShortPulse`), while a separate hosted production project ref `ftgrqgjrchpimronuhop` (`ShortPulse – PRODUCTION – Live`) already existed.
- 2026-05-08: A live Vercel environment audit from this shell was blocked because no authenticated `vercel` credentials or `SHORTPULSE_VERCEL_API_TOKEN` were available.
- 2026-05-08: Nuclo should preserve the distinction between repo-visible contract docs, retained artifacts, and the top-level `docs/agents/nuclo/workspace/` workspace. The workspace is useful, but it is not higher authority than canonical docs.
- 2026-05-08: Once live credentials were available, direct inspection showed Vercel branch routing was mostly correct, but `Development`, `Preview`, and `Production` still resolved core Supabase runtime variables to the staging Supabase project.
- 2026-05-08: Direct production runtime inspection showed base URL envs were still pointed at the Vercel default domain instead of `https://www.shortpulse.ai`.
- 2026-05-08: The hosted production Supabase project existed but was not app-ready during live inspection; core app tables, auth users, and storage buckets were not yet present.
- 2026-05-08: Earlier live inspection suggested `production` had stronger GitHub protection posture than `staging-preview`, but this should be treated as historically provisional rather than current truth.
- 2026-05-08: Production Stripe secrets were unresolved or empty in Vercel during the live audit and must be treated as a release gate if live billing depends on them.
- 2026-05-08: Nuclo's current canonical retained plan is `docs/records/artifacts/agent/nuclo/reports/2026-05-08-environment-separation-and-production-cutover-plan.md`.
- 2026-05-08: The approved environment decision is local development plus `working-development` and `staging-preview` sharing staging Supabase, while `production` must move to the dedicated production Supabase project.
- 2026-05-08: Live production bootstrap work proved `docs/supabase_full_schema.sql` is materially behind staging `public` schema. It can seed part of a fresh project, but it is not sufficient for staging-parity bootstrap by itself.
- 2026-05-08: A direct staging-vs-production table diff is the fastest way to measure production bootstrap progress and should be preferred over assumption-based migration guessing during future cutover prep.
- 2026-05-08: Vercel `Production` and GitHub `production` must remain untouched until production database parity is materially closer to staging and the missing-table set is resolved.
- 2026-05-08: A custom-format `pg_dump`/`pg_restore` path succeeded in bringing the dedicated production project's `public` schema, routines, and policies to staging parity after resetting only the production `public` schema and excluding the dump's `CREATE SCHEMA public` entry.
- 2026-05-08: Once schema parity was achieved, direct row-count inspection showed the staging Supabase project is already carrying live runtime data, including auth users, billing profiles, projects, media files, generation history, worker history, and more than ten thousand storage object records.
- 2026-05-08: Because the live production app has been pointing at staging Supabase, production cutover is no longer a `schema/config only` problem. It is now a data migration, auth migration, and storage object migration problem.
- 2026-05-08: Storage bucket definitions can match while storage object blobs remain absent. Nuclo must treat `storage.objects` and actual object copy as separate cutover checks.
- 2026-05-08: Later same-day migration work seeded the dedicated production project with the core live database/auth footprint, including auth users, identities, sessions, refresh tokens, project data, media file data, generation data, and storage object metadata.
- 2026-05-08: A broad row-count comparison after the live-state seed showed only one database-side drift surface remaining: `public.worker_runs`, caused by continued writes on the staging-backed runtime during migration.
- 2026-05-08: The remaining high-risk cutover blocker is storage blob migration. The `media_library` bucket currently represents about `14 GB` of object payload across `10425` objects.
- 2026-05-08: Nuclo now has a dedicated ops toolkit under `scripts/ops/` for Vercel env audits, GitHub environment audits, Supabase schema parity, Supabase row-count drift, and storage metadata parity.
- 2026-05-08: Nuclo now has a canonical environment-ledger template at `docs/agents/nuclo/environment-ledger-template.md`, so future cutovers should start from a reviewed matrix instead of reconstructing fields from chat.
- 2026-05-08: `rclone` is already installed in this environment, and Nuclo now has a wrapper at `scripts/ops/supabase_storage_rclone_sync.sh` so the remaining blob migration can move onto a supported S3-compatible transfer path once S3 access keys exist for both Supabase projects.
- 2026-05-09: Supabase S3 access keys now exist for staging and the dedicated production project in local operator state, enabling supported `rclone` verification for the `media_library` bucket.
- 2026-05-09: Supported S3 verification now proves `media_library` payload parity as of the last completed check: `10425` matching files, `0 differences found`, and matching bucket metadata/object totals from both the S3 and database sides.
- 2026-05-09: A reusable hot-table delta sync helper now exists at `scripts/ops/supabase_hot_table_delta_sync.sh` for watermark-based catch-up of `auth.refresh_tokens`, `public.worker_instances`, `public.worker_runs`, `public.app_error_logs`, `public.app_error_events`, and `public.growth_attribution_identities`.
- 2026-05-09: Exact cutover parity cannot be held indefinitely while the staging-backed runtime is still live. Once storage parity was solved, repeated full row-count diffs showed new drift continuing across media-generation tables (`media_files`, `storage.objects`, `media_events`, `project_media_items`, `generation_publications`) plus active worker/auth tables. This is now a coordinated freeze-window problem, not a bulk migration tooling problem.
- 2026-05-09: A second reusable delta-sync helper now exists at `scripts/ops/supabase_media_generation_delta_sync.sh` for watermark-based catch-up of `public.media_files`, `public.generation_publications`, `public.project_media_items`, and `public.media_events`.
- 2026-05-09: The Supabase ops scripts now auto-detect Homebrew `libpq` at `/opt/homebrew/opt/libpq/bin/psql`, which removes the non-interactive PATH failure that previously blocked some scripted checks in this shell.
- 2026-05-09: The freeze scope for final production cutover is every runtime still pointed at the staging Supabase project, including the current live `https://www.shortpulse.ai` deployment until the Vercel production switch happens.
- 2026-05-09: The canonical freeze execution packet is `docs/records/artifacts/agent/nuclo/reports/2026-05-09-freeze-window-cutover-checklist.md`.
- 2026-05-09: The freeze-window checklist was audited and corrected. The maintenance window is mandatory, not optional, because route flags alone do not stop live writes from customer traffic, billing/webhooks, telemetry, and admin/operator activity while `https://www.shortpulse.ai` still points at the staging Supabase project.
- 2026-05-09: The corrected freeze protocol includes pausing staging-side Supabase Cron jobs after the accepted-job drain, then resuming them only after either successful production stabilization or a verified rollback.
- 2026-05-09: `SHORTPULSE_FAL_ADMISSION_MODE=enforce` is not a valid zero-submit cutover freeze mechanism and must not be treated as the primary stop control in future production cutovers.
- 2026-05-09: An earlier GitHub audit in this shell reported weak or missing release-branch protection, but later direct GitHub API inspection showed the current source of truth is rulesets, not classic branch protection. `production` is covered by active ruleset `Production` (`id=13060916`), and `staging-preview` is now covered by active ruleset `Staging Preview` (`id=16179471`).
- 2026-05-09: The production Supabase cutover is now live. `https://www.shortpulse.ai` points at production deployment `dpl_EE6Q5PWdwoRaqrqJGMdRKFoihwuY`, and that deployment is wired to the dedicated production Supabase project `ftgrqgjrchpimronuhop`.
- 2026-05-09: Final freeze-window parity required expanding `scripts/ops/supabase_hot_table_delta_sync.sh` to also sync `public.ai_credit_reservations` and `public.ai_credit_ledger`, and expanding `scripts/ops/supabase_media_generation_delta_sync.sh` to also sync `public.ai_generations`, `public.generation_attempts`, `public.ai_generation_outputs`, and `public.generation_projection`.
- 2026-05-09: Hosted internal ops route `404` during cutover turned out to be a runtime-flag artifact, not proof that the routes were missing from the deployment. The route files are identical across `origin/production`, `origin/staging-preview`, and `origin/working-development`, and after restoring the intended route flags plus `SHORTPULSE_MEDIA_DERIVATIVES_CRON_SECRET`, all three hosted internal routes returned `401 Unauthorized` as expected.
- 2026-05-09: During cutover, production provider keys and customer-facing upload/reconciler runtime flags had to be restored from local operator state in `frontend/.env.local` after the temporary freeze values had been applied earlier in the maintenance window.
- 2026-05-09: The hosted-route recovery pass also restored `SHORTPULSE_MEDIA_DERIVATIVES_ENABLED`, `SHORTPULSE_USER_HEALTH_FLEET_ENABLED`, and `SHORTPULSE_MEDIA_DERIVATIVES_CRON_SECRET` on production so those internal operator routes reached the expected protected state.
- 2026-05-09: Production still has active internal comp contracts, so leaving the internal billing renewals worker disabled would have created a future silent failure. The route was restored by enabling `SHORTPULSE_INTERNAL_BILLING_RENEWALS_ENABLED`, seeding `SHORTPULSE_INTERNAL_BILLING_RENEWALS_CRON_SECRET` from operator `CRON_SECRET`, and redeploying until `/api/internal/billing-contract-renewals/run` returned `401 Unauthorized`.
- 2026-05-09: Production cleanup removed the stale duplicate Supabase env names from Vercel Production (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SECRET_KEY`, `SUPABASE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_JWT_SECRET`), leaving the canonical runtime keys as the active Supabase contract surface.
- 2026-05-09: `scripts/verify_deployment_route_parity.mjs` now supports tokenless operation through authenticated `vercel` CLI state. `SHORTPULSE_VERCEL_API_TOKEN` and `VERCEL_API_TOKEN` remain optional overrides, not hard requirements.
- 2026-05-09: The current post-cutover operator priority is credential rotation for the Supabase staging/production `sb_` keys, staging/production DB passwords, GitHub `SUPABASE_DB_URL` consumers, and the Vercel protection bypass secret that were exposed in chat or screenshots during the cutover lane.
- 2026-05-09: The canonical rotation SOP is now `docs/sops/sop_secret_exposure_rotation.md`, and the ShortPulse-specific execution packet for this incident is `docs/records/artifacts/agent/nuclo/reports/2026-05-09-post-cutover-secret-rotation-runbook.md`.
- 2026-05-09: `staging-preview` governance is now hardened with required checks `Vercel` and `Supabase Preview` on ruleset `Staging Preview` (`id=16179471`), bringing the staging integration branch onto a real required-check contract instead of review-only protection.
- 2026-05-09: `scripts/ops/github_env_audit.sh` now inspects `repos/<repo>/rules/branches/<branch>` so ruleset-based protection and required checks are reported correctly instead of being understated as `protected=false`.
- 2026-05-09: The deferred secret-rotation closeout path must validate staging-facing consumers as well as production. `scripts/ops/secret_rotation_validate.sh` now audits `development`, `preview`, and `production`, requires an explicit staging preview URL unless preview parity is intentionally skipped, and probes the full restored production internal-route set.
- 2026-05-10: The correct live branch-to-runtime database mapping is restored: Vercel `Development` and `Preview` now resolve staging Supabase `jwmcytzyhcvacjwqtynn`, while Vercel `Production` resolves production Supabase `ftgrqgjrchpimronuhop`.
- 2026-05-10: `scripts/check_vercel_env_contract.mjs` and `scripts/ops/vercel_env_audit.sh` now support authenticated `vercel` CLI sessions without requiring a token, and `scripts/ops/supabase_public_schema_parity.sh` is repaired and usable again.
- 2026-05-10: Production schema drift relative to staging was closed by applying the missing `117`, `119`, and `121` migration effects to production, restoring `create_pulse_builtin_runtime`, `agent_prompt_runtime`, and the QuickSwap media-integrity function/trigger set.
- 2026-05-10: Nuclo’s standing Supabase manager contract is now explicit: schema and hosted-database operations are in scope, but deleting auth users or user-owned data is not.
- 2026-05-10: The remaining Vercel contract-warning cleanup is complete. Live `vercel env` inventory now shows only the canonical Supabase env keys (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`) across development, preview, and production, the earlier stale warning-flag rows are gone, and `scripts/ops/vercel_env_audit.sh --environment development --environment preview --environment production` now passes cleanly with no warning output.
- 2026-05-10: Nuclo now has a fuller standing operator layer for future Supabase/Vercel work: hosted Supabase migration apply/validation, Vercel env repair, production smoke testing, the destructive-data guard, and a SQL operator index are now documented so future runs can start from repeatable procedures instead of reconstructing them from memory.
- 2026-05-12: A dedicated working-development Supabase project now exists alongside staging and production, and its local operator credentials are stored in the gitignored root `.env.agent.local` file rather than a repo-tracked secret note.
- 2026-05-12: The gitignored root `.env.agent.local` file now also carries namespaced staging and production Supabase operator credentials, including canonical project refs, host URLs, server/client keys, pooler URLs, and the staging GitHub DB secret form.
- 2026-05-12: The dedicated working-development Supabase project `bgdhqbenqltxildlgkyu` is now bootstrapped to staging parity for the full `public` schema, routines, and policies without cloning staging user-owned runtime data into development.
- 2026-05-12: Vercel `Development` and local `frontend/.env.local` now resolve the dedicated working-development Supabase project, while Vercel `Preview` remains on staging and Vercel `Production` remains on production.
- 2026-05-12: Future Vercel contract audits must enforce development-vs-preview separation as well as preview-vs-production separation for Supabase/base-URL keys.
- 2026-05-12: `supabase projects list` plus `supabase/.temp/project-ref` are the stronger local evidence for the currently linked hosted project. `supabase/.temp/linked-project.json` can lag and should be treated as advisory cache, not canonical truth.
- 2026-05-13: Schema parity alone was not enough for the dedicated working-development bootstrap. The local generation worker failed until staging's `public` ACL posture was synced into development, because schema-only restore had left `service_role` and function execute grants behind.
- 2026-05-13: `scripts/ops/supabase_public_acl_sync.sh` is now the reusable fix for hosted schema-only bootstrap drift. Use it after staging-to-dev or staging-to-production schema restores when runtime workers or RPCs show permission errors despite passing schema parity.
- 2026-05-13: The initial dedicated working-development bootstrap missed at least one critical non-`public` object: staging had custom trigger `on_auth_user_created_billing_setup` on `auth.users`, while dev initially did not. Fresh dev signups therefore skipped billing/bootstrap row creation until the trigger was restored and the affected user was backfilled.
- 2026-05-13: A fresh working-development user with zero `projects` rows will hit AI Studio `Project unavailable / Project not found` if the route carries a stale or foreign `projectId`. This is partly expected caller-owned lookup behavior and partly a UX recovery gap, because the AI Studio error shell does not expose in-place project creation or project selection.
- 2026-05-13: Restoring `sql/migrate_new_user_plan_default_to_free.sql` against the dedicated dev project repaired the missing `auth.users.on_auth_user_created_billing_setup` attachment. A fresh smoke-test signup on dev now automatically gets `billing_profiles`, `ai_credit_balance`, and the `signup_seed` ledger baseline again.
- 2026-05-13: Fresh working-development signup does not auto-create a project. Current evidence points to manual first-project creation as the live contract, with the real product issue being stale-project recovery rather than missing auto-project bootstrap.
- 2026-05-13: AI Studio recovery is now less brittle on `working-development`: the gated `Project unavailable` shell exposes `Open projects`, keeps `ProjectsModal` mounted while project bootstrap is gated, and dashboard-mounted `ProjectsModal` now passes its create-project callback.
- 2026-05-13: The first full per-user staging-to-dev continuity seed for `kirkartman00@gmail.com` proved the branch/code work was intact and the real drift was data isolation. The dedicated dev project now contains the staging project rows for that user, including the old `15f9d3da-02d0-4169-95da-b738042af999` project id, while staging-specific deep links only fail locally when their referenced storage objects have not been copied yet.
- 2026-05-13: Staging user data can contain narrow legacy contamination that dev integrity triggers reject. The first continuity seed found two `character_quick_swap_items` rows pointing at `character_media_assets.asset_kind='sheet_slot'` instead of `quickswap`; the staging-to-dev seed helper now skips those invalid legacy rows instead of failing the whole copy.
- 2026-05-14: Staging was still behind working-development on migration `123`: `public.generation_projection` was missing `companion_art_status`, `companion_art_storage_path`, `companion_art_attempt_count`, and index `ix_generation_projection_companion_art_pending`. Applying `sql/migrations/123_add_audio_companion_art_projection_fields.sql` to staging closed that drift.
- 2026-05-14: `scripts/ops/supabase_public_schema_parity.sh` now checks columns and indexes in addition to tables, routines, and policies. This was required because the earlier name-only parity pass falsely reported dev/staging parity even while staging was missing migration `123` column/index objects.
- 2026-05-14: `scripts/check_vercel_env_contract.mjs` no longer carries the default preview branch into development-only audits. `node scripts/check_vercel_env_contract.mjs --environment development` now passes cleanly instead of false-failing with `--git-branch can only be used when auditing preview`.
- 2026-05-14: During the earlier ladder-era phase, Nuclo contract and memory were updated to operate only on `working-development`. Treat that instruction as historical context only; it is superseded by the 2026-05-23 pre-launch `production`-only rule.
- 2026-05-23: The current pre-launch operating posture overrides Nuclo's earlier ladder-era local branch default. Until the user explicitly changes the rule again, Nuclo should operate only on `production`.

## Open Follow-Ups

- Decide whether hosted development should remain local-only or eventually become a separate hosted Vercel project.
- Rotate the credentials exposed in chat after the environment transition is considered stable.
