# Phase-5 Retained Namespace

Purpose: retain historical phase-5 rollout inputs that still need to stay discoverable for audit or traceability.

## Status

This is a retained historical namespace, not an active agent contract.

Do not treat `phase-5/` as a current operating lane unless the task explicitly requires historical rollout reconstruction or evidence review.

## Current Use

- keep legacy phase-5 snapshot inputs or retained artifacts that would lose context if they were moved ad hoc,
- preserve traceability for older rollout packets that are still referenced elsewhere.

New active agent work should use the current contract and artifact surfaces under `docs/agents/` and `docs/records/artifacts/agent/`.
