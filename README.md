# ShortPulse v1

Short-form analytics and creative workspace surfaces built on Next.js with Supabase auth/storage and internal API routes for provider proxying, billing, uploads, and runtime orchestration.

## Tech

- Frontend: Next.js (pages router), Phosphor icons, modular CSS.
- Auth/storage: Supabase client with persisted sessions, `saved_creators` table, and a private `media_library` bucket.
- Analytics: In-browser scoring of a demo cohort with user-triggered refresh and filtering controls.
- AI Studio providers: Next.js API routes under `/api/fal/*` proxy active provider submit/status requests (server-side keys required). Image/edit flows still use Fal-backed families such as Seedream, Nano Banana, FLUX Fill, and Bria background remove. Public AI Studio video generation is Kie-only through `/api/fal/kie-veo-submit`, `/api/fal/kie-veo-status`, `/api/fal/kie-kling-submit`, `/api/fal/kie-kling-status`, `/api/fal/kie-seedance-submit`, `/api/fal/kie-seedance-status`, `/api/fal/kie-seedance-2-submit`, `/api/fal/kie-seedance-2-status`, `/api/fal/kie-seedance-2-fast-submit`, and `/api/fal/kie-seedance-2-fast-status`.
- AI Studio audio generation: authenticated ElevenLabs routes under `/api/elevenlabs/*` now back the Sound workflows. `/api/elevenlabs/voices` loads live provider voices and merges in the user-scoped saved-voice cache from Supabase, `DELETE /api/elevenlabs/voices/:voiceId` removes a selected voice from the AI Studio library by clearing saved-voice persistence and deleting the upstream provider voice when applicable, `/api/elevenlabs/text-to-voice/design` and `/api/elevenlabs/text-to-voice/create` power the `Create New Voice` Voice Design modal while persisting created voices into `user_preferences.ai_studio_saved_voices`, `/api/elevenlabs/text-to-speech` generates voiceover audio, `/api/upload-video` now serves as the stable local-video staging adapter for Voice Changer intake, `/api/media/stage-voice-changer-source` stages local Voice Changer audio files through an authenticated server path, `/api/media/extract-audio` stages extracted WAV audio from trusted stored or signed video references for Voice Changer intake and rejects source videos above the staged extraction ceiling, `/api/elevenlabs/speech-to-speech` generates the final Voice Changer output from a staged `sourceStoragePath`, trusted `sourceUrl`, or compatibility local upload and now also persists a sibling remuxed video output whenever that staged audio came from a video source, `/api/elevenlabs/music` generates prompt-first music tracks from the Music panel controls, and `/api/elevenlabs/sound-effects` generates Sound Effects audio from prompt-driven SFX controls. Those routes now honor `user_preferences.media_autosave_enabled`: when autosave is OFF they still return playable in-session outputs but skip background `media_files` creation.
- AI Studio session persistence APIs: authenticated save/get/list routes under `/api/ai/sessions/*` provide server-side durable snapshot persistence (`POST /api/ai/sessions/save`, `GET /api/ai/sessions/:sid`, `GET /api/ai/sessions?limit=&cursor=`) behind `SHORTPULSE_AI_STUDIO_SESSIONS_API_ENABLED` (default enabled). Client session persistence stays default-off behind `NEXT_PUBLIC_AI_STUDIO_SESSION_PERSISTENCE_ENABLED` and then uses scoped controls (`NEXT_PUBLIC_AI_STUDIO_SESSION_WRITE_SHADOW_ENABLED`, `NEXT_PUBLIC_AI_STUDIO_SESSION_REMOTE_SHADOW_ENABLED`, `NEXT_PUBLIC_AI_STUDIO_SESSION_RESTORE_SHADOW_ENABLED`, `NEXT_PUBLIC_AI_STUDIO_SESSION_RESTORE_APPLY_ENABLED`, `NEXT_PUBLIC_AI_STUDIO_SESSION_RESTORE_APPLY_AGENT_ENABLED`) so disabled mode is applied consistently across write, restore, and session-list paths. Current workspace snapshots now include Expert Create Pulse mode, the active pinned Pulse preset id, and an authoritative Pulse workflow session snapshot (`status`, current step, collected inputs, prompt preview, completed artifact, completion source) so guided `workflow_gpt` Pulses can restore with more than shell state alone.
- Projects foundation APIs: authenticated project collection/item/workspace routes under `/api/projects*` now back the dashboard `New Project`, saved-project cards, AI Studio project-title authority, project-specific Media Library custom folders, and the project-owned workspace snapshot seam. `POST /api/projects/create` creates a user-owned `projects` row in Supabase, `GET /api/projects?limit=` lists recent caller-owned projects for the dashboard, `GET|PATCH /api/projects/:projectId` reads or renames one owned project, `GET|PUT /api/projects/:projectId/workspace` loads or saves one caller-owned workspace snapshot, and `/api/projects/:projectId/media/folders/*` owns project-scoped folder CRUD plus membership assignment for saved media and prompts. AI Studio still coexists with legacy `sid`, but `projectId` now resolves the owned project record before restore proceeds, routes visible Media Library project-title edits through `projects.title`, and switches project routes onto project-owned workspace persistence instead of the legacy remote `sid` snapshot path. Saved media and prompts created from a project route now also attach to that active project through project association tables while the global Media Library inventory remains user-scoped, project workspace saves backfill those associations from restore-relevant ids in the snapshot as a server-side safety net, project workspace reads now refresh generated-output delivery only from generation rows explicitly associated to that project, and the Media Library custom-folder area now scopes to the active project while `All Media` remains global. See `docs/sops/sop_ai_studio_projects_foundation.md` for the current shipped Projects contract and migration boundary.
- AI Studio style extraction helper route: authenticated `POST /api/ai/extract-style` extracts reusable style descriptors plus a creative style title from uploaded/dropped images used in Styles Library create flows, accepts the derived base64 `imageDataUrl` directly, and uses the structured OpenAI vision lane with server-owned retry/fallback handling.
- AI Studio safety-control runtime knobs are centrally gated: `STUDIO_AGENT_SAFETY_PROFILE_ACTIVE` (default `prod_safe_v1`), `STUDIO_AGENT_SAFETY_INPUT_PRECHECK_ENABLED` (default true, server pre-provider gate for `/api/ai/studio-agent`), `STUDIO_AGENT_SAFETY_INPUT_PRECHECK_GENERATE_PROMPT_ENABLED` (default true), `STUDIO_AGENT_SAFETY_INPUT_PRECHECK_GENERATION_SUBMIT_ENABLED` (default true), `STUDIO_AGENT_SAFETY_IMAGE_PREFLIGHT_ENABLED` (default true), `STUDIO_AGENT_SAFETY_IMAGE_PREFLIGHT_FAIL_MODE` (default `prod_closed_nonprod_open`), shared/scoped field-mode overrides (`STUDIO_AGENT_SAFETY_INPUT_PRECHECK_FIELD_MODES`, `STUDIO_AGENT_SAFETY_INPUT_PRECHECK_FIELD_MODES_STUDIO_AGENT`, `STUDIO_AGENT_SAFETY_INPUT_PRECHECK_FIELD_MODES_GENERATE_PROMPT`, `STUDIO_AGENT_SAFETY_INPUT_PRECHECK_FIELD_MODES_GENERATION_SUBMIT`), client mirrors (`NEXT_PUBLIC_STUDIO_AGENT_SAFETY_INPUT_PRECHECK_ENABLED`, `NEXT_PUBLIC_STUDIO_AGENT_SAFETY_INPUT_PRECHECK_FIELD_MODES`, `NEXT_PUBLIC_STUDIO_AGENT_SAFETY_INPUT_PRECHECK_FIELD_MODES_STUDIO_AGENT`), `STUDIO_AGENT_SAFETY_POSTPROCESS_MODE` (default `enforce`), `STUDIO_AGENT_SAFETY_DEV_ABSOLUTE_ZERO_ENABLED` (default false), `STUDIO_AGENT_SAFETY_PROVIDER_ERROR_MODE` (default `production_normalized`), `STUDIO_AGENT_SAFETY_AUTOROLLBACK_ENABLED` (default false), `STUDIO_AGENT_SAFETY_ROLLBACK_COOLDOWN_HOURS` (bounded `1..168`, default `24`), and control-plane runtime sync controls `STUDIO_AGENT_SAFETY_RUNTIME_CONTROL_PLANE_SYNC_ENABLED` (default true) plus `STUDIO_AGENT_SAFETY_RUNTIME_CONTROL_PLANE_CACHE_TTL_MS` (bounded `1000..60000`, default `5000`).
- Kie provider routing is mixed-mode: Veo remains dark-path/runtime-gated, while active Kie video lanes (`kie-ai/kling-3.0`, `kie-ai/seedance-1.5-pro`, `kie-ai/seedance-2`, `kie-ai/seedance-2-fast`) use the always-on Kie runtime path when `KIE_API_KEY` is configured.
- Fal runtime v2 operational routes: `/api/fal/webhook` (signature-verified webhook ingestion), `/api/fal/queue-status` (authenticated read-only queued-submit status handoff that now distinguishes `queued` vs `dispatching` pre-provider phases), and `/api/internal/generation-recovery/run` (cron-secret protected reconciler + queue dispatcher trigger).
- Local generation worker helper: `npm -C frontend run dev:generation-worker` runs the shared generation control-plane loop directly in local development, writes a heartbeat under `frontend/.tmp/`, and is required for localhost queue mode to drain.
- Media derivative operations route: `/api/internal/media-derivatives/run` (cron-secret protected image-derivative worker that claims `media_files` image rows, generates local `sharp` derivatives, and writes `thumb_*` variants under `media_asset_variants`).
- Queue/recovery scheduling is externalized: configure Supabase Cron to call `/api/internal/generation-recovery/run` every minute via `sql/configure_generation_recovery_scheduler_supabase.sql`; keep `SHORTPULSE_FAL_RECONCILER_CRON_SECRET` configured for auth.
- Local AI Studio media uploads: `/api/upload-image` and `/api/upload-video` are compatibility adapters for transient reference uploads. They reuse the shared server-authoritative upload validation/storage path, preserve legacy `{ url, path, size }` responses, and emit usage telemetry for sunset review.
- Media Library server-authoritative uploads: `/api/media/upload` validates destination + file signature server-side, writes user-scoped storage paths, persists `media_files` rows, and returns signed preview metadata from the canonical derivative-first path (flagged by `SHORTPULSE_MEDIA_UPLOAD_API_ENABLED`).
- Media Library server-authoritative URL copy fallback: `/api/media/copy-from-url` performs authenticated trusted-host remote fetch + storage persistence when browser-side media fetch is blocked (CORS/security/network families).
- Media preview signing: `/api/media/sign-batch` signs user-scoped authoritative preview paths in a single authenticated request with surface-aware preview-profile headers (route/modal/panel). Transform-backed delivery is compatibility-only and disabled by default unless both `SHORTPULSE_MEDIA_SIGNED_TRANSFORMS_ENABLED=true` and `NEXT_PUBLIC_MEDIA_SIGNED_TRANSFORMS_ENABLED=true`.
- AI Studio Media Library folder APIs: legacy user-scoped routes under `/api/media/folders/list|create|rename|delete|membership-batch` still back non-project surfaces, while project routes now use `/api/projects/:projectId/media/folders/list|create|rename|move|delete|membership-batch` for project-scoped folder CRUD and membership assign/unassign/move operations. `/api/media/list` and `/api/media/prompts/list` now accept optional `projectId` on folder-scoped requests so project folders resolve through project membership tables while `all_items` remains the global root inventory. Folder-canvas persistence APIs are still `/api/ai/media-folder-canvas/[folderId]` (load) and `/api/ai/media-folder-canvas/save` (save) for the legacy user-scoped folder canvas lane.
- Billing/credits: Supabase-backed plan/profile/credit ledger model with Stripe-ready checkout, portal, webhook routes, and authenticated credit snapshot reads at `/api/credits/snapshot` (available + pending reservation holds).
- Admin billing controls: `/api/admin/billing/contracts/update` grants or revokes non-public payment-exempt recurring access, `/api/admin/billing/portal` opens Stripe billing for the selected user from the admin console, and `/api/internal/billing-contract-renewals/run` renews monthly credits for `internal_comp` contracts on a cron-secret protected internal path.
- Internal comp renewal scheduling is externalized: configure Supabase Cron to call `/api/internal/billing-contract-renewals/run` hourly via `sql/configure_internal_billing_renewal_scheduler_supabase.sql`; keep `SHORTPULSE_INTERNAL_BILLING_RENEWALS_ENABLED=true` and align `SHORTPULSE_INTERNAL_BILLING_RENEWALS_CRON_SECRET` with the Vault secret used by the scheduler.
- Ops telemetry: authenticated app/runtime failures can be ingested at `/api/log/client-error`, viewed as grouped incidents via `/api/admin/errors`, and inspected as raw occurrences via `/api/admin/error-events`; admins can update one (`/api/admin/errors-status`) or many (`/api/admin/errors-status-bulk`) incident statuses and smoke-test visibility via `/api/admin/errors-test`.
- Admin access gating: `/api/admin/access` provides lightweight server-authoritative admin access checks so admin pages do not depend on `/api/admin/users` list fetches for authorization gating.
- Admin billing diagnostics: `/api/admin/billing-diagnostics?userId=<uuid>` returns the selected user’s current billing profile, subscriber contract, linked/public offer snapshots, live Stripe subscription reconciliation when server credentials are present, and drift findings for grandfathered-pricing/support investigations.
- Billing contract verification script: `npm -C frontend run billing:contracts:verify -- --limit 25` compares local billing profiles/contracts against live Stripe subscription pricing and can fail non-zero with `--strict`.
- Admin fleet health automation: `/api/internal/admin-user-health-fleet/run` runs a bounded active-user fleet scan (cron-secret protected), persists compact snapshots/findings, and optionally emits report-only incidents (`source=ops.user_health_fleet`).
- Admin fleet health report API: `/api/admin/user-health-fleet` returns latest (or selected) fleet run summaries, findings, and paginated user snapshots with filter/search support.
- Dashboard announcements: authenticated users read the active global dashboard bulletin via `/api/announcements/active`; admins manage current state with `/api/admin/announcements/current|publish|clear`.
- Agent safety control-plane admin APIs: `/api/admin/agent-safety-policy/active`, `/api/admin/agent-safety-policy/activate`, `/api/admin/agent-safety-policy/rollback`, and `/api/admin/agent-safety-policy/version` provide authenticated profile activation/rollback/version controls with cooldown-aware rollback safety.
- Optional alert tuning: set `SHORTPULSE_ADMIN_ALERT_TOTAL_15M`, `SHORTPULSE_ADMIN_ALERT_HIGH_15M`, and `SHORTPULSE_ADMIN_ALERT_GENERATION_15M` to control Admin event-spike thresholds; set `SHORTPULSE_ADMIN_ALERT_USER_HEALTH_FLEET_CRITICAL_RISK` and `SHORTPULSE_ADMIN_ALERT_USER_HEALTH_FLEET_WARNING_COST_WITHOUT_SUCCESS_CENTS` for fleet report-only incident escalation thresholds.

## Setup

1. Copy `frontend/.env.example` to `frontend/.env.local` (or export the values in your shell) and set:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `FAL_KEY`
   - `OPENAI_API_KEY`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `STRIPE_SECRET_KEY`
   - `STRIPE_WEBHOOK_SECRET`
   - optional provider key for Kie routes: `KIE_API_KEY` (or `SHORTPULSE_KIE_API_KEY`)
   - for AI Studio save/restore testing, set `NEXT_PUBLIC_AI_STUDIO_SESSION_PERSISTENCE_ENABLED=true`
2. Install and run the frontend:
   ```bash
   cd frontend
   npm install
   npm run dev
   ```
   This starts the Next.js app, including server API routes under `frontend/pages/api/*`.
   If local queue mode is enabled (`SHORTPULSE_FAL_QUEUE_ENABLED=true` and `APP_BASE_URL=http://localhost:3000`), run the generation worker in a second terminal:
   ```bash
   cd frontend
   npm run dev:generation-worker
   ```

## Optional Supabase bootstrap

- Supabase tooling policy: use Supabase CLI for Supabase access and avoid Docker-based local Supabase workflows (`supabase start/stop`, `supabase db reset --local`, `supabase db lint --local`, or direct `docker` commands).
- Saved creators: run `sql/create_saved_creators_table.sql` to add the `saved_creators` table with RLS.
- Media library: run `sql/storage_policies.sql` to create the private `media_library` bucket and folder-scoped storage policies. The UI also expects a `media_files` table (see `docs/supabase_full_schema.sql` for a combined script).
- Private tab support: run `sql/migrations/003_add_private_media_source.sql` and `sql/migrations/004_add_private_media_integrity_checks.sql` to enforce `media_files.source` values and private path/file-type integrity.
- Billing + credits: run `sql/create_billing_credit_tables.sql` to provision plans, credit packages, billing profiles, ledger, and signup allocation triggers.
- Legacy billing environments: run `sql/migrate_ai_credit_ledger_legacy_to_v2.sql` to add `source/source_ref/metadata/created_by` columns and compatibility triggers before using `/admin` credit adjustments.
- App error telemetry: run `sql/create_app_error_logs_table.sql` and `sql/migrations/015_add_app_error_events.sql` to provision grouped incidents plus immutable per-occurrence events.
- SQL migration operations SOP: `docs/sops/sop_sql_migration_operations.md` (canonical run order, drift/repair loop, and common SQL error fixes).

## Manual data actions

- Performance Analytics (`/performance`) provides a demo refresh + filtering workflow for the sample dataset.
- Root entry (`/`) now routes users through `/dashboard` (unauthenticated users are redirected to `/auth` by the dashboard guard).
- Saved Creators and Media Library actions write/read directly through the Supabase client from the frontend (Saved Creators is post‑MVP).

## Frontend surfaces

- **Dashboard (`/dashboard`)**: Launchpad with plan/status chips, a `New Project` quick action, and a recent-projects surface that opens saved projects into AI Studio using `?projectId=<uuid>`. `New Project` creates a real user-owned `projects` row in Supabase before routing into AI Studio. The hero helper slot renders an active global announcement when one exists and fails soft to default helper copy otherwise. Set `NEXT_PUBLIC_DASHBOARD_HIDE_LEGACY_SECTIONS=false` to temporarily restore legacy quick-start/workflow cards, tools grid, footer helper text, and Searches header metric during redesign work.
- **Performance Analytics (`/performance`)**: Authenticated demo analytics surface (staged rollout; dashboard currently points to `/performance-soon`).
- **Performance Placeholder (`/performance-soon`)**: Temporary landing page that explains the analytics workspace is still under construction.
- **Saved Creators (`/saved-creators`)**: Post‑MVP (Coming Soon); per-user handle list.
- **Media Library (`/media-library`)**: Upload/download/delete/rename/move files across media tabs in a private Supabase bucket, including a Private image tab (`<auth.uid()>/private/images/...`).
- **Profile (`/profile`)**: Profile/account/billing UI with plan badges, Stripe billing actions, credit purchase entry points, and an AI Studio media autosave account setting.
- **AI Studio (`/ai-studio`)**: Creative canvas for prompt systems, model/aspect selection, previewing, and saving image/video outputs. Session identity uses `?sid=<uuid>`; plain `/ai-studio` creates a new session id. The dashboard `New Project` path now also forwards `?projectId=<uuid>` so Projects and session identity can coexist during the migration off legacy session-owned persistence. When `projectId` is present, AI Studio resolves the owned project record before restore continues, fails closed on invalid/cross-user project ids, treats the visible Media Library project title as `projects.title` authority rather than session-title local state, and reads/writes workspace snapshots through `/api/projects/:projectId/workspace` instead of the legacy remote `sid` snapshot lane. Browser-global workflow settings, chat-mode local storage, and selected-character local storage are suppressed on project routes so project-owned restore becomes the authoritative workspace path for the current snapshot envelope. Saved media and prompts created from project routes now attach to the active project through project association tables without changing the global Media Library inventory, and workspace save/read now also maintain project-owned generated-output association so reopen refreshes only project-associated generated outputs instead of scanning user-global generation state. Generated-output hydration is also suppressed on project routes so fresh projects do not inherit user-global output state. Project-scoped folder ownership and broader generated-output authority cleanup are still follow-up phases. Beginner mode is temporarily force-disabled and toggle controls are hidden by runtime policy flags. Expert Edit is now the only Edit experience. Set `NEXT_PUBLIC_AI_STUDIO_MARKUP_MODEL_LOCK_ENABLED=true` to lock Markup tool model branding/submission to Pulse Markup v1 (disabled by default). Set `NEXT_PUBLIC_AI_STUDIO_MARKUP_COLLAPSED_OPEN_MODAL_ENABLED=true` to prototype collapsed `Tools` opening expanded Markup modal directly; closing modal re-collapses tools (disabled by default). Set `NEXT_PUBLIC_AI_STUDIO_MARKUP_STROKE_SECONDARY_REFERENCE_ENABLED=true` to attach a second Markup-only reference input containing flattened layers plus markup strokes when strokes are present (disabled by default). Set `NEXT_PUBLIC_AI_STUDIO_EDIT_GENERATION_MODE_TOGGLE_ENABLED=true` to show a selector-row Standard/Inpaint/Markup generation mode toggle (disabled by default). The right-rail Reference Grid now accepts local image, video, and audio files from the file picker or drag-and-drop, and renders audio uploads as playable reference cards. Expert Edit prompt supports direct references to the primary drop zone via `@main` plus secondary-slot references via `@img1`, `@img2`, and `@img3`, including drag-to-insert from the secondary reference slots and an anchored picker that opens from bare `@` or `Tab`, shows the primary tile, cycles with `Tab`, and inserts the selected token on `Enter`. When `NEXT_PUBLIC_STUDIO_AGENT_DIRECT_OPENAI_BYPASS_ENABLED=true`, the Create chat lane defaults to the backend-gated direct OpenAI bypass and can forward staged image attachments as multimodal input so users can ask for image descriptions that return generation-ready prompts.
  The top-right AI Studio header keeps a visible `Canvas` toggle plus `Quick Slot Inventory` and `Reference Grid` switches available across the four primary workflows (`Create`, `Edit`, `Video`, `Sound`).
  Create chat image attachments are now displayed on the matching sent user message bubble after send instead of remaining pinned in the prompt composer while the turn is in flight.
  The primary `Sound` button now lands on `Voice` by default. `Sound -> Voice` opens the dedicated Voices workflow panel with its own layout shell, top voice library, bottom script composer, and right-rail `Voiceover` / `Voice Changer` controls independent from the generic Sound panel.
  `Sound -> Music` now opens a dedicated Music workflow panel with its own split music-library/composer layout and music-specific composition, arrangement, energy, and output controls, and submits prompt-first generation through authenticated ElevenLabs wiring into the Reference Grid audio flow.
  Dropping local audio or video into `Sound -> Voice -> Voice Changer` now stages the source into private storage immediately, but the transport differs by source type: local video uses `POST /api/upload-video` so the original clip is staged through the repo's stable storage-only adapter before audio extraction, while local audio uses `POST /api/media/stage-voice-changer-source`. Video drops then extract a server-side WAV vocal sample first and replace the staged source with that derived audio before `Generate` is enabled. Video-derived voice changer runs now hard-lock to the current ElevenLabs default speech-to-speech voice settings (`stability: 1`, `similarity_boost: 1`, `speed: 1`, `use_speaker_boost: true`) and hide the right-rail Voice Shaping sliders while that source is active. When the staged source originated from video, final generation also remuxes the converted voice back onto the original video and inserts that derived video into the Reference Grid alongside the converted audio. Voice Changer extraction rejects staged videos above the extraction size ceiling with a user-facing retry message instead of failing later inside generation.
  `Generate` inside `Sound -> Voice` now calls the authenticated ElevenLabs routes and inserts the returned audio into the Reference Grid as a playable audio result card.
  `Sound -> Sound Effects` opens a dedicated Sound Effects workflow panel with its own split library/composer layout, prompt-only generation lane, and ElevenLabs-backed `Duration`, `Loop`, `Prompt Influence`, and `Format` controls.
  `Generate` inside `Sound -> Sound Effects` now calls the authenticated ElevenLabs Sound Effects route and inserts the returned audio into the Reference Grid as a playable audio result card.
  In Expert Edit, pressing `Delete` while a layer is selected now removes that selected layer, provided focus is not inside an editable text field.
  Expert Edit now uses a full properties-panel wrapper surface around the entire Edit panel body so the stage and control rails sit inside the same primary background container as Expert Create.
  The inline Expert Edit `Layers` panel now lives in the left rail directly below `Prompt Presets`, while layer functionality remains active through the stage selection model, keyboard delete, and other existing edit controls.
  The inline Expert Edit `Prompt Presets` title now renders inside the same card as the presets list instead of a separate title card above it.
  Double-clicking image/video references in the Reference Grid or Quick Slot Inventory opens the detail modal, where `Save` persists that media into `All Media` in the Media Library and sits to the left of `Download`.
  The generic Canvas workspace is no longer exposed as a primary AI Studio toolbar tool, but the top-right `Canvas` header toggle restores the shared right-rail Canvas section across the four primary workflows (`Create`, `Edit`, `Video`, `Sound`) alongside Quick Slot Inventory and Reference Grid. The Canvas section now defaults off on first load. Media Library folder canvases remain separate secondary surfaces. The AI Studio Character properties panel now lands on `Character Profile` first and serves as the primary character-management surface; the standalone `/character` route remains the secondary entrypoint. The embedded Character panel keeps leaner QuickSwap chrome than `/character` by omitting the legacy helper paragraph and hiding the collapse toggle in expert mode.
  `Libraries -> Media Library` opens a primary left-panel Media Library surface (tool id `media-library`) with root/custom folder organization and media/prompt browse/add flows. `All Media` remains the global user inventory and the root surface renders inline tabs (`All Media`, `Images`, `Videos`, `Audio`, `Prompts`) within the same root folder. The aggregate `All Media` tab shows saved media cards plus saved prompt cards in one mixed masonry feed, `Images`/`Videos` preserve true aspect ratio, `Prompts` uses text cards, and `Audio` remains a reserved placeholder. Near-bottom infinite scroll auto-load remains on media tabs with one global root media paginator footer retained as manual fallback. On project routes, the visible project title and the custom-folder area above `All Media` are project-scoped: folder CRUD and folder membership are isolated to the active `projectId`, and switching to a different project does not carry custom folders across. Folder-scoped media/prompt listings on project routes resolve through project membership tables while `All Media` remains global. Gesture runtime (`NEXT_PUBLIC_AI_STUDIO_MEDIA_LIBRARY_GESTURE_V2_ENABLED`) enables custom drag ghosts, right-click media ingest from `All Media`, and root permanent delete actions. Double-clicking `All Media` image/video cards opens a preview-only detail modal (no ingest side effects). The root saved-count row expand affordance now toggles into a collapse affordance while expanded and restores the prior shell width plus folder/reference split when collapsed. Legacy custom-folder canvas spaces remain enabled for user-scoped folders, with independent camera/scene persistence (`user + folder`) and Shift-drag export behavior via `/api/ai/media-folder-canvas/[folderId]` + `/api/ai/media-folder-canvas/save`; project-scoped folder canvas cutover is still a follow-up. Set `NEXT_PUBLIC_AI_STUDIO_MEDIA_LIBRARY_PANEL_ENABLED=false` to fallback to the legacy modal path during staged rollback.
  `Libraries -> Elements` opens a primary left-panel Elements Library surface (tool id `elements`) with two embedded modes: `Manage Elements` and `Element Profile`. The first implementation pass is UI-only and ships with local library state for browsing, create/edit, and delete flows while preserving a future seam for Kling-compatible image-backed and video-backed element assets.
  `Libraries -> Styles` opens the primary left-panel Styles Library and keeps the right rail in reference-grid-only mode; the first style tile is a fixed `None` slot and cannot be edited, deleted, or reordered. The right-rail Styles panel mirrors this with the same `None` tile as the first option. The Styles Library supports drag/drop reordering of custom style cards in-grid, drag/drop image intake from your computer/Reference Grid/Quick Slot (from visible right-rail surfaces) to auto-create custom styles, and an `Add Style` tile that opens a create-style modal. Direct Canvas image drag-out is deferred in V1. Clicking a real style card in the Styles Library opens an edit modal for `Style`, `Reference Image`, and `Style Prompt` without changing the active Create/Edit workflow style; only right-rail Styles tile selection applies styles to generation workflows, and selecting a right-rail style closes that panel immediately. Dropped/uploaded reference images are center-cropped to a square for the style preview card. `Libraries -> Presets` now opens one primary left-panel Presets Library with two internal sections: `Pulses` and `Prompt Presets`. The `Pulses` section is backed by shared per-user Pulse persistence. Clicking any Pulse card opens the editor: built-in starter Pulses save as personal overrides on their stable seeded ids, while custom Pulses remain fully user-owned and deletable. Custom Pulse authoring now centers on `Preset Name` plus hidden `System Instructions`, while advanced runtime controls remain optional behind advanced settings. New custom Pulses default to GPT-style `workflow_gpt` behavior with immediate start-on-click semantics; saved Pulse records still persist runtime behavior data (`systemInstructions`, `runtimeMode`, `activationMode`, optional `starterAssistantMessage`, optional `workflowStageHints`, `outputMode`, `memoryPolicy`) for built-in and advanced cases. The seeded workflow starters include `Video Prompt Magic`, `Multi Sequence Video Prompt`, and `Story Builder`, all of which run as built-in guided Pulses that auto-start when clicked in Expert Create `Pulse` mode. The editor still exposes workflow starter templates plus a structured workflow builder (`Role & Goal`, `Step Flow`, `Final Output Shape`, `Additional Rules`) for advanced workflow authoring, and workflow Pulses can also persist optional `Workflow Stage Labels` that feed the Create chat banner when replies do not use explicit `Step N` formatting. In Expert Create `Pulse` mode, the left pulse rail uses the same shared Pulse catalog with pinned preset chips, drag/drop from `More Presets`, editable saved custom pulses plus built-in override editing, and hidden Pulse activation that threads the active preset into `/api/ai/studio-agent` without mutating the visible Create composer. The Pulse rail now presents an explicit `Deactivate Pulse` topbar action whenever a Pulse session is active. Guided Pulses can auto-start on click and return step-by-step assistant turns before emitting a final prompt artifact. When a guided Pulse is active, the Create chat surface now shows an in-chat `Active Pulse` banner with the active Pulse name, status, optional inferred/current step, and the exact first/current step prompt for guided follow-through. That banner reads the authoritative persisted Pulse workflow session state, including completed artifacts and their completion source, rather than re-deriving completion from raw chat alone. Pulse mode also forces Create chat mode on, hides the inline chat-mode toggle, hides the Create styles button/right-rail Styles toggle until the user returns to `Standard`, and restores the active Create Pulse mode plus active pinned Pulse preset id from session persistence. The `Prompt Presets` section uses the Expert Edit preset catalog with `Custom 1`, `Custom 2`, and `Custom 3` placeholder tiles plus a full-size `Create New Preset` tile. Clicking a preset card opens an edit modal for preset name + prompt text. Each preset card includes a top-right delete button that opens a yes/no confirmation modal before permanently deleting that preset from the user library. Clicking `Create New Preset` opens the modal in create mode for the next custom slot (`Custom 4+`). Saves persist via the shared per-user preset override preference.
- **AI Studio Stage Bakeoff (`/dev/ai-studio-stage-bakeoff`)**: Dev-only Phase 1 engineering lab for comparing the next stage substrate. It mounts isolated DOM/CSS, Konva, and Fabric.js candidates against the same workspace/artboard/layer checklist and returns `404` in production.
  New-style creation from image intake now auto-runs style extraction to populate `Style Prompt`, auto-generates a style title for the new card name fields, and analyzes from the best available source image while keeping manual prompt editing available.
  When a style is selected in Create/Edit, the style prompt is appended to the hidden generation submission prompt while preserving the visible user prompt text. Expert Edit prompt references support `@main` for the primary image and `@img1`, `@img2`, `@img3` for the secondary reference slots; the picker exposes the primary tile alongside populated secondary slots.
  Active AI Studio video workflows are Kie-only. Standard Video mode now surfaces Kie Veo, Kie Kling, and Kie Seedance 1.5. In Standard mode with empty frame slots, the workflow still defaults to Kie Veo text-to-video behavior, but the video model modal also allows manual selection of Kie Kling and Kie Seedance 1.5. If Kling is selected in that state, the first-frame image becomes required before Generate is enabled. Kie Seedance 1.5 supports prompt-only generation, single-image animation, and first/last-frame generation, and exposes a Seedance-only `fixed_lens` control in the video settings panel. Kling Standard now exposes three product shot modes: `Single`, `Multi`, and `Custom`. `Single` and `Multi` both submit to the standard Kie Kling single-shot route with the primary prompt, first frame, and optional last frame. `Multi` is a single-prompt multi-scene writing mode and can reference attached elements with `@ElementName` notation. `Custom` is the only true multi-shot Kling path; it submits `multi_prompt[]` and uses the first frame only.
  The left toolbar includes a `Sessions` action that lists recent persisted sessions and supports explicit save-and-switch restoration back into AI Studio.
- **Character Manager (`/character`)**: Character creation and management workspace with persisted reference intake and persisted character-sheet assignments. Character Sheet preset tabs are dynamic (`1..10`): new users start with only tab `1` labeled `Double click me`, `+` adds tabs, double-click rename autosaves per user/character, and tabs after `1` can be deleted via `X` with confirmation (removes that tab's saved preset references). Character Profile description is also preset-scoped per active tab and persists in metadata. Character Profile voice controls are currently hidden while the voice feature is quarantined for a later rollout; see `docs/character-profile-voice-quarantine.md`. AI Studio Character Mode injects active-tab description first, then falls back to legacy character description when empty. The same preset-tab behavior is used in the AI Studio Character Properties panel via shared shell logic. Beginner toggle controls are temporarily hidden by the same runtime policy used by AI Studio.
- **Character Placeholder (`/character-soon`)**: Legacy fallback landing page retained during Character Manager rollout.
- **Admin (`/admin`)**: Internal operator overview for support and credit workflows. The landing page now focuses on a compact selected-user snapshot for manual credit adjustments, a current-admin email summary with the signed-in admin pinned to the top of the loaded user list, recent credit ledger inspection, and deep links into dedicated admin workspaces.
- **Admin Errors (`/admin/errors`)**: Operator incident-triage workspace for grouped app/runtime failures, bulk incident actions, and raw event-stream inspection.
- **Admin Announcements (`/admin/announcements`)**: Operator bulletin workspace for publishing or clearing the one active dashboard announcement shown to signed-in users.
- **Admin User Health (`/admin/user-health`)**: Operator diagnostics page for user-level generation + credit-drainage health checks (lookup by user id or email, findings, and recommended next actions).
- **Admin Fleet Health (`/admin/user-health-fleet`)**: Operator fleet triage page for active-user hourly health snapshots, risk/severity/finding filters, and direct drill-down links into per-user health and generation trace workflows.

## Security

- Only the Supabase anon key is used on the client; never share the service role key.
- Enable RLS on `saved_creators` and `media_files` (per-user isolation) and keep the `media_library` bucket private with paths prefixed by `auth.uid()`.
- Route protection: `/dashboard`, `/performance`, `/performance-soon`, `/saved-creators`, `/media-library`, `/profile`, `/ai-studio`, `/creator-studio`, `/character`, `/character-soon`, and `/admin*` expect authenticated sessions and redirect to `/auth` when missing.
- API protection: announcement routes, provider proxy routes, media routes, billing routes, upload routes, and admin routes require bearer-authenticated Supabase sessions.

## Testing

- Unit tests:
  ```bash
  cd frontend
  npm run test
  ```
- Full validation:
  ```bash
  cd frontend
  npm run validate
  ```
- One-pass repo sweep (major CI-aligned breakpoints):
  ```bash
  bash scripts/run_repo_sweep.sh
  ```
- Dead-code guard rail:
  ```bash
  cd frontend
  npm run deadcode:check
  ```
- E2E harness command (add specs incrementally):
  ```bash
  cd frontend
  npm run test:e2e
  ```

## Docs

- Start at `docs/README.md`.
- Character Manager operations runbook: `docs/sops/sop_character_manager_operations.md`.
- SQL migration operations runbook: `docs/sops/sop_sql_migration_operations.md`.
- Expert Edit prompt reference tokens runbook: `docs/sops/sop_ai_studio_expert_edit_prompt_references.md`.
- AI Studio session persistence (full canvas durability) packet:
  - SOP: `docs/sops/sop_ai_studio_session_persistence_reference_only.md`
  - ADR: `docs/adr/0031-ai-studio-full-canvas-session-persistence.md`

## Repo layout

- `frontend/`: Next.js app
- `docs/`: documentation
- `sql/`: Supabase bootstrap scripts

## Roadmap / changelog

- Roadmap: `ROADMAP.md`
- Changelog: `docs/change_log.md`

-
