/**
 * Backward-compatible export surface for the Standard Create panel contract.
 * The canonical Standard panel builder now lives under createRuntime.
 */
export { buildStandardCreatePanelProps } from "../../createRuntime/standardPanel/standardCreatePanelContract";
