-- No automatic rollback is provided for migration 208.
-- Advancing model-pricing control-plane identity sequences is a monotonic repair
-- that prevents duplicate primary keys on future admin pricing saves.
-- Rewinding these sequences could recreate the production collision.
