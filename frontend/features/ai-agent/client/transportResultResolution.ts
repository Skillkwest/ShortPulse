/**
 * Compatibility module for older failure-resolution imports.
 * Success parsing is now owned by Standard and Pulse response-parser modules.
 */
export { resolveStudioAgentTransportFailure } from "./transportFailureResolution";
