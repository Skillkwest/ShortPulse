# Abismia Tools

Purpose: retain Abismia-specific helper inventory and future tooling needs for UI/UX and intended runtime behavior work.

## Current Tools

- Local signed-in browser credential source for Abismia validation: `/Users/worldbuilder/.codex/secrets/shortpulse-abismia.env`. The file is outside the repo and must stay out of tracked docs, reports, screenshots, logs, and test fixtures.
- General repo inspection and browser validation are core repo capabilities, not Abismia-specific tooling.

## Future Tooling Needs

- reusable UI runtime-state review checklist
- redacted UI audit report starter template
- interaction-regression checklist for shared surfaces
- human-experience walkthrough template for trust, agency, perceived speed, and cognitive-load findings

## Rules

- Add an item here only when it is a reusable Abismia-specific helper, template, or repeatable review aid.
- Do not use this file as a general list of things Codex can already do.
- Never store credential values here.
