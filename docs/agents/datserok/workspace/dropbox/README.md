# Datserok Dropbox

Purpose: temporary intake folder for files the user wants Datserok to inspect during project-persistence work.

## Rules

- Treat files here as temporary working inputs, not durable memory.
- Promote only sanitized, durable outputs into Datserok's retained artifact area when a run truly needs them.
- Delete stale intake files after review when they are no longer needed.
